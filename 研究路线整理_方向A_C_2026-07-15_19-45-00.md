# RL4EQ 当前架构与方向A/C落地路线整理

生成时间：2026-07-15 19:45:00  
项目目录：`D:\Research\RL4EQ`  
架构约束：根目录 `开发框架.md` 保持不变，仍以“离线监督预训练 + 在线 RL 控制参数高效微调”为唯一主线。

## 1. 当前实现架构

当前代码已经对齐 `开发框架.md` 的两阶段结构：

```text
Offline Stage:
  随机无线信道生成
  -> AdapterEqualizer 监督预训练
  -> 输出可被在线阶段加载的 pretrained/model_best.pt

Online Stage:
  接收 Pilot + Data
  -> Pilot Equalization
  -> Pilot Loss / SNR / CFO / 历史状态
  -> RL Agent 选择少量参数更新策略
  -> Adapter/输出头/同步头参数高效更新
  -> Equalize Data
  -> 与 MMSE/RLS/LMS/ZF 等传统 baseline 对比
```

主入口保持为：

| 文件 | 职责 |
|---|---|
| `pretrain.py` | 离线监督预训练 `AdapterEqualizer`，输出 `model_best.pt`、`model_final.pt`、配置和曲线。 |
| `online_train.py` | 在线 PEFT + PPO 自适应训练与评估，统计 PEFT、MMSE 和传统 baseline 指标。 |
| `compare.py` | SNR、信道、失配场景、低 pilot overhead 等对比实验入口。 |
| `agent/neural_equalizer.py` | Transformer + Adapter 神经均衡器，支持 MMSE residual、同步/相位/CFO 相关扩展。 |
| `agent/adaptation_controller.py` | 执行每帧少量参数更新，计算 pilot-only reward 和在线指标。 |
| `agent/adaptation_policy.py` | 单步帧级 PPO 策略和 imitation learning 预训练接口。 |
| `baseline/*.py` | MMSE、ZF、LMS、RLS、matched filter 等传统均衡 baseline。 |
| `tests/test_parameter_efficient_adaptation.py` | 主线约束测试。 |

## 2. 当前不足

当前实验结论仍不支持“RL 端到端均衡全面超过传统均衡算法”的强表述。主要原因是：

1. 当前 BPSK + 线性多径 + 高已知位比例场景与 MMSE/RLS 的物理假设高度匹配，传统算法是强 baseline。
2. PPO action 主要控制学习率、步数和可训练模块，和信道物理过程的耦合仍偏间接。
3. pilot loss 下降与 data BER 改善不一定强相关，存在 pilot overfitting 风险。
4. imitation 诊断显示 observation 对 data-oracle action 的预测能力仍弱，说明策略输入信息不足。
5. CFO head、同步/相位 latent head 已有接口，但需要显式监督信号才能稳定发挥作用。

因此论文目标应调整为更稳健的表述：

```text
基于 Pilot 约束强化学习的模型辅助残差神经均衡：
用 RL 控制伪导频门控与参数高效在线适应，而不是让 RL 直接替代 MMSE/RLS。
```

## 3. 方向A：诊断闭环

方向A的目标是先判断“当前 action space 与 reward 是否有潜力”，避免盲目增加 PPO 复杂度。

已落地/本轮强化的内容：

1. 固定独立测试集 seeds，避免每轮换测试分布造成结论漂移。
2. 对同一批帧同时评估：
   - `skip`
   - 固定 PEFT 策略，如 `head-slow`、`adapter-fast`、`both-fast`
   - pseudo action，如 `pseudo-both-fast`、`pseudo-both-deep`
   - PPO policy
   - pilot-oracle action
   - data-oracle action，仅用于仿真诊断
   - MMSE baseline
3. 输出 `strategy_diagnostics.json` 和 `strategy_matrix.png`。
4. 输出 pilot reward 与 data BER 的相关性：
   - `delta_pilot_loss_vs_delta_ber_data`
   - `BER_pilot_vs_BER_data`
   - `pilot_data_correlation.png`

判断逻辑：

| 诊断现象 | 解释 | 下一步 |
|---|---|---|
| data-oracle 明显优于固定策略，但 PPO 不优 | observation/reward/policy 不足，应增强策略输入或 imitation。 |
| data-oracle 也不优于固定策略 | action space 或 equalizer 本身不足，应改 PEFT/pseudo-label 机制。 |
| pilot-oracle 优于 PPO 但 data-oracle 不优 | pilot reward 与 data BER 错配，应重构 reward。 |
| neural no-adapt 明显弱于 MMSE | 先修离线预训练/MMSE residual 输入，不应优先调 PPO。 |

## 4. 方向C：Pseudo-Pilot Data-Aided Adaptation

方向C的目标是借鉴 data-aided channel estimation 文献，让 RL 控制“哪些 data 判决可以作为伪导频参与在线更新”。

在线阶段仍严格不使用真实 data label。伪导频只来自以下可观测条件：

```text
data 位置满足：
  neural confidence >= τ_neural
  MMSE confidence >= τ_mmse
  neural decision == MMSE decision
  按 confidence 排序后不超过 max_ratio
```

本轮强化后的 action 语义：

| action | 伪导频门控 | 更新模块 | 特点 |
|---|---|---|---|
| `pseudo-both-fast` | `τ_neural=0.85, τ_mmse=0.65, max_ratio=0.25` | adapter + output | 保守使用高置信伪导频。 |
| `pseudo-both-deep` | `τ_neural=0.75, τ_mmse=0.60, max_ratio=0.40` | adapter + output | 使用更多伪导频，风险更高但信息量更大。 |

实验记录新增伪导频诊断字段：

| 字段 | 含义 |
|---|---|
| `pseudo_count` | 当前策略每帧平均选中的伪导频数量。 |
| `pseudo_ratio` | 被选中伪导频占 data 段比例。 |
| `pseudo_conf_mean` | 选中伪导频的 neural/MMSE 最小置信度均值。 |
| `neural_mmse_disagreement` | data 段 neural 与 MMSE 判决不一致比例。 |

## 5. 论文路线建议

建议论文主线不要写成“RL 均衡器直接超过 MMSE/RLS”，而应写成：

```text
Pilot-constrained model-assisted RL adaptation for neural channel equalization
```

核心贡献可以表述为：

1. 提出一个严格 pilot-constrained 的在线神经均衡自适应框架，避免 data label 泄漏。
2. 将传统 MMSE 从单纯 baseline 转为 model-assisted teacher/reference，神经网络学习残差修正。
3. 引入 RL 控制的 pseudo-pilot gate，使高置信 data 判决在不使用真实标签的前提下参与在线更新。
4. 用固定 seeds、pilot/data oracle、固定 PEFT、传统 baseline 和相关性分析建立严谨消融闭环。
5. 在低 pilot overhead、CFO、Doppler、非线性和模型失配场景中寻找神经/RL 方法的优势区间。

## 6. 后续优先级

1. 先看方向A诊断结果：data-oracle 是否显著优于固定 PEFT。
2. 若有上限，增强 observation：加入候选 action probing、MMSE pilot 指标、residual autocorrelation、delay/CFO 估计。
3. 若无上限，优化 offline residual-assisted 预训练和 pseudo gate 质量，而不是继续扩大 PPO。
4. 在论文实验中必须保留 RLS/MMSE/ZF/LMS，并明确 `BER_data` 只用于仿真评估，不能作为在线 reward。
