# 迭代记录：CFO/慢相位复杂度与传统补偿校准

时间戳：2026-08-12 11:50:31

## 1. 本轮目标

本轮按照最新研究对齐结果执行：不再把 `BER_data < 0.01` 作为第一成功门槛，而是把第一性目标调整为：

```text
在 Level B 主场景中逐级增加 residual CFO 与慢相位扰动复杂度；
传统 baseline 必须具备合理 Pilot 补偿，不能人为打残；
Proposed 神经/RL 方法应在所有主 SNR 条件下明显超过传统非神经、非 RL baseline；
并且随着在线帧数增加，Proposed 优势应越来越明显。
```

本轮仍不加入：

- 硬件非线性；
- 信道编码；
- 高阶调制；
- 多载波、MIMO、RIS；
- 旧 A2C、旧逐符号 PPO、Data Oracle。

## 2. 已实现内容

### 2.1 信道 impairment

新增 `env/impairments.py`，实现：

- `clean`
- `cfo_light`
- `phase_light`
- `cfo_phase_light`
- `cfo_phase_mid`

信道 `env/extreme_delay_channel.py` 已支持：

- episode 级 residual CFO；
- 跨帧连续的全局符号相位；
- 慢相位随机游走；
- seed 可复现；
- 默认 clean 行为保持兼容。

真实 CFO/相位参数不进入 `receiver_view()`，因此传统 baseline 和 online policy 不能直接读取真实 impairment。

### 2.2 传统合理补偿 baseline

新增 `baseline/synchronization_compensation.py`：

- `estimate_pilot_phase_line()`：用 Adapt Pilot 估计相位直线；
- `apply_phase_correction()`：按估计相位反旋整帧；
- `unwrap_phase()`：兼容当前 PyTorch 版本的相位展开。

扩展 `baseline/traditional_equalizers.py`：

- 新增 `CFO-Corrected LMMSE-FIR`
- 新增 `CFO-Corrected DFE-RLS`
- 所有 traditional result 标记：
  - `uses_neural_network=False`
  - `uses_rl=False`
  - `uses_reward_or_data_labels=False`

重要修复：

- 早期直接用 `rx[pilot] * conj(tx[pilot])` 估计 CFO，在长 ISI 下会被多径严重污染。
- 已改为用 acquisition CIR、soft tail 和 Adapt Pilot 构造传统可见的 pilot 参考波形，再估计相位斜率。
- 修复 traditional baseline 不更新 `soft_tail` 的问题。之前跨帧 ISI 状态一直停留在 acquisition tail，导致第 2 帧以后补偿和均衡不公平。

### 2.3 acquisition CFO-aware CIR 估计

新增传统可见的 acquisition CFO 网格搜索：

```text
全已知 acquisition frame
-> 网格搜索 CFO
-> 对每个 CFO 反旋 rx
-> LS 估计 CIR
-> 选择残差最小者
```

该流程只使用 acquisition 已知发送符号和接收信号，不读取 Reward/Data 标签，不使用真实 CFO。

已接入：

- `evaluation/research_diagnostics.py`
- `compare.py`
- `training/windowed_discrete_ppo.py`
- `training/rl_modulated_online.py`
- `training/continual_ppo.py`
- `training/curriculum.py`
- `training/meta_training.py`

目的：在 impaired 场景下，Proposed 和传统方法都不要继续使用被 CFO 污染的 acquisition CIR。

### 2.4 校准入口

`scripts/calibrate_traditional_difficulty.py` 已支持：

```powershell
--enable-cfo
--enable-phase-perturbation
--impairment-profile clean|cfo_light|phase_light|cfo_phase_light|cfo_phase_mid
```

仍禁止：

- `--enable-nonlinearity`
- `--enable-coding`
- `--enable-higher-order-modulation`

原因：这些路线不在当前主线中，不能被误混入本轮实验。

### 2.5 compare / 配置 / 文档契约

`compare.py` 支持：

```powershell
--impairment-profile
```

并在输出 row 与 summary 中记录 `impairment_profile`，resume key 也包含该字段，避免 clean/impaired 矩阵混写。

`configs/continual_ppo.json` 增加：

```json
"impairment_profile": "clean"
```

同步更新：

- `README.md`
- `AGENTS.md`
- `开发框架.md`
- `RL信道均衡研究分析.md`

文档主线已统一为：

```text
clean Level B 是 sanity check；
主攻逐级 CFO/慢相位 Level B；
传统 baseline 必须做合理补偿；
第一目标是明显超过传统，而不是只硬追 BER<0.01。
```

## 3. 验证结果

### 3.1 单元与契约测试

全量测试命令：

```powershell
.\.venv-gpu\Scripts\python.exe -m pytest -q -p no:cacheprovider
```

结果：

```text
149 passed in 351.71s (0:05:51)
```

新增/覆盖的关键测试包括：

- CFO 相位跨帧连续，不按帧重置；
- phase impairment seed 可复现；
- clean 默认行为兼容；
- pilot-based CFO correction 可恢复已知旋转；
- compensated traditional baseline 不使用神经网络、不使用 RL、不读取 Reward/Data 标签；
- 长 ISI 下补偿必须使用 CIR 参考波形，不能直接用当前 pilot；
- acquisition CFO 网格搜索可在不读取真实 impairment 的情况下估计 CFO；
- traditional baseline 必须更新 soft tail；
- 校准 CLI 可真实启用 CFO/相位扰动；
- 文档契约不再把 `BER_data < 0.01` 写成第一门槛。

### 3.2 CFO+慢相位校准 smoke

命令：

```powershell
.\.venv-gpu\Scripts\python.exe scripts/calibrate_traditional_difficulty.py `
  --delays 20 `
  --snrs 10 `
  --pilot-totals 128 `
  --seeds 0 `
  --frames 4 `
  --methods DFE-RLS "CFO-Corrected DFE-RLS" LMMSE-FIR "CFO-Corrected LMMSE-FIR" `
  --enable-cfo `
  --enable-phase-perturbation `
  --impairment-profile cfo_phase_light `
  --output-dir logs/traditional_difficulty_cfo_phase_tailfix_smoke_2026-08-12
```

结果摘要：

```text
DFE-RLS                       mean BER_data = 0.6003
LMMSE-FIR                     mean BER_data = 0.6061
CFO-Corrected DFE-RLS         mean BER_data = 0.2780
CFO-Corrected LMMSE-FIR       mean BER_data = 0.2565
```

逐帧观察：

```text
CFO-Corrected LMMSE-FIR:
frame 1: BER_data = 0.0729
frame 2: BER_data = 0.1068
frame 3: BER_data = 0.3307
frame 4: BER_data = 0.5156

CFO-Corrected DFE-RLS:
frame 1: BER_data = 0.1068
frame 2: BER_data = 0.1484
frame 3: BER_data = 0.3854
frame 4: BER_data = 0.4714
```

## 4. 结果分析

### 4.1 为什么一开始传统 baseline 接近随机

最初的 CFO/相位补偿直接用：

```text
rx[pilot] * conj(tx[pilot])
```

估计相位误差。

这在短记忆或无 ISI 场景下成立，但在 Level B 极端长 ISI 下不成立。因为当前 pilot 接收样本不是当前 pilot 符号本身，而是多个历史符号和多个 tap 的叠加。直接相乘会把多径 ISI 当作相位误差，导致 CFO 估计被污染。

这不是论文可用结果，因为它相当于给传统 baseline 一个不适合长 ISI 的同步前端。

### 4.2 修复后传统 baseline 为什么仍会随帧数退化

修复后：

- 第 1 帧和第 2 帧明显改善；
- 第 3/4 帧仍退化。

当前最可能原因是：

1. acquisition 只估计一个 episode 初始 CFO/CIR；
2. 后续 tap 仍在 Gauss-Markov 演化；
3. 慢相位随机游走跨帧累积；
4. 当前 traditional compensation 没有 decision-directed phase tracking；
5. DFE/LMMSE 的 soft tail 虽已更新，但相位 tracker 没有跨帧状态。

因此当前 compensated baseline 是“有基本合理补偿”，但还不是“足够强的传统同步补偿 baseline”。

### 4.3 这对研究方向意味着什么

本轮没有证明 Proposed 已经超过传统。

本轮真正完成的是：

```text
把主攻方向从 clean linear BPSK Level B
推进到 residual CFO / slow phase drift 的复杂度阶梯；
并开始补齐传统 baseline 的公平补偿能力。
```

这符合用户最新要求：不能只找 Proposed 有机会赢的弱区，而要逐级增加信道复杂度，并给传统方法合理补偿。

当前证据说明：

- clean Level B 下传统算法强，这是合理现象，不是研究失败；
- CFO/慢相位确实会使传统算法明显困难；
- 但传统补偿必须继续增强，否则不能作为最终公平 baseline；
- Proposed 也必须在同一 impaired profile 下重新训练/比较，不能沿用 clean checkpoint 直接下结论。

## 5. 下一阶段建议

下一阶段应优先做三件事。

### 5.1 给传统 baseline 增加 decision-directed phase tracker

目标：

```text
acquisition CFO/CIR 初始化
-> 每帧 Adapt Pilot refine phase
-> Data/decision-directed 平滑跟踪 phase drift
-> 再进入 LMMSE/DFE
```

该 tracker 仍然是传统方法：

- 不使用神经网络；
- 不使用 RL；
- 不读取 Reward/Data 标签；
- 只使用判决、接收信号、Adapt Pilot 和传统滤波规则。

这一步是公平性要求。如果传统 tracker 加强后仍明显弱于 Proposed，论文结论才更站得住。

### 5.2 重新训练 impaired-aware Offline NN

当前 clean checkpoint 不能代表最终 Proposed。

训练应混合：

- clean；
- cfo_light；
- phase_light；
- cfo_phase_light；
- 必要时加入更弱的 `cfo_tiny/phase_tiny` 过渡档。

目标不是立刻 BER<0.01，而是先观察：

```text
Offline NN only 是否能在 impaired Level B 下接近或超过 compensated traditional baseline。
```

如果 Offline NN only 都明显弱于传统，PPO 很难靠动作选择解决根本接收机能力不足。

### 5.3 再启动窗口级 RL

只有当：

```text
Offline NN only ≈ 或 > compensated traditional baseline
```

之后，才启动：

```text
RL-Modulated Neural Block Equalizer
```

并验证：

- RL 是否低于 Offline NN only；
- RL 优势是否随 frames 增加；
- Reward Pilot 改善是否能预测 Data BER 改善；
- 所有主 SNR `[0, 5, 10, 15]` 是否逐配置成立。

## 6. 当前结论

当前研究方向不是“创新点根本实现不了”，也不是“方向完全错了”。

更准确的判断是：

```text
clean linear BPSK + 足够 Pilot 的 Level B 不足以自然击败传统均衡器；
要形成神经/RL 优势，必须把主场景放到传统线性均衡器难以持续跟踪的同步残差/时变条件下；
同时必须给传统 baseline 合理同步补偿，不能让 baseline 因实现不完整而失败。
```

本轮已经把工程基础推进到这个方向，但还没有达到最终论文目标。

下一步不能直接写“Proposed 显著优于传统”；应先完成 traditional DD phase tracker 和 impaired-aware Offline NN 训练，再做正式矩阵。
