# 迭代记录：长 Episode 退化、CIR 漂移与传统 baseline 再定位

时间戳：2026-08-04 00:41:09 +08:00

## 1. 本轮目标

上一轮发现：

```text
短评估 Offline NN overall BER_data ≈ 0.008353
64-frame 长 episode Offline NN overall BER_data ≈ 0.039216
```

这说明长 episode 中存在明显退化。本轮按系统化调试思路，不直接继续训练 PPO，而是先定位退化来源：

1. tail 误差累积是否是主因；
2. acquisition CIR 固定不更新是否是主因；
3. 信道 Gauss-Markov 漂移是否是主因；
4. decision-directed CIR 更新能否恢复长 episode 性能；
5. 在修正 CIR 更新后，RL 是否仍低于 Offline NN；
6. 当前 proposed 与传统 baseline 的真实差距。

## 2. 新增/修改代码

### 2.1 新增长 episode 诊断模块

新增：

```text
evaluation/long_episode_diagnostics.py
scripts/diagnose_long_episode_drift.py
tests/test_long_episode_diagnostics.py
```

诊断模块支持：

```text
tail_modes:
  soft
  hard
  oracle
  zero

cir_modes:
  fixed
  adapt_pilot
  decision_directed
  oracle

rhos:
  0.99
  1.0
```

说明：

- `oracle tail` 和 `oracle CIR` 只用于诊断，不进入正式 baseline。
- `decision_directed` CIR 更新只使用接收端可见信息：Adapt Pilot 标签 + Data 区域硬判决，不使用 Data 标签。
- `adapt_pilot` CIR 只用当前帧 Adapt Pilot 做 LS 估计。

### 2.2 compare 增加 decision-directed CIR 更新开关

`compare.py` 新增：

```powershell
--cir-update fixed
--cir-update decision_directed
```

当前只用于 proposed 方法族：

```text
Offline NN only
RL-Modulated Neural Block Equalizer
```

输出字段：

```text
cir_update_mode
cir_update_uses_data_labels
```

其中：

```text
cir_update_uses_data_labels = false
```

## 3. 关键实验结果

### 3.1 Smoke 诊断

命令：

```powershell
.\.venv-gpu\Scripts\python.exe scripts\diagnose_long_episode_drift.py `
  --config configs\continual_ppo.json `
  --pretrained pretrained\level_b_focus_d40_snr10_1600_from_1500_p128\model_best.pt `
  --output-dir logs\long_episode_diag_smoke_2026-08-03 `
  --delay 40 --snr 10 --frames 8 --seeds 0 `
  --tail-modes soft hard oracle `
  --cir-modes fixed decision_directed oracle `
  --rhos 0.99 1.0 `
  --pilot-total 128 --pilot-layout two_block --device cuda
```

初步结果：

```text
rho=0.99:
  fixed CIR            ≈ 0.008464
  decision_directed CIR≈ 0.004557
  oracle CIR           ≈ 0.003906

rho=1.0:
  fixed / decision / oracle CIR 基本相同，≈0.006510
```

初步判断：

```text
退化主要来自 CIR 漂移，而不是 tail。
```

### 3.2 64-frame 单 seed 全组合诊断

命令：

```powershell
.\.venv-gpu\Scripts\python.exe scripts\diagnose_long_episode_drift.py `
  --config configs\continual_ppo.json `
  --pretrained pretrained\level_b_focus_d40_snr10_1600_from_1500_p128\model_best.pt `
  --output-dir logs\long_episode_diag_d40s10_64f_seed0_2026-08-03 `
  --delay 40 --snr 10 --frames 64 --seeds 0 `
  --tail-modes soft hard oracle `
  --cir-modes fixed decision_directed oracle `
  --rhos 0.99 1.0 `
  --pilot-total 128 --pilot-layout two_block --device cuda
```

核心结果：

| rho | tail | CIR | mean BER_data |
|---:|---|---|---:|
| 0.99 | soft | fixed | 0.053101 |
| 0.99 | soft | decision_directed | 0.017944 |
| 0.99 | soft | oracle | 0.016113 |
| 1.0 | soft | fixed | 0.008626 |
| 1.0 | soft | decision_directed | 0.008301 |
| 1.0 | soft | oracle | 0.008138 |

`rho=0.99 + soft tail + fixed CIR` 按 frame bin：

| frame bin | mean BER_data |
|---|---:|
| 1–16 | 0.021322 |
| 17–32 | 0.047852 |
| 33–48 | 0.048014 |
| 49–64 | 0.095215 |

`rho=0.99 + soft tail + decision_directed CIR`：

| frame bin | mean BER_data |
|---|---:|
| 1–16 | 0.011230 |
| 17–32 | 0.020833 |
| 33–48 | 0.022461 |
| 49–64 | 0.017253 |

`rho=1.0 + soft tail + fixed CIR`：

| frame bin | mean BER_data |
|---|---:|
| 1–16 | 0.007487 |
| 17–32 | 0.008789 |
| 33–48 | 0.009277 |
| 49–64 | 0.008952 |

判断：

```text
如果信道不漂移（rho=1.0），长 episode 不退化。
如果信道漂移但 CIR 固定，BER 随帧号显著恶化。
decision-directed CIR 更新可以消除大部分后期退化。
```

### 3.3 3-seed CIR 主效应确认

命令：

```powershell
.\.venv-gpu\Scripts\python.exe scripts\diagnose_long_episode_drift.py `
  --config configs\continual_ppo.json `
  --pretrained pretrained\level_b_focus_d40_snr10_1600_from_1500_p128\model_best.pt `
  --output-dir logs\long_episode_diag_d40s10_64f_3seed_cir_2026-08-03 `
  --delay 40 --snr 10 --frames 64 --seeds 0 1 2 `
  --tail-modes soft `
  --cir-modes fixed decision_directed oracle `
  --rhos 0.99 1.0 `
  --pilot-total 128 --pilot-layout two_block --device cuda
```

总体结果：

| rho | CIR mode | mean BER_data |
|---:|---|---:|
| 0.99 | fixed | 0.054810 |
| 0.99 | decision_directed | 0.014608 |
| 0.99 | oracle | 0.012546 |
| 1.0 | fixed | 0.010322 |
| 1.0 | decision_directed | 0.009956 |
| 1.0 | oracle | 0.009549 |

`rho=0.99` 按 frame bin：

| CIR mode | 1–16 | 17–32 | 33–48 | 49–64 |
|---|---:|---:|---:|---:|
| fixed | 0.018338 | 0.035590 | 0.070367 | 0.094944 |
| decision_directed | 0.010091 | 0.015299 | 0.019423 | 0.013618 |
| oracle | 0.008138 | 0.011719 | 0.018121 | 0.012207 |

结论：

```text
CIR 漂移是长 episode 退化主因。
decision-directed CIR 更新接近 oracle CIR，上限差距很小。
```

### 3.4 3-seed tail 对照

命令：

```powershell
.\.venv-gpu\Scripts\python.exe scripts\diagnose_long_episode_drift.py `
  --config configs\continual_ppo.json `
  --pretrained pretrained\level_b_focus_d40_snr10_1600_from_1500_p128\model_best.pt `
  --output-dir logs\long_episode_diag_d40s10_64f_3seed_tail_2026-08-03 `
  --delay 40 --snr 10 --frames 64 --seeds 0 1 2 `
  --tail-modes soft hard oracle zero `
  --cir-modes fixed `
  --rhos 0.99 `
  --pilot-total 128 --pilot-layout two_block --device cuda
```

总体结果：

| tail mode | mean BER_data |
|---|---:|
| soft | 0.054810 |
| hard | 0.054905 |
| oracle | 0.054688 |
| zero | 0.054905 |

结论：

```text
tail 不是当前长 episode 退化主因。
即使用 oracle tail，fixed CIR 下仍会严重退化。
```

### 3.5 Adapt Pilot 当前帧重估 CIR 诊断

命令：

```powershell
.\.venv-gpu\Scripts\python.exe scripts\diagnose_long_episode_drift.py `
  --config configs\continual_ppo.json `
  --pretrained pretrained\level_b_focus_d40_snr10_1600_from_1500_p128\model_best.pt `
  --output-dir logs\long_episode_diag_d40s10_64f_3seed_cir_adapt_2026-08-03 `
  --delay 40 --snr 10 --frames 64 --seeds 0 1 2 `
  --tail-modes soft `
  --cir-modes fixed adapt_pilot decision_directed oracle `
  --rhos 0.99 `
  --pilot-total 128 --pilot-layout two_block --device cuda
```

总体结果：

| CIR mode | mean BER_data |
|---|---:|
| adapt_pilot | 0.097385 |
| fixed | 0.054810 |
| decision_directed | 0.014608 |
| oracle | 0.012546 |

判断：

```text
当前 naive Adapt Pilot LS 重估不可用，甚至比 fixed acquisition CIR 更差。
原因大概率是多块 Pilot 分布下，后续 Pilot 前面夹杂未知 Data，直接 LS 把未知 Data 当 0/不可见，会导致设计矩阵失配。
```

这说明后续若要做“只用 Pilot 的 CIR 更新”，不能用现在这种 naive LS，需要专门设计块式/迭代式 Pilot-aware CIR tracker。

### 3.6 compare 中启用 decision-directed CIR 更新后的 proposed 矩阵

命令：

```powershell
.\.venv-gpu\Scripts\python.exe compare.py `
  --config configs/continual_ppo.json `
  --methods "Offline NN only" "RL-Modulated Neural Block Equalizer" `
  --pretrained pretrained\level_b_focus_d40_snr10_1600_from_1500_p128\model_best.pt `
  --delays 20 30 40 --snrs 10 15 20 `
  --num-seeds 3 --frames 64 `
  --pilot-total 128 --pilot-layout two_block `
  --update-interval 8 `
  --cir-update decision_directed `
  --output-dir logs\rl_vs_offline_9cfg_64f_3seed_peft_ddcir_2026-08-03 `
  --resume
```

总体结果：

| method | mean BER_data |
|---|---:|
| Offline NN only | 0.011961 |
| RL-Modulated Neural Block Equalizer | 0.011995 |

逐配置 `RL - Offline`：

| delay | SNR | delta |
|---:|---:|---:|
| 20 | 10 | +0.000149 |
| 20 | 15 | -0.000095 |
| 20 | 20 | +0.000285 |
| 30 | 10 | -0.000149 |
| 30 | 15 | +0.000190 |
| 30 | 20 | +0.000095 |
| 40 | 10 | -0.000109 |
| 40 | 15 | -0.000136 |
| 40 | 20 | +0.000068 |

`delay=40, SNR=10` 按 frame bin：

| method | 1–16 | 17–32 | 33–48 | 49–64 |
|---|---:|---:|---:|---:|
| Offline NN only | 0.014486 | 0.016222 | 0.018772 | 0.027127 |
| RL-Modulated | 0.014540 | 0.016113 | 0.018880 | 0.026638 |

判断：

```text
DD-CIR 修复了主要退化后，RL 的额外 PEFT 增益基本消失。
当前 RL 与 Offline NN 几乎打平，overall 略差。
```

这不是坏事，它说明上一轮 RL 的主要优势来源是补偿 fixed CIR 过期，而不是 PEFT 本身已经学到了强泛化策略。

### 3.7 传统 baseline 长 episode 结果

命令：

```powershell
.\.venv-gpu\Scripts\python.exe compare.py `
  --config configs\continual_ppo.json `
  --methods "DFE-RLS" "SC-FDE-MMSE" `
  --delays 20 30 40 --snrs 10 15 20 `
  --num-seeds 3 --frames 64 `
  --pilot-total 128 --pilot-layout two_block `
  --output-dir logs\traditional_9cfg_64f_3seed_2026-08-03 `
  --resume
```

总体结果：

| method | mean BER_data |
|---|---:|
| DFE-RLS | 0.005584 |
| SC-FDE-MMSE | 0.090968 |

逐配置 DFE-RLS：

| delay | SNR | BER_data |
|---:|---:|---:|
| 20 | 10 | 0.005154 |
| 20 | 15 | 0.002780 |
| 20 | 20 | 0.002143 |
| 30 | 10 | 0.008341 |
| 30 | 15 | 0.004964 |
| 30 | 20 | 0.003933 |
| 40 | 10 | 0.011230 |
| 40 | 15 | 0.006510 |
| 40 | 20 | 0.005195 |

判断：

```text
SC-FDE-MMSE 在长 episode 下很差；
DFE-RLS 仍是最强传统 baseline，也是当前 proposed 必须真正超过的对象。
```

## 4. 本轮核心结论

### 4.1 长 episode 退化不是 tail 主导

证据：

```text
soft tail   0.054810
hard tail   0.054905
oracle tail 0.054688
zero tail   0.054905
```

四种 tail 几乎一样。

因此之前“tail 误差累积导致长 episode BER 升高”的假设不成立，至少不是当前主因。

### 4.2 主因是 CIR 漂移 + 固定 acquisition CIR

证据：

```text
rho=0.99 fixed CIR            0.054810
rho=0.99 decision_directed CIR 0.014608
rho=0.99 oracle CIR            0.012546

rho=1.0 fixed CIR              0.010322
rho=1.0 oracle CIR             0.009549
```

如果信道不漂移，fixed CIR 就不崩；如果信道漂移，fixed CIR 后期严重崩。

### 4.3 decision-directed CIR 更新是当前最有效的非 oracle 修复

DD-CIR 把最难配置从：

```text
0.054810 -> 0.014608
```

接近 oracle CIR：

```text
0.012546
```

这说明当前必须把 CIR tracking 纳入 proposed 设计，而不能固定 acquisition CIR。

### 4.4 naive Adapt Pilot LS 重估不可用

`adapt_pilot` CIR 结果：

```text
0.097385
```

比 fixed CIR 更差。

原因判断：

```text
多块 Pilot 中间夹杂 Data；
后续 Pilot 的接收值受到未知 Data ISI 影响；
直接只用 Adapt Pilot 构造 LS 会漏掉未知 Data 对 Pilot 的贡献；
因此估计矩阵失配。
```

### 4.5 当前 RL 的真实贡献需要重新定位

上一轮 fixed CIR 下，RL 可以略低于 Offline NN：

```text
Offline NN fixed CIR ≈ 0.039216
RL fixed CIR         ≈ 0.038167
```

本轮 DD-CIR 后：

```text
Offline NN DD-CIR ≈ 0.011961
RL DD-CIR         ≈ 0.011995
```

也就是说：

```text
RL 的上一轮优势主要是在补偿 fixed CIR 过期导致的退化；
当 CIR tracking 修好后，当前 PEFT-RL 还没有稳定额外收益。
```

这是非常关键的负结果，后续论文不能把 fixed-CIR 下的 RL 增益当成真正超越 Offline NN 的证据。

### 4.6 当前 proposed 尚未超过 DFE-RLS

当前长 episode：

```text
DFE-RLS                 0.005584
Offline NN + DD-CIR     0.011961
RL-Modulated + DD-CIR   0.011995
```

因此：

```text
当前 proposed 没有超过最强传统 baseline DFE-RLS。
```

## 5. 下一阶段建议

### 5.1 主线必须加入 CIR tracking

以后比较 proposed 时，不应再使用 fixed acquisition CIR 作为默认状态。

建议 proposed 标准状态改为：

```text
Offline NN + DD-CIR
RL-PEFT + DD-CIR
```

并明确：

```text
DD-CIR 使用硬判决，不使用 Data 标签。
```

### 5.2 RL 不应再为 fixed-CIR 退化“背锅”

fixed-CIR 的长 episode 退化是接收机状态设计问题，不应让 PPO 通过 PEFT 去补偿一个错误的 CIR tracking 设计。

正确顺序是：

```text
先把 CIR tracking 做成强基线；
再让 RL 在强状态估计基础上做 PEFT 策略选择。
```

### 5.3 下一步真正要攻关的是超过 DFE-RLS

DFE-RLS 当前非常强，尤其在主工作区几乎全配置低于 0.01，只有 `delay=40, SNR=10` 略高：

```text
DFE-RLS d40/s10 = 0.011230
```

proposed 要超过它，下一步不能只继续 PPO。优先方向：

1. 让 Offline NN + DD-CIR 在每配置达到 DFE-RLS 附近。
2. 加强神经接收机对 decision-directed CIR 误差的鲁棒性。
3. 把 PEFT action 从轻量 head/adapter 扩展到更有效的“CIR-conditioned PEFT”。
4. 在低 SNR / d40 主瓶颈上重新训练，而不是盲目全矩阵 PPO。

### 5.4 naive Adapt Pilot LS 不应进入主线

当前结果明确失败。若要使用纯 Pilot CIR 更新，需要重新设计：

- 块式 Pilot-aware LS；
- 显式建模未知 Data ISI；
- 或先用神经均衡器硬判决补全 Data，再做 decision-directed refinement。

这实际上已经接近 DD-CIR 路线。

## 6. 验证记录

新增相关测试：

```powershell
.\.venv-gpu\Scripts\python.exe -m pytest tests\test_long_episode_diagnostics.py tests\test_windowed_discrete_ppo.py -q -p no:cacheprovider
```

结果：

```text
7 passed in 18.22s
```

## 7. 当前阶段结论

本轮最重要的结论是：

```text
长 episode BER 升高的主因不是 tail，而是 CIR 漂移后仍使用 fixed acquisition CIR。
```

decision-directed CIR 更新可以显著修复这个问题，但修复后 RL 的额外收益消失。因此当前研究路线需要再次收紧：

```text
神经均衡器 + 在线 CIR tracking 是前置条件；
RL-PEFT 只有在这个强接收机状态上还能进一步降低 BER，才是真正的 RL 创新贡献。
```

下一步不建议跑正式矩阵，也不建议继续优化 fixed-CIR 下的 RL。应该先把 proposed 标准接收机改为 `Offline NN + DD-CIR`，再围绕 `delay=40/SNR=10` 攻关，使其接近或超过 DFE-RLS。
