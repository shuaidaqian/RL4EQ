# 迭代记录：phase_tiny 500-step 训练与 DFE 差距分析

时间戳：2026-08-12 13:35:46

## 1. 本轮目标

继续上一轮路线：

1. 训练 `phase_tiny` impaired-aware Offline NN 到 500 steps。
2. 跑 3 delays × 4 SNR 的 phase_tiny 小矩阵。
3. 判断 Offline NN 是否已超过最强传统 baseline。
4. 若仍未超过，定位模型增强切入点，而不是盲目扩大 RL。

## 2. 训练执行

训练命令：

```powershell
.\.venv-gpu\Scripts\python.exe pretrain.py `
  --config configs/continual_ppo_phase_tiny.json `
  --stage all `
  --steps 500 `
  --batch-size 4 `
  --amp `
  --save-dir pretrained/phase_tiny_500steps_2026-08-12
```

训练耗时较长，前台命令在 30 分钟 timeout 后进程继续运行，最终成功写出：

```text
pretrained/phase_tiny_500steps_2026-08-12/model_best.pt
pretrained/phase_tiny_500steps_2026-08-12/model_final.pt
pretrained/phase_tiny_500steps_2026-08-12/last.pt
pretrained/phase_tiny_500steps_2026-08-12/model_config.json
pretrained/phase_tiny_500steps_2026-08-12/pretrain_metrics.json
```

环境检查：

```text
Python: D:\Research\RL4EQ\.venv-gpu\Scripts\python.exe
PyTorch: 2.11.0+cu128
CUDA: 12.8
GPU: NVIDIA GeForce GTX 1650
torch.cuda.is_available(): True
```

## 3. phase_tiny 小矩阵 compare

命令：

```powershell
.\.venv-gpu\Scripts\python.exe compare.py `
  --config configs/continual_ppo_phase_tiny.json `
  --method-group all `
  --pretrained pretrained/phase_tiny_500steps_2026-08-12/model_best.pt `
  --delays 20 30 40 `
  --snrs 0 5 10 15 `
  --num-seeds 1 `
  --frames 8 `
  --pilot-total 128 `
  --pilot-layout prefix `
  --impairment-profile phase_tiny `
  --resume `
  --output-dir logs/compare_phase_tiny_500steps_2026-08-12
```

结果文件：

```text
logs/compare_phase_tiny_500steps_2026-08-12/frame_metrics.jsonl
logs/compare_phase_tiny_500steps_2026-08-12/summary.json
```

## 4. 逐配置结果

表中 `gap = Offline NN BER - best traditional BER`，负数表示 Offline NN 超过当前最强 traditional baseline。

```text
delay snr | Offline NN | RL | best traditional | gap

20   0 dB | 0.1455 | 0.1455 | 0.1523 (DFE-RLS) | -0.0068
20   5 dB | 0.0710 | 0.0710 | 0.0508 (DFE-RLS) | +0.0202
20  10 dB | 0.0365 | 0.0365 | 0.0133 (DFE-RLS) | +0.0231
20  15 dB | 0.0241 | 0.0241 | 0.0091 (DFE-RLS) | +0.0150

30   0 dB | 0.1484 | 0.1484 | 0.1523 (DFE-RLS) | -0.0039
30   5 dB | 0.0729 | 0.0719 | 0.0579 (DFE-RLS) | +0.0150
30  10 dB | 0.0368 | 0.0368 | 0.0150 (DFE-RLS) | +0.0218
30  15 dB | 0.0228 | 0.0228 | 0.0088 (DFE-RLS) | +0.0140

40   0 dB | 0.1546 | 0.1546 | 0.1543 (DFE-RLS) | +0.0003
40   5 dB | 0.0710 | 0.0693 | 0.0505 (DFE-RLS) | +0.0205
40  10 dB | 0.0374 | 0.0374 | 0.0104 (DFE-RLS) | +0.0270
40  15 dB | 0.0215 | 0.0215 | 0.0062 (DFE-RLS) | +0.0153
```

## 5. 方法均值

```text
DFE-RLS                              0.0567
NN + Rule Modulation                 0.0650
RL-Modulated Neural Block Equalizer  0.0700
Offline NN only                      0.0702
NN + Fixed Modulation                0.0702
NN + Discrete PEFT Scheduler         0.0702
SC-FDE-MMSE                          0.0717
LMMSE-FIR                            0.1898
RLS Linear                           0.1905
LMS                                  0.2686
NLMS                                 0.3958
CFO-Corrected DFE-RLS                0.3980
CFO+DD-Phase LMMSE-FIR               0.3985
CFO-Corrected LMMSE-FIR              0.4008
CFO+DD-Phase DFE-RLS                 0.4070
```

## 6. 结果判断

### 6.1 有进展

相比 100-step，500-step Offline NN 更稳定：

- 已明显超过 LMMSE-FIR、RLS Linear、LMS、NLMS；
- 均值上略优于 SC-FDE-MMSE；
- 在 0 dB 的 delay=20/30 配置上，Offline NN 已略优于 DFE-RLS。

这说明 `phase_tiny` 下神经块均衡器确实有可学习空间，不是完全无效。

### 6.2 仍未达到论文目标

当前最强 traditional baseline 是 DFE-RLS。

Offline NN 仍在大多数中高 SNR 配置上输给 DFE-RLS：

```text
SNR 5/10/15 dB 的主要 gap 约为 0.014–0.027 BER。
```

RL 当前没有学出明显增益：

```text
RL-Modulated Neural Block Equalizer ≈ Offline NN only
```

Rule modulation 略优于 Offline NN：

```text
NN + Rule Modulation mean BER = 0.0650
Offline NN only mean BER = 0.0702
```

这说明动作/调制空间存在少量有效方向，但 PPO 还没有学会稳定选择。

## 7. 帧趋势分析

按 frame 聚合：

```text
frame 1: Offline 0.0673 | Rule 0.0597 | DFE 0.0484 | SC-FDE 0.0556
frame 2: Offline 0.0428 | Rule 0.0410 | DFE 0.0397 | SC-FDE 0.0445
frame 3: Offline 0.0538 | Rule 0.0512 | DFE 0.0447 | SC-FDE 0.0558
frame 4: Offline 0.0412 | Rule 0.0401 | DFE 0.0378 | SC-FDE 0.0497
frame 5: Offline 0.0645 | Rule 0.0562 | DFE 0.0495 | SC-FDE 0.0638
frame 6: Offline 0.0757 | Rule 0.0649 | DFE 0.0495 | SC-FDE 0.0577
frame 7: Offline 0.0961 | Rule 0.0938 | DFE 0.0731 | SC-FDE 0.0920
frame 8: Offline 0.1202 | Rule 0.1135 | DFE 0.1113 | SC-FDE 0.1547
```

结论：

- 所有方法后期都有退化；
- Offline NN 没有形成“帧数越多优势越明显”的趋势；
- Rule modulation 能减轻部分退化，但幅度有限；
- DFE-RLS 仍是当前最强、最稳定的 traditional baseline。

## 8. 本轮新增模型增强接口

为了下一轮改神经接收机本体，已给 `condition_from_cir()` 增加 phase-aware latent 接口：

```python
condition_from_cir(
    cir,
    snr_db,
    phase_residual=0.0,
    cfo_residual=0.0,
)
```

测试覆盖：

```text
tests/test_receiver_architecture.py::test_condition_from_cir_can_encode_phase_residual_features
```

当前只是接口准备，还没有把 phase residual 全面接入训练和 compare。原因是如果只在 compare 推理时加入 phase latent，而训练时没有对应分布，会造成训练/推理不一致。因此下一轮应系统接入：

```text
Adapt Pilot phase residual estimation
-> CIRCondition.latent_residual[0:2]
-> curriculum training
-> compare / online
```

## 9. 测试验证

相关测试：

```powershell
.\.venv-gpu\Scripts\python.exe -m pytest `
  tests/test_receiver_architecture.py `
  tests/test_traditional_baselines.py `
  tests/test_evaluation_contract.py::test_docs_share_single_research_contract `
  -q -p no:cacheprovider
```

结果：

```text
20 passed in 19.72s
```

全量测试：

```powershell
.\.venv-gpu\Scripts\python.exe -m pytest -q -p no:cacheprovider
```

结果：

```text
153 passed in 291.02s (0:04:51)
```

## 10. 下一步

当前不建议启动正式 RL 大矩阵。

原因：

```text
Offline NN only 尚未稳定超过最强 traditional baseline；
RL 当前没有明显优于 Offline NN only；
正式 RL 矩阵只会放大已有接收机短板。
```

下一步优先做神经接收机本体增强：

1. 把 Adapt Pilot phase residual / CFO residual 估计接入训练与推理。
2. 让 `CIRCondition.latent_residual` 显式携带相位状态。
3. 重新训练 phase_tiny/phase_light checkpoint。
4. 先要求 Offline NN only 超过 DFE-RLS/SC-FDE-MMSE。
5. 再启动 PPO，让 RL 学习何时使用 Rule-like modulation 或 PEFT 动作。

当前最关键判断：

```text
研究方向仍有希望，但下一步必须增强神经接收机本体；
不能继续把主要资源投入 PPO，直到 Offline NN 本身超过 DFE-RLS。
```
