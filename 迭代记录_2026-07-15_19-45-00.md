# 迭代记录：方向A诊断闭环 + 方向C伪导频门控

生成时间：2026-07-15 19:45:00  
架构约束：保持 `开发框架.md` 不变。  
本轮目标：

1. 将当前架构、不足、论文目标和方向A/C路线整理成 Markdown。
2. 按方向A执行固定 seeds 策略诊断。
3. 按方向C执行 pseudo-pilot data-aided online adaptation。
4. 验证最终效果，输出可视化结果。
5. 反思是否符合预期，并清理冗余缓存。

## 1. 文档输出

新增：

```text
研究路线整理_方向A_C_2026-07-15_19-45-00.md
```

内容包括：

- 当前 Offline/Online 两阶段实现架构。
- 当前方法相对 MMSE/RLS 的不足。
- 论文主线建议：不要写成“RL 直接全面超过传统均衡”，应写成“model-assisted RL pseudo-pilot residual PEFT equalizer”。
- 方向A：固定 seeds、固定 PEFT、PPO、pilot-oracle、data-oracle、MMSE 的诊断闭环。
- 方向C：neural/MMSE 一致性驱动的 pseudo-pilot data-aided adaptation。

## 2. 代码修改

### 2.1 方向C：按 action 配置伪导频门控

修改文件：

```text
agent/adaptation_controller.py
online_train.py
tests/test_parameter_efficient_adaptation.py
```

主要变化：

1. `AdaptationStrategy` 新增伪导频门控参数：

```text
pseudo_neural_threshold
pseudo_mmse_threshold
pseudo_max_ratio
```

2. `pseudo-both-fast` 使用较保守门控：

```text
neural_threshold = 0.85
mmse_threshold = 0.65
max_ratio = 0.25
```

3. `pseudo-both-deep` 使用更宽松门控：

```text
neural_threshold = 0.75
mmse_threshold = 0.60
max_ratio = 0.40
```

4. `_build_pseudo_labels()` 支持：

```text
max_ratio
return_stats
```

5. 新增 `_pseudo_labels_for_strategy()`：

```text
非 pseudo action: 不使用 data 伪标签
pseudo action: 按当前 action 的阈值和比例生成伪标签
```

6. 在线记录和策略诊断新增指标：

```text
pseudo_count
pseudo_ratio
pseudo_conf_mean
neural_mmse_disagreement
```

### 2.2 方向A：策略诊断输出伪导频统计

原先策略诊断已经能输出：

```text
skip / fixed PEFT / PPO / pilot-oracle / data-oracle / MMSE
delta_pilot_loss_vs_delta_ber_data
BER_pilot_vs_BER_data
```

本轮补齐：

```text
每个策略的 pseudo_count / pseudo_ratio / pseudo_conf_mean / neural_mmse_disagreement
```

这样可以判断 pseudo action 是否真的使用了 data-aided 信息，而不是只改变学习率/步数。

## 3. 测试结果

执行：

```bash
python -B -m pytest -p no:cacheprovider tests/test_parameter_efficient_adaptation.py -q
```

结果：

```text
40 passed in 21.94s
```

新增测试：

1. `test_pseudo_gate_thresholds_change_selected_pseudo_labels`
   - 验证不同门控阈值和 `max_ratio` 会改变伪导频选择比例。
2. `test_strategy_diagnostics_returns_fixed_ppo_oracle_and_correlation`
   - 验证方向A诊断输出中包含 pseudo 统计字段。

## 4. 验证实验

使用 checkpoint：

```text
pretrained/selective_distill_2026-07-15_00-18-50/model_best.pt
```

原因：

```text
pretrained/model_best.pt 与当前模型结构不兼容。
其权重 input_proj 为 125 维，而当前 channel-encoder 结构会拼接 channel embedding。
selective_distill checkpoint 的 input_proj 为 128 维，可与当前 use_mmse_features + channel_encoder 结构严格匹配。
```

实验配置：

```text
SNR = 10 dB
profile = Rayleigh
num_frames = 12
seed = 2400
d_model = 128
n_layers = 3
adapter_rank = 16
window_K = 14
use_channel_encoder = True
channel_dim = 64
use_sync_head = True
sync_dim = 48
use_mmse_features = True
reset_each_frame = True
```

输出目录：

```text
logs/direction_A_C_2026-07-15_19-45-00/
```

关键文件：

```text
logs/direction_A_C_2026-07-15_19-45-00/summary.json
logs/direction_A_C_2026-07-15_19-45-00/direction_A_strategy_diagnostics/strategy_diagnostics.json
logs/direction_A_C_2026-07-15_19-45-00/direction_A_strategy_diagnostics/strategy_matrix.png
logs/direction_A_C_2026-07-15_19-45-00/direction_A_strategy_diagnostics/pilot_data_correlation.png
logs/direction_A_C_2026-07-15_19-45-00/direction_C_online_pseudo/online_adapt_metrics.png
```

## 5. 方向A结果：策略诊断

Rayleigh, SNR=10 dB, 12 frames：

| 方法 | BER_data |
|---|---:|
| skip | 0.05664 |
| head-slow | 0.05664 |
| head-fast | 0.05664 |
| adapter-slow | 0.05664 |
| adapter-fast | 0.05241 |
| both-slow | 0.05664 |
| both-fast | 0.05208 |
| both-deep | 0.04688 |
| pseudo-both-fast | 0.05436 |
| pseudo-both-deep | 0.05273 |
| cfo-light | 0.05404 |
| cfo-strong | 0.05176 |
| PPO policy | 0.05306 |
| pilot-oracle action | 0.04688 |
| data-oracle action | 0.04590 |
| MMSE | 0.01432 |

相关性：

| 指标 | 数值 |
|---|---:|
| `delta_pilot_loss_vs_delta_ber_data` | 0.60524 |
| `BER_pilot_vs_BER_data` | 0.55271 |

伪导频使用情况：

| 方法 | BER_data | pseudo_ratio |
|---|---:|---:|
| pseudo-both-fast | 0.05436 | 0.24154 |
| pseudo-both-deep | 0.05273 | 0.39062 |

### 方向A反思

不完全符合预期。

正面结果：

- 诊断闭环可运行。
- pilot 指标与 data BER 存在中等正相关，说明 pilot reward 并非完全无效。
- data-oracle action 从 `skip=0.05664` 改善到 `0.04590`，说明 action space 存在一定上限。

负面结果：

- data-oracle 仍显著弱于 MMSE `0.01432`，说明当前 action space 的上限不足。
- PPO `0.05306` 没有超过最好的固定策略 `both-deep=0.04688`。
- pseudo action 没有超过普通 `both-deep`，说明当前伪导频质量或使用方式还不足。

结论：

```text
方向A证明了当前 RL action/reward 有可诊断上限，但这个上限不足以支撑“超过传统均衡”的论文结论。
下一步应优先提升 action space 和 pseudo-label 质量，而不是继续增加 PPO 训练轮数。
```

## 6. 方向C结果：在线伪导频适应

Rayleigh, SNR=10 dB, 12 frames：

| 方法 | BER_data | BER_pilot | latency_ms | pseudo_ratio |
|---|---:|---:|---:|---:|
| PEFT/PPO + pseudo action space | 0.04720 | 0.03581 | 38.95 | 0.06250 |
| MMSE | 0.01953 | 0.01107 | 0.00 | 0.00000 |
| RLS | 0.01790 | 0.01302 | 0.00 | 0.00000 |
| LMS | 0.04850 | 0.05013 | 0.00 | 0.00000 |
| ZF | 0.12402 | 0.11719 | 0.00 | 0.00000 |
| matched filter | 0.10645 | 0.09831 | 0.00 | 0.00000 |

PEFT/PPO 平均指标：

```text
BER_data = 0.04720
BER_pilot = 0.03581
pilot_loss = 0.09831
adapt_params = 44348.92
adapt_steps = 1.00
latency_ms = 38.95
parameter_delta_norm = 0.00440
pseudo_count = 16.00
pseudo_ratio = 0.06250
pseudo_conf_mean = 0.19102
neural_mmse_disagreement = 0.00863
```

### 方向C反思

不符合“超过 MMSE/RLS”的预期。

原因判断：

1. PPO 实际选择 pseudo action 的频率有限，在线平均 `pseudo_ratio=0.0625`，低于策略诊断中 pseudo action 的可用比例。
2. 当前 pseudo 标签由 neural/MMSE 一致性生成，本质上仍受 MMSE 判决边界限制；如果目标是超过 MMSE，仅靠一致性伪标签很难纠正 MMSE 自身错误。
3. data-oracle action 也只能到 `0.04590`，说明当前 PEFT action space 的可达性能远弱于 MMSE/RLS。
4. RLS 在该场景下仍是强 baseline，且之前审计显示它没有 data label 泄漏。

结论：

```text
方向C已完成工程闭环，但当前效果只能支撑“模型辅助伪导频在线自适应的诊断研究”，不能支撑“RL 均衡优于传统强 baseline”的主结论。
```

## 7. 后续建议

按优先级执行：

1. **候选 action probing observation**
   - 对每个 action 做 pilot-only 虚拟更新。
   - 将每个候选 action 的 `pilot_loss_after`、`pilot_BER_after`、`parameter_delta`、`pseudo_count` 输入 policy。
   - 目的：让 PPO 不再只看当前帧状态，而能看到“每个动作试探后的 pilot 可观测后果”。

2. **pseudo-label gate 从一致性改为纠错型门控**
   - 当前 gate 只选择 neural/MMSE 一致样本，主要增加训练量，不能修正 MMSE 错误。
   - 建议加入 disagreement-aware gate：
     - 高 neural 置信但 MMSE 低置信；
     - neural 与 MMSE 分歧但 pilot 邻域残差支持 neural；
     - 用 residual head 输出 correction confidence。

3. **把 data-oracle upper bound 作为每轮硬指标**
   - 如果 data-oracle 不接近或超过 MMSE，说明 RL policy 再强也无用。
   - 先让 oracle action 上限超过 MMSE/RLS，再训练 PPO。

4. **修复主 checkpoint 配置一致性**
   - `pretrained/model_best.pt` 与 `model_config.json` 不一致。
   - 后续应让在线阶段自动读取 checkpoint 内或同目录配置，并拒绝加载不匹配配置。

5. **转向 MMSE residual correction 的论文表述**
   - 与其声称 RL 直接均衡更强，不如强调：
     - MMSE 是 teacher/reference；
     - neural residual 修正传统模型失配；
     - RL 负责选择高价值伪导频和更新强度；
     - 优势应在 CFO/Doppler/非线性/低 pilot overhead 场景中验证。

## 8. 本轮结论

本轮完成了方向A和方向C的工程落地与验证：

- 方向A：固定 seeds 策略诊断、oracle 上限、pilot-data 相关性可视化已经可用。
- 方向C：伪导频门控 action 已支持按策略配置阈值和比例，并输出伪导频诊断指标。
- 测试通过：`40 passed`。
- 当前性能仍弱于 MMSE/RLS，不能作为“超过传统均衡”的论文证据。

下一步应聚焦：

```text
candidate action probing + disagreement-aware pseudo gate + checkpoint 配置一致性
```

而不是继续单纯扩大 PPO 训练规模。
