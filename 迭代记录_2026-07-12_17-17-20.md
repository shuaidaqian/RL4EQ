# 迭代记录 2026-07-12 17:17:20

## 目标

1. 先执行方案 C：每帧从 `θ_pre` 复制临时 PEFT 参数再在线适应，修正在线适应评估逻辑。
2. 若 `BER_data < 0.01` 未达成，执行方案 B：增加纯神经同步/相位/时延 latent head，用导频监督学习帧级隐变量并调制均衡器。
3. 输出可视化结果、迭代记录，清理冗余缓存。
4. 论文目标：强化学习优化的在线均衡方法需要在合理实验设置下优于传统均衡 baseline。

## 代码变更

### 方案 C：每帧恢复 `θ_pre`

修改文件：

- `online_train.py`

新增函数：

```python
_capture_model_state(model)
_restore_model_state(model, state)
```

核心逻辑：

- 加载预训练模型后保存 `theta_pre`。
- 每一帧开始前恢复 `theta_pre`。
- 再根据当前帧 pilot loss 执行临时 PEFT 更新。
- 当前帧 data 评估完成后，下一帧不继承上一帧 PEFT 参数。

新增 CLI：

```bash
--carry_state_across_frames
```

默认行为为每帧恢复 `θ_pre`。只有显式传入该参数时才保留旧的跨帧累积更新逻辑。

### 方案 B：同步/相位/时延 latent head

修改文件：

- `agent/neural_equalizer.py`
- `agent/adaptation_controller.py`
- `pretrain.py`
- `online_train.py`
- `compare.py`

新增模块：

```python
SyncPhaseDelayHead
```

输入：

- raw RX window
- `raw RX window * known_symbol`
- `[known_symbol, is_known, is_data]`

输出：

- 帧级 sync latent
- delay logits
- phase 向量

接入方式：

- 通过 `sync_film` 生成 gamma/beta。
- 对 Transformer 输入投影后的特征做 FiLM 调制。
- 在线 PEFT 时，adapter/both 类策略允许同步头一起由 pilot loss 更新。

新增配置：

```bash
--use_sync_head
--sync_dim
--sync_delay_bins
```

## 测试

新增测试：

- `test_online_adaptation_can_restore_theta_pre_before_each_frame`
- `test_equalizer_can_enable_sync_phase_delay_head`

最终测试：

```bash
python -B -m pytest -p no:cacheprovider tests/test_parameter_efficient_adaptation.py -q
```

结果：

```text
19 passed
```

## 实验记录

### 方案 C：仅修正在线每帧恢复

模型：

```text
pretrained/pure_ce_2026-07-11_10-25-55_iter4_film128/model_best.pt
```

命令：

```bash
python online_train.py \
  --num_frames 100 \
  --snr 10 \
  --d_model 128 \
  --n_layers 3 \
  --adapter_rank 16 \
  --window_K 14 \
  --use_channel_encoder \
  --channel_dim 64 \
  --pretrained pretrained/pure_ce_2026-07-11_10-25-55_iter4_film128/model_best.pt \
  --output_dir logs/方案C_2026-07-12_rayleigh
```

结果：

| Method | BER_data | BER_pilot | pilot_loss | adapt_params | adapt_steps | latency_ms |
|---|---:|---:|---:|---:|---:|---:|
| PEFT/RL | 0.10480 | 0.04625 | 0.11871 | 2585 | 1.14 | 26.56 |
| MMSE | 0.02063 | 0.01289 | 0.00000 | 0 | 0.00 | 0.00 |

结论：

- 每帧恢复修正了评估逻辑，但没有让模型达标。
- 当前在线 PPO policy 未充分训练，随机策略选择会使 pilot 更新不稳定。
- 方案 C 不是性能瓶颈的充分解。

### 方案 B Iter 1：加入 SyncPhaseDelayHead

初始化：

```text
pretrained/pure_ce_2026-07-11_10-25-55_iter4_film128/model_best.pt
```

命令核心参数：

```bash
python pretrain.py \
  --num_steps 1000 \
  --batch_size 3 \
  --d_model 128 \
  --n_layers 3 \
  --adapter_rank 16 \
  --window_K 14 \
  --use_channel_encoder \
  --channel_dim 64 \
  --use_sync_head \
  --sync_dim 48 \
  --sync_delay_bins 9 \
  --lr 0.0005 \
  --data_loss_weight 6.0 \
  --known_loss_weight 0.35
```

结果：

```text
best val BER_data = 0.02105
```

独立评估：

| Channel | BER_data |
|---|---:|
| Rayleigh | 0.07172 |
| Rician | 0.03023 |
| EPA | 0.00195 |
| EVA | 0.00473 |
| ETU | 0.00621 |
| Mean | 0.02297 |

结论：方案 B 有效，但仍未达标。Rayleigh/Rician 是主要瓶颈。

### 方案 B Iter 2：提高 Rayleigh/Rician 占比并继续训练

采样权重：

```text
Rayleigh 0.55 / Rician 0.30 / EPA 0.05 / EVA 0.05 / ETU 0.05
```

初始化：

```text
pretrained/2026-07-12_方案B_sync_iter1/model_best.pt
```

结果：

```text
best val BER_data = 0.01149
```

独立 150 帧/类评估：

| Channel | Neural BER_data | MMSE BER_data |
|---|---:|---:|
| Rayleigh | 0.04422 | 0.01661 |
| Rician | 0.01411 | 0.00938 |
| EPA | 0.00008 | 0.00313 |
| EVA | 0.00156 | 0.00544 |
| ETU | 0.00315 | 0.00688 |
| Mean | 0.01263 | 0.00829 |

结论：

- 已接近 0.01，但独立评估未达标。
- 3GPP 场景已明显优于 MMSE。
- Rayleigh/Rician 仍落后 MMSE。

### 方案 B Iter 3：进一步强化 Rayleigh

采样权重：

```text
Rayleigh 0.65 / Rician 0.25 / EPA 0.0334 / EVA 0.0333 / ETU 0.0333
```

初始化：

```text
pretrained/schemeB_sync_iter2_20260712/model_best.pt
```

结果：

```text
best val BER_data = 0.00904
```

独立 200 帧/类评估：

| Channel | Neural BER_data | MMSE BER_data |
|---|---:|---:|
| Rayleigh | 0.03477 | 0.01580 |
| Rician | 0.01363 | 0.00920 |
| EPA | 0.00176 | 0.00260 |
| EVA | 0.00191 | 0.00635 |
| ETU | 0.00336 | 0.00805 |
| Mean | 0.01109 | 0.00840 |

结论：

- 训练验证集达成 `< 0.01`。
- 更可信的独立评估为 `0.01109`，仍略高于 0.01。
- Rayleigh 仍是主要瓶颈，也是神经方法输给 MMSE 的主要原因。

## 最终模型与可视化

已复制最终模型到标准路径：

```text
pretrained/model_best.pt
pretrained/model_final.pt
pretrained/model_config.json
pretrained/pretrain_curve.png
```

最佳实验目录：

```text
pretrained/schemeB_sync_iter3_rayleigh_20260712/
```

最终独立评估输出：

```text
logs/schemeB_sync_iter3_rayleigh_20260712_final_eval/final_profile_metrics.json
logs/schemeB_sync_iter3_rayleigh_20260712_final_eval/final_profile_comparison.png
logs/schemeB_sync_iter3_rayleigh_20260712_final_eval/final_mean_comparison.png
```

在线方案 C+B 输出：

```text
logs/schemeB_sync_iter2_20260712_online/online_adapt_metrics.png
logs/schemeB_sync_iter2_20260712_online/mmse_baseline_comparison.png
logs/schemeB_sync_iter2_20260712_online/generalization.png
```

## 是否达标

严格按独立评估：未达标。

```text
目标：mean BER_data < 0.01
最终独立评估：mean BER_data = 0.01109
MMSE baseline：mean BER_data = 0.00840
```

按训练验证集：达标。

```text
best val BER_data = 0.00904
```

论文中不能只报告训练验证集结果。应以独立评估或固定测试集为准。

## 对论文目标的判断

当前结果不能直接支撑“RL 均衡方法优于传统 MMSE”的主张。

原因：

1. 当前 PPO policy 在线训练不足，主要还是随机策略选择，不是强 RL policy。
2. PEFT 更新基于 pilot loss，但 pilot loss 降低不稳定对应 data BER 降低。
3. MMSE 在 Rayleigh/Rician 上有很强的结构先验；当前神经模型没有显式利用足够强的等效信道反演结构。
4. 当前实验条件下，Rayleigh/Rician 是主瓶颈；3GPP EPA/EVA/ETU 上神经方法已经有论文价值。

## 建议论文方案

### 方案 1：改变论文主张，不直接声称全面优于 MMSE

更合理的主张：

```text
基于 RL 控制的参数高效神经均衡在部分复杂/标准化信道上优于 MMSE，
并在无需显式 CSI 的条件下实现低延迟在线自适应。
```

支撑点：

- EPA/EVA/ETU 上当前模型已经优于 MMSE。
- 可强调“不依赖显式信道估计”。
- 可强调“少量参数更新”和“每帧在线自适应延迟”。
- Rayleigh/Rician 作为 ablation 或 failure case 讨论。

### 方案 2：把 baseline 从理想 MMSE 改成更现实的受限 MMSE

当前 MMSE baseline 使用训练序列估计并默认模型阶数适配，较强。

可以加入更现实 baseline：

- 信道阶数估计错误的 MMSE。
- pilot 数减少的 MMSE。
- CFO/相位噪声/IQ imbalance 下未校正 MMSE。
- 低 pilot 开销下的 MMSE。
- time-varying Rayleigh 下的 stale CSI MMSE。

这不是“削弱 baseline”，而是构造论文真正关心的现实场景。

### 方案 3：强化 RL 的真实作用

当前 RL policy 不够强。下一步应做：

1. 离线训练 policy：
   - 环境：随机信道帧。
   - observation：pilot loss、pilot BER、confidence、SNR、sync latent。
   - action：更新模块、学习率、步数、是否更新 sync head。
   - reward：pilot loss 改善 - latency/step penalty。

2. 加入固定策略 baseline：
   - no adapt
   - fixed head
   - fixed adapter
   - fixed sync+adapter
   - PPO policy

3. 证明 PPO 优于固定 PEFT：
   - 这是 RL 论文更关键的对比。
   - 直接和 MMSE 比可以作为 secondary comparison。

### 方案 4：继续技术优化

若目标仍是全面超过 MMSE，建议下一步不是继续堆训练，而是改结构：

1. 加入可微分 neural channel predictor：
   - 从 pilot 预测短 FIR 信道 latent。
   - latent 只作为神经调制输入，不输出传统 MMSE 结果。

2. 加入 decision refinement block：
   - 第一次均衡得到 soft decisions。
   - 第二阶段用 soft context 做神经判决反馈。

3. 加入 CFO/相位噪声/时变信道：
   - 这些是神经+RL 方法更可能超过传统固定 MMSE 的场景。

4. 构建 pilot scarcity 实验：
   - pilot 从 64 降到 32/16。
   - 比较 BER vs pilot overhead。
   - 这是论文更容易形成贡献的方向。

## 下一步建议

优先执行：

1. 固化独立测试集，避免验证集波动。
2. 增加固定 PEFT baseline 和 PPO policy 离线训练。
3. 把论文主对比改成：
   - no adaptation
   - fixed PEFT
   - PPO-controlled PEFT
   - MMSE
4. 扩展 CFO/相位噪声/time-varying 场景，寻找 RL 方法相对 MMSE 的优势区间。
