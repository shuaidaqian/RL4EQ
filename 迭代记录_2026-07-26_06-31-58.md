# 迭代记录：Level B Gate、真实 Continual PPO 开发实验与正式矩阵阻塞说明

时间戳：2026-07-26 06:31:58 +08:00

## 1. 本轮执行原则

本轮严格按用户指定顺序推进：

1. 先核对分支与环境。
2. 替换并复核 `CurriculumTrainer._validate_level_b()` 的真实 Level B 九配置 gate。
3. 先让 Perfect-CIR / 块展开式检测 gate 达到每个 Level B 主配置 `BER_data < 0.01`。
4. 再完成 Best Fixed 搜索，要求每个主配置 `BER_data < 0.1`。
5. 再用真实 Reward/Data paired samples 验证 Spearman `>= 0.6`。
6. 上述 gate 全部通过后，才启动 Continual PPO `5 seeds × 300 frames` 开发实验。
7. 只有开发实验达标后，才允许进入 `10 seeds × 1000 frames` 正式配对矩阵。

本轮没有把未完成或不合格的正式矩阵伪装成完成结果。

## 2. 分支、环境与基线测试

工作区：

```text
D:\Research\RL4EQ
```

当前分支：

```text
codex/continual-ppo-unfolded-equalizer
```

分支基点：

```text
HEAD      = c5a966b24241c451e338888599420b02822f1fc5 加本轮未提交修改
main      = 69b406481939788205762b2288553faa4f74dcbb
RL4EQ/main= 69b406481939788205762b2288553faa4f74dcbb
merge-base(HEAD, main)=69b406481939788205762b2288553faa4f74dcbb
```

GPU 环境：

```text
Python 3.12.7
torch 2.11.0+cu128
CUDA runtime 12.8
GPU: NVIDIA GeForce GTX 1650
```

fresh 全量测试：

```powershell
.\.venv-gpu\Scripts\python.exe -m pytest -q -p no:cacheprovider
```

结果：

```text
80 passed in 157.17s
```

## 3. 本轮发现并修复的关键可信度问题

### 3.1 真实 tail 泄漏

审查发现原主路径将 `frame.tail_symbols` 直接传给检测器。第二个普通帧之后，`frame.tail_symbols` 来自上一帧真实发送尾部，可能包含 Data 区域真实符号。对于跨帧 ISI 任务，这相当于隐藏的数据标签泄漏。

已修复：

- `training/curriculum.py`
- `training/meta_training.py`
- `training/continual_ppo.py`
- `compare.py`

修复方式：

- episode 开始时使用 acquisition 给出的 `start.initial_soft_tail` 初始化 `ReceiverState`。
- 主 gate、online runner 和 compare runner 检测时传入 `receiver_state.soft_tail`。
- 每帧检测后用 `result.soft_tail` 更新 receiver state。
- `compare.py` 中每个 method 维护独立 `ReceiverState`，避免不同方法共享 tail 状态。

新增回归测试：

```text
tests/test_continual_ppo.py::test_real_online_experiment_uses_receiver_soft_tail_after_first_frame
```

该测试确认第二帧首次检测使用上一帧检测器输出的 `soft_tail`，而不是环境真实 `frame.tail_symbols`。

### 3.2 Best Fixed Data tie-breaker

审查发现 `evaluate_best_fixed_level_b()` 原先在 Reward Pilot BER 相同情况下使用 `mean_ber_data` 作为 tie-breaker。这样会让 Data BER 参与候选选择。

已修复：

```python
best = min(
    scored,
    key=lambda item: (
        item["mean_reward_pilot_ber"],
        item["candidate"]["cg_iterations"],
        item["candidate"]["refine_iterations"],
    ),
)
```

现在 Best Fixed 候选选择只使用：

1. Reward Pilot BER。
2. 非 Data 的确定性复杂度 tie-breaker。

Data BER 只用于仿真 gate 与报告。

新增回归测试：

```text
tests/test_meta_adaptation.py::test_best_fixed_selection_does_not_use_data_ber_as_tiebreaker
```

该测试构造 Reward Pilot 完全打平但 Data BER 相反的场景，确认选择不会受 Data BER 影响。

### 3.3 删除英文数据标签上界术语残留

代码、测试和当前文档中的英文数据标签上界术语字符串已清除。正式方法列表不包含任何数据标签上界方法。

保留的概念表述改为中文“数据标签上界”，只用于说明“禁止使用”或 forbidden 自检字段，不作为方法名或正式结果。

### 3.4 将 online runner 从固定检测器改为最小真实 Continual PPO 闭环

原 `training/continual_ppo.py` 文档中明确写着：

```text
当前 PPO policy 使用已通过 gate 的固定强动作初始化；每 32 帧记录一次 policy update。
后续可在同一 schema 下替换为真正的 PPO 梯度更新。
```

这意味着不能把原 runner 当成真实 Continual PPO 开发实验。

本轮已接入最小真实闭环：

- `ObservationEncoder` 从接收端可见信息构造 observation。
- `ContinualPolicy.sample()` 每帧采样分层动作。
- 动作影响检测迭代数、refine 次数和判决导向 CIR 跟踪强度。
- 动作前后只用 Reward Pilot loss 改善计算 reward。
- Data BER 只写入仿真评估字段。
- 每 `update_interval` 帧按 rollout 执行 clipped PPO 更新。
- 在线结果写出：
  - `policy_learning = clipped_ppo_reward_pilot`
  - `policy_action_mode`
  - `policy_loss`
  - `reward_pilot_loss_before`
  - `reward_pilot_loss_after`

新增测试：

```text
tests/test_continual_ppo.py::test_real_online_experiment_reports_level_b_frame_metrics
tests/test_continual_ppo.py::test_safe_policy_prior_starts_from_best_fixed_like_action
```

### 3.5 安全先验

第一次 sanity run 暴露出随机 PPO 首轮动作会明显劣化：

```text
delay=40, SNR=10, 5 seeds × 30 frames
mean_ber_data = 0.0141493056
actions = skip/update/random mixed
policy updates = 0，因为 30 < update_interval 32
```

根因：

```text
未经初始化的策略近似均匀随机，部署初期会选择大量 skip/rollback/弱检测动作。
```

修复：

```text
_initialize_safe_policy_prior(policy)
```

默认偏向 Best Fixed 等价安全动作：

```text
mode = detector-refine
detector iteration choice = 6
实际检测约等价 cg_iterations=32, refine_iterations=2
```

修复后 sanity：

```text
delay=40, SNR=10, 5 seeds × 30 frames
mean_ber_data = 0.0027430556
max_frame_ber_data = 0.0364583321
actions = detector-refine × 150
policy updates = 0，因为 30 < update_interval 32
```

## 4. Level B 三个前置 gate 结果

命令：

```powershell
.\.venv-gpu\Scripts\python.exe scripts\run_level_b_gates.py `
  --config configs\continual_ppo.json `
  --output-dir logs\gates_level_b_20260726 `
  --frames-per-config 5 `
  --seeds 0 1 2 3 4 `
  --pilot-total 128 `
  --pilot-layout two_block
```

输出摘要：

```text
Perfect-CIR gate:
  gate_pass = true
  mean_ber_data = 0.000289351860475209
  max_config_ber_data = 0.0013020833721384406

Best Fixed gate:
  gate_pass = true
  selected = cg_iterations=32, refine_iterations=2
  selection_metric = mean_reward_pilot_ber
  mean_reward_pilot_ber = 0.0002777777777777778
  mean_ber_data = 0.0004976851948433452
  max_config_ber_data = 0.001770833358168602

Reward/Data Spearman gate:
  gate_pass = true
  spearman = 0.7967869465504047
  num_pairs = 36
```

结论：

```text
三个前置 gate 全部通过。
```

注意：这些结果是在修复真实 tail 泄漏之后重新跑出的，不沿用旧结果。

## 5. Continual PPO 5 seeds × 300 frames 开发矩阵

执行方式：

- Level B 主配置：`delay ∈ {20,30,40}` × `SNR ∈ {10,15,20}`。
- 每配置 `5 seeds × 300 frames`。
- `update_interval = 32`。
- 每配置 45 次 PPO policy update。
- Data BER 只用于仿真评估。

逐配置结果：

| delay | SNR | mean BER_data | PPO update 次数 |
|---:|---:|---:|---:|
| 20 | 10 | 0.0043315972 | 45 |
| 20 | 15 | 0.0032239584 | 45 |
| 20 | 20 | 0.0025503472 | 45 |
| 30 | 10 | 0.0038802084 | 45 |
| 30 | 15 | 0.0033958333 | 45 |
| 30 | 20 | 0.0016302083 | 45 |
| 40 | 10 | 0.0057934028 | 45 |
| 40 | 15 | 0.0039652778 | 45 |
| 40 | 20 | 0.0025503472 | 45 |

整体：

```text
overall_mean_ber_data = 0.0034801312
worst_config = delay=40, SNR=10
worst_config_mean_ber_data = 0.0057934028
```

结论：

```text
开发矩阵按“每个主配置 BER_data < 0.01”门槛达标。
```

## 6. 正式 10 seeds × 1000 frames 配对矩阵状态

正式矩阵本轮没有启动。

原因不是 gate 未通过，而是继续运行前存在两个必须解决的问题：

### 6.1 `compare.py` 的 Continual PPO 仍不是同一真实 PPO runner

虽然 `online_train.py` 现在已经是真实最小 Continual PPO 闭环，但 `compare.py` 中的 `"Continual PPO"` 方法仍走 `_run_real_method()` 的固定检测器设置：

```text
Continual PPO -> acquisition_cir, cg_iterations=32, refine_iterations=2
```

这会导致正式配对矩阵中的 `"Continual PPO"` 实际是 Best Fixed 风格检测器，而不是在线策略闭环。因此不能直接运行并作为正式 PPO 结果。

需要下一步修复：

```text
compare.py 必须把 Continual PPO 接入与 online_train.py 相同的 policy state / rollout / reward / update 逻辑，
并与 baseline 使用相同 seed、相同接收帧、相同信道轨迹。
```

### 6.2 当前检测器速度不适合直接跑 12 方法正式矩阵

实测耗时：

```text
单配置 5 seeds × 300 frames 的 Continual PPO 分片：
约 20 分钟左右。
```

估算：

```text
Continual PPO 单独 9 配置 × 10 seeds × 1000 frames：
约 20 小时量级。

如果 compare.py 中 12 个方法逐方法都跑块检测：
耗时会进一步放大到多天量级。
```

正式矩阵启动前建议先做：

1. 将 Continual PPO 真实 runner 合并进 paired compare。
2. 对 baseline 检测结果去重或共享同配置检测。
3. 优化 `baseline/block_equalizers.py::_coordinate_refine_single()` 的逐符号 Python loop。
4. 增加正式矩阵 resume / append-safe 机制，避免长运行中断后重跑。

## 7. 当前结论与不能声称的内容

可以声称：

- Level B 三个前置 gate 已在修复 true-tail 泄漏后重新通过。
- Best Fixed 候选选择不再使用 Data BER 作为 tie-breaker。
- Reward/Data Spearman gate 通过，当前 grouped paired samples 的相关性为 `0.7968`。
- `online_train.py` 已接入最小真实 Continual PPO 闭环，开发矩阵 9 个主配置均低于 `BER_data < 0.01`。

不能声称：

- 不能声称正式 10 seeds × 1000 frames 配对矩阵已完成。
- 不能声称 Continual PPO 在正式矩阵中显著优于传统 baseline。
- 不能声称当前 `compare.py` 的 `"Continual PPO"` 已经是完整 online PPO 闭环。
- 不能把旧数据标签上界结果写入当前研究结论。

## 8. 后续最小实施路径

下一步应按以下顺序继续：

1. 重构 `compare.py`：
   - baseline 方法使用同一批帧；
   - `"Continual PPO"` 调用真实 online policy state；
   - 每个方法保持独立 receiver state；
   - Data BER 只在评估写出；
   - Reward Pilot 只用于 PPO 动作后 reward；
   - 所有结果写统一 schema。

2. 给正式矩阵增加 resume：
   - 每个 `delay/SNR/seed/method` 完成后立即 append JSONL；
   - 已完成组合跳过；
   - summary 可从 JSONL 重建。

3. 优化检测器热点：
   - 优先优化 `_coordinate_refine_single()`；
   - 至少减少重复检测；
   - formal matrix 不应重复运行完全相同的 `(cir, tail, cg, refine)`。

4. 重新跑 formal matrix：
   - `10 seeds × 1000 frames`；
   - Level B 主平均；
   - Level C 单独压力测试；
   - paired mean/std/95% CI；
   - 不混入未见压力场景到主平均。

## 9. 本轮文件变化摘要

主要新增：

```text
scripts/run_level_b_gates.py
```

主要修改：

```text
agent/continual_policy.py
training/continual_ppo.py
training/curriculum.py
training/meta_training.py
compare.py
tests/test_continual_ppo.py
tests/test_meta_adaptation.py
tests/test_evaluation_contract.py
docs/superpowers/plans/2026-07-23-continual-ppo-extreme-delay.md
docs/superpowers/specs/2026-07-23-continual-ppo-extreme-delay-design.md
迭代记录_2026-07-25_23-11-00.md
```

## 10. 清理计划

保留：

```text
logs/gates_level_b_20260726/
logs/ppo_dev_20_10_300/
logs/ppo_dev_20_15_300/
logs/ppo_dev_20_20_300/
logs/ppo_dev_30_10_300/
logs/ppo_dev_30_15_300/
logs/ppo_dev_30_20_300/
logs/ppo_dev_40_10_300/
logs/ppo_dev_40_15_300/
logs/ppo_dev_40_20_300/
```

删除：

```text
__pycache__/
*/__pycache__/
logs/ppo_sanity_40_10_30/
logs/ppo_sanity_40_10_30_safe_prior/
logs/gates_level_b_20260726_stdout.log
logs/gates_level_b_20260726_stderr.log
```

这些删除项是缓存或中间 sanity 产物，不作为正式证据保存。
