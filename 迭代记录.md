# 纯神经 Offline/Online 均衡迭代记录

## 目标

本轮目标是验证并优化“完全不掺传统均衡算法”的主链路：

- Offline 输入：raw received IQ window + `known_symbol` + `is_known` + `is_data`
- Online 输入：raw received IQ window + 已知导频/训练符号元信息 + pilot loss / 历史状态
- 禁止：MMSE / LS / DFE / 频域均衡 / 时域均衡输出进入主模型输入或 RL 更新
- 允许：MMSE 仅作为 baseline 对比

目标指标：

```text
SNR = 10 dB
信道 = Rayleigh 16 抽头 + 3GPP EPA + 3GPP EVA + 3GPP ETU
主指标 = 四类信道平均 BER_data
目标 = mean BER_data < 0.01
```

## 主链路清理

已完成：

- `pretrain.py` 改回 raw IQ 状态，不再导入 `env.equalized_state`。
- `agent/adaptation_controller.py` 改回 raw IQ 状态，不再导入 `env.equalized_state`。
- `online_train.py` 的神经模型推理使用 raw IQ 状态。
- `MMSEEqualizer` 仅保留在 `online_train.py::evaluate_mmse()` 和 baseline 对比中。
- 删除 `env/equalized_state.py`。
- 增加测试 `test_main_training_path_does_not_import_traditional_equalizer_state()`，防止主训练链路重新引入传统均衡状态。

## 状态定义

当前纯神经状态：

```text
state_dim = 2 * (2K + 1) + 3

前 2 * (2K + 1) 维:
  raw received IQ window

末 3 维:
  known_symbol: train/pilot 位置为真实 BPSK 符号，data 位置为 0
  is_known: train/pilot 为 1，data 为 0
  is_data: data 为 1，train/pilot 为 0
```

默认 `K=10` 时，`state_dim=45`。

## 迭代结果

### Iter1: raw64 基线

命令：

```bash
python pretrain.py --num_steps 500 --batch_size 4 --d_model 64 --n_layers 2 --adapter_rank 8 --save_dir pretrained\pure_iter1_raw64 --val_interval 50 --val_frames 10 --snr_min 10 --snr_max 10 --data_loss_weight 4.0 --known_loss_weight 0.5
```

结果：

```text
best val mean BER_data = 0.09902
```

结论：

- 纯 raw IQ 主链路可以学习到信道结构。
- 但距离 `<0.01` 仍差约一个数量级。

### Iter2: raw128 大模型

命令：

```bash
python pretrain.py --num_steps 800 --batch_size 4 --d_model 128 --n_layers 3 --adapter_rank 16 --save_dir pretrained\pure_iter2_raw128 --val_interval 100 --val_frames 10 --snr_min 10 --snr_max 10 --data_loss_weight 4.0 --known_loss_weight 0.5
```

结果：

```text
best val mean BER_data = 0.02900
```

独立 50 帧分项评估：

```text
Rayleigh = 0.13445
EPA      = 0.00039
EVA      = 0.00727
ETU      = 0.00914
mean     = 0.03781
```

结论：

- 增大模型容量明显有效。
- 3GPP EPA/EVA/ETU 基本达标。
- Rayleigh 16 抽头是主要瓶颈。

### Iter3: Rayleigh16 加权采样

改动：

```text
训练采样增加 Rayleigh16 固定目标信道权重。
```

命令：

```bash
python pretrain.py --num_steps 1000 --batch_size 4 --d_model 128 --n_layers 3 --adapter_rank 16 --save_dir pretrained\pure_iter3_rayleigh_weighted --val_interval 100 --val_frames 10 --snr_min 10 --snr_max 10 --data_loss_weight 4.0 --known_loss_weight 0.5
```

结果：

```text
best val mean BER_data = 0.02090
```

独立 80 帧分项评估：

```text
Rayleigh = 0.11479
EPA      = 0.00161
EVA      = 0.01284
ETU      = 0.01147
mean     = 0.03518
```

结论：

- 验证集有改善，但独立评估仍显示 Rayleigh 泛化不足。

### Iter4: 扩大 raw window 到 K=16

改动：

```text
支持 --window_K。
K=16 时 state_dim=69。
```

命令：

```bash
python pretrain.py --num_steps 1000 --batch_size 4 --d_model 128 --n_layers 3 --adapter_rank 16 --window_K 16 --save_dir pretrained\pure_iter4_k16_raw128 --val_interval 100 --val_frames 10 --snr_min 10 --snr_max 10 --data_loss_weight 4.0 --known_loss_weight 0.5
```

结果：

```text
best val mean BER_data = 0.03945
```

结论：

- 增加窗口没有解决 Rayleigh 瓶颈。
- 瓶颈不是简单的局部 ISI 覆盖不足。

### Iter5: Rayleigh16 70% 采样，训练更久

命令：

```bash
python pretrain.py --num_steps 1500 --batch_size 4 --d_model 128 --n_layers 3 --adapter_rank 16 --save_dir pretrained\pure_iter5_rayleigh70 --val_interval 150 --val_frames 10 --snr_min 10 --snr_max 10 --data_loss_weight 4.0 --known_loss_weight 0.5
```

结果：

```text
best val mean BER_data = 0.01758
```

独立 100 帧分项评估：

```text
Rayleigh = 0.08117
EPA      = 0.00012
EVA      = 0.00586
ETU      = 0.01367
mean     = 0.02521
```

结论：

- 更高 Rayleigh 权重继续改善 Rayleigh。
- 仍未达到 `<0.01`。

### Iter6: Rayleigh16 90% 采样，验证帧数增大

命令：

```bash
python pretrain.py --num_steps 2000 --batch_size 4 --d_model 128 --n_layers 3 --adapter_rank 16 --save_dir pretrained\pure_iter6_rayleigh90 --val_interval 200 --val_frames 30 --snr_min 10 --snr_max 10 --data_loss_weight 4.0 --known_loss_weight 0.5
```

结果：

```text
best val mean BER_data = 0.01335
```

独立 100 帧分项评估：

```text
Rayleigh = 0.05133
EPA      = 0.00531
EVA      = 0.00273
ETU      = 0.00793
mean     = 0.01683
```

结论：

- 继续逼近目标。
- Rayleigh 仍是唯一主瓶颈。

### Iter7: 从 Iter6 低学习率 Rayleigh 微调

命令：

```bash
python pretrain.py --num_steps 1000 --batch_size 4 --d_model 128 --n_layers 3 --adapter_rank 16 --save_dir pretrained\pure_iter7_rayleigh_finetune --init_checkpoint pretrained\pure_iter6_rayleigh90\model_best.pt --lr 1e-4 --val_interval 100 --val_frames 50 --snr_min 10 --snr_max 10 --data_loss_weight 4.0 --known_loss_weight 0.5
```

结果：

```text
best val mean BER_data = 0.01246
```

最终独立 100 帧分项评估：

```text
Rayleigh = 0.03840
EPA      = 0.00941
EVA      = 0.00473
ETU      = 0.00582
mean     = 0.01459
```

在线 80 帧 Rayleigh 评估：

```text
PEFT BER_data = 0.04751
MMSE BER_data = 0.03540
```

结论：

- 当前最佳纯神经模型。
- 平均 `BER_data=0.01459`，未达到 `<0.01`。
- 在线随机 PPO/PEFT 更新没有进一步改善 Rayleigh，反而略差。

### Iter8: 更大模型 raw192

命令：

```bash
python pretrain.py --num_steps 1200 --batch_size 4 --d_model 192 --n_layers 4 --adapter_rank 24 --save_dir pretrained\pure_iter8_raw192 --val_interval 150 --val_frames 50 --snr_min 10 --snr_max 10 --data_loss_weight 4.0 --known_loss_weight 0.5
```

结果：

```text
best val mean BER_data = 0.01709
```

结论：

- 单纯增大 Transformer 容量没有突破。
- 当前瓶颈更像是 raw IQ 纯神经结构对 Rayleigh 16 抽头信道的条件化方式不足，而不是参数量不足。

## 最终结果

最佳模型：

```text
pretrained/pure_iter7_rayleigh_finetune/model_best.pt
pretrained/pure_neural_best.pt
```

最终独立评估：

```text
Rayleigh BER_data = 0.03840
EPA      BER_data = 0.00941
EVA      BER_data = 0.00473
ETU      BER_data = 0.00582
mean     BER_data = 0.01459
```

MMSE baseline：

```text
Rayleigh BER_data = 0.03004
EPA      BER_data = 0.00277
EVA      BER_data = 0.00461
ETU      BER_data = 0.00859
mean     BER_data = 0.01150
```

结论：

```text
未达到 mean BER_data < 0.01。
当前最佳纯神经平均 BER_data = 0.01459。
```

## 可视化输出

```text
logs/final_pure_neural_generalization/profile_ber.png
logs/final_pure_neural_generalization/profile_ber.json
logs/final_pure_neural_online/online_adapt_metrics.png
logs/final_pure_neural_online/mmse_baseline_comparison.png
logs/final_pure_neural_online/generalization.png
logs/final_pure_neural_snr/snr_mmse_comparison.png
```

## 未达标原因分析

1. **Rayleigh 16 抽头是主瓶颈。**  
   3GPP EPA/EVA/ETU 已基本达到或接近 `<0.01`，但 Rayleigh 仍在 `0.03840`。

2. **raw IQ 纯神经模型缺少显式信道条件化机制。**  
   MMSE/LS 能通过训练序列显式估计信道，当前模型只能靠 Transformer 从 raw window 和 known symbol 中隐式学习，Rayleigh 随机相位和 16 抽头 ISI 下更难。

3. **在线 PEFT/RL reward 只看 pilot loss，pilot 已接近 0，无法有效指导 data 段进一步下降。**  
   Iter7 中 pilot BER 几乎为 0，但 data BER 仍有明显误差，说明 pilot loss 对 data 段剩余错误不够敏感。

4. **单纯加大模型没有解决结构性问题。**  
   Iter8 的 1.3M 参数模型不如 Iter7，说明需要更强归纳偏置，而不是继续堆参数。

## 建议的下一步优化方案

我建议下一步不要继续盲目训练，而是在以下方案中选择一个或多个执行。

### 方案 A：纯神经信道编码器，不输出传统均衡结果

加入一个 Neural Channel Encoder：

```text
输入: train/pilot 的 raw rx + known_symbol
输出: latent channel embedding z_h
Neural Equalizer 输入: raw IQ window + known_symbol + z_h
```

注意：这里不输出 LS/MMSE 信道估计，也不做传统均衡；只是神经网络自己从 pilot 学一个隐变量。  
这是最符合“不掺传统均衡算法”的下一步。

预期：

```text
Rayleigh BER_data 可能从 0.038 降到 0.01 附近。
```

### 方案 B：数据段伪标签/一致性在线目标

当前在线 reward 只来自 pilot。可增加不依赖真实 Data 标签的目标：

```text
前后两次增强接收信号的预测一致性
低置信 data 样本熵惩罚
高置信 data 伪标签自训练
```

风险：

```text
伪标签错误会放大 burst error，需要很谨慎。
```

### 方案 C：改为序列到序列整帧 Equalizer

当前逐位置输出虽然通过 Transformer 看到整帧，但 loss 是逐 bit BCE。可以加入结构化约束：

```text
因果/非因果双分支
data block 级上下文 pooling
pilot-conditioned attention
```

预期：

```text
改善长 ISI 下 data 段错误传播。
```

### 方案 D：加入编码系统目标

如果最终通信系统允许 LDPC，则可以把目标从 raw bit BER 改为：

```text
Neural Equalizer soft output -> LDPC BP decode -> decoded BER
```

这可能较容易达到 `<0.01`，但目标不再是“均衡器裸 BER”。

## 当前建议

我建议优先执行 **方案 A：纯神经信道编码器**。

原因：

- 不违反“不掺传统均衡算法”。
- 直接针对 Rayleigh 瓶颈。
- 比继续加大 Transformer 更有针对性。
- 与你的 Offline/Online 框架一致：offline 学信道，online 用 pilot 学调整。
