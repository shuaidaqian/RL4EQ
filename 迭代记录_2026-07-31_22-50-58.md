# 迭代记录：baseline 修复后正式矩阵结果分析

时间戳：2026-07-31 22:50:58  
工作区：`D:\Research\RL4EQ`  
主线：Level B 极端稀疏长回波信道下的 Continual PPO 块均衡  
结果目录：`logs/formal_affected_baseline_fix_20260728`

## 1. 本轮结论

本轮完成了 baseline 修复后的受影响方法正式矩阵，并对结果做了完整性校验和配对分析。结论必须分开表述：

1. 修复前旧矩阵不能作为最终 baseline 对比依据。
2. 修复后公平矩阵已经完成，数据完整。
3. Continual PPO 达到了绝对 BER 门槛：每个 Level B 主配置平均 `BER_data < 0.01`。
4. Continual PPO 没有超过 Best Fixed；修复后 Best Fixed 明显更强。
5. 当前“PPO 作为第一主贡献进一步降低 BER 并优于固定策略”的研究假设不成立，需要调整。
6. 下一阶段应优先分析 PPO 为什么退化到弱于固定策略，而不是继续扩大实验矩阵。

## 2. 为什么 baseline 修复前的旧矩阵不能作为最终对比

旧矩阵目录：

```text
logs/formal_10seed_1000
```

旧矩阵完整性本身没有问题：

```text
行数 = 1,080,000
方法数 = 12
每方法行数 = 90,000
summary.json = 已生成
```

但旧矩阵的受影响 baseline 实现不公平。具体问题是：

- Continual PPO 的主动作 `detector-refine` 不只是固定检测迭代，它还在每帧后执行 decision-directed CIR update。
- 旧的 `Best Fixed`、`Sparse CIR + Kalman/RLS`、`Drift-Aware Pilot Rule`、`Contextual Bandit` 等 baseline 只维护 receiver soft tail，没有维护同等的在线 CIR 跟踪状态。
- 因此旧矩阵中的这些 baseline 实际变成“固定 acquisition-CIR 跑 1000 帧”，在信道慢变的 Level B episode 中会随时间漂移失效。
- 这会系统性削弱 baseline，导致 PPO 看起来相对 baseline 有巨大优势。

旧矩阵中部分受影响方法的总体 `BER_data`：

| 方法 | 旧矩阵 mean BER_data |
|---|---:|
| Best Fixed | 0.17112578 |
| Sparse CIR + Kalman/RLS | 0.17439470 |
| Drift-Aware Pilot Rule | 0.17439470 |
| Contextual Bandit | 0.17439470 |
| Continual PPO | 0.00530035 |

这个结果不能作为最终论文对比，因为它没有保证 fixed/rule/bandit baseline 与 PPO 使用等价的 online receiver state 更新能力。

## 3. 本轮修复内容

修复目标是让受影响 baseline 与 PPO 使用公平的在线接收机状态维护。

核心修改：

- 将 decision-directed CIR update 抽为公共函数：

```text
agent/cir_estimator.py::decision_directed_cir_update()
```

- `training/continual_ppo.py`、`training/meta_training.py` 和 `compare.py` 复用同一 CIR 更新逻辑。
- `Best Fixed` gate 每帧后更新 CIR。
- `compare.py` 中以下方法每帧后更新 CIR：

```text
Sparse CIR + Kalman/RLS
DFE-RLS
Best Fixed
Drift-Aware Pilot Rule
Contextual Bandit
```

- 静态 baseline 保持不更新：

```text
Perfect-CSI Block
Block LMMSE/CG
Analytic Iterative BPSK
Legacy LMMSE-FIR
Legacy DFE
No Adapt
```

- 为 `compare.py` 增加 `--methods`，用于只重跑受影响方法。

新增一致性测试：

```text
tests/test_evaluation_contract.py::test_compare_best_fixed_matches_gate_online_cir_and_tail_dynamics
tests/test_evaluation_contract.py::test_compare_cli_can_run_selected_methods_for_affected_matrix
```

相关测试与全量测试均已通过：

```text
tests/test_evaluation_contract.py + tests/test_continual_ppo.py:
42 passed

full pytest:
84 passed
```

## 4. 修复后公平矩阵完成情况

新矩阵目录：

```text
logs/formal_affected_baseline_fix_20260728
```

矩阵规模：

```text
6 methods × 9 Level B configs × 10 seeds × 1000 frames = 540,000 rows
```

完整性校验：

| 检查项 | 结果 |
|---|---:|
| `frame_metrics.jsonl` 行数 | 540,000 |
| 目标行数 | 540,000 |
| `summary.json` | 已生成 |
| 重复 key | 0 |
| NaN/Inf BER | 0 |
| 方法集合 | 正确 |
| 每方法行数 | 90,000 |
| 每方法每配置行数 | 10,000 |
| PPO `policy_learning` 字段 | 正常 |
| PPO `policy_loss` 非空行数 | 2,790 |

方法集合：

```text
Best Fixed
Contextual Bandit
Continual PPO
DFE-RLS
Drift-Aware Pilot Rule
Sparse CIR + Kalman/RLS
```

## 5. 修复后总体结果

按全部 90,000 帧级样本求平均：

| 方法 | mean BER_data |
|---|---:|
| Best Fixed | 0.00257448 |
| DFE-RLS | 0.00325321 |
| Sparse CIR + Kalman/RLS | 0.00330556 |
| Contextual Bandit | 0.00330556 |
| Drift-Aware Pilot Rule | 0.00330579 |
| Continual PPO | 0.00530055 |

按 90 个 `delay/SNR/seed` 单元统计的均值与 95% 置信区间：

| 方法 | mean BER_data | 95% CI |
|---|---:|---:|
| Best Fixed | 0.00257448 | ±0.00044135 |
| DFE-RLS | 0.00325321 | ±0.00050276 |
| Sparse CIR + Kalman/RLS | 0.00330556 | ±0.00050012 |
| Contextual Bandit | 0.00330556 | ±0.00050012 |
| Drift-Aware Pilot Rule | 0.00330579 | ±0.00050012 |
| Continual PPO | 0.00530055 | ±0.00069198 |

结论：

- PPO 已经进入低 BER 区间。
- 但在公平 baseline 修复后，Best Fixed 是当前最强方法。
- PPO 不是当前矩阵中的最优在线策略。

## 6. PPO 绝对 BER 门槛

PPO 每个 Level B 主配置结果：

| delay | SNR | PPO mean BER_data | 是否低于 0.01 |
|---:|---:|---:|---|
| 20 | 10 | 0.00604505 | 是 |
| 20 | 15 | 0.00343281 | 是 |
| 20 | 20 | 0.00338958 | 是 |
| 30 | 10 | 0.00740677 | 是 |
| 30 | 15 | 0.00441016 | 是 |
| 30 | 20 | 0.00438802 | 是 |
| 40 | 10 | 0.00923516 | 是 |
| 40 | 15 | 0.00487734 | 是 |
| 40 | 20 | 0.00452005 | 是 |

最差配置：

```text
delay = 40
SNR = 10 dB
PPO mean BER_data = 0.00923516
```

结论：

```text
PPO 达到“每个主配置 BER_data < 0.01”的绝对目标。
```

## 7. PPO 未超过 Best Fixed

按配对 `delay/SNR/seed` 单元计算 `baseline - PPO`。正数表示 baseline 比 PPO 更差，即 PPO 更好；负数表示 baseline 比 PPO 更好。

| baseline | baseline - PPO mean | 95% CI | PPO 胜出单元 |
|---|---:|---:|---:|
| Best Fixed | -0.00272607 | ±0.00034398 | 0 / 90 |
| DFE-RLS | -0.00204734 | ±0.00032945 | 0 / 90 |
| Sparse CIR + Kalman/RLS | -0.00199499 | ±0.00033011 | 0 / 90 |
| Contextual Bandit | -0.00199499 | ±0.00033011 | 0 / 90 |
| Drift-Aware Pilot Rule | -0.00199476 | ±0.00033016 | 0 / 90 |

结论：

- PPO 在 90 个 `delay/SNR/seed` 单元中没有一次超过 Best Fixed。
- PPO 也没有超过 DFE-RLS、Sparse CIR + Kalman/RLS、Rule、Bandit。
- 当前不能写“PPO 显著优于固定微调/规则/Bandit baseline”。

## 8. 每配置对比

| delay | SNR | Best Fixed | DFE-RLS | Sparse CIR + Kalman/RLS | Contextual Bandit | Drift-Aware Pilot Rule | Continual PPO |
|---:|---:|---:|---:|---:|---:|---:|---:|
| 20 | 10 | 0.00295807 | 0.00366354 | 0.00366875 | 0.00366875 | 0.00367057 | 0.00604505 |
| 20 | 15 | 0.00130000 | 0.00173958 | 0.00175729 | 0.00175729 | 0.00175729 | 0.00343281 |
| 20 | 20 | 0.00103151 | 0.00142526 | 0.00151458 | 0.00151458 | 0.00151458 | 0.00338958 |
| 30 | 10 | 0.00414870 | 0.00508490 | 0.00507917 | 0.00507917 | 0.00507917 | 0.00740677 |
| 30 | 15 | 0.00201563 | 0.00262370 | 0.00263724 | 0.00263724 | 0.00263724 | 0.00441016 |
| 30 | 20 | 0.00157500 | 0.00212318 | 0.00225052 | 0.00225052 | 0.00225078 | 0.00438802 |
| 40 | 10 | 0.00535260 | 0.00647708 | 0.00648021 | 0.00648021 | 0.00648021 | 0.00923516 |
| 40 | 15 | 0.00267917 | 0.00339479 | 0.00342005 | 0.00342005 | 0.00342005 | 0.00487734 |
| 40 | 20 | 0.00210964 | 0.00274688 | 0.00294219 | 0.00294219 | 0.00294219 | 0.00452005 |

这个表显示：

- 所有配置中 Best Fixed 都优于 PPO。
- PPO 的绝对 BER 达标，但相对强固定策略没有增益。
- 随 delay 增大、SNR 降低，所有方法 BER 都升高；PPO 的最差点仍在 `delay=40, SNR=10 dB`。

## 9. PPO 行为分析

PPO 动作分布：

| 动作 | 次数 | 占比 |
|---|---:|---:|
| detector-refine | 89,841 | 99.8233% |
| skip | 38 | 0.0422% |
| update-equalizer | 35 | 0.0389% |
| rollback | 30 | 0.0333% |
| joint-update | 29 | 0.0322% |
| update-channel | 27 | 0.0300% |

其他 PPO 指标：

```text
policy_loss 非空行数 = 2,790
mean reward = 0.1005105
```

初步解释：

- PPO policy 绝大多数时间选择 `detector-refine`，说明 safe prior 基本支配了策略。
- 少量探索动作没有带来比 Best Fixed 更低的 BER。
- PPO 的 reward 平均为正，但这个 reward 没有转化为 Data BER 上的相对优势。
- 当前 reward / action / update 机制可能只是学到“保持接近固定强策略”，但没有稳定找到超过 Best Fixed 的策略。

## 10. 当前研究假设需要调整

原假设：

```text
Continual PPO 通过在线选择适配动作，可以在 Level B 极端长回波信道中进一步降低 BER，并优于 Best Fixed / Rule / Bandit baseline。
```

当前结果支持的部分：

```text
Level B 极端长回波信道下，在线块接收机可以稳定达到 BER_data < 0.01。
Continual PPO 本身也达到了绝对 BER 门槛。
```

当前结果不支持的部分：

```text
Continual PPO 没有优于 Best Fixed。
Continual PPO 没有优于 DFE-RLS / Sparse CIR + Kalman/RLS / Rule / Bandit。
```

因此论文主张需要调整。当前不能把“PPO 显著优于固定策略”作为第一主贡献。

更稳妥的阶段性表述是：

```text
在 EME-inspired Level B 极端稀疏长回波块均衡场景下，严格 pilot/data 隔离的在线接收机框架能够实现 BER_data < 0.01；
然而，在公平引入在线 CIR 跟踪后，强 Best Fixed 策略仍优于当前 Continual PPO；
这说明 PPO 控制器的 reward、动作空间和探索机制仍未充分利用在线决策自由度。
```

## 11. 下一阶段优先问题：为什么 PPO 弱于固定策略

下一阶段不建议先扩大矩阵，也不建议直接写论文结论。优先分析 PPO 退化原因。

建议按以下顺序排查：

### 11.1 Reward 与 Data BER 的一致性

当前 PPO reward 主要来自 Reward Pilot loss 改善，但最终目标是降低 Data BER。

需要检查：

- 每种动作的 Reward Pilot loss 改善与 Data BER 改善是否仍保持强相关；
- 在修复 baseline 后，Reward Pilot 是否偏向鼓励 `detector-refine`；
- 单帧 Reward Pilot 过短时，reward 是否噪声过大；
- reward 惩罚项是否让策略不敢探索高成本但可能改善 Data BER 的动作。

### 11.2 Safe prior 是否压制探索

动作分布显示：

```text
detector-refine 占比 99.8233%
```

这意味着 PPO 基本没有探索有效动作空间。

需要检查：

- safe prior bias 是否过强；
- entropy bonus 是否太弱；
- PPO update interval / rollout 长度是否不足以改变策略；
- 低频动作是否有足够样本估计优势。

### 11.3 动作空间是否缺少“超过 Best Fixed”的可达动作

如果 Best Fixed 的 `cg=32, refine=2, alpha=0.2` 已经接近当前检测器能力上限，那么 PPO 只在同类动作附近微调，很难超过它。

需要检查：

- 是否存在任何非 Data oracle 的动作组合能在 Reward Pilot 上稳定超过 Best Fixed；
- 是否需要引入更细粒度的 CIR alpha、CG/refine 组合、rollback 条件、pilot 子块权重；
- 当前动作是否只是“选择检测强度”，而不是实质性 PEFT。

### 11.4 PPO 更新是否真的改变策略

虽然 `policy_loss` 有 2,790 次非空记录，但动作分布几乎不变。

需要检查：

- policy logits / entropy 随帧是否变化；
- value loss 是否主导 policy loss；
- advantage 是否被 reward 尺度压扁；
- recurrent hidden state 是否携带有用漂移信息。

### 11.5 Fixed baseline 是否已经足够强，应转移贡献点

修复后 Best Fixed 总体：

```text
mean BER_data = 0.00257448
```

这说明强固定在线 CIR 跟踪策略已经非常有效。后续有两种选择：

1. 继续把 PPO 作为第一主贡献，但必须先证明它能稳定超过 Best Fixed。
2. 调整论文主贡献，把 PPO 从“性能第一贡献”降级为“在线控制探索结果/负结果分析”，主贡献转向 benchmark、隔离协议、块接收机和公平评估。

当前结果更支持第二种，除非下一阶段能找到明确的 PPO 改进路径。

## 12. 当前可以写和不能写的论文结论

可以写：

- Level B 极端稀疏长回波信道是传统和神经接收机都需要重新评估的困难场景。
- 严格 Adapt/Reward/Data 隔离是必要的，否则容易产生 pilot loss 虚低或 data label 泄漏。
- 修复后公平比较表明，带在线 CIR 跟踪的 Best Fixed 是非常强的 baseline。
- 当前 PPO 绝对性能达标，但相对强固定策略没有优势。
- 该负结果说明 RL4EQ 中的 RL 组件必须面对强 fixed/rule baseline，而不能只和弱 baseline 比。

不能写：

- “PPO 显著优于 Best Fixed”。
- “PPO 显著优于 Rule/Bandit baseline”。
- “RL 是当前结果中降低 BER 的主要来源”。
- “旧矩阵证明 PPO 有巨大优势”。

## 13. 下一步建议

短期建议：

1. 固化当前公平矩阵结果，不再使用旧矩阵的受影响 baseline 数据。
2. 写一个 `analysis/formal_affected_baseline_fix_report.py`，自动输出本文档中的所有表格，避免后续手工统计出错。
3. 增加动作级诊断：
   - 每种动作的 Reward Pilot loss 改善；
   - 每种动作的 Data BER 改善；
   - action probability / entropy 随帧变化；
   - policy logits 是否从 safe prior 中真正移动。
4. 增加 Best Fixed 附近的非 RL 网格：
   - CIR alpha；
   - CG/refine；
   - rollback 策略；
   - Reward Pilot 长度；
   - update interval。
5. 如果非 RL 网格仍然优于 PPO，应调整论文定位，不把 PPO 优于 Best Fixed 作为核心 claim。

中期建议：

- 重新设计 PPO reward，使 reward 更直接约束 Data BER 的代理目标，但仍不能读取 Data 标签。
- 增加 per-config adaptive prior，而不是全配置统一 safe prior。
- 引入更强的 exploration schedule 或离线行为克隆初始化，但必须避免 Data oracle。
- 考虑把 RL 问题从“选择检测器动作”改为“选择信道跟踪策略/置信门控策略”，因为当前 Best Fixed 的优势主要来自稳定 CIR 跟踪。

## 14. 文件与数据状态

本轮关键输出：

```text
logs/formal_affected_baseline_fix_20260728/frame_metrics.jsonl
logs/formal_affected_baseline_fix_20260728/summary.json
迭代记录_2026-07-31_22-50-58.md
```

旧矩阵：

```text
logs/formal_10seed_1000
```

旧矩阵保留用于追溯 baseline 修复前的问题，但不能作为最终受影响 baseline 对比。

## 15. 最终判断

当前阶段的严谨结论：

```text
修复后公平矩阵已完成；
PPO 达到每个 Level B 主配置 BER_data < 0.01；
PPO 未超过 Best Fixed；
当前 PPO 第一主贡献假设不成立；
下一阶段应优先解释和修复 PPO 弱于固定策略的问题。
```
