# 迭代记录：DD 相位跟踪与 impaired-aware 预训练初筛

时间戳：2026-08-12 12:26:00

## 1. 本轮执行目标

按照上一轮“下一步建议”依次执行：

1. 给传统 baseline 增加 decision-directed phase tracker。
2. 跑 clean / CFO / phase / CFO+phase 复杂度阶梯扫描。
3. 判断是否需要更弱的 `tiny` 过渡档。
4. 做 impaired-aware Offline NN smoke/小规模训练。
5. 只有 Offline NN 站稳后再做 RL smoke。

当前仍遵守主线约束：

- 不加入硬件非线性；
- 不加入信道编码；
- 不加入高阶调制；
- 不恢复旧 A2C、旧逐符号 PPO、Data Oracle；
- Proposed 是唯一使用神经网络和 RL 的方法；
- 传统 baseline 不使用神经网络、不使用 RL、不读取 Reward/Data 标签。

## 2. 已完成实现

### 2.1 Traditional decision-directed phase tracker

扩展 `baseline/traditional_equalizers.py`：

新增方法：

```text
CFO+DD-Phase LMMSE-FIR
CFO+DD-Phase DFE-RLS
```

新增状态：

```python
TraditionalPhaseState
```

工作流：

```text
Adapt Pilot + CIR reference
-> pilot phase line fit
-> 平滑更新 phase_state
-> 反旋整帧
-> 传统 LMMSE/DFE 均衡
-> 用硬判决重构参考波形
-> decision-directed residual phase 更新 phase_state
```

标签隔离：

- 不读取 Reward Pilot 标签；
- 不读取 Data 标签；
- 不读取真实 CFO/相位；
- 不使用神经网络；
- 不使用 RL。

### 2.2 compare / evaluation 状态接入

`compare.py` 的 `BaselineMethodState` 新增：

```python
phase_state: TraditionalPhaseState | None
```

`evaluation/research_diagnostics.py` 的传统难度扫描也为每个 method 维护独立 `TraditionalPhaseState`。

### 2.3 tiny impairment profiles

由于 `cfo_light` 对传统方法过强，本轮新增更平滑过渡档：

```text
cfo_tiny
phase_tiny
cfo_phase_tiny
```

参数范围：

```text
cfo_tiny:       |CFO| = 0.0002–0.0008 cycles/symbol
phase_tiny:     phase_noise_std = 0.0002–0.0008
cfo_phase_tiny: 两者同时启用
```

已同步到：

- `env/impairments.py`
- `scripts/calibrate_traditional_difficulty.py`
- `compare.py`

### 2.4 phase_tiny 训练配置

新增：

```text
configs/continual_ppo_phase_tiny.json
```

该配置用于 impaired-aware Offline NN 初筛，不改变主配置 `configs/continual_ppo.json` 的默认 clean 设置。

## 3. 测试结果

全量测试命令：

```powershell
.\.venv-gpu\Scripts\python.exe -m pytest -q -p no:cacheprovider
```

结果：

```text
152 passed in 293.31s (0:04:53)
```

新增/覆盖测试：

- DD phase tracker 方法不读取 Reward/Data 标签；
- DD phase tracker 在多帧 CFO/相位 smoke 中不劣于单纯 CFO-Corrected baseline；
- `cfo_phase_tiny` 比 `cfo_phase_light` 更弱；
- traditional group 包含补偿与 DD phase tracking 方法；
- 文档主线仍保持“明显超过传统 + 合理补偿 + CFO/相位”。

## 4. 传统难度阶梯扫描

扫描设置：

```text
delay = 20
SNR = 0 / 5 / 10 / 15 dB
pilot_total = 128
layout = prefix
seed = 0
frames = 8
methods:
  DFE-RLS
  CFO-Corrected DFE-RLS
  CFO+DD-Phase DFE-RLS
  LMMSE-FIR
  CFO-Corrected LMMSE-FIR
  CFO+DD-Phase LMMSE-FIR
```

### 4.1 clean

```text
SNR 0:  best LMMSE-FIR, BER = 0.2630
SNR 5:  best LMMSE-FIR, BER = 0.1331
SNR 10: best LMMSE-FIR, BER = 0.0479
SNR 15: best LMMSE-FIR, BER = 0.0176
```

clean 下传统 baseline 随 SNR 提升明显变强，符合预期。

### 4.2 phase_tiny / phase_light

```text
phase_tiny:
SNR 0:  best LMMSE-FIR, BER = 0.2637
SNR 5:  best LMMSE-FIR, BER = 0.1673
SNR 10: best LMMSE-FIR, BER = 0.0954
SNR 15: best LMMSE-FIR, BER = 0.0609

phase_light:
SNR 0:  best LMMSE-FIR, BER = 0.2673
SNR 5:  best LMMSE-FIR, BER = 0.1686
SNR 10: best LMMSE-FIR, BER = 0.0951
SNR 15: best LMMSE-FIR, BER = 0.0615
```

结论：

- 慢相位扰动是较平滑、合理的复杂度阶梯；
- 传统 baseline 仍可工作，不是随机；
- 这是当前更适合做 Proposed 初筛的 impaired 场景。

### 4.3 cfo_tiny / cfo_light

```text
cfo_tiny:
SNR 0:  best LMMSE-FIR,              BER = 0.3460
SNR 5:  best LMMSE-FIR,              BER = 0.2461
SNR 10: best LMMSE-FIR,              BER = 0.1820
SNR 15: best CFO+DD-Phase LMMSE-FIR, BER = 0.0931

cfo_light:
SNR 0:  best CFO+DD-Phase LMMSE-FIR,  BER = 0.4844
SNR 5:  best CFO-Corrected LMMSE-FIR, BER = 0.4538
SNR 10: best CFO-Corrected LMMSE-FIR, BER = 0.2780
SNR 15: best CFO-Corrected LMMSE-FIR, BER = 0.2477
```

结论：

- 即使 `cfo_tiny`，低 SNR 下传统方法仍显著变难；
- `cfo_light` 对当前传统补偿来说太强，不能直接作为主平均；
- CFO 更适合作为下一阶段压力维度，而不是当前第一主场景。

### 4.4 cfo_phase_tiny / cfo_phase_light

```text
cfo_phase_tiny:
SNR 0:  best LMMSE-FIR,              BER = 0.3486
SNR 5:  best LMMSE-FIR,              BER = 0.2718
SNR 10: best CFO+DD-Phase LMMSE-FIR, BER = 0.2236
SNR 15: best CFO+DD-Phase LMMSE-FIR, BER = 0.0635

cfo_phase_light:
SNR 0:  best CFO+DD-Phase DFE-RLS,   BER = 0.4759
SNR 5:  best CFO-Corrected LMMSE-FIR, BER = 0.3447
SNR 10: best CFO+DD-Phase LMMSE-FIR, BER = 0.1563
SNR 15: best CFO-Corrected DFE-RLS,  BER = 0.1742
```

结论：

- CFO+phase 联合扰动在低 SNR 下仍偏强；
- 高 SNR 下有可研究空间；
- 低 SNR 不能直接要求 Proposed 全部超过传统且 BER 很低，需要继续校准。

## 5. impaired-aware Offline NN 初筛

### 5.1 phase_tiny smoke checkpoint

命令：

```powershell
.\.venv-gpu\Scripts\python.exe pretrain.py `
  --config configs/continual_ppo_phase_tiny.json `
  --stage all `
  --steps 2 `
  --batch-size 1 `
  --amp `
  --save-dir pretrained/phase_tiny_smoke_2026-08-12
```

结果：

```text
saved pretrained/phase_tiny_smoke_2026-08-12
```

2-step checkpoint 只验证链路，compare 中 Offline NN 仍接近随机：

```text
Offline NN only ≈ 0.4987
```

### 5.2 phase_tiny 100-step checkpoint

命令：

```powershell
.\.venv-gpu\Scripts\python.exe pretrain.py `
  --config configs/continual_ppo_phase_tiny.json `
  --stage all `
  --steps 100 `
  --batch-size 2 `
  --amp `
  --save-dir pretrained/phase_tiny_100steps_2026-08-12
```

结果：

```text
saved pretrained/phase_tiny_100steps_2026-08-12
```

compare 设置：

```text
delay = 20
SNR = 10 / 15
frames = 4
seed = 0
pilot_total = 128
profile = phase_tiny
```

关键结果：

```text
SNR 10:
Offline NN only                      BER = 0.0306
RL-Modulated Neural Block Equalizer  BER = 0.0306
LMMSE-FIR                            BER = 0.0462
RLS Linear                           BER = 0.0443
SC-FDE-MMSE                          BER = 0.0039
DFE-RLS                              BER = 0.0000

SNR 15:
Offline NN only                      BER = 0.0143
RL-Modulated Neural Block Equalizer  BER = 0.0143
LMMSE-FIR                            BER = 0.0267
RLS Linear                           BER = 0.0221
SC-FDE-MMSE                          BER = 0.0000
DFE-RLS                              BER = 0.0000
```

判断：

- 100-step Offline NN 已经明显离开随机区；
- 在 phase_tiny 的 SNR=10/15 上，Offline NN 已超过部分传统自适应方法，如 LMMSE-FIR/RLS Linear；
- 但仍没有超过最强传统 baseline：DFE-RLS 和 SC-FDE-MMSE；
- RL 当前与 Offline NN 基本一致，尚未产生增益。

## 6. RL smoke

命令：

```powershell
.\.venv-gpu\Scripts\python.exe online_train.py `
  --config configs/continual_ppo_phase_tiny.json `
  --pretrained pretrained/phase_tiny_100steps_2026-08-12/model_best.pt `
  --frames 8 `
  --num-seeds 1 `
  --window-size 4 `
  --update-interval 4 `
  --delays 20 `
  --snrs 10 `
  --pilot-total 128 `
  --pilot-layout prefix `
  --amp `
  --output-dir logs/online_phase_tiny_100steps_smoke_2026-08-12
```

结果：

```text
saved logs/online_phase_tiny_100steps_smoke_2026-08-12
```

这只说明 RL 链路能在 phase_tiny checkpoint 上运行，不说明 RL 已经优于 Offline NN 或传统 baseline。

## 7. 当前结论

当前方向比 clean Level B 更接近研究目标，但还没有达到最终论文目标。

明确结论：

1. `phase_tiny/phase_light` 是当前最平滑、最合理的 impaired 主攻候选。
2. `cfo_tiny/cfo_light` 对低 SNR 影响很强，暂时更适合作为压力维度。
3. 100-step phase_tiny Offline NN 已经有学习能力，超过部分传统方法。
4. Proposed 仍未超过最强传统 baseline：DFE-RLS / SC-FDE-MMSE。
5. 当前不应启动正式 10 seeds × 1000 frames RL 矩阵。
6. 下一步应继续增强神经接收机本体，而不是先加大 PPO。

## 8. 下一步建议

优先级如下：

### 8.1 继续训练 phase_tiny / phase_light Offline NN

建议下一轮：

```text
phase_tiny:
  steps = 500 或 1000
  batch_size = 4
  delays = 20/30/40
  SNR = 0/5/10/15

phase_light:
  作为验证 profile，不先混入主训练
```

目标：

```text
Offline NN only 在 phase_tiny 上逐配置超过 LMMSE/RLS，并尽量接近或超过 DFE-RLS/SC-FDE-MMSE。
```

### 8.2 增强神经块均衡器本体

如果 500/1000 steps 后仍无法超过 DFE-RLS/SC-FDE-MMSE，应优先改模型，而不是继续调 PPO：

- 增加 unfolded iterations；
- 显式输入 phase/CFO-aware condition；
- 让 conditioner 使用 pilot phase residual 特征；
- 加入跨帧 hidden/state 或相位状态；
- 对 SC-FDE-MMSE 强 baseline 做专门对抗分析。

### 8.3 RL 只在 Offline NN 站稳后扩大

只有当：

```text
Offline NN only 接近或超过最强 traditional baseline
```

再启动：

```text
RL-Modulated Neural Block Equalizer
```

否则 PPO 只是搜索一个还不够强的接收机的扰动动作，难以稳定超过传统。

## 9. 是否符合预期

部分符合，部分不符合。

符合：

- 已建立 CFO/phase 复杂度阶梯；
- 已补传统合理补偿和 DD phase tracker；
- 已验证 phase_tiny 下 NN 能从随机 BER 降到 0.01–0.03 级别；
- 工程链路和测试稳定。

不符合：

- Proposed 尚未超过最强传统 baseline；
- RL 尚未带来可见增益；
- CFO 相关场景对低 SNR 仍过强，需要作为压力测试或继续细化。

这说明当前不是“方向完全错了”，而是需要把主攻点从 PPO 提前转到“神经接收机本体 + phase-aware 条件建模”。只有接收机本体站住，RL 的在线调度才有意义。
