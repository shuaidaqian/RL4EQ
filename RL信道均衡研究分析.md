# RL 信道均衡研究分析：EME 启发的极端长时延扩展方向

## 1. 研究方向重新定义

本项目不再把主要目标设为“在常见 Rayleigh、Rician 或 3GPP 短时延信道上全面超过传统均衡算法”。对于近似线性、信道记忆较短且训练序列充分的场景，LMMSE、RLS 和 DFE 已经利用了明确的卷积结构和统计先验。神经网络需要同时学习信道估计、逆滤波和判决边界，往往难以稳定超过经过调优的传统方法。

新的研究问题是：当信道具有少量强回波，但最大相对时延达到 20–40 个符号时，能否通过离线大规模监督学习获得一个长记忆神经均衡器，再利用在线 Pilot 和强化学习控制的少参数微调，跟踪具体 episode 内缓慢变化的信道？

## 2. EME 启发而非完整物理 EME

地月反射通信具有巨大的绝对传播时延、极高路径损耗、频率漂移和月面散射。绝对传播时延主要影响链路同步和交互时延；完成粗同步后，一个整体平移并不会直接形成符号间干扰。因此本项目不把约 2.5 秒往返传播时延直接塞入 512 符号帧中。

本项目提取与均衡直接相关的 EME 启发特征：稀疏强回波、长相对时延、长程 ISI、路径相位随机和跨帧慢变复增益。这是一种可控的极端信道压力测试，而不是对特定月面、天线、带宽和载频条件的物理复现。

## 3. 为什么极端长时延适合神经均衡

传统 FIR 均衡器必须随时延扩展增长滤波器长度。Pilot 有限时，长滤波器带来更高估计方差、更大的矩阵求解开销和更慢的 RLS 收敛。稀疏长回波还会产生间隔很大的干扰关系，短局部滤波器无法覆盖，盲目扩大窗口又会显著增加参数量。

神经模型可以用扩张卷积高效覆盖多个时间尺度，再用 Transformer 建模整帧的长距离依赖。Pilot Conditioner 提取当前帧的信道条件，Adapter 则提供小规模在线可塑性。这一结构不是保证必然超过传统算法，而是为长记忆条件提供更匹配的表达能力。

## 4. 离线阶段是否使用 Pilot + Data

只使用固定 `Pilot + Data` 帧做离线训练存在两个问题：模型可能记忆固定 Pilot 位置和固定 PN 序列；基础均衡能力也可能过度依赖已知符号。完全不使用 Pilot 又会导致 Pilot Conditioner 和在线接口没有经过训练。

因此离线阶段采用两部分：

1. 完整随机序列长记忆预训练。所有位置都按未知 Data 处理，模型先学习从接收序列恢复 BPSK 比特的通用能力。
2. 随机 Pilot 条件对齐训练。Pilot 总长度在 48/64/80 中变化，PN 序列随帧变化，训练 Pilot Conditioner 和未来在线开放的 Adapter/输出头。

这一方案同时保留通用均衡能力和在线接口一致性。

## 5. Adapt Pilot 与 Reward Pilot 必须分离

旧式实现常把当前 Pilot 的真实发送符号直接拼到同一位置的网络输入，再使用该位置的 Pilot loss 做 reward。模型可以复制标签而不是完成均衡，得到很低的 Pilot loss，但这种改善不一定传递到 Data。

新帧将 64 个已知符号拆为 Adapt Pilot 48 和 Reward Pilot 16。Adapt Pilot 用于条件提取、observation 和梯度更新；Reward Pilot 标签在动作执行前不可见，只用于动作后的留出性能。这样 reward 测量的是微调对未参与更新的已知符号是否具有泛化，而不是训练集记忆。

## 6. 强化学习的职责

PPO 不直接决定每个 Data 比特。离线神经均衡器负责信号恢复，PPO 只在每帧选择是否更新、更新 Adapter 或输出头、使用何种学习率以及更新多少步。

这一定位的研究问题是资源受限在线学习中的控制：不同信道状态下，固定更新策略可能过度更新、更新不足或造成遗忘，PPO 能否利用 Pilot 指标和历史趋势选择更合适的参数高效动作？

在线 reward 只来自 Reward Pilot loss 改善，并扣除更新步数、更新参数量和参数变化范数。`BER_data` 只能作为仿真评估指标。

## 7. 公平 baseline 与证据链

正式比较包括 LMMSE-FIR、DFE-RLS、预训练无微调、固定 PEFT、Pilot 规则控制、PPO-PEFT，以及只作诊断的 Data Oracle。

传统 baseline 和神经方法必须使用同一接收帧、同一 Adapt Pilot 和同一信道种子。传统算法不能访问 Reward Pilot 或 Data 标签。Data Oracle 可以使用 Data 标签枚举动作，但只能说明动作空间上限，不能作为可部署方法。

研究成功需要同时满足两层证据：离线神经均衡器在极端长时延场景优于最强传统 baseline；PPO 在线策略进一步优于无微调、固定微调和简单规则。如果第一层失败，说明模型或训练分布不足；如果第二层失败，说明 RL observation、reward 或动作空间没有提供额外价值。

## 8. 实验矩阵

主实验最大时延为 20、30、40 个符号，SNR 为 -5、0、5、10、15、20 dB。每个配置使用 5 个固定随机种子并进行配对比较。外推实验使用训练未见的 50 符号最大时延和 8–10 条路径。

必须报告 `BER_data`、两类 Pilot BER、Pilot loss、在线更新参数量、更新步数、参数变化、CPU/GPU latency 和 generalization。结果报告均值、标准差和 95% 置信区间。

## 9. 当前可以和不可以声称的结论

当前代码建立了新的信道、模型、Pilot 留出 reward、PEFT 控制和公平比较框架。短 smoke 或单种子实验只能验证实现可运行，不能证明方法优于传统算法。

在完整多种子实验完成前，可以声称“提出并实现了一套面向极端稀疏长时延扩展的离线预训练与在线 RL 微调框架”；不可以声称“神经均衡全面超过传统方法”或“PPO 显著优于固定微调”。这些结论必须等待真实重跑数据和置信区间支持。

## 10. 2026-07-20 单种子中等规模可行性结果

本轮在 CPU 版 PyTorch 上完成了计划中的中等规模验证：离线 Stage A 1000 步、Stage B 1000 步、batch size 4；在线 PPO 10 个 episode、每个 50 帧；比较矩阵覆盖最大时延 20/30/40 和 SNR 0/10/20，每个配置 50 帧、单个随机种子。

离线阶段验证结果：

| 阶段 | 验证 `BER_data` |
|---|---:|
| 无 Pilot 长记忆基础训练 Stage A | 0.49005 |
| Pilot 条件对齐训练 Stage B | 0.20681 |

Stage A 在 1000 步内基本没有形成稳定的无条件均衡能力；Stage B 后 BER 明显下降，说明当前网络更依赖 Pilot Conditioner 提供的帧级条件。后续完整训练应重点检查 Stage A 的训练分布、优化难度以及是否需要更明确的局部逆卷积辅助目标，而不能把 Stage B 的改善简单解释为模型已经学会通用盲均衡。

在线 500 帧汇总：

| 指标 | 数值 |
|---|---:|
| `BER_data` | 0.16955 |
| `BER_adapt_pilot` | 0.15896 |
| `BER_reward_pilot` | 0.17075 |
| `pilot_loss` | 0.29983 |
| `latency_ms` | 61.98 |
| `generalization` | 0.24777 |
| 平均 reward | -0.00464 |

PPO 训练阶段使用了全部 7 种动作，但平均 reward 为负，说明更新带来的 Reward Pilot 改善通常不足以抵消步数、参数量和参数漂移惩罚。

统一比较的聚合结果：

| 方法 | 平均 `BER_data` |
|---|---:|
| LMMSE-FIR | 0.46202 |
| DFE-RLS | 0.36807 |
| Pretrained / No Adapt | 0.23144 |
| Fixed PEFT | 0.23140 |
| Pilot Rule | 0.23145 |
| PPO-PEFT | 0.23144 |
| Data Oracle | 0.22988 |

这组单种子结果提供两条不同的信号：

1. 在当前构造的极端稀疏长回波场景中，长上下文神经模型明显优于当前实现的两个传统 baseline，支持继续研究这一场景；但仍需多种子评估和更充分的传统算法调优后才能形成正式结论。
2. 在线微调几乎没有产生额外收益，连使用 Data 标签枚举动作的 Oracle 上限也只比无微调低约 0.00156。当前瓶颈更可能是 Adapter/输出头动作空间的可改善上限，而不只是 PPO 没有学会选择动作。

因此下一阶段优先级应是先提高可微调参数空间的有效上限，例如把 Adapter 放置、Pilot Conditioner 可调范围和适配损失重新做消融；只有 Data Oracle 能稳定明显优于预训练模型后，继续优化 PPO 才有充分意义。
