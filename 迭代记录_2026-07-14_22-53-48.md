# 迭代记录 2026-07-14 22:53:48

本轮目标：在保持 `开发框架.md` 不变的前提下，依次完成：

1. 增加常用传统均衡算法 baseline，并在所有信道上对比。
2. 固定独立测试集 seeds；用 data-oracle action 生成标签，先做 imitation learning 预训练 policy，再用 pilot-only reward 微调。
3. 将 MMSE-assisted 从“两维特征拼接”升级为 residual learning：输入 MMSE soft / residual / neural-MMSE disagreement，输出 neural residual correction，并加入 residual consistency loss。

## 1. 传统均衡 baseline 扩展

### 实现内容

- 新增 `baseline/traditional_equalizers.py`。
- 新增传统算法：
  - matched filter
  - zero forcing
  - LMS FIR equalizer
  - RLS FIR equalizer
  - MMSE equalizer
- 新增 `online_train.evaluate_traditional_baselines(...)`，在同一帧上统一统计每个 baseline 的：
  - `BER_data`
  - `BER_pilot`
- 新增 `compare.run_channel_baseline_comparison(...)`，支持 Rayleigh、Rician、EPA、EVA、ETU 全信道对比，并输出 JSON 与可视化图。

### 验证命令

```bash
python -B - <<'PY'
from compare import run_channel_baseline_comparison
run_channel_baseline_comparison(
    profiles=[None, "rician", "epa", "eva", "etu"],
    num_frames=3,
    seed=20260714,
    snr=10.0,
    window_K=14,
    output_dir="logs/task_sequence_2026-07-14_22-53-48/task1_traditional_baselines",
)
PY
```

### 输出文件

- `logs/task_sequence_2026-07-14_22-53-48/task1_traditional_baselines/channel_baseline_comparison.json`
- `logs/task_sequence_2026-07-14_22-53-48/task1_traditional_baselines/channel_baseline_comparison.png`

### 结果摘要

| Channel | MF BER_data | ZF BER_data | LMS BER_data | RLS BER_data | MMSE BER_data |
|---|---:|---:|---:|---:|---:|
| Rayleigh | 0.13021 | 0.15885 | 0.06641 | 0.03646 | 0.06510 |
| Rician | 0.09375 | 0.02734 | 0.01042 | 0.01042 | 0.00651 |
| EPA | 0.00000 | 0.00651 | 0.00000 | 0.00000 | 0.00391 |
| EVA | 0.03385 | 0.00391 | 0.00000 | 0.00000 | 0.00260 |
| ETU | 0.02734 | 0.00521 | 0.00000 | 0.00000 | 0.00521 |

### 反思

符合预期：传统 baseline 已可统一评估和可视化，且不同信道上的强弱差异明显。

不完全符合论文实验要求：当前 `num_frames=3` 是闭环验证规模，EPA/EVA/ETU 出现 0 BER 有小样本偶然性。正式论文实验应至少使用固定 100-1000 帧 seeds，并报告均值、标准差或置信区间。

## 2. 固定 seeds + data-oracle imitation policy

### 实现内容

- 新增 `configs/eval_seeds.json`，作为独立固定测试集。
- 新增：
  - `online_train.generate_fixed_eval_seeds(...)`
  - `online_train.load_eval_seeds(...)`
  - `online_train.build_oracle_action_dataset(...)`
- `build_oracle_action_dataset(...)` 在仿真中遍历 action table，用 `BER_data` 选择 data-oracle action 标签。该步骤只用于离线 imitation 标签生成，不进入在线 reward。
- `DiscretePPOPolicy` 新增：
  - `pretrain_imitation(observations, labels, epochs)`
- `run_online_adaptation(...)` 新增参数：
  - `eval_seeds`
  - `imitation_frames`
  - `imitation_epochs`
- 在线阶段仍然只用 pilot-only reward 做 PPO 微调，没有把真实 data 标签混入 reward。

### 验证命令

```bash
python -B - <<'PY'
from pathlib import Path
from online_train import run_online_adaptation, load_eval_seeds

seed_map = load_eval_seeds("configs/eval_seeds.json")
run_online_adaptation(
    num_frames=5,
    seed=20260714,
    snr=10.0,
    d_model=128,
    n_layers=3,
    adapter_rank=16,
    window_K=14,
    use_channel_encoder=True,
    channel_dim=64,
    use_sync_head=True,
    sync_dim=48,
    sync_delay_bins=9,
    pretrained="pretrained/model_best.pt",
    eval_seeds=seed_map["rayleigh"][:5],
    imitation_frames=3,
    imitation_epochs=5,
    output_dir=Path("logs/task_sequence_2026-07-14_22-53-48/task2_imitation_policy"),
)
PY
```

### 输出文件

- `logs/task_sequence_2026-07-14_22-53-48/task2_imitation_policy/online_summary.json`
- `logs/task_sequence_2026-07-14_22-53-48/task2_imitation_policy/online_adapt_metrics.png`
- `logs/task_sequence_2026-07-14_22-53-48/task2_imitation_policy/mmse_baseline_comparison.png`
- `logs/task_sequence_2026-07-14_22-53-48/task2_imitation_policy/generalization.png`

### 结果摘要

固定 Rayleigh seeds：

```text
20260714, 20260715, 20260716, 20260717, 20260718
```

| Method | BER_data | BER_pilot | pilot_loss | latency_ms |
|---|---:|---:|---:|---:|
| PEFT + imitation + PPO | 0.05313 | 0.01875 | 0.04254 | 18.94 |
| MMSE | 0.02188 | 0.02344 | 0.00000 | 0.00 |
| matched filter | 0.09453 | 0.07656 | 0.00000 | 0.00 |
| zero forcing | 0.11406 | 0.08750 | 0.00000 | 0.00 |
| LMS | 0.01719 | 0.01719 | 0.00000 | 0.00 |
| RLS | 0.00781 | 0.00781 | 0.00000 | 0.00 |

Imitation loss：

```text
2.32893
```

### 反思

部分符合预期：固定 seeds 生效，data-oracle 标签生成和 imitation 预训练链路已经打通。

不符合性能预期：PEFT + imitation + PPO 在 Rayleigh 上仍弱于 LMS/RLS/MMSE。原因不是实现闭环缺失，而是当前 imitation 数据量太小，且 policy observation 仍难以稳定预测 data-oracle action。更关键的是，在 Rayleigh 这类线性信道上，RLS/MMSE 的结构先验很强，纯 action selection 的 RL 很难自然超过它。

后续改进建议：

1. 扩大 imitation 数据集到 1000-10000 帧，覆盖 Rayleigh/Rician/EPA/EVA/ETU。
2. 报告 imitation policy 的 action accuracy，以及与 data-oracle 的 regret。
3. 加入 candidate action probing 特征，让 policy 看到每个 action 的 pilot-only 虚拟更新结果，而不是只看全局 observation。
4. 将论文主张从“RL 全面优于传统均衡”调整为“RL 控制 residual/pseudo-label/online adaptation，在模型错配场景优于固定传统算法”。

## 3. MMSE-assisted residual learning

### 实现内容

- `EnvConfig.use_mmse_features=True` 时，状态维度从旧的 2 维 MMSE feature 升级为 3 维：
  - MMSE soft normalized
  - RX-MMSE residual normalized
  - MMSE confidence
- 状态布局调整为：

```text
rx_window + mmse_features + meta
```

这样最后 3 维仍然稳定表示：

```text
known_symbol, known_mask, data_mask
```

- `EqualizerConfig` 新增：
  - `use_mmse_residual`
  - `mmse_feature_dim`
- `AdapterEqualizer` 新增 residual head：

```text
base neural logits
MMSE soft / residual / confidence
neural-MMSE disagreement
-> residual_head
-> logits = base_logits + residual_correction
```

- `pretrain.py` 在 `use_mmse_features=True` 时：
  - 自动启用 residual head。
  - 加入轻量 residual consistency loss：`0.02 * residual_correction^2`。
  - 支持从旧 `pretrained/model_best.pt` 兼容初始化扩维后的 `input_proj.0.weight`：旧 raw window 和 meta 权重复用，新增 MMSE feature 权重置零。

### 验证 3.1：从零轻量训练

输出：

- `pretrained/residual_assisted_2026-07-14_22-53-48/`
- `logs/task_sequence_2026-07-14_22-53-48/task3_residual_learning/residual_summary.json`
- `logs/task_sequence_2026-07-14_22-53-48/task3_residual_learning/online_eval/online_adapt_metrics.png`

结果：

| Method | BER_data | BER_pilot |
|---|---:|---:|
| residual PEFT from scratch | 0.50000 | 0.51250 |
| MMSE | 0.01250 | 0.01250 |
| RLS | 0.00703 | 0.00156 |

结论：从零 8 步训练完全不够，模型基本不可用。

### 验证 3.2：从标准 θ_pre 兼容初始化后训练

输出：

- `pretrained/residual_assisted_init_2026-07-14_22-53-48/`
- `logs/task_sequence_2026-07-14_22-53-48/task3_residual_learning_init/residual_summary.json`
- `logs/task_sequence_2026-07-14_22-53-48/task3_residual_learning_init/online_eval/online_adapt_metrics.png`
- `logs/task_sequence_2026-07-14_22-53-48/task3_residual_learning_init/online_eval/mmse_baseline_comparison.png`
- `logs/task_sequence_2026-07-14_22-53-48/task3_residual_learning_init/online_eval/generalization.png`

离线验证：

| Metric | Value |
|---|---:|
| best_data_ber | 0.00820 |
| num_steps | 20 |
| trainable_offline_params | 579501 |

在线 Rayleigh 固定 5 帧：

| Method | BER_data | BER_pilot | pilot_loss | latency_ms |
|---|---:|---:|---:|---:|
| residual PEFT + imitation + PPO | 0.03906 | 0.03281 | 0.10758 | 47.20 |
| MMSE | 0.01953 | 0.01563 | 0.00000 | 0.00 |
| matched filter | 0.07734 | 0.06719 | 0.00000 | 0.00 |
| zero forcing | 0.04531 | 0.03438 | 0.00000 | 0.00 |
| LMS | 0.04063 | 0.04844 | 0.00000 | 0.00 |
| RLS | 0.01016 | 0.01875 | 0.00000 | 0.00 |

### 反思

部分符合预期：

- residual learning 结构已落地。
- 从 `θ_pre` 兼容初始化后，离线验证 BER 能到 0.0082，说明 residual-assisted 模型本身具备学习能力。
- 在线 residual PEFT 比从零训练显著好。

不符合最终性能预期：

- 在线 5 帧 BER_data=0.03906，仍弱于 MMSE=0.01953 和 RLS=0.01016。
- 当前 residual head 只训练 20 步，且在线 policy 只用 3 帧 imitation 标签，训练规模不足。
- residual consistency 只约束 correction 幅度，没有显式蒸馏 MMSE 的可靠区域，也没有区分 MMSE 正确/错误样本。

后续改进建议：

1. 对 residual-assisted 模型做正式离线训练：至少 1000-5000 steps，batch size 4-16，固定验证 seeds。
2. 加入 selective distillation：
   - known/pilot 上 MMSE 正确时，约束 neural 接近 MMSE。
   - MMSE 错误或低置信时，允许 neural residual 修正。
3. 将 residual head 的在线更新限制为 residual head + adapter，减少输出头全局漂移。
4. 在线 policy 训练从“小样本 imitation”升级为：
   - 大规模 data-oracle imitation
   - 固定验证集 early stopping
   - pilot-only PPO fine-tune
5. 增加 CFO/Doppler/IQ imbalance/非线性 PA 等传统 MMSE/RLS 更容易失配的信道场景。当前 Rayleigh 线性信道不是 RL 方法最有优势的主战场。

## 清理与测试

### 清理内容

- 删除 `online_train.py` 中 `run_pilot_overhead_study(...)` 返回后的不可达旧绘图代码。
- 保持 `开发框架.md` 不变。
- 未恢复历史并行实现文件。

### 测试

```bash
python -B -m pytest -p no:cacheprovider tests/test_parameter_efficient_adaptation.py -q
```

当前最新测试结果：

```text
29 passed
```

## 本轮总体结论

1. 传统 baseline 扩展已经完成，并能在 Rayleigh/Rician/EPA/EVA/ETU 上统一对比和可视化。
2. 固定独立测试 seeds 已落地，后续结论不再依赖每轮随机换测试分布。
3. data-oracle imitation policy 链路已经打通，但小规模验证下尚未带来优于传统算法的性能。
4. MMSE-assisted residual learning 已从简单两维拼接升级为 residual correction 架构；从旧 `θ_pre` 兼容初始化后离线表现明显可用，但在线效果还未超过 MMSE/RLS。
5. 若目标是支撑论文，下一步不应继续只调 PPO，而应扩大 residual-assisted 离线训练和 imitation 数据规模，并把实验场景转向传统模型更容易失配的 CFO/Doppler/非线性/低 pilot overhead 场景。
