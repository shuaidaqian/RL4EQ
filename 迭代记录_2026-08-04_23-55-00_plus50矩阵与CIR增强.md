# 迭代记录：+50 九配置矩阵、CIR condition augmentation 与增强接收机

时间戳：2026-08-04 23:55:00  
分支：`codex/continual-ppo-unfolded-equalizer`  
工作区：`D:\Research\RL4EQ`

## 1. 本轮目标

按当前门槛式路线继续推进：

1. 先用 `+50 checkpoint` 跑完整 Level B 九配置矩阵，判断它是只改善 `d40/s10`，还是整体改善。
2. 加入 CIR condition augmentation，让离线训练混合：
   - true CIR
   - acquisition CIR
   - noisy CIR
   - DD-like CIR
3. 如果 oracle CIR 仍弱于 DFE-RLS，就增强神经块检测器本体。
4. 只有当 `Offline NN + DD-CIR` 接近或超过 DFE-RLS 后，再启动 PPO。

## 2. +50 checkpoint 九配置矩阵

checkpoint：

```text
pretrained/level_b_focus_d40_snr10_1650_from_1600_p128_2026-08-04_01-16-35/model_best.pt
```

输出目录：

```text
logs/nn_cir_modes_9cfg_plus50_2026-08-04_23-19-45
logs/nn_cir_modes_9cfg_plus50_2026-08-04_23-19-45/aggregate_summary.json
```

评估设置：

```text
Level B
delays = 20, 30, 40
SNR = 10, 15, 20 dB
frames = 64
seeds = 0, 1, 2
pilot_total = 128
pilot_layout = two_block
tail mode = soft
CIR modes = fixed / decision_directed / oracle
```

### 2.1 总体结果

| checkpoint | fixed CIR | DD-CIR | oracle CIR |
|---|---:|---:|---:|
| 1600 before | 0.042042 | 0.009065 | 0.007404 |
| +50 checkpoint | 0.039361 | 0.008381 | 0.006736 |
| 改变量 | -0.002681 | -0.000684 | -0.000668 |

结论：

- `+50 checkpoint` 不是只改善 `d40/s10`，而是九配置整体改善。
- DD-CIR 平均 BER 从 `0.009065` 降到 `0.008381`。
- oracle CIR 平均 BER 从 `0.007404` 降到 `0.006736`。

### 2.2 与 DFE-RLS 对比

传统 DFE-RLS 来源：

```text
logs/traditional_9cfg_64f_3seed_2026-08-03/frame_metrics.jsonl
```

总体：

```text
DFE-RLS overall             = 0.005584
Offline NN + DD-CIR (+50)   = 0.008381
Offline NN + oracle (+50)   = 0.006736
```

差距：

```text
DD-CIR - DFE-RLS     = +0.002797
oracle CIR - DFE-RLS = +0.001153
```

结论：

- +50 是当前更好的旧结构 checkpoint。
- 但 proposed 的神经块均衡器即使用 oracle CIR，仍没有超过 DFE-RLS。
- 因此 PPO 不能启动为主实验；否则 PPO 仍是在一个未达门槛的接收机上做动作搜索。

## 3. CIR condition augmentation 实现

修改文件：

```text
training/curriculum.py
configs/continual_ppo.json
configs/continual_ppo_focus_d40_snr10.json
configs/continual_ppo_focus_snr10.json
tests/test_meta_adaptation.py
```

### 3.1 新增训练 CIR 来源

新增 `TrainingCIRSelection` 与 `CurriculumTrainer._select_training_cir()`。

配置：

```json
"cir_condition_augmentation": {
  "enabled": true,
  "modes": ["true", "acquisition", "noisy", "dd_like"],
  "noise_std": 0.05,
  "dd_flip_probability": 0.02,
  "dd_blend_alpha": 0.2
}
```

含义：

- `true`：使用当前帧真值 CIR，仅用于离线监督训练增强。
- `acquisition`：使用 episode acquisition 阶段估计出的 CIR，保持原训练分布。
- `noisy`：对 acquisition CIR 加复高斯扰动并归一化。
- `dd_like`：离线构造带少量符号翻转的判决导向 CIR，模拟在线 DD-CIR 误差。

### 3.2 标签使用边界

`dd_like` 只在离线训练中使用，目的是模拟在线判决导向估计误差。  
它不改变在线规则：

- online observation 不使用 Data 标签；
- online reward 不使用 Data 标签；
- online 动作选择不使用 Data 标签；
- online PEFT/调制更新仍只能由 Adapt/Reward Pilot 驱动。

### 3.3 测试

新增测试：

```text
test_curriculum_cir_condition_augmentation_cycles_configured_sources
test_curriculum_train_reports_condition_cir_source_counts
```

TDD 过程：

1. 先运行测试，确认失败：

```text
2 failed
```

失败原因：

```text
CurriculumTrainer 缺少 _select_training_cir()
metrics 缺少 condition_cir_sources
```

2. 实现后重新运行：

```text
2 passed
```

3. 运行完整 `tests/test_meta_adaptation.py`：

```text
20 passed
```

### 3.4 augmentation smoke

命令：

```powershell
.\.venv-gpu\Scripts\python.exe pretrain.py `
  --config configs\continual_ppo_focus_d40_snr10.json `
  --stage estimated_cir_level_b `
  --steps 2 `
  --batch-size 4 `
  --amp `
  --resume pretrained\level_b_focus_d40_snr10_1650_from_1600_p128_2026-08-04_01-16-35\model_best.pt `
  --save-dir pretrained\augmentation_smoke_2026-08-04
```

结果：

```json
"condition_cir_sources": {
  "true": 2,
  "acquisition": 2,
  "noisy": 2,
  "dd_like": 2
}
```

结论：

augmentation 管线有效，来源采样符合预期。

## 4. augmentation 100-step 小实验

checkpoint：

```text
pretrained/level_b_d40s10_aug100_from_plus50_2026-08-04_23-47-04/model_best.pt
```

评估目录：

```text
logs/nn_cir_modes_d40s10_aug100_2026-08-04_23-48-25
logs/d40s10_aug100_summary_2026-08-04_23-52-40.json
```

训练来源采样：

```json
"condition_cir_sources": {
  "true": 25,
  "acquisition": 25,
  "noisy": 25,
  "dd_like": 25
}
```

结果：

| checkpoint | fixed CIR | DD-CIR | oracle CIR |
|---|---:|---:|---:|
| +50 | 0.052355 | 0.014065 | 0.011529 |
| aug100 | 0.059041 | 0.015734 | 0.012682 |

结论：

- augmentation 100-step 在 `d40/s10` 上退化。
- 不能扩大该 checkpoint 到九配置矩阵。
- 当前 augmentation 实现作为训练机制保留，但当前超参数不能作为最终方案。

判断：

```text
短训退化不代表 augmentation 方向错误，
但说明 true/acquisition/noisy/dd_like 均匀混合 + 当前 noise/flip 参数并不适合直接从 +50 checkpoint 微调。
```

后续如果继续调 augmentation，应改成更温和的比例，例如：

```text
acquisition 50%
dd_like 25%
noisy 15%
true 10%
```

并降低：

```text
noise_std: 0.05 -> 0.02
dd_flip_probability: 0.02 -> 0.005 或 0.01
```

## 5. 神经块检测器增强

由于 +50 checkpoint 的 oracle CIR 仍弱于 DFE-RLS，本轮进入模型本体增强。

修改文件：

```text
agent/unfolded_equalizer.py
tests/test_receiver_architecture.py
configs/continual_ppo_enhanced.json
```

### 5.1 新增可选模型字段

新增：

```python
conditioner_uses_cir_summary: bool = False
```

默认值为 `False`，用于保持旧 checkpoint 兼容。

当设置为 `True` 时，FiLM conditioner 输入从：

```text
latent_residual
```

扩展为：

```text
latent_residual
+ |CIR|
+ support_probability
+ log1p(noise_variance)
+ confidence
```

目的：

- 让 conditioner 显式感知当前 CIR support、噪声和置信度；
- 不再只依赖线性算子中使用的 complex CIR；
- 提高模型对 DD-CIR 条件误差的表达能力。

### 5.2 新增增强配置

新增：

```text
configs/continual_ppo_enhanced.json
```

关键设置：

```json
"model": {
  "frame_len": 512,
  "max_delay": 40,
  "iterations": 6,
  "d_model": 96,
  "num_heads": 4,
  "adapter_rank": 8,
  "lora_rank": 8,
  "conditioner_uses_cir_summary": true
}
```

说明：

- `iterations=6` 增强 unfolded 检测能力；
- `conditioner_uses_cir_summary=true` 让 conditioner 显式使用 CIR/support/noise；
- 该配置需要从头训练或使用同结构 checkpoint resume；
- 不兼容旧 `iterations=4 / conditioner=false` checkpoint，这是预期行为。

### 5.3 测试

新增测试：

```text
test_unfolded_equalizer_optional_conditioner_uses_cir_support_and_noise
```

TDD 过程：

1. 先运行测试，确认失败：

```text
UnfoldedConfig.__init__() got an unexpected keyword argument 'conditioner_uses_cir_summary'
```

2. 实现后运行：

```text
2 passed
```

覆盖：

- 新字段可用；
- 开启后 support/noise/confidence 会影响输出；
- 旧 strict checkpoint 测试仍通过。

3. 接收机架构测试：

```text
8 passed
```

4. 相关测试集合：

```text
tests/test_receiver_architecture.py
tests/test_meta_adaptation.py
tests/test_windowed_discrete_ppo.py

34 passed
```

### 5.4 enhanced smoke

命令：

```powershell
.\.venv-gpu\Scripts\python.exe pretrain.py `
  --config configs\continual_ppo_enhanced.json `
  --stage estimated_cir_level_b `
  --steps 2 `
  --batch-size 1 `
  --amp `
  --save-dir pretrained\enhanced_smoke_2026-08-04
```

结果：

```text
saved pretrained\enhanced_smoke_2026-08-04
```

strict-load 验证：

```json
{
  "strict_load": true,
  "iterations": 6,
  "conditioner_uses_cir_summary": true
}
```

compare smoke：

```powershell
.\.venv-gpu\Scripts\python.exe compare.py `
  --config configs\continual_ppo_enhanced.json `
  --method-group proposed `
  --pretrained pretrained\enhanced_smoke_2026-08-04\model_best.pt `
  --delays 20 `
  --snrs 10 `
  --num-seeds 1 `
  --frames 1 `
  --pilot-total 64 `
  --pilot-layout multi_block `
  --resume `
  --output-dir logs\enhanced_compare_smoke_2026-08-04
```

结果：

```text
saved logs\enhanced_compare_smoke_2026-08-04
```

说明：

2-step enhanced smoke 只验证管线，不用于性能结论。

## 6. 当前门槛判断

### 6.1 PPO 仍不启动

原因：

```text
+50 Offline NN + DD-CIR overall = 0.008381
DFE-RLS overall                 = 0.005584
```

虽然 `BER_data < 0.01` 已满足总体目标，但还没有超过传统非神经、非 RL baseline。  
按当前研究契约，PPO 暂不启动。

### 6.2 当前最佳旧结构 checkpoint

当前旧结构最佳 checkpoint 仍是：

```text
pretrained/level_b_focus_d40_snr10_1650_from_1600_p128_2026-08-04_01-16-35/model_best.pt
```

它的优势是九配置整体改善。  
缺点是仍弱于 DFE-RLS。

### 6.3 augmentation 机制保留，但当前超参数不通过

当前 augmentation 机制是必要的工程能力，但 `均匀 4-mode + noise_std=0.05 + flip=0.02` 的短训结果不通过。  
后续应降低扰动强度，改成非均匀比例，再做小实验。

### 6.4 enhanced 模型进入候选路线

`configs/continual_ppo_enhanced.json` 已具备：

- 更深 unfolded iterations；
- 显式 CIR/support/noise conditioner；
- augmentation 训练机制；
- strict-load 与 compare smoke 通过。

下一步应该从 enhanced 配置做短中等训练，而不是继续旧结构 PPO。

## 7. 下一步建议

推荐下一步按门槛继续：

1. 训练 enhanced 配置：

```powershell
.\.venv-gpu\Scripts\python.exe pretrain.py `
  --config configs\continual_ppo_enhanced.json `
  --stage all `
  --steps 200 `
  --batch-size 1 `
  --amp `
  --save-dir pretrained\enhanced_stage_all_200
```

2. 先只评估 `d40/s10`：

```text
fixed / DD-CIR / oracle CIR
frames = 64
seeds = 0,1,2
```

3. 若 `oracle CIR` 没有接近或超过 DFE-RLS：

```text
停止 enhanced 当前配置；
改调模型结构或训练目标；
不启动 PPO。
```

4. 若 `oracle CIR` 超过 DFE-RLS，但 DD-CIR 仍弱：

```text
重点调 CIR tracker / DD-like augmentation；
不急着 PPO。
```

5. 只有当：

```text
Offline NN + DD-CIR 接近或超过 DFE-RLS
```

再启动：

```text
RL-Modulated Neural Block Equalizer
```

并要求 PPO 的贡献是进一步降低 BER，而不是补偿 fixed CIR 或弱模型本体。

## 8. 本轮结论

本轮推进结果：

```text
+50 checkpoint 已证明确实整体改善；
但仍未超过 DFE-RLS；
CIR condition augmentation 已实现并通过测试；
augmentation 当前超参数短训失败，不能扩大；
增强神经块检测器已实现为可选新结构；
enhanced smoke、strict-load、compare smoke 均通过；
PPO 继续暂缓。
```

当前最重要的判断：

```text
下一阶段优先训练 enhanced Offline NN。
只有 enhanced Offline NN + DD-CIR 达到或超过 DFE-RLS，
RL 才有合理的上升空间。
```
