# 迭代记录：Reward / Data 相关性诊断

时间戳：2026-08-08 10:28:44（Asia/Shanghai）

## 1. 诊断目标

本轮只做一件事：检查当前 reward 设计是否真的能稳定预测 Data BER 的改善。

如果 `Reward Pilot` 的改善不能预测 `Data` 的改善，那么：

- 现在的 RL reward 设计不够好；
- 动作空间也可能不够合适；
- 这时不应该贸然启动 PPO。

如果相关性足够强，再考虑进入 RL。

## 2. 诊断配置

使用当前最强的 prefix-only checkpoint：

- [pretrained/prefix_focus_hard_snr10_15_1400_from_plus50_2026-08-08/model_best.pt](D:/Research/RL4EQ/pretrained/prefix_focus_hard_snr10_15_1400_from_plus50_2026-08-08/model_best.pt)

诊断命令使用：

```powershell
.\.venv-gpu\Scripts\python.exe scripts\diagnose_research_assumptions.py --config configs/continual_ppo.json --pretrained pretrained/prefix_focus_hard_snr10_15_1400_from_plus50_2026-08-08/model_best.pt --delays 20 30 40 --snrs 10 15 --pilot-totals 64 96 128 --pilot-layouts prefix --seeds 0 1 --frames 4 --device cuda --output-dir logs\research_diagnostics_prefix_hard_2026-08-08
```

这次诊断覆盖的是当前主候选区间：

- delays: 20 / 30 / 40
- SNR: 10 / 15
- pilot_total: 64 / 96 / 128
- layout: prefix

总计生成的 paired 样本数：

```text
num_pairs = 1440
```

## 3. 核心结论

### 3.1 Reward / Data 相关性极弱

真实 paired 诊断结果：

```text
reward_loss_delta_vs_data_ber_delta
spearman = 0.02331087424897167
gate_pass = false
num_pairs = 1440
```

这基本等于“没有稳定相关性”。

也就是说，当前 reward 的改善几乎不能稳定预测 Data BER 的改善。

### 3.2 替代 surrogate 也不行

所有候选 surrogate 都没有达到门槛，最好的也只有：

```text
action-level best surrogate = loss_plus_margin
spearman = 0.13939393939393938
gate_pass = false
```

其它 surrogate 更低。

这说明问题不是某一个小公式写错了，而是当前 reward 代理整体不够强。

### 3.3 动作空间对 Data 改善几乎没有帮助

动作选择结果非常保守：

```text
selected_frames = 144
mean_data_ber_improvement = 0.0
fraction_data_improved = 0.0
action_counts = {
  "identity": 143,
  "lora_scale_1.2": 1
}
```

这表示：

- 大多数情况下，按当前 surrogate 选出来的最好动作仍然是 identity；
- 少数非 identity 动作也没有带来稳定的 Data 改善；
- 当前动作空间更像“扰动”，不是“稳定可用的改进方向”。

### 3.4 低维 modulation 与 PEFT 的诊断都不理想

低维 modulation 总体表现：

```text
fraction_reward_improved = 0.3715277777777778
fraction_data_improved   = 0.027083333333333334
fraction_both_improved   = 0.009027777777777777
```

这说明：

- reward 改善并不等价于 data 改善；
- 两者同时改善的比例非常低；
- 很多动作只是在 reward 上“看起来好一点”，但没有真正改善 Data。

PEFT 诊断更直接：

```text
adapter_lora:
  mean_reward_loss_improvement = 7.4855e-05
  mean_data_ber_improvement    = 0.0

head:
  mean_reward_loss_improvement = 1.1218e-05
  mean_data_ber_improvement    = 0.0
```

这说明当前可用的 PEFT 动作本身也没有形成可稳定利用的 Data 改善信号。

## 4. Offline NN 本体情况

诊断时的 Offline NN 参考值：

```text
mean_ber_data = 0.0010494966910401774
```

最优配置：

```text
delay=20, snr=15, pilot=64, prefix
mean_ber_data = 0.0
```

最差配置：

```text
delay=40, snr=10, pilot=64, prefix
mean_ber_data = 0.0027901786961592734
```

这说明：

- Offline NN 本体已经很强；
- 但 reward / action 的诊断信号仍然没能和 Data 真正对齐；
- 所以如果此时启动 PPO，很可能是在一个 reward 代理不稳定的基础上做策略搜索。

## 5. 对 RL 启动条件的判断

本轮诊断结果非常明确：

```text
当前不适合直接启动 RL。
```

原因有三点：

1. Reward 与 Data 的 Spearman 只有 `0.0233`，远低于 `0.6`。
2. 最好的 surrogate 也只有 `0.1394`，距离可用门槛还很远。
3. 动作选择几乎总是 identity，说明当前动作空间没有形成稳定的“有益动作选择”信号。

因此，现在需要先调整：

- reward 设计；
- action space；
- 可能还要调整窗口级反馈的定义。

## 6. 下一步建议

下一步不要启动 PPO。建议按优先级做下面三件事：

### 6.1 重新设计 reward surrogate

目标是让 reward 变化更贴近 Data BER 改善，而不是只看 reward loss 的微小波动。

### 6.2 收缩或重构动作空间

当前动作空间太容易退化成 identity 或无效扰动。

应优先考虑：

- 更少但更有意义的动作；
- 更强的 PEFT 动作；
- 明确区分“可用于在线更新的动作”和“只适合离线诊断的动作”。

### 6.3 在新 reward / 新动作空间下重测 Spearman

只有当新的 reward surrogate 能把 Spearman 提升到足够高，才考虑启动 RL。

## 7. 验证

本轮诊断脚本已成功执行，并生成：

- [logs/research_diagnostics_prefix_hard_2026-08-08/research_diagnostics.json](D:/Research/RL4EQ/logs/research_diagnostics_prefix_hard_2026-08-08/research_diagnostics.json)
- [logs/research_diagnostics_prefix_hard_2026-08-08/modulation_action_rows.jsonl](D:/Research/RL4EQ/logs/research_diagnostics_prefix_hard_2026-08-08/modulation_action_rows.jsonl)
- [logs/research_diagnostics_prefix_hard_2026-08-08/peft_rows.jsonl](D:/Research/RL4EQ/logs/research_diagnostics_prefix_hard_2026-08-08/peft_rows.jsonl)
- [logs/research_diagnostics_prefix_hard_2026-08-08/level_b_difficulty.json](D:/Research/RL4EQ/logs/research_diagnostics_prefix_hard_2026-08-08/level_b_difficulty.json)

## 8. 本轮最终判断

一句话结论：

```text
当前 reward / Data 相关性太弱，RL 暂不应启动；先改 reward 设计和动作空间，再重测 Spearman。
```

