# 迭代记录：CIR 模式诊断与 d40/s10 增量训练

时间戳：2026-08-04 01:26:00  
分支：`codex/continual-ppo-unfolded-equalizer`  
工作区：`D:\Research\RL4EQ`

## 1. 本轮目标

本轮继续实验，但不直接继续训练 PPO。原因是上一轮结果已经显示：

- fixed acquisition CIR 会导致长 episode 明显退化；
- DD-CIR 能显著修复退化；
- 但 `RL-Modulated Neural Block Equalizer + DD-CIR` 仍没有超过 `Offline NN only + DD-CIR`；
- `Offline NN + DD-CIR` 也没有超过强传统 `DFE-RLS`。

因此本轮优先验证：

1. 当前神经均衡器的主要瓶颈是 fixed CIR、DD-CIR 估计误差，还是模型本体检测能力。
2. 对关键配置 `delay=40, SNR=10 dB` 继续同分布监督训练是否能稳定改善。

## 2. 使用 checkpoint

基准 checkpoint：

```text
pretrained/level_b_focus_d40_snr10_1600_from_1500_p128/model_best.pt
```

配置：

```text
configs/continual_ppo.json
configs/continual_ppo_focus_d40_snr10.json
```

主评估设置：

```text
Level B
pilot_total = 128
pilot_layout = two_block
rho = 0.99
frames = 64
seeds = 0, 1, 2
CIR modes = fixed / decision_directed / oracle
tail mode = soft
```

其中：

- `fixed`：episode 起始 acquisition CIR 后不再更新；
- `decision_directed`：使用模型硬判决 + Adapt Pilot 标签进行在线 DD-CIR 更新，不使用 Data 标签；
- `oracle`：使用仿真真值 CIR，仅用于诊断，不进入正式方法。

## 3. 九配置 CIR 模式诊断

输出目录：

```text
logs/nn_cir_modes_9cfg_2026-08-04_00-48-48
logs/nn_cir_modes_9cfg_2026-08-04_00-48-48/aggregate_summary.json
```

### 3.1 逐配置结果

| delay | SNR | fixed CIR | DD-CIR | oracle CIR | DD - oracle |
|---:|---:|---:|---:|---:|---:|
| 20 | 10 | 0.047282 | 0.010213 | 0.007853 | 0.002360 |
| 20 | 15 | 0.035197 | 0.005547 | 0.004489 | 0.001058 |
| 20 | 20 | 0.030870 | 0.004408 | 0.003608 | 0.000800 |
| 30 | 10 | 0.053345 | 0.013943 | 0.011312 | 0.002631 |
| 30 | 15 | 0.039958 | 0.008599 | 0.006578 | 0.002021 |
| 30 | 20 | 0.036255 | 0.006999 | 0.005737 | 0.001261 |
| 40 | 10 | 0.054810 | 0.014608 | 0.012546 | 0.002062 |
| 40 | 15 | 0.042263 | 0.009359 | 0.007731 | 0.001628 |
| 40 | 20 | 0.038398 | 0.007907 | 0.006782 | 0.001126 |

### 3.2 按 delay 聚合

| delay | fixed CIR | DD-CIR | oracle CIR |
|---:|---:|---:|---:|
| 20 | 0.037783 | 0.006723 | 0.005317 |
| 30 | 0.043186 | 0.009847 | 0.007876 |
| 40 | 0.045157 | 0.010625 | 0.009020 |

### 3.3 总体聚合

| 模式 | 平均 BER_data |
|---|---:|
| fixed CIR | 0.042042 |
| DD-CIR | 0.009065 |
| oracle CIR | 0.007404 |

## 4. 与 DFE-RLS 对照

传统基线来源：

```text
logs/traditional_9cfg_64f_3seed_2026-08-03/frame_metrics.jsonl
```

`DFE-RLS` 结果：

| delay | SNR | DFE-RLS BER_data |
|---:|---:|---:|
| 20 | 10 | 0.005154 |
| 20 | 15 | 0.002780 |
| 20 | 20 | 0.002143 |
| 30 | 10 | 0.008341 |
| 30 | 15 | 0.004964 |
| 30 | 20 | 0.003933 |
| 40 | 10 | 0.011230 |
| 40 | 15 | 0.006510 |
| 40 | 20 | 0.005195 |

总体：

```text
DFE-RLS overall BER_data = 0.005584
Offline NN + DD-CIR      = 0.009065
Offline NN + oracle CIR  = 0.007404
```

## 5. 本轮关键判断

### 5.1 fixed CIR 不是可接受正式设置

fixed CIR 下总体 BER_data 为 `0.042042`，远高于 DD-CIR 的 `0.009065`。  
因此之前 fixed-CIR 场景下 RL 看似有增益，主要是在补偿过期 CIR，而不是证明 RL-PEFT 真实超越传统均衡器。

后续 proposed 标准接收机必须默认使用：

```text
Offline NN + DD-CIR
RL-PEFT + DD-CIR
```

不能再把 fixed acquisition CIR 当作正式 proposed 设置。

### 5.2 DD-CIR 有效，但不是全部瓶颈

DD-CIR 相比 fixed CIR 显著改善：

```text
0.042042 -> 0.009065
```

但 oracle CIR 还能进一步改善：

```text
DD-CIR     = 0.009065
oracle CIR = 0.007404
gap        = 0.001661
```

这说明 CIR 跟踪误差仍然有影响。  
训练时只见 acquisition/estimated CIR，测试时使用 DD-CIR，存在条件分布不一致。

### 5.3 神经模型本体仍弱于强传统 DFE-RLS

更重要的是：即使用 oracle true CIR，当前神经模型总体 `0.007404` 仍弱于 DFE-RLS 的 `0.005584`。

所以问题不是“传统方法只靠一个简单 baseline 就会失败”。当前实现里的 DFE-RLS 是强 baseline，它可以从 Adapt Pilot 初始化并通过判决导向持续跟踪，在无 CFO、无相位扰动、无非线性、无编码、pilot_total=128 的设置下非常强。

这意味着：

- 当前不能写“神经/RL 已超过传统均衡器”；
- 当前不能把创新点建立在“极端长时延下传统方法一定很差”这个未经限定的假设上；
- 必须让 proposed 的神经块接收机先接近或超过 DFE-RLS，之后 PPO 才有机会作为主贡献进一步降低 BER。

## 6. d40/s10 增量训练实验

目标：验证单纯继续同分布监督训练是否能改善关键点 `delay=40, SNR=10 dB`。

### 6.1 失败的 400-step 尝试

命令意图：

```text
estimated_cir_level_b
steps = 400
batch_size = 4
resume = level_b_focus_d40_snr10_1600_from_1500_p128
```

结果：

- 进程长时间运行，超过快速判别预算；
- 训练入口没有 step 级日志，无法判断中间进度；
- 手动停止该任务；
- 没有使用该任务产生最终结论。

工程结论：

后续训练入口需要补充周期性日志或 progress 输出，否则长训练不可控。

### 6.2 50-step/batch1 快速训练

输出 checkpoint：

```text
pretrained/level_b_focus_d40_snr10_1650_from_1600_p128_2026-08-04_01-16-35/model_best.pt
```

评估目录：

```text
logs/nn_cir_modes_d40s10_after_50_2026-08-04_01-17-20
```

结果：

| checkpoint | fixed CIR | DD-CIR | oracle CIR |
|---|---:|---:|---:|
| 1600 before | 0.054810 | 0.014608 | 0.012546 |
| 1650 after +50 | 0.052355 | 0.014065 | 0.011529 |

结论：

50-step 继续训练有效，`d40/s10` 的 DD-CIR 与 oracle CIR 都下降：

```text
DD-CIR:     0.014608 -> 0.014065
oracle CIR: 0.012546 -> 0.011529
```

但它仍没有超过 DFE-RLS：

```text
DFE-RLS d40/s10 = 0.011230
```

### 6.3 继续到 +200 steps

输出 checkpoint：

```text
pretrained/level_b_focus_d40_snr10_1800_from_1650_p128_2026-08-04_01-20-54/model_best.pt
```

评估目录：

```text
logs/nn_cir_modes_d40s10_after_200_2026-08-04_01-22-25
```

聚合文件：

```text
logs/d40s10_incremental_training_summary_2026-08-04_01-25-20.json
```

结果：

| checkpoint | fixed CIR | DD-CIR | oracle CIR |
|---|---:|---:|---:|
| 1600 before | 0.054810 | 0.014608 | 0.012546 |
| 1650 after +50 | 0.052355 | 0.014065 | 0.011529 |
| 1800 after +200 | 0.049967 | 0.014160 | 0.012288 |

结论：

- fixed CIR 继续下降，但 fixed CIR 不是正式设置；
- DD-CIR 和 oracle CIR 相比 +50 checkpoint 反弹；
- 单纯继续同分布训练不是稳定解；
- 当前最佳小实验 checkpoint 是 +50，而不是 +200。

## 7. 当前研究判断更新

### 7.1 不是“创新点根本实现不了”

本轮结果说明神经方法不是完全不可行：

- `Offline NN + DD-CIR` 九配置总体已经达到 `0.009065`，低于 `0.01`；
- `Offline NN + oracle CIR` 总体达到 `0.007404`；
- `d40/s10` 继续训练 50 steps 后 oracle CIR 可到 `0.011529`，已经非常接近 DFE-RLS 的 `0.011230`。

所以方向没有被证伪。

### 7.2 但原始假设需要收窄

原始直觉“极端时延扩展下传统均衡器一定比不过神经/RL”过强。  
在当前可控 Level B 条件下：

```text
无 CFO
无额外相位扰动
无非线性
无信道编码
pilot_total = 128
DFE-RLS 允许判决导向持续跟踪
```

DFE-RLS 是很强的传统 baseline。

因此论文问题不能写成：

```text
传统均衡器在极端长时延下一定失败，神经/RL 自然更好。
```

更合理的写法应是：

```text
在极端稀疏长回波、有限 Pilot、信道缓慢漂移的块传输场景下，
研究神经块均衡器能否通过离线监督预训练和在线 RL-PEFT，
在强传统自适应 DFE 基线附近进一步降低 BER。
```

### 7.3 PPO 仍不能提前作为主实验

当前顺序必须保持：

1. Offline NN + DD-CIR 接近或超过 DFE-RLS；
2. Reward Pilot 改善与 Data BER 改善有足够相关性；
3. 再训练 PPO 选择 PEFT 动作；
4. PPO 的目标是进一步降低 Offline NN + DD-CIR 的 BER，而不是补偿 fixed CIR 错误。

## 8. 下一步建议

下一步不应继续盲目加 PPO，也不应继续长时间堆同分布训练。推荐按以下顺序：

### 8.1 先固定最佳候选 checkpoint

暂时把以下 checkpoint 作为 d40/s10 当前最佳候选：

```text
pretrained/level_b_focus_d40_snr10_1650_from_1600_p128_2026-08-04_01-16-35/model_best.pt
```

不要使用 +200 checkpoint 作为默认，因为 DD/oracle 指标反弹。

### 8.2 做全九配置验证

对 +50 checkpoint 跑完整 9 配置 DD/oracle 矩阵。  
如果它只改善 d40/s10、但破坏其他配置，则不能作为主 checkpoint。

### 8.3 加入训练分布改造，而不是继续简单堆步数

需要改造训练数据/条件输入：

- 训练时加入 noisy / drifted / DD-like CIR condition augmentation；
- 混合 true CIR、acquisition CIR、扰动 CIR，让模型对 CIR 条件误差更鲁棒；
- 验证 `DD-CIR - oracle CIR` gap 是否缩小。

目标不是让模型记忆 d40/s10，而是让它在在线 DD-CIR 条件下稳定。

### 8.4 再考虑模型本体增强

若 oracle CIR 仍弱于 DFE-RLS，则说明不是 CIR tracking 问题，而是检测器能力问题。可考虑：

- 增加 unfolded iterations；
- 增强块 denoiser；
- 让条件模块显式使用 CIR/support/noise，而不是主要依赖线性算子中的 CIR；
- 加入与块检测目标更一致的损失或 curriculum。

### 8.5 PPO 之后再启动

只有当 `Offline NN + DD-CIR` 在主配置上接近或超过 DFE-RLS 后，再启动：

```text
离散安全动作 + 窗口级 reward + 真实 PEFT
```

否则 PPO 很可能只学习到扰动已训练模型，无法稳定降低 Data BER。

## 9. 本轮结论

本轮最重要结论是：

```text
fixed CIR 退化已确认；
DD-CIR 是必须保留的 proposed 标准组件；
但当前神经模型即使用 oracle CIR 仍弱于 DFE-RLS；
单纯继续同分布训练有短期改善，但不稳定；
下一阶段应优先做 CIR condition augmentation 和神经块检测器能力增强，
而不是继续 PPO。
```

当前最佳可用数字：

```text
Offline NN + DD-CIR overall     = 0.009065
Offline NN + oracle CIR overall = 0.007404
DFE-RLS overall                 = 0.005584
```

当前关键点 `d40/s10` 最佳小实验：

```text
1650 after +50:
DD-CIR     = 0.014065
oracle CIR = 0.011529

DFE-RLS:
0.011230
```

因此 proposed 已接近强 DFE-RLS，但尚未超过。下一步必须围绕“缩小 oracle/DD 与 DFE-RLS 的差距”继续攻关。
