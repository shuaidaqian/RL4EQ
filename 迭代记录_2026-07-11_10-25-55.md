# 迭代记录 2026-07-11 10:25:55

## 目标

- 取消 Rayleigh 信道固定 16 抽头，改为可变抽头 Rayleigh。
- 新增 Rician channel，用于预训练模型泛化。
- 执行方案 A：加入 Neural Channel Encoder。
- 验证最终预训练模型效果，输出可视化结果。
- 目标：SNR=10 dB 下 Rayleigh + Rician + 3GPP EPA/EVA/ETU 泛化平均 `BER_data < 0.01`。
- 约束：主链路纯神经，不在离线/在线模型输入或 RL 更新中使用 MMSE/LS/DFE；传统算法仅作为 baseline。

## 代码迭代

1. 新增 `env/rician_channel.py`
   - 实现 `RicianMultipathChannel`。
   - 支持 `k_factor_db`、可变抽头、AWGN、`set_snr()`、`summary()`。

2. 修改 `env/comm_env.py`
   - `_make_channel()` 支持 `type="rician"`。
   - `type="rayleigh"` 作为配置标记，不再误传给 Rayleigh 构造函数。

3. 修改 `pretrain.py`
   - `_sample_channel_config()` 删除 `rayleigh16` 固定主采样。
   - Rayleigh/Rician 抽头数随机变化。
   - 抽头数受 `window_K + 1` 约束，避免模型输入窗口看不全符号间干扰。
   - 验证集加入 `rayleigh / rician / epa / eva / etu`。
   - CLI 增加 `--use_channel_encoder`、`--channel_dim`。

4. 修改 `agent/neural_equalizer.py`
   - `EqualizerConfig` 增加 `use_channel_encoder`、`channel_dim`。
   - 新增 `NeuralChannelEncoder`。
   - 使用已知训练/导频位置的 `raw RX window * known_symbol` 作为神经相关特征。
   - 加入帧级信道向量拼接和 FiLM 条件调制。

5. 修改 `online_train.py` / `compare.py`
   - 支持 Rician profile。
   - 支持 Channel Encoder 参数透传。
   - 默认 Rayleigh/Rician 也按窗口约束抽样。

6. 修复 `env/rayleigh_channel.py`
   - 可变时延扩展场景下，卷积输出裁剪回输入帧长，避免监督标签错位。

## 测试

新增/更新契约测试：

- Rician 信道输出合法帧。
- Rayleigh 不再固定 16 抽头，抽头数可变。
- 离线采样包含 Rician。
- 采样器遵守观测窗口约束。
- Rayleigh 可变时延时输出长度保持 512。
- 启用 Channel Encoder 时模型仍输出 `(B,T)` logits/probs，并包含 FiLM 条件调制。

最终测试命令：

```bash
python -B -m pytest -p no:cacheprovider tests/test_parameter_efficient_adaptation.py -q
```

结果：

```text
17 passed
```

## 实验记录

### Iter 1：基础 Neural Channel Encoder

命令核心参数：

```bash
python pretrain.py --num_steps 400 --batch_size 3 --d_model 96 --n_layers 2 \
  --adapter_rank 12 --window_K 12 --use_channel_encoder --channel_dim 48 \
  --snr_min 10 --snr_max 10 --data_loss_weight 5.0 --known_loss_weight 0.5
```

结果：

```text
best val BER_data = 0.17227
```

独立拆分：

| Channel | BER_data |
|---|---:|
| Rayleigh | 0.39688 |
| Rician | 0.31543 |
| EPA | 0.00908 |
| EVA | 0.05459 |
| ETU | 0.05195 |
| Mean | 0.16559 |

结论：模型主要学会已知段，数据段迁移不足。

### Iter 2：修复 Rayleigh 输出长度

问题：可变 `delay_spread > num_taps - 1` 时，Rayleigh 输出长度超过 512，导致监督标签错位。

修复：`RayleighMultipathChannel.convolve()` 输出裁剪到输入长度。

结果：

```text
best val BER_data = 0.15228
```

独立拆分：

| Channel | BER_data |
|---|---:|
| Rayleigh | 0.39502 |
| Rician | 0.28965 |
| EPA | 0.00830 |
| EVA | 0.03359 |
| ETU | 0.06445 |
| Mean | 0.15820 |

结论：长度错位修复必要，但 Rayleigh/Rician 仍未解决。

### Iter 3：移除额外时延错位

问题：采样器让 `delay_spread` 大于 `num_taps - 1`，等价于引入额外未知整体时延，数据标签仍存在对齐困难。

修复：Rayleigh/Rician 采样中令 `delay_spread = num_taps - 1`，保留抽头数随机。

结果：

```text
best val BER_data = 0.04603
```

独立拆分：

| Channel | BER_data |
|---|---:|
| Rayleigh | 0.18037 |
| Rician | 0.07681 |
| EPA | 0.00024 |
| EVA | 0.01108 |
| ETU | 0.01504 |
| Mean | 0.05671 |

结论：主要收益来自消除标签/时延错位。3GPP 已接近目标，Rayleigh/Rician 是瓶颈。

### Iter 4：增强 Channel Encoder + FiLM + 更大模型

命令核心参数：

```bash
python pretrain.py --num_steps 900 --batch_size 3 --d_model 128 --n_layers 3 \
  --adapter_rank 16 --window_K 14 --use_channel_encoder --channel_dim 64 \
  --snr_min 10 --snr_max 10 --data_loss_weight 6.0 --known_loss_weight 0.35
```

结果：

```text
best val BER_data = 0.03716
```

独立拆分：

| Channel | BER_data |
|---|---:|
| Rayleigh | 0.13332 |
| Rician | 0.05824 |
| EPA | 0.00000 |
| EVA | 0.01078 |
| ETU | 0.01363 |
| Mean | 0.04320 |

在线 Rayleigh PEFT 验证：

| Method | BER_data |
|---|---:|
| PEFT/RL online | 0.14167 |
| MMSE baseline | 0.03275 |

结论：当前在线 PEFT 在逐帧随机 Rayleigh 上会被跨帧分布漂移拖累，未改善主指标。

### Iter 5：增大观测窗口到 K=24

命令核心参数：

```bash
python pretrain.py --num_steps 1200 --batch_size 3 --d_model 128 --n_layers 3 \
  --adapter_rank 16 --window_K 24 --use_channel_encoder --channel_dim 64 \
  --snr_min 10 --snr_max 10 --data_loss_weight 6.0 --known_loss_weight 0.35
```

结果：

```text
best val BER_data = 0.04547
```

结论：单纯扩大窗口没有超过 Iter 4，说明瓶颈不只是可观测上下文长度，还包括模型归纳偏置和在线适应策略。

## 最终评估

最终选择模型：

```text
pretrained/pure_ce_2026-07-11_10-25-55_iter4_film128/model_best.pt
```

独立评估设置：

- SNR = 10 dB
- 每类信道 120 帧
- Rayleigh/Rician 抽头数可变且受 `window_K + 1` 约束
- 对比：纯神经预训练模型 vs MMSE baseline

| Channel | Neural BER_data | MMSE BER_data |
|---|---:|---:|
| Rayleigh | 0.09967 | 0.01654 |
| Rician | 0.05684 | 0.01003 |
| EPA | 0.00007 | 0.00322 |
| EVA | 0.01051 | 0.00586 |
| ETU | 0.00788 | 0.00635 |
| Mean | 0.03499 | 0.00840 |

可视化输出：

- `logs/pure_ce_2026-07-11_10-25-55_final_eval/final_profile_comparison.png`
- `logs/pure_ce_2026-07-11_10-25-55_final_eval/final_mean_comparison.png`
- `pretrained/pure_ce_2026-07-11_10-25-55_iter4_film128/pretrain_curve.png`
- `logs/pure_ce_2026-07-11_10-25-55_iter4_online_rayleigh/online_adapt_metrics.png`

## 是否达标

未达标。

目标为五类信道平均 `BER_data < 0.01`，当前纯神经最终平均为 `0.03499`。MMSE baseline 在同一评估下为 `0.00840`，说明信道任务本身存在可达的传统解，但当前纯神经模型在 Rayleigh/Rician 上仍明显弱于 MMSE。

## 下一步优化方案

建议暂停继续盲目加训练，优先选择下面方案之一：

1. 方案 B：加入可微分神经同步/延迟估计头
   - 当前 Rayleigh/Rician 的主要错误来自随机多径相位和残余对齐不稳定。
   - 增加一个纯神经 delay/phase latent head，通过导频监督学习帧级时延和相位，再调制均衡器。
   - 仍保持纯神经，不引入 MMSE/LS。

2. 方案 C：把在线 PEFT 改为每帧从 θ_pre 复制一份临时参数再适应
   - 当前在线训练在逐帧随机信道下会把上一帧随机信道的适应结果带到下一帧，造成负迁移。
   - 每帧基于 θ_pre 初始化 Adapter/Head 临时副本，只用本帧 pilot 更新，再 equalize data。
   - 更符合“每帧 Pilot 学习当前信道”的实验目标。

3. 方案 D：加入神经判决反馈结构
   - Transformer 输出先给出粗判决，再用一个 causal refinement block 结合局部软判决上下文二次均衡。
   - 可提升长多径 Rayleigh/Rician 的数据段恢复能力。

4. 方案 E：课程学习训练
   - 先训练短抽头 Rayleigh/Rician，再逐步扩展到更长抽头和 3GPP。
   - 当前混合采样对 CPU 小批量训练较难，容易让 Rayleigh/Rician 收敛不足。

推荐优先执行：方案 C + 方案 B。

原因：方案 C 修正在线适应评估逻辑，成本低；方案 B 针对 Rayleigh/Rician 的核心难点，仍符合纯神经约束。
