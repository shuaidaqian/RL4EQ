# 迭代记录：候选动作探测 + 纠错型伪导频门控 + checkpoint 诊断

生成时间：2026-07-15 23:24:19  
架构约束：保持 `开发框架.md` 不变。  
执行依据：上一轮后续优先级。

## 1. 本轮目标

按优先级执行：

1. 增加 candidate action probing observation。
2. 将 pseudo-label gate 从单纯一致性扩展为 disagreement-aware 纠错型门控。
3. 修复或诊断 checkpoint 配置/结构不一致问题。
4. 验证最终效果，输出可视化结果。
5. 反思是否符合预期，并清理缓存和无用代码。

## 2. 代码修改

### 2.1 Candidate Action Probing Observation

修改文件：

```text
online_train.py
tests/test_parameter_efficient_adaptation.py
```

新增常量：

```text
CANDIDATE_PROBE_DIM = 6
```

新增函数：

```text
policy_observation_dim(use_action_probing, strategies)
_candidate_probe_features(...)
_build_policy_observation(...)
```

实现逻辑：

```text
base observation = 原 14 维 pilot-only observation

if use_action_probing:
  对每个候选 action 从 θ_pre 恢复模型
  执行一次 pilot-only 虚拟在线更新
  记录该 action 的 pilot 可观测摘要
  将摘要拼接到 PPO observation
```

每个 action 拼接 6 个特征：

| 特征 | 含义 |
|---|---|
| `probe_pilot_loss` | 候选动作虚拟更新后的 pilot loss |
| `probe_BER_pilot` | 候选动作虚拟更新后的 pilot BER |
| `probe_reward` | 候选动作 pilot-only reward |
| `probe_parameter_delta_norm` | 参数漂移范数 |
| `probe_pseudo_ratio` | 候选动作使用的伪导频比例 |
| `probe_adapt_steps / 5` | 归一化更新步数 |

在线训练和 imitation 数据集新增参数：

```text
use_action_probing: bool = False
```

命令行新增：

```bash
--use_action_probing
```

### 2.2 Disagreement-Aware Pseudo Gate

修改文件：

```text
agent/adaptation_controller.py
online_train.py
tests/test_parameter_efficient_adaptation.py
```

新增 action：

```text
pseudo-disagree-light
```

配置：

```text
lr = 2e-4
steps = 1
train_adapter = True
train_output = True
train_sync = True
pseudo_label_gate = disagree-neural
pseudo_weight = 0.20
pseudo_neural_threshold = 0.90
pseudo_mmse_threshold = 0.70
pseudo_max_ratio = 0.10
```

原有一致性门控：

```text
data & neural/MMSE agree & neural_conf >= τ_neural & mmse_conf >= τ_mmse
```

新增纠错型门控：

```text
data & neural/MMSE disagree & neural_conf >= τ_neural & mmse_conf <= τ_mmse
```

新增统计字段：

```text
pseudo_agreement_count
pseudo_disagreement_count
```

目的：

```text
一致性 gate 只能扩充高置信训练样本，难以修正 MMSE 自身错误；
disagreement gate 尝试捕捉 neural 高置信但 MMSE 低置信的样本，为 residual correction 提供纠错入口。
```

### 2.3 Checkpoint 结构不匹配诊断

修改文件：

```text
online_train.py
tests/test_parameter_efficient_adaptation.py
```

`_load_pretrained_if_available()` 现在捕获 `load_state_dict()` 的结构不匹配错误，返回可读诊断：

```text
预训练权重结构不匹配，使用随机初始化: ...
```

这样实验不会因为旧 checkpoint 与当前模型结构不一致而直接崩溃。后续更严格的做法仍应是：保存并读取 checkpoint 同目录 `model_config.json`，自动构建匹配模型，并在配置不一致时拒绝继续。

## 3. 测试结果

新增测试：

| 测试 | 覆盖内容 |
|---|---|
| `test_action_probing_policy_observation_appends_candidate_summaries` | action probing 会扩展 policy observation，并输出每个候选 action 的摘要。 |
| `test_disagreement_pseudo_gate_can_select_neural_confident_disagreement` | disagreement gate 能选择 neural 高置信且 neural/MMSE 不一致的 data 样本。 |
| `test_mismatched_checkpoint_returns_diagnostic_message` | 不匹配 checkpoint 返回诊断信息，不中断流程。 |

执行：

```bash
python -B -m pytest -p no:cacheprovider tests/test_parameter_efficient_adaptation.py -q
```

结果：

```text
43 passed in 29.18s
```

## 4. 验证实验

使用 checkpoint：

```text
pretrained/selective_distill_2026-07-15_00-18-50/model_best.pt
```

原因：

```text
该 checkpoint 与当前 use_mmse_features + channel_encoder + residual head 结构匹配。
pretrained/model_best.pt 仍存在结构/配置不一致问题，本轮只做诊断兜底，不把它作为主实验权重。
```

实验配置：

```text
SNR = 10 dB
profile = Rayleigh
num_frames = 8
seed = 2500
d_model = 128
n_layers = 3
adapter_rank = 16
window_K = 14
use_channel_encoder = True
channel_dim = 64
use_sync_head = True
sync_dim = 48
use_mmse_features = True
reset_each_frame = True
```

输出目录：

```text
logs/priority_next_2026-07-15_23-24-19/
```

关键文件：

```text
logs/priority_next_2026-07-15_23-24-19/summary.json
logs/priority_next_2026-07-15_23-24-19/priority1_action_probing_strategy/strategy_diagnostics.json
logs/priority_next_2026-07-15_23-24-19/priority1_action_probing_strategy/strategy_matrix.png
logs/priority_next_2026-07-15_23-24-19/priority1_action_probing_strategy/pilot_data_correlation.png
logs/priority_next_2026-07-15_23-24-19/priority2_disagreement_gate_online/online_adapt_metrics.png
logs/priority_next_2026-07-15_23-24-19/ablation_no_action_probing_online/summary.json
```

## 5. 策略诊断结果

Rayleigh, SNR=10 dB, 8 frames：

| 方法 | BER_data |
|---|---:|
| skip | 0.04883 |
| both-deep | 0.04785 |
| pseudo-both-fast | 0.04980 |
| pseudo-both-deep | 0.04980 |
| pseudo-disagree-light | 0.04980 |
| PPO policy | 0.05127 |
| pilot-oracle action | 0.04785 |
| data-oracle action | 0.04346 |
| MMSE | 0.02051 |

相关性：

| 指标 | 数值 |
|---|---:|
| `delta_pilot_loss_vs_delta_ber_data` | 0.12466 |
| `BER_pilot_vs_BER_data` | 0.39916 |

伪导频使用情况：

| 方法 | BER_data | pseudo_ratio | pseudo_disagreement_count |
|---|---:|---:|---:|
| pseudo-both-fast | 0.04980 | 0.25000 | 0.00 |
| pseudo-both-deep | 0.04980 | 0.39844 | 0.00 |
| pseudo-disagree-light | 0.04980 | 0.00342 | 0.88 |

### 反思

不符合预期。

1. data-oracle action 只到 `0.04346`，仍远弱于 MMSE `0.02051`。
2. pilot loss 改善与 data BER 改善相关性只有 `0.12466`，说明当前 pilot-only reward 对 data BER 的解释力较弱。
3. disagreement gate 能选到少量分歧样本，但比例只有 `0.00342`，信息量太小，不足以改变整体 BER。
4. pseudo action 没有优于 `both-deep`，说明伪导频当前仍没有形成有效纠错。

结论：

```text
当前 action space 的 oracle 上限仍不足。继续训练 PPO 不会解决核心问题。
```

## 6. 在线实验结果

开启 action probing：

| 方法 | BER_data | BER_pilot | latency_ms | probe_action_count |
|---|---:|---:|---:|---:|
| PEFT/PPO | 0.06055 | 0.02734 | 65.29 | 13.00 |
| MMSE | 0.01709 | 0.01074 | 0.00 | 0.00 |
| RLS | 0.02686 | 0.01074 | 0.00 | 0.00 |
| LMS | 0.03809 | 0.02637 | 0.00 | 0.00 |
| ZF | 0.05078 | 0.03711 | 0.00 | 0.00 |

不开启 action probing，同 seeds 对照：

| 方法 | BER_data | BER_pilot | latency_ms | probe_action_count |
|---|---:|---:|---:|---:|
| PEFT/PPO | 0.06055 | 0.02734 | 52.56 | 0.00 |
| MMSE | 0.01709 | 0.01074 | 0.00 | 0.00 |

### 反思

不符合预期。

本轮 action probing 没有改善 BER；在该 8 帧固定测试集上，开启与不开启 probing 的 PEFT/PPO BER 完全一致，但 latency 从 `52.56 ms` 增加到 `65.29 ms`。原因判断：

1. 目前 PPO policy 仍是随机初始化加极少 imitation/PPO 更新，短帧数下无法充分利用扩展 observation。
2. probing 特征只拼接到 policy 输入，没有用监督目标强制 policy 学会“选 probe 后 pilot 最优或 data-oracle 最优的 action”。
3. 当前 data-oracle 上限仍弱于 MMSE，说明即便 action 选对，PEFT 更新机制本身也不够强。
4. candidate probing 是有价值的诊断/特征机制，但需要配合 imitation learning 或 rule selector 才可能转化为性能。

## 7. 泛化结果

在线实验后无更新评估：

| 信道 | BER_data |
|---|---:|
| Rician | 0.05078 |
| EPA | 0.00000 |
| EVA | 0.00000 |
| ETU | 0.00000 |
| mean | 0.01270 |

注意：这是小样本 smoke 结果，不能作为论文主表结论。

## 8. 本轮结论

已完成三个优先级的工程实现：

1. `candidate action probing observation` 已可选启用，并记录每帧探测动作数量。
2. `pseudo-disagree-light` 已加入，支持 neural 高置信、MMSE 低置信且二者分歧的纠错型伪导频。
3. checkpoint 结构不匹配已有诊断兜底，不再直接中断实验。
4. 测试通过：`43 passed`。

但性能上没有达到预期：

```text
PEFT/PPO BER_data = 0.06055
MMSE BER_data     = 0.01709
RLS BER_data      = 0.02686
data-oracle BER   = 0.04346
```

这说明当前瓶颈不是 PPO observation 是否包含 probing，而是：

```text
action space + 在线 PEFT 更新机制 + 伪标签质量 的上限不足。
```

## 9. 下一步建议

优先级从高到低：

1. **实现 probe-rule selector baseline**
   - 不训练 PPO，直接选择 `probe_pilot_loss` 最低且 `parameter_delta_norm` 受限的 action。
   - 如果 rule selector 仍不优于固定策略，说明 probing 本身不能带来收益。

2. **把 imitation label 从 data-oracle action 改成 probe-aware oracle**
   - 输入包含每个 action 的 probe 摘要。
   - label 仍用 data-oracle action。
   - 训练 policy 判断“probe 结果与 data BER 之间的映射”。

3. **扩展 PEFT action space 的实质能力**
   - 当前 action 多数只是 lr/steps/module 差异。
   - 建议加入：
     - residual head only
     - sync/CFO head only
     - MMSE residual correction strength
     - stronger regularization id
     - pseudo loss warmup/anneal

4. **先让 data-oracle upper bound 接近 MMSE**
   - 如果 data-oracle 仍弱于 MMSE，任何 RL policy 都没有可能超过 MMSE。
   - 这是下一轮是否继续 RL 训练的硬门槛。

5. **修复 checkpoint/config 自动对齐**
   - 当前只是诊断兜底。
   - 后续应从 checkpoint 同目录读取 `model_config.json` 自动构建模型，避免手动参数错配。

## 10. 清理

已清理：

```text
__pycache__/
.pytest_cache/
```

未删除：

```text
logs/priority_next_2026-07-15_23-24-19/
```

该目录包含本轮实验结果和可视化，应保留用于回顾。
