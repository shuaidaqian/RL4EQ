# 迭代记录：prefix-only Offline NN 重新微调与收敛分析

时间戳：2026-08-08 01:01:58（Asia/Shanghai）

## 1. 本轮目标

上一轮已经确认：

- prefix-only 是正确契约；
- 传统 baseline 已进入合理困难区间；
- 旧 +50 checkpoint 在全 48 个主配置上都超过了最强传统 baseline；
- 但 SNR=10 仍未全部压到 `BER_data < 0.01`。

所以本轮目标不是启动 PPO，而是：

1. 先把 Offline NN 在 prefix-only 主工作区间进一步压低；
2. 观察单纯增加监督微调步数是否仍然有效；
3. 判断后续应该继续堆训练步数，还是应该换训练分布/训练策略。

## 2. 新建临时 prefix-only focus 配置

新增临时配置文件：

- [configs/continual_ppo_prefix_focus_snr10_15.json](D:/Research/RL4EQ/configs/continual_ppo_prefix_focus_snr10_15.json)

该配置的核心特点：

```text
pilot_layout = prefix
pilot_total  = 128
main_delays  = [20, 30, 40]
main_snrs    = [10, 15]
```

训练验证侧也收窄到了 SNR=10/15，减少无关样本干扰。

同时把 CIR 条件 augmentation 调轻：

```text
modes = acquisition, dd_like, acquisition, noisy
noise_std = 0.015
dd_flip_probability = 0.005
dd_blend_alpha = 0.1
```

这一版不是最终配置，只是为了判断“在 prefix-only 上继续微调是否有效”。

## 3. 续训能力确认

先做 smoke：

```powershell
.\.venv-gpu\Scripts\python.exe pretrain.py --config configs/continual_ppo_prefix_focus_snr10_15.json --stage estimated_cir_level_b --steps 2 --batch-size 1 --amp --resume pretrained/level_b_focus_d40_snr10_1650_from_1600_p128_2026-08-04_01-16-35/model_best.pt --save-dir pretrained/prefix_focus_snr10_15_smoke_2026-08-08
```

smoke 成功，说明：

- `pretrain.py` 可以严格加载旧 checkpoint；
- 新 focus 配置与当前模型结构兼容；
- 训练保存链路正常。

## 4. 200-step 微调结果

从旧 +50 checkpoint 继续训练 200 steps：

```powershell
.\.venv-gpu\Scripts\python.exe pretrain.py --config configs/continual_ppo_prefix_focus_snr10_15.json --stage estimated_cir_level_b --steps 200 --batch-size 4 --amp --resume pretrained/level_b_focus_d40_snr10_1650_from_1600_p128_2026-08-04_01-16-35/model_best.pt --save-dir pretrained/prefix_focus_snr10_15_200_from_plus50_2026-08-08
```

输出：

- [pretrained/prefix_focus_snr10_15_200_from_plus50_2026-08-08/model_best.pt](D:/Research/RL4EQ/pretrained/prefix_focus_snr10_15_200_from_plus50_2026-08-08/model_best.pt)
- [pretrained/prefix_focus_snr10_15_200_from_plus50_2026-08-08/pretrain_metrics.json](D:/Research/RL4EQ/pretrained/prefix_focus_snr10_15_200_from_plus50_2026-08-08/pretrain_metrics.json)

训练 loss 呈波动下降，说明没有立刻发散。

### 200-step 微调评估

配置：

```text
delay = 20/30/40
snr   = 10/15
pilot = 64/96/128
seeds = 0/1/2
frames = 16
```

与旧 +50 checkpoint 对比：

| 指标 | 旧 +50 checkpoint | 200-step checkpoint |
|---|---:|---:|
| 18 个候选配置平均 BER | 0.010024 | 0.008402 |
| SNR=10 平均 BER | 0.012762 | 0.011063 |
| SNR=15 平均 BER | 0.007286 | 0.005741 |
| `< 0.01` 配置数 | 9 / 18 | 11 / 18 |

每个配置都比旧 +50 更好，说明 prefix-only 继续监督微调是有效的。

但是结论也很明确：

- SNR=15 已全部进入 `<0.01`。
- SNR=10 仍只有 `delay=20, pilot=96/128` 进入 `<0.01`。
- `delay=30/40` 的 SNR=10 仍在 `0.0104–0.0127`，离门槛还有一小段距离。

## 5. 800-step 继续微调结果

从 200-step checkpoint 再训练 600 steps，得到累计 800-step checkpoint：

```powershell
.\.venv-gpu\Scripts\python.exe pretrain.py --config configs/continual_ppo_prefix_focus_snr10_15.json --stage estimated_cir_level_b --steps 600 --batch-size 4 --amp --resume pretrained/prefix_focus_snr10_15_200_from_plus50_2026-08-08/model_best.pt --save-dir pretrained/prefix_focus_snr10_15_800_from_plus50_2026-08-08
```

输出：

- [pretrained/prefix_focus_snr10_15_800_from_plus50_2026-08-08/model_best.pt](D:/Research/RL4EQ/pretrained/prefix_focus_snr10_15_800_from_plus50_2026-08-08/model_best.pt)

### 800-step 微调评估

和 200-step、旧 +50 对比结果如下：

| 指标 | 旧 +50 | 200-step | 800-step |
|---|---:|---:|---:|
| 18 个候选配置平均 BER | 0.010024 | 0.008402 | 0.007803 |
| SNR=10 平均 BER | 0.012762 | 0.011063 | 0.010482 |
| SNR=15 平均 BER | 0.007286 | 0.005741 | 0.005125 |
| `< 0.01` 配置数 | 9 / 18 | 11 / 18 | 12 / 18 |

关键点：

- 800-step 相对 200-step 在 18 / 18 个配置上继续改善，没有退化。
- 但改进幅度在收窄，说明继续单纯加训练步数的边际收益已经下降。
- SNR=15 继续稳固；
- SNR=10 仍有一半配置没过线，主要卡在 delay=30/40。

逐点上：

```text
SNR=10:
  d20/p96, d20/p128 已达标
  d30/p64, d30/p96, d30/p128 仍略高于 0.01
  d40/p64, d40/p96, d40/p128 仍略高于 0.01

SNR=15:
  全部达标
```

## 6. 当前结论

本轮结论可以明确写成三句：

1. prefix-only 重新训练是对的，且继续微调确实有效。
2. 继续堆监督步数还能提升，但已经进入边际收益递减区间。
3. 现在的主要瓶颈不是“训练完全没用”，而是：
   - 训练分布还不够针对 `SNR=10, delay=30/40`；
   - 需要更有针对性的课程或更强的中间监督；
   - 这比单纯加步数更重要。

## 7. 对后续路线的判断

本轮之后，不建议继续无脑增加同一配置上的监督步数。

更合理的下一步是：

### 7.1 重新组织训练分布

把训练重点放到更难的但仍有希望达成的区域：

```text
delay = 20/30/40
snr = 10/15
pilot = 64/96/128
```

考虑做更明确的 curriculum：

- 先让模型在 `SNR=15` 和 `delay=20/30` 稳定；
- 再提升 `delay=40` 和 `SNR=10`；
- 不是平均地喂所有样本。

### 7.2 继续保留 0/5 dB 作为压力测试

当前证据表明 0/5 dB 不是适合主门槛的区域，继续把它强行写进“所有主配置必须 `<0.01`”只会扭曲研究目标。

### 7.3 暂不启动 PPO

原因不变：

- Offline NN 仍未把 `SNR=10` 的全主候选压到 `<0.01`；
- 如果现在启动 PPO，RL 会在一个还没完全站稳的 receiver 上做动作搜索；
- 这会把“接收机本体不足”和“RL 策略不足”混成一个问题。

## 8. 验证

本轮验证通过：

### 关键测试

```powershell
.\.venv-gpu\Scripts\python.exe -m pytest tests\test_evaluation_contract.py::test_config_exposes_new_snr_and_pilot_sweep_contract tests\test_meta_adaptation.py::test_curriculum_train_reports_condition_cir_source_counts tests\test_meta_adaptation.py::test_pretrain_smoke_writes_strict_loadable_checkpoint -q -p no:cacheprovider
```

结果：

```text
3 passed in 39.33s
```

### 全量测试

```powershell
.\.venv-gpu\Scripts\python.exe -m pytest -q -p no:cacheprovider
```

结果：

```text
124 passed in 285.89s (0:04:45)
```

## 9. 本轮对研究方向的影响

本轮没有推翻前面的判断，反而更清楚地说明：

- 传统 baseline 的困难度定义已经被 prefix-only 修正；
- Offline NN 的能力在 prefix-only 上是可持续提升的；
- 但当前要想把 `SNR=10` 的 `delay=30/40` 也全部压进 `<0.01`，单靠继续加训练步数不一定够；
- 下一步更应该调整训练分布，而不是急着上 PPO。

## 10. 下一步建议

建议下一步做两件事：

1. 继续调整 prefix-only 的监督训练课程，让 `SNR=10`、`delay=30/40` 成为训练重点。
2. 重新检查 reward / Data 相关性，确认在新 checkpoint 上是否已经够稳定，再决定是否进入 RL 阶段。

目前最稳妥的判断是：

```text
Offline NN 还在变强；
PPO 仍应继续等待；
接下来的关键不是“再加多少步”，而是“训练分布如何更有针对性”。
```
