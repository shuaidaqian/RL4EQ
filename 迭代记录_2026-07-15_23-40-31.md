# 迭代记录：Probe Rule Selector、Probe-Aware Imitation、扩展 Action Space 与 Checkpoint 自动对齐

生成时间：2026-07-15 23:40:31  
架构约束：保持 `开发框架.md` 不变。  
执行依据：上一轮“下一步建议”的优先级顺序。

## 1. 本轮目标

按优先级依次执行：

1. 实现 `probe-rule selector baseline`。
2. 将 imitation learning 改为 probe-aware observation。
3. 扩展 PEFT action space 的实际能力。
4. 继续以 data-oracle upper bound 判断 RL 是否有提升空间。
5. 实现 checkpoint/config 自动对齐，避免手动模型参数错配。

## 2. 优先级 1：Probe Rule Selector Baseline

### 实现

修改文件：

```text
online_train.py
tests/test_parameter_efficient_adaptation.py
```

新增函数：

```text
_select_probe_rule_action(...)
```

规则：

```text
对每个 action 做 pilot-only probe
优先选择 probe_pilot_loss 最低的 action
若 parameter_delta_norm <= 0.03，则优先在可行集合内选择
排序键：
  probe_pilot_loss
  probe_BER_pilot
  probe_parameter_delta_norm
  probe_adapt_steps
```

`run_strategy_diagnostics()` 新增方法：

```text
probe_rule_selector
```

### 验证

测试：

```text
test_probe_rule_selector_is_reported_by_strategy_diagnostics
```

结果包含：

```text
probe_rule_selector
probe_action_count == len(make_strategy_table())
```

### 实验结果

Rayleigh, SNR=10 dB, 8 frames：

| 方法 | BER_data |
|---|---:|
| skip | 0.04004 |
| both-deep | 0.03662 |
| probe_rule_selector | 0.03662 |
| pilot_oracle_action | 0.03662 |
| data_oracle_action | 0.03467 |
| MMSE | 0.01904 |

### 反思

部分符合预期。

`probe_rule_selector` 能达到 `pilot_oracle_action`，说明 probe 的 pilot 可观测信息足够选择 pilot 最优动作；但它仍明显弱于 MMSE，且 data-oracle 也只有 `0.03467`，说明问题不是 selector 不会选，而是当前 action space 的可达上限仍不足。

## 3. 优先级 2：Probe-Aware Imitation Learning

### 实现

已有 `build_oracle_action_dataset()` 支持 data-oracle label。本轮补齐：

```text
use_action_probing=True 时，imitation observation = base 14维 + 每个 action 的 probe 摘要
```

新增函数：

```text
policy_observation_dim(...)
_candidate_probe_features(...)
_build_policy_observation(...)
```

每个 action 的 probe 摘要包含 6 维：

| 特征 | 含义 |
|---|---|
| `probe_pilot_loss` | 候选动作虚拟更新后的 pilot loss |
| `probe_BER_pilot` | 候选动作虚拟更新后的 pilot BER |
| `probe_reward` | pilot-only reward |
| `probe_parameter_delta_norm` | 参数漂移 |
| `probe_pseudo_ratio` | 伪导频比例 |
| `probe_adapt_steps / 5` | 归一化更新步数 |

### 验证

测试：

```text
test_probe_aware_imitation_dataset_uses_expanded_observation
```

验证：

```text
dataset["observations"].shape[1] == policy_observation_dim(True, make_strategy_table())
```

### 实验结果

固定 seeds，8 frames，`imitation_frames=6`，`imitation_epochs=4`：

| 方法 | BER_data | BER_pilot | latency_ms | probe_action_count |
|---|---:|---:|---:|---:|
| PEFT/PPO + probe-aware imitation | 0.04785 | 0.02734 | 87.07 | 15.00 |
| PEFT/PPO no-probe 对照 | 0.04785 | 0.02734 | 80.49 | 0.00 |
| MMSE | 0.00635 | 0.00391 | 0.00 | 0.00 |

### 反思

不符合预期。

probe-aware imitation 在当前小样本设置下没有改善 BER，只增加了在线开销。原因判断：

1. data-oracle upper bound 仍弱，policy 学到最优 action 也无法接近 MMSE。
2. `imitation_frames=6` 太少，只能验证管线，不能证明策略学习有效。
3. 当前 observation 扩展没有配套更强的 action space，信息量变多但可执行动作仍不够强。

## 4. 优先级 3：扩展 PEFT Action Space

### 实现

修改文件：

```text
agent/neural_equalizer.py
agent/adaptation_controller.py
tests/test_parameter_efficient_adaptation.py
```

`AdapterEqualizer.enable_parameter_efficient_tuning()` 新增：

```text
train_residual
```

`set_trainable_targets()` 新增：

```text
train_residual: bool | None = None
```

`AdaptationStrategy` 新增：

```text
train_residual
```

新增 action：

| action | 含义 |
|---|---|
| `residual-light` | 只训练 residual head，用于 MMSE residual correction。 |
| `sync-only` | 只训练 sync/CFO 相关头和 FiLM，不动 adapter/output。 |

当前 action 数量从 13 增至 15。

### 验证

新增测试：

```text
test_equalizer_can_train_residual_head_without_output_head
test_action_table_contains_residual_and_sync_focused_actions
```

验证：

```text
residual-only 模式下，所有可训练参数名都以 residual_head 开头
action table 包含 residual-light / sync-only
```

### 实验结果

策略诊断中的新增 action：

| 方法 | BER_data |
|---|---:|
| residual-light | 0.04004 |
| sync-only | 0.03857 |
| both-deep | 0.03662 |
| data-oracle_action | 0.03467 |
| MMSE | 0.01904 |

### 反思

不符合预期。

`residual-light` 和 `sync-only` 没有超过 `both-deep`，说明单独训练 residual/sync 头在当前帧级 pilot-only 监督下不足以带来更强修正。可能原因：

1. residual head 离线已接近收敛，在线只用 pilot 更新时单独调整收益有限。
2. sync-only 需要明确 CFO/相位/时延监督或更强的物理一致性 reward，目前仅靠 pilot BCE 不够。
3. 真正需要扩展的是“校正强度/正则/伪标签使用方式”，而不只是多开一个参数子集。

## 5. 优先级 4：Data-Oracle Upper Bound 复核

### 结果

本轮固定 seeds：

| 方法 | BER_data |
|---|---:|
| skip | 0.04004 |
| probe_rule_selector | 0.03662 |
| data-oracle_action | 0.03467 |
| MMSE | 0.01904 |

相关性：

| 指标 | 数值 |
|---|---:|
| `delta_pilot_loss_vs_delta_ber_data` | 0.44124 |
| `BER_pilot_vs_BER_data` | 0.89163 |

### 反思

不符合论文性能目标。

虽然 pilot 与 data 在这组 seeds 上相关性较强，但 data-oracle 仍明显弱于 MMSE。这是关键结论：

```text
在当前 action space 下，RL policy 再强也不能超过 MMSE。
```

因此下一轮如果继续追求“超过传统均衡”，必须先改变 online adaptation 的可达上限，例如：

```text
MMSE residual correction strength action
residual consistency regularization action
pseudo loss weight/threshold 连续控制
多步在线更新但带 stronger drift penalty
低 pilot overhead / nonlinear / CFO 场景专项 action
```

## 6. 优先级 5：Checkpoint/Config 自动对齐

### 实现

修改文件：

```text
online_train.py
tests/test_parameter_efficient_adaptation.py
```

新增函数：

```text
infer_equalizer_kwargs_from_checkpoint(pretrained)
_maybe_align_kwargs_to_pretrained(pretrained, kwargs)
```

推断字段：

```text
d_model
n_layers
adapter_rank
window_K
use_channel_encoder
channel_dim
use_sync_head
sync_dim
sync_delay_bins
use_mmse_features
use_cfo_head
```

已接入：

```text
build_oracle_action_dataset()
run_strategy_diagnostics()
run_online_adaptation()
```

### 验证

新增测试：

```text
test_checkpoint_shape_can_infer_matching_equalizer_kwargs
```

额外 smoke：

```text
故意传错 d_model/window_K/use_mmse_features
pretrained = pretrained/model_best.pt
入口自动从 checkpoint 权重形状推断并加载
```

结果：

```text
已加载预训练: pretrained/model_best.pt | missing=0 unexpected=0
```

推断结果：

```json
{
  "d_model": 128,
  "n_layers": 3,
  "adapter_rank": 16,
  "window_K": 14,
  "use_channel_encoder": true,
  "channel_dim": 64,
  "use_sync_head": true,
  "sync_dim": 48,
  "sync_delay_bins": 9,
  "use_mmse_features": false,
  "use_cfo_head": false
}
```

smoke 指标：

| 方法 | BER_data | BER_pilot |
|---|---:|---:|
| PEFT | 0.00000 | 0.00000 |
| MMSE | 0.00391 | 0.00000 |

注意：这是 1 帧 smoke，只证明 checkpoint 自动对齐和加载路径正确，不作为性能结论。

## 7. 完整测试

执行：

```bash
python -B -m pytest -p no:cacheprovider tests/test_parameter_efficient_adaptation.py -q
```

结果：

```text
48 passed in 34.19s
```

## 8. 可视化与日志

输出目录：

```text
logs/priority_sequence_2026-07-15_23-40-31/
```

关键文件：

```text
logs/priority_sequence_2026-07-15_23-40-31/summary.json
logs/priority_sequence_2026-07-15_23-40-31/priority1_probe_rule_strategy/strategy_diagnostics.json
logs/priority_sequence_2026-07-15_23-40-31/priority1_probe_rule_strategy/strategy_matrix.png
logs/priority_sequence_2026-07-15_23-40-31/priority1_probe_rule_strategy/pilot_data_correlation.png
logs/priority_sequence_2026-07-15_23-40-31/priority2_probe_aware_imitation_online/online_adapt_metrics.png
logs/priority_sequence_2026-07-15_23-40-31/ablation_no_probe_online/online_adapt_metrics.png
logs/priority_sequence_2026-07-15_23-40-31/priority5_checkpoint_auto_align_smoke/summary.json
```

## 9. 总体结论

本轮所有优先级均已实现并验证，工程闭环更完整：

1. `probe_rule_selector` 可作为 PPO 前的强规则 baseline。
2. `probe-aware imitation` 管线已可用。
3. action space 新增 residual-only / sync-only。
4. data-oracle upper bound 已继续作为硬诊断指标。
5. checkpoint 能自动按权重形状对齐模型构造参数。

但性能仍不满足论文目标：

```text
data-oracle_action = 0.03467
MMSE               = 0.01904
```

当前最重要的事实是：

```text
策略选择已经不是唯一瓶颈，当前在线 PEFT action space 的可达性能仍弱于传统 MMSE。
```

## 10. 下一步建议

如果目标仍是支撑“RL 优化均衡优于传统方法”的论文，需要优先提升 oracle 上限：

1. 增加 `MMSE residual correction strength` action，让 RL 控制 residual 修正强度，而不是只控制训练模块。
2. 增加 pseudo loss weight / threshold 的连续或离散组合，让 RL 真的控制伪导频门控强度。
3. 设计针对低 pilot overhead、CFO、非线性场景的专项 action，而不是只在标准 Rayleigh 线性场景争胜。
4. 在 data-oracle 超过或接近 MMSE 之前，不建议扩大 PPO 训练规模；否则只是更稳定地学习一个上限不足的 action space。

## 11. 清理

已清理：

```text
__pycache__/
.pytest_cache/
```

保留：

```text
logs/priority_sequence_2026-07-15_23-40-31/
```

该目录包含本轮实验结果、summary 和可视化图，便于后续回顾。
