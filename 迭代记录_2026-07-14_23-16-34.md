# 迭代记录 2026-07-14 23:16:34

本轮目标：在保持 `开发框架.md` 不变的前提下，扩大 residual-assisted 离线训练和 imitation 数据规模，并把实验场景扩展到传统模型更容易失配的 CFO、Doppler、非线性、低 pilot overhead 场景；执行后验证最终效果，输出可视化和分析。

## 1. 本轮实现内容

### 1.1 环境层失配 impairment

在 `env/comm_env.py` 中为 `EnvConfig` 新增：

```python
impairments: dict = field(default_factory=dict)
```

当前支持：

- `cfo_norm`：归一化载波频偏，对整帧接收 IQ 做逐符号相位旋转。
- `nonlinear_alpha`：简化 AM/AM 非线性压缩，用于模拟功放或接收链路非线性。

实现放在环境层，而不是写进每个具体信道类，因此 Rayleigh、Rician、EPA、EVA、ETU 都可以复用同一套 impairment。

### 1.2 低 pilot overhead 帧结构

在 `env/frame_structure.py` 新增：

```python
frame_config_for_known_ratio(ratio)
```

用于构造低已知位比例帧，例如：

- `0.50`
- `0.25`
- `0.125`

保持 `frame_len=512` 不变，只改变 `train_len` 和 `pilot_len`。

### 1.3 residual-assisted 离线训练扩展

`pretrain.py` 新增参数：

```bash
--train_known_ratios 0.5,0.25,0.125
--impairment_prob 0.65
```

对应代码参数：

```python
train_known_ratios=[0.5, 0.25, 0.125]
impairment_prob=0.65
```

离线训练采样器现在可以在训练期间混合：

- Rayleigh / Rician / EPA / EVA / ETU
- CFO
- Doppler
- 非线性
- 低 pilot overhead

并继续支持从旧 `pretrained/model_best.pt` 兼容初始化 residual-assisted 模型。

### 1.4 imitation 与在线评估场景扩展

`online_train.py` 新增：

```python
mismatch_scenarios(...)
```

包含：

| 场景 | 说明 |
|---|---|
| `cfo` | Rayleigh + CFO |
| `doppler` | Rayleigh + time-varying Doppler |
| `nonlinear` | Rayleigh + 非线性压缩 |
| `low_pilot` | Rayleigh + 25% 已知位 |

`run_online_adaptation(...)` 和 `build_oracle_action_dataset(...)` 现在可接收：

```python
frame_config
impairments
channel_config
```

这样 imitation 数据集和在线评估能严格使用同一类失配场景。

### 1.5 失配场景对比入口

`compare.py` 新增：

```python
run_mismatch_scenario_comparison(...)
```

输出：

- PEFT / RL residual-assisted 方法
- MMSE
- matched filter
- zero forcing
- LMS
- RLS

并保存 JSON 和图。

## 2. 测试验证

新增测试覆盖：

- CFO/非线性 impairment 会改变接收 IQ，且保持数值有限。
- `mismatch_scenarios(...)` 包含 CFO、Doppler、非线性、低 pilot overhead。
- 离线预训练支持混合低 pilot overhead。
- 失配场景对比函数能导出指标和可视化。

最终测试命令：

```bash
python -B -m pytest -p no:cacheprovider tests/test_parameter_efficient_adaptation.py -q
```

测试结果：

```text
33 passed
```

## 3. 扩大 residual-assisted 离线训练

### 3.1 训练配置

```python
num_steps=80
batch_size=2
SNR=10 dB
d_model=128
n_layers=3
adapter_rank=16
window_K=14
use_channel_encoder=True
use_sync_head=True
use_mmse_features=True
init_checkpoint="pretrained/model_best.pt"
train_known_ratios=[0.5, 0.25, 0.125]
impairment_prob=0.65
```

输出目录：

```text
pretrained/residual_mismatch_expanded_2026-07-14_23-16-34/
```

### 3.2 训练结果

| Step | train_BER | val_data | val_pilot |
|---:|---:|---:|---:|
| 1 | 0.3213 | 0.0409 | 0.0354 |
| 20 | 0.0039 | 0.0169 | 0.0135 |
| 40 | 0.2451 | 0.0227 | 0.0177 |
| 60 | 0.2441 | 0.0070 | 0.0063 |
| 80 | 0.3301 | 0.0151 | 0.0063 |

最佳离线验证：

```text
best_data_ber = 0.00703125
```

对比上一轮 residual-assisted 初始化训练：

```text
上一轮 best_data_ber = 0.008203125
本轮 best_data_ber   = 0.00703125
```

### 3.3 反思

符合预期：

- 扩大训练步数、加入 mixed overhead 和 impairment 后，离线验证 `BER_data` 从 0.00820 改善到 0.00703。
- 训练中 `train_BER` 波动较大，这是混合低 overhead 和 impairment 后的预期现象；不同 batch 难度差异显著。

不完全符合预期：

- 当前仍只是 80 steps 的小规模验证，不是论文级训练规模。
- 低 overhead 与 impairment 混合会改变数据分布，后续需要固定验证集按场景分开 early stopping，而不是只看综合验证均值。

## 4. 失配场景在线评估

### 4.1 评估配置

```python
scenario_names=["cfo", "doppler", "nonlinear", "low_pilot"]
num_frames=6
SNR=10 dB
imitation_frames=8
imitation_epochs=8
pretrained="pretrained/residual_mismatch_expanded_2026-07-14_23-16-34/model_best.pt"
use_mmse_features=True
```

输出目录：

```text
logs/mismatch_expanded_2026-07-14_23-16-34/
```

输出文件：

```text
logs/mismatch_expanded_2026-07-14_23-16-34/mismatch_scenario_comparison.json
logs/mismatch_expanded_2026-07-14_23-16-34/mismatch_scenario_comparison.png
```

### 4.2 结果摘要

| 场景 | PEFT BER_data | MMSE BER_data | ZF BER_data | LMS BER_data | RLS BER_data |
|---|---:|---:|---:|---:|---:|
| CFO | 0.49544 | 0.50260 | 0.47721 | 0.50781 | 0.50195 |
| Doppler | 0.05859 | 0.01888 | 0.09049 | 0.04753 | 0.01888 |
| Nonlinear | 0.08724 | 0.05013 | 0.14453 | 0.05924 | 0.04557 |
| Low pilot | 0.12109 | 0.05208 | 0.15495 | 0.11068 | 0.00651 |

可视化：

```text
logs/mismatch_expanded_2026-07-14_23-16-34/mismatch_scenario_comparison.png
```

## 5. 是否符合预期

### 5.1 达成的部分

1. residual-assisted 离线训练规模已扩大。
2. imitation 数据规模已从上一轮的 3 帧扩大到 8 帧，并在每个失配场景单独生成 data-oracle action 标签。
3. CFO、Doppler、非线性、低 pilot overhead 场景已落地，并能统一输出 PEFT/MMSE/ZF/LMS/RLS 对比。
4. 本轮 residual-assisted 离线验证优于上一轮，说明模型结构和训练入口是有效的。

### 5.2 未达到预期的部分

在线效果仍未稳定超过传统算法。

具体看：

- CFO：PEFT=0.49544，MMSE=0.50260，RLS=0.50195。PEFT 只略好于 MMSE/RLS，但整体接近随机；ZF=0.47721 反而略好。
- Doppler：PEFT=0.05859，MMSE/RLS=0.01888，传统方法明显更强。
- Nonlinear：PEFT=0.08724，MMSE=0.05013，RLS=0.04557，传统方法仍更强。
- Low pilot：PEFT=0.12109，RLS=0.00651，RLS 远强于当前 PEFT。

因此，目前结果不能支撑“强化学习均衡方法整体优于传统均衡”的论文结论。

## 6. 原因分析

### 6.1 CFO 场景

CFO 场景下所有方法都接近 0.5 BER，说明当前系统缺少显式 CFO 估计/补偿能力。RL 只控制 PEFT 更新策略，没有直接控制 CFO correction，因此很难从 pilot loss 中恢复全帧相位旋转。

### 6.2 Doppler 场景

当前 Doppler 实现仍是块级信道生成为主，帧内 time-varying 影响有限；同时 RLS 这类自适应 FIR 对此类线性时变问题非常强。PEFT 的动作空间只更新神经参数，响应速度不如 RLS。

### 6.3 非线性场景

非线性压缩已让 ZF 明显变差，但 MMSE/RLS 仍能保持较低 BER。当前 residual head 没有显式建模非线性逆变换，也没有在 loss 中对 MMSE 错误区域进行选择性修正。

### 6.4 低 pilot overhead

低 pilot 场景下 PEFT 仍弱，说明混合 overhead 训练 80 steps 不足以让模型适应 25% 已知位结构。RLS 在小样本下异常强，后续需要更大固定 seeds 验证它是否稳定，也要检查 RLS 是否从训练段获得了过强信息优势。

## 7. 后续改进建议

按优先级建议如下：

1. 增加显式 CFO latent head，并让 RL action 控制 CFO correction 强度。  
   仅靠 PEFT 更新神经参数无法高效处理整帧相位旋转。

2. 把 imitation 数据扩大到至少 1000 帧。  
   当前 8 帧只是链路验证，不足以训练稳定 policy。需要记录 imitation action accuracy 和 oracle regret。

3. 将 residual learning 改为 selective distillation。  
   在 pilot/training 上判断 MMSE 是否正确：MMSE 正确时蒸馏，MMSE 错误时训练 neural residual correction。

4. 对低 pilot overhead 单独训练模型。  
   当前混合训练比例 `[0.5, 0.25, 0.125]` 会造成目标冲突。建议分别训练 50%、25%、12.5% 模型，并做 cross-overhead 泛化表。

5. 对 RLS baseline 做严格公平性审计。  
   需要确认 RLS 只使用 training 段，不使用 pilot/data 标签；同时在更多 seeds 下报告均值和方差。

6. 将论文表述调整为更稳妥的方向。  
   当前可写成：  
   “Model-assisted RL controls residual PEFT under pilot-only online observability; current gains depend strongly on mismatch type, and explicit synchronization heads are necessary for CFO-dominated channels.”  
   不建议写成“RL 均衡全面超过传统均衡”。

## 8. 本轮结论

本轮完成了结构扩展和实验闭环扩展，但性能结论仍偏负面：

- 离线 residual-assisted 模型有改善：`best_data_ber=0.00703`。
- 在线 PEFT/RL 在 CFO 场景只略优于 MMSE/RLS，但整体接近随机。
- 在线 PEFT/RL 在 Doppler、非线性、低 pilot overhead 场景仍弱于 MMSE/RLS。

下一步不应继续单纯增加 PPO 复杂度。更有效的路线是：

```text
显式 CFO/同步 latent head
-> selective residual distillation
-> 大规模 data-oracle imitation
-> pilot-only PPO fine-tune
-> 按固定 seeds 做场景级消融
```

## 9. 清理状态

- 未修改 `开发框架.md`。
- 未恢复历史并行实现。
- 已清理不可达旧代码和缓存目录。
- 新增功能均有测试覆盖。
