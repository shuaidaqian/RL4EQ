# 迭代记录：DFE-RLS 公平性检查与 receiver-level RL 动作

时间戳：2026-08-11 01:00:38（Asia/Shanghai）

## 1. 本轮目标

上一轮结论是：

```text
Offline NN 在部分 compare 切片中已低于 BER_data < 0.01，
但仍弱于 DFE-RLS；
PPO/PEFT 动作没有稳定超过 Offline NN only。
```

本轮继续推进两个问题：

1. 先确认 `DFE-RLS` 为什么强，是否存在标签泄漏或不公平实现。
2. 如果 baseline 公平，则把 RL 动作空间从“主要 PEFT”扩展到“接收机级控制”，优先让 RL 选择 DD-CIR 更新强度。

## 2. DFE-RLS 数据流审查

检查文件：

```text
baseline/traditional_equalizers.py
compare.py
tests/test_traditional_baselines.py
```

结论：

- `DFE-RLS` 调用入口是：

```python
run_traditional_equalizer(method, frame.receiver_view(), state.cir, state.receiver_state.soft_tail, snr_db)
```

- `receiver_view()` 只暴露：
  - `rx_symbols`
  - `adapt_mask`
  - `adapt_symbols`
  - `model_region_ids`

- `DFE-RLS` 只使用：
  - 接收符号；
  - Adapt Pilot 位置；
  - Adapt Pilot 已知发送符号；
  - RLS/DFE 传统递推；
  - Data 区域硬判决反馈。

- 不读取：
  - Reward Pilot 标签；
  - Data 标签；
  - true CIR；
  - 神经网络；
  - RL policy。

已有测试：

```text
tests/test_traditional_baselines.py::test_traditional_baselines_do_not_read_reward_or_data_labels
```

会替换 Reward/Data 标签并确认传统 baseline 输出不变。

当前判断：

```text
DFE-RLS 当前没有直接标签泄漏。
```

它强的主要原因是：它每帧用 Adapt Pilot 直接训练一个长 FIR + feedback 的传统均衡器。由于本研究对象是整帧缓冲、非因果块接收机，传统 baseline 使用整帧接收信号本身是公平的；只要不读 Reward/Data 标签即可。

## 3. DFE-RLS 稳定性小矩阵

配置：

```text
checkpoint = pretrained/focus_d40_snr10_seq64best_lr1e5_plus600_alpha0p6_2026-08-10/model_best.pt
delays = 30, 40
SNR = 10 dB
pilot_total = 128
frames = 32
num_seeds = 2
cir_update = decision_directed
cir_alpha = 0.6
```

输出：

```text
logs/compare_stability_d30d40_s10_2seed32f_2026-08-11
```

结果：

```text
d30/s10:
  DFE-RLS                              BER_data = 0.0075276693
  Offline NN only                      BER_data = 0.0121256514
  RL-Modulated Neural Block Equalizer  BER_data = 0.0121256514

d40/s10:
  DFE-RLS                              BER_data = 0.0094401042
  Offline NN only                      BER_data = 0.0144856775
  RL-Modulated Neural Block Equalizer  BER_data = 0.0145670577
```

结论：

- 在 SNR=10 dB 的 2 seed 小矩阵上，DFE-RLS 优势稳定；
- 不是单个 seed 的偶然；
- 当前神经接收机在 d30/d40 hard 配置上仍没有超过 DFE-RLS。

## 4. 低 SNR 小矩阵

配置：

```text
delays = 30, 40
SNR = 0, 5 dB
pilot_total = 128
frames = 32
num_seeds = 1
```

输出：

```text
logs/compare_low_snr_d30d40_0_5_1seed32f_2026-08-11
```

结果：

```text
d30/s0:
  DFE-RLS         BER_data = 0.1341145847
  Offline NN only BER_data = 0.1271972691
  RL-Modulated    BER_data = 0.1271972691

d30/s5:
  DFE-RLS         BER_data = 0.0336100260
  Offline NN only BER_data = 0.0323079435
  RL-Modulated    BER_data = 0.0305175789

d40/s0:
  DFE-RLS         BER_data = 0.1275227868
  Offline NN only BER_data = 0.1301269575
  RL-Modulated    BER_data = 0.1301269575

d40/s5:
  DFE-RLS         BER_data = 0.0332031249
  Offline NN only BER_data = 0.0315755221
  RL-Modulated    BER_data = 0.0316569021
```

结论：

- 在 SNR=5 dB，Offline NN 已经略优于 DFE-RLS；
- d30/s5 下 RL-alpha 动作有进一步改善；
- 但绝对 BER 约 `0.03`，不满足 `BER_data < 0.01`。

这说明：

```text
神经方法超过传统不是完全没有迹象；
但在满足 BER<0.01 的 SNR=10 工作区间，DFE-RLS 仍然更强。
```

## 5. 新增 receiver-level CIR alpha 动作

### 5.1 动机

前几轮 PEFT 动作大多只是在扰动已经较强的 Offline NN。

相比之下，DD-CIR alpha 搜索明确有效：

```text
alpha=0.6/0.7 明显优于 alpha=0.2/0.4。
```

因此本轮把 RL 动作空间扩展为接收机级控制：

```text
cir_alpha_slow     alpha = 0.3
cir_alpha_nominal  alpha = 0.6
cir_alpha_fast     alpha = 0.8
```

这些动作：

- 不更新 PEFT 参数；
- 不使用 Data 标签；
- 只控制 DD-CIR 更新强度；
- 同一动作作用于窗口。

### 5.2 修改文件

```text
agent/discrete_safe_policy.py
training/windowed_discrete_ppo.py
tests/test_windowed_discrete_ppo.py
```

### 5.3 新增测试

```text
tests/test_windowed_discrete_ppo.py::test_discrete_safe_action_catalog_is_bounded_and_has_identity
tests/test_windowed_discrete_ppo.py::test_windowed_frame_can_use_receiver_level_cir_alpha_action
```

测试覆盖：

- 动作表包含 `cir_alpha_slow / nominal / fast`；
- 这些动作没有 PEFT 参数；
- 窗口帧 DD-CIR 更新实际使用动作指定 alpha；
- 输出行记录 `selected_cir_update_alpha`；
- `cir_update_uses_data_labels=false`。

## 6. receiver-level alpha 动作 compare

配置：

```text
delays = 30, 40
SNR = 10 dB
frames = 32
num_seeds = 2
pilot_total = 128
```

旧动作表结果：

```text
d30/s10:
  DFE-RLS         0.0075276693
  Offline NN only 0.0121256514
  RL-Modulated    0.0121256514

d40/s10:
  DFE-RLS         0.0094401042
  Offline NN only 0.0144856775
  RL-Modulated    0.0145670577
```

新 receiver-level alpha 动作结果：

```text
d30/s10:
  DFE-RLS         0.0075276693
  Offline NN only 0.0121256514
  RL-Modulated    0.0121256514

d40/s10:
  DFE-RLS         0.0094401042
  Offline NN only 0.0144856775
  RL-Modulated    0.0144449874
```

动作统计：

```text
sampled:
  identity                        80
  cir_alpha_nominal               16
  cir_alpha_slow                   8
  cir_alpha_fast                   8
  peft_adapter_lora_light          8
  peft_adapter_lora_conservative   8

selected_alpha:
  0.6  112
  0.3    8
  0.8    8
```

结论：

- receiver-level alpha 动作让 d40/s10 上 RL 从略弱于 Offline NN 变成略强于 Offline NN；
- 改善很小，但方向正确；
- 仍然远弱于 DFE-RLS。

## 7. 新动作表在线训练

配置：

```text
d40/s10/p128
frames = 300
window_size = 4
update_interval = 16
cir_alpha = 0.6
```

输出：

```text
logs/online_receiver_alpha_actions_d40s10p128_300f_2026-08-11
```

在线训练自身：

```text
RL mean BER_data            = 0.0156076393
same-state offline_identity = 0.0156076393
```

动作统计：

```text
sampled:
  identity                        252
  peft_adapter_lora_conservative   12
  cir_alpha_fast                   12
  cir_alpha_nominal                 8
  peft_head_light                   8
  peft_adapter_lora_light           4
  cir_alpha_slow                    4

selected_alpha:
  0.6  284
  0.8   12
  0.3    4
```

结论：

- 300-frame PPO 训练后，策略仍主要学到 identity；
- 没有超过 same-state Offline identity；
- policy 学习效果仍不足。

## 8. 加载 trained policy 的 compare

输出：

```text
logs/compare_receiver_alpha_trained_policy_d30d40_s10_2seed32f_2026-08-11
```

结果与未训练 receiver-level alpha 动作完全一致：

```text
d30/s10:
  DFE-RLS         0.0075276693
  Offline NN only 0.0121256514
  RL-Modulated    0.0121256514

d40/s10:
  DFE-RLS         0.0094401042
  Offline NN only 0.0144856775
  RL-Modulated    0.0144449874
```

结论：

- 新动作空间本身带来一点静态探索收益；
- PPO 训练没有进一步放大该收益；
- 当前 reward / update 机制仍不足以让策略稳定学习。

## 9. policy checkpoint 动作表兼容保护

因为动作表从 7 个动作扩展到 10 个动作，旧 policy checkpoint 不再兼容。

本轮修改：

```text
training/windowed_discrete_ppo.py::_load_discrete_policy_if_available
compare.py
```

新增参数：

```python
expected_action_names
```

行为：

- 如果 policy checkpoint 的 `action_names` 与当前动作表不一致：
  - `required=False`：安全跳过旧 checkpoint；
  - `required=True`：抛出清晰 `ValueError("动作表不兼容...")`。

新增测试：

```text
tests/test_windowed_discrete_ppo.py::test_discrete_policy_loader_skips_incompatible_action_catalog_when_optional
```

## 10. 当前结论

1. `DFE-RLS` 当前没有发现标签泄漏；它强主要是因为每帧用 Adapt Pilot 训练传统 DFE/RLS 均衡器。
2. 在 SNR=10 的 d30/d40 hard 区间，DFE-RLS 优势稳定。
3. 在 SNR=5，神经方法可以略优于 DFE-RLS，但绝对 BER 太高，不能作为当前论文主成功区间。
4. receiver-level `cir_alpha` 动作比纯 PEFT 更合理，能让 RL 在 d40/s10 上略微超过 Offline NN only。
5. 当前 PPO 训练仍没有学出额外收益，主要仍停留在 identity/nominal alpha。
6. 下一阶段必须继续重构 RL 的学习信号或把接收机级动作扩大到更有物理意义的控制项。

## 11. 下一步建议

下一步优先级：

### 11.1 继续 receiver-level 动作，而不是回到 PEFT 为主

建议新增动作：

```text
dd_cir_skip
dd_cir_alpha_0.3
dd_cir_alpha_0.6
dd_cir_alpha_0.8
extra_refine_light
extra_refine_strong
rollback_receiver_state
```

PEFT 动作保留，但不作为主探索方向。

### 11.2 改 reward 粒度

当前 policy 训练没学出额外收益，说明 Reward Pilot 的窗口信号还不够强。

建议尝试：

```text
窗口级 reward = Reward Pilot BER 改善为主
只在窗口结束更新策略
同一窗口内 receiver-level 动作保持不变
```

并减少单帧 loss 对策略更新的影响。

### 11.3 重新检查 DFE-RLS 的公平命名

当前 `DFE-RLS` 实际是 frame-buffer 条件下的传统块 DFE/RLS。它没有标签泄漏，但很强。

论文里应明确：

```text
传统 baseline 是强非神经块接收机，而不是弱逐符号因果 baseline。
```

如果最终目标是超过传统方法，则必须超过这个强 baseline，而不是通过削弱 baseline 获胜。

