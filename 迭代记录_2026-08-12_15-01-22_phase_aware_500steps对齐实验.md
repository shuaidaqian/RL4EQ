# 迭代记录：phase-aware 500-step 对齐实验

时间戳：2026-08-12 15:01:22

## 1. 本轮目标

上一轮已经把 Adapt Pilot phase residual / CFO residual 接入训练与推理，但 100-step 初筛结果混合，不能判断是否有效。

本轮目标是做公平对齐：

```text
phase-aware 500-step
vs
上一轮非 phase-aware 500-step
```

比较场景保持一致：

```text
profile = phase_tiny
delay = 20 / 30 / 40
SNR = 0 / 5 / 10 / 15 dB
pilot_total = 128
pilot_layout = prefix
seed = 0
frames = 8
method_group = all
```

## 2. phase-aware 500-step 训练

命令：

```powershell
.\.venv-gpu\Scripts\python.exe pretrain.py `
  --config configs/continual_ppo_phase_tiny.json `
  --stage all `
  --steps 500 `
  --batch-size 4 `
  --amp `
  --save-dir pretrained/phase_tiny_phaseaware_500steps_2026-08-12
```

结果：

```text
saved pretrained/phase_tiny_phaseaware_500steps_2026-08-12
```

产物：

```text
pretrained/phase_tiny_phaseaware_500steps_2026-08-12/model_best.pt
pretrained/phase_tiny_phaseaware_500steps_2026-08-12/model_final.pt
pretrained/phase_tiny_phaseaware_500steps_2026-08-12/last.pt
pretrained/phase_tiny_phaseaware_500steps_2026-08-12/model_config.json
pretrained/phase_tiny_phaseaware_500steps_2026-08-12/pretrain_metrics.json
```

## 3. compare 小矩阵

命令：

```powershell
.\.venv-gpu\Scripts\python.exe compare.py `
  --config configs/continual_ppo_phase_tiny.json `
  --method-group all `
  --pretrained pretrained/phase_tiny_phaseaware_500steps_2026-08-12/model_best.pt `
  --delays 20 30 40 `
  --snrs 0 5 10 15 `
  --num-seeds 1 `
  --frames 8 `
  --pilot-total 128 `
  --pilot-layout prefix `
  --impairment-profile phase_tiny `
  --resume `
  --output-dir logs/compare_phaseaware_500steps_2026-08-12
```

结果：

```text
saved logs/compare_phaseaware_500steps_2026-08-12
```

## 4. 方法均值对比

### 非 phase-aware 500-step

```text
DFE-RLS                              0.0567
NN + Rule Modulation                 0.0650
RL-Modulated Neural Block Equalizer  0.0700
Offline NN only                      0.0702
SC-FDE-MMSE                          0.0717
LMMSE-FIR                            0.1898
RLS Linear                           0.1905
```

### phase-aware 500-step

```text
DFE-RLS                              0.0567
NN + Rule Modulation                 0.0655
Offline NN only                      0.0694
RL-Modulated Neural Block Equalizer  0.0696
SC-FDE-MMSE                          0.0717
LMMSE-FIR                            0.1898
RLS Linear                           0.1905
```

## 5. 逐配置差距

```text
d20 s0:
  old Offline = 0.1455
  phase-aware Offline = 0.1439
  DFE-RLS = 0.1523
  gapDFE = -0.0085

d20 s5:
  old Offline = 0.0710
  phase-aware Offline = 0.0710
  DFE-RLS = 0.0508
  gapDFE = +0.0202

d20 s10:
  old Offline = 0.0365
  phase-aware Offline = 0.0371
  DFE-RLS = 0.0133
  gapDFE = +0.0238

d20 s15:
  old Offline = 0.0241
  phase-aware Offline = 0.0182
  DFE-RLS = 0.0091
  gapDFE = +0.0091

d30 s0:
  old Offline = 0.1484
  phase-aware Offline = 0.1497
  DFE-RLS = 0.1523
  gapDFE = -0.0026

d30 s5:
  old Offline = 0.0729
  phase-aware Offline = 0.0716
  DFE-RLS = 0.0579
  gapDFE = +0.0137

d30 s10:
  old Offline = 0.0368
  phase-aware Offline = 0.0326
  DFE-RLS = 0.0150
  gapDFE = +0.0176

d30 s15:
  old Offline = 0.0228
  phase-aware Offline = 0.0205
  DFE-RLS = 0.0088
  gapDFE = +0.0117

d40 s0:
  old Offline = 0.1546
  phase-aware Offline = 0.1589
  DFE-RLS = 0.1543
  gapDFE = +0.0046

d40 s5:
  old Offline = 0.0710
  phase-aware Offline = 0.0736
  DFE-RLS = 0.0505
  gapDFE = +0.0231

d40 s10:
  old Offline = 0.0374
  phase-aware Offline = 0.0306
  DFE-RLS = 0.0104
  gapDFE = +0.0202

d40 s15:
  old Offline = 0.0215
  phase-aware Offline = 0.0251
  DFE-RLS = 0.0062
  gapDFE = +0.0189
```

## 6. 结果判断

### 6.1 phase-aware condition 有小幅作用

Offline NN 均值：

```text
old500 Offline NN only        = 0.0702
phase-aware Offline NN only   = 0.0694
```

有小幅改善，但幅度很小。

部分中高 SNR 配置改善明显：

```text
d20 s15: 0.0241 -> 0.0182
d30 s10: 0.0368 -> 0.0326
d30 s15: 0.0228 -> 0.0205
d40 s10: 0.0374 -> 0.0306
```

但也有配置变差：

```text
d40 s0:  0.1546 -> 0.1589
d40 s5:  0.0710 -> 0.0736
d40 s15: 0.0215 -> 0.0251
```

### 6.2 仍未超过 DFE-RLS

DFE-RLS 均值：

```text
DFE-RLS = 0.0567
```

phase-aware Offline NN：

```text
Offline NN only = 0.0694
```

差距仍明显。

逐配置看，只有低 SNR 的部分配置超过 DFE-RLS：

```text
d20 s0
d30 s0
```

大多数 5/10/15 dB 配置仍输给 DFE-RLS。

### 6.3 RL 仍没有学出增益

phase-aware 500-step：

```text
Offline NN only                      0.0694
RL-Modulated Neural Block Equalizer  0.0696
```

RL 没有超过 Offline NN。

Rule modulation：

```text
NN + Rule Modulation = 0.0655
```

Rule 仍好于 Offline/RL，说明动作空间中存在有效调制，但 PPO 没有学会稳定选择。

## 7. 当前结论

本轮完成了必要的公平对齐实验。

结论明确：

```text
仅把 phase_residual / cfo_residual 两个标量放进 conditioner latent，
不足以让神经接收机稳定超过 DFE-RLS。
```

phase-aware condition 是有益但不够强的增量。

当前瓶颈仍是：

```text
神经接收机本体能力 < DFE-RLS
PPO 当前没有学会 Rule-like 有效调制
```

因此不应启动正式 RL 矩阵。

## 8. 下一步建议

下一步应停止继续堆同结构预训练步数，转向结构增强或动作监督。

优先级建议：

### 8.1 增强 phase-aware conditioner

当前只给两个标量：

```text
phase_residual
cfo_residual
```

信息太弱。建议改成：

```text
每个 Adapt Pilot block 的：
  phase intercept
  phase slope
  residual phase variance
  reference SNR / residual energy
```

即使当前主布局是 prefix，也可以按 prefix 内部窗口切成多个子块，提取局部相位统计。

### 8.2 在 unfolded iterations 中加入显式相位校正分支

当前 unfolded equalizer 只通过 FiLM 间接利用相位状态。

建议增加：

```text
phase predictor
-> 对 rx/proposal/residual 做显式 phase de-rotation
-> 再进入 H/H^H unfolded update
```

这更符合问题结构。

### 8.3 把 Rule modulation 作为 PPO 前的监督先验

当前：

```text
Rule modulation mean BER = 0.0655
PPO/RL mean BER = 0.0696
```

说明 Rule 策略有价值，PPO 没学到。

下一步可以做：

```text
用 Rule action 生成伪标签
-> 先行为克隆初始化 PPO policy
-> 再做 online PPO 微调
```

这样 RL 不再从接近随机的策略开始探索。

### 8.4 暂不进入正式 RL 矩阵

启动条件仍应保持：

```text
Offline NN only 或 Rule/BC-initialized RL
先接近或超过 DFE-RLS
```

否则正式矩阵只是确认当前短板。

## 9. 验证

相关测试：

```powershell
.\.venv-gpu\Scripts\python.exe -m pytest `
  tests/test_traditional_baselines.py `
  tests/test_receiver_architecture.py `
  tests/test_evaluation_contract.py::test_compare_cli_writes_real_level_b_metrics `
  -q -p no:cacheprovider
```

结果：

```text
21 passed in 26.69s
```

全量测试：

```powershell
.\.venv-gpu\Scripts\python.exe -m pytest -q -p no:cacheprovider
```

结果：

```text
154 passed in 294.79s (0:04:54)
```
