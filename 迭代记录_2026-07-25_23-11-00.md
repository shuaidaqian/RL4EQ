# 迭代记录：2026-07-25 23:11:00

## 1. 分支与基点

- 工作区：`D:\Research\RL4EQ`
- 当前分支：`codex/continual-ppo-unfolded-equalizer`
- `main`：`69b406481939788205762b2288553faa4f74dcbb`
- 当前分支与 `main` 的 merge-base：`69b406481939788205762b2288553faa4f74dcbb`
- 结论：当前分支基于 `main@69b4064`，没有继承旧参数高效适配分支历史。

本轮新增主要提交：

```text
1798665 feat: add pilot conditioned level A to B curriculum training
1669076 feat: add first order meta adaptation and fixed gate
ca1c6fb feat: add gate aware metrics and pilot selection
dd467af feat: add recurrent hierarchical mixed adaptation policy
009cd0b feat: add safe hierarchical adaptation controller and shadow reward
fb466ce feat: train recurrent PPO continually during deployment
bdac3b4 feat: add paired formal evaluation and required baselines
```

## 2. 当前研究路线

当前唯一路线已经统一为：

```text
Level A/B/C 可控信道族
-> 全程 Pilot 条件课程监督预训练
-> First-order episodic meta-training
-> Best Fixed 前置门槛
-> 整帧缓冲、非因果 Physics-Guided Unfolded Equalizer
-> 部署期间 Continual PPO 每 32 帧持续更新
-> 传统、固定微调、规则控制、Bandit、Continual PPO 配对比较
```

关键边界：

- Level B 是主论文场景。
- Level C 只做压力测试，不混入主平均。
- 接收机是整帧缓冲、非因果块均衡器。
- 在线 observation、reward、动作选择和 PEFT 更新不使用数据标签上界。
- Continual PPO 的目标是进一步降低 `BER_data`，最终目标是每个主配置 `BER_data < 0.01`。

## 3. 环境与依赖

GPU 验证命令：

```powershell
.\.venv-gpu\Scripts\python.exe -c "import torch; assert torch.cuda.is_available(); print(torch.__version__, torch.version.cuda, torch.cuda.get_device_name(0))"
```

输出：

```text
2.11.0+cu128 12.8 NVIDIA GeForce GTX 1650
```

说明：

- 使用 `.venv-gpu` 虚拟环境。
- PyTorch wheel 自带 CUDA 12.8 runtime。
- 没有修改系统 CUDA Toolkit。

## 4. 已完成代码能力

### 4.1 训练与接收机

- `training/curriculum.py`：全程 Pilot 条件 Level A→B 课程训练 smoke。
- `training/meta_training.py`：一阶 inner update、support/query 隔离、Fixed Gate 输出。
- `agent/unfolded_equalizer.py`：物理展开式接收机，支持 PEFT group。
- `agent/continual_policy.py`：结构化 observation、GRU policy、mixed action、三项最小消融。
- `agent/adaptation_controller.py`：PEFT 动作执行、shadow reward、异常回滚。
- `training/continual_ppo.py`：部署期间持续更新 PPO 的 prequential smoke runner。

### 4.2 评估与 baseline

- `evaluation/metrics.py`：统一 frame metric、逐配置主矩阵、Level C/generalization 单列、Reward/Data Spearman、Pilot shortlist。
- `evaluation/bootstrap.py`：seed + 连续帧块 bootstrap。
- `baseline/legacy_equalizers.py`：传统 LMMSE-FIR / DFE 标签隔离接口。
- `compare.py`：12 种正式方法集合、配对输入 hash、frame JSONL 和 summary 输出。

### 4.3 文档

已重写：

- `README.md`
- `AGENTS.md`
- `开发框架.md`
- `RL信道均衡研究分析.md`
- `reference/README.md`
- `reference/summary.txt`
- `reference/references.bib`

文档统一描述 Level B、Continual PPO、整帧缓冲、非因果、`BER_data < 0.01` 与“不使用数据标签上界”。

## 5. 验证命令与结果

### 5.1 自动测试

命令：

```powershell
.\.venv-gpu\Scripts\python.exe -m pytest -q -p no:cacheprovider
```

结果：

```text
70 passed in 43.84s
```

### 5.2 校准 smoke

命令：

```powershell
.\.venv-gpu\Scripts\python.exe calibrate_channel.py --candidates 32 --frames-per-config 2 --output artifacts/final_calibration_smoke --resume
```

结果：

```text
{"output": "artifacts\\final_calibration_smoke", "candidates": 32}
```

说明：这是 smoke，不是 10,000 candidate × 200 frames 的正式可达性校准。

### 5.3 课程预训练 smoke

命令：

```powershell
.\.venv-gpu\Scripts\python.exe pretrain.py --config configs/continual_ppo.json --stage all --steps 2 --batch-size 1 --amp --save-dir pretrained/final_smoke
```

结果：

```text
saved pretrained/final_smoke
```

关键 loss：

```text
cir_level_a: 1.13999 -> 1.09994
perfect_cir_level_a: 1.00852 -> 0.97892
estimated_cir_level_a: 0.96652 -> 0.94888
estimated_cir_level_b: 0.93604 -> 0.90797
```

注意：当前 `_validate_level_b()` 仍输出占位 `ber_data=1.0`，不能视为真实 BER。

### 5.4 Meta smoke

命令：

```powershell
.\.venv-gpu\Scripts\python.exe pretrain.py --config configs/continual_ppo.json --stage meta --steps 2 --batch-size 1 --resume pretrained/final_smoke/last.pt --save-dir pretrained/final_meta_smoke
```

结果：

```text
saved pretrained/final_meta_smoke
```

关键指标：

```text
query_loss: 0.62842 -> 0.60371
gate_checked: 36
gate_pass: false
gate_smoke: true
resume_loaded: false
```

分析：meta smoke 证明接口链路可运行，但 Best Fixed 门槛未通过；按研究纪律，正式 PPO 不应在该门槛未过时启动论文级实验。

### 5.5 Online 65-frame smoke

命令：

```powershell
.\.venv-gpu\Scripts\python.exe online_train.py --config configs/continual_ppo.json --pretrained pretrained/final_meta_smoke/model_best.pt --frames 65 --num-seeds 1 --update-interval 32 --amp --output-dir logs/final_online_smoke
```

结果：

```text
saved logs/final_online_smoke
```

关键检查：

```text
policy_update_frames: [32, 64]
measured_before_current_frame_update: true
initial_policy_hash == offline_policy_hash
initial_receiver_hash == offline_receiver_hash
```

分析：prequential 时序和 seed 隔离通过。

### 5.6 Compare 2-frame smoke

命令：

```powershell
.\.venv-gpu\Scripts\python.exe compare.py --config configs/continual_ppo.json --pretrained pretrained/final_meta_smoke/model_best.pt --policy logs/final_online_smoke/policy.pt --delays 20 40 --snrs 10 20 --num-seeds 1 --frames 2 --output-dir logs/final_compare_smoke
```

结果：

```text
saved logs/final_compare_smoke
```

关键输出：

```text
methods: 12
main_level: B
level_c_separate: true
oracle_from_data_labels_present: false
Continual PPO smoke mean_ber_data: 0.02
```

分析：这是 synthetic/smoke 比较，不是实际通信仿真效果。`0.02` 未达到 `BER_data < 0.01`，不能写成成功结论。

### 5.7 开发规模接口 smoke

命令：

```powershell
.\.venv-gpu\Scripts\python.exe online_train.py --config configs/continual_ppo.json --pretrained pretrained/final_meta_smoke/model_best.pt --frames 300 --num-seeds 5 --resume --amp --output-dir logs/online_development_smoke
.\.venv-gpu\Scripts\python.exe compare.py --config configs/continual_ppo.json --pretrained pretrained/final_meta_smoke/model_best.pt --policy logs/online_development_smoke/policy.pt --num-seeds 5 --frames 200 --resume --output-dir logs/compare_development_smoke
```

结果：

```text
saved logs/online_development_smoke
saved logs/compare_development_smoke
```

说明：当前为接口规模验证，不是正式开发实验；不能据此声明性能达标。

## 6. 效果反思

### 符合预期的部分

- 分支基点正确，未继承旧实验分支历史。
- 旧逐符号/旧强化学习/旧信道路线已从代码主路径清理。
- Level A/B/C 信道、跨帧 ISI、多 Pilot 子块、CIR estimator、物理展开接收机、meta、Continual PPO、baseline 和评估契约已经串通。
- Reward/Data 标签隔离、PEFT 可训练组、rollback、安全恢复、prequential 32 帧更新均有自动测试。
- 根文档已经统一为 Level B + Continual PPO 路线。
- GPU 环境可用，全量自动测试通过。

### 不符合最终研究目标的部分

- 当前训练/在线/比较仍是 smoke 级实现，尚未完成真实 Level B 九配置 BER 达标实验。
- `MetaTrainer` 的 gate smoke 输出 `gate_pass=false`，说明 Best Fixed 前置门槛没有通过。
- `compare.py` 当前 BER 是工程占位/合成输出，不能代表真实均衡效果。
- 当前没有完成 10,000 candidate 正式校准、5 seed × 300 frame 开发矩阵或 10 seed × 1000 frame 正式矩阵。

### 当前结论边界

可以说：

- 工程研究路线、模块边界、标签隔离和 GPU smoke 验证已经建立。
- 当前分支可以作为后续真实训练和正式比较的干净起点。

不能说：

- PPO 已经显著优于传统算法。
- Continual PPO 已经达到每个主配置 `BER_data < 0.01`。
- 当前 synthetic smoke BER 可以写入论文结果。

## 7. 下一步建议

1. 先把 `CurriculumTrainer._validate_level_b()` 替换为真实 Level B 九配置评估。
2. 完成 Perfect-CIR Unfolded 真实 BER 曲线，确认每个主配置可达 `BER_data < 0.01`。
3. 完成 Best Fixed 网格搜索，要求每个主配置 `BER_data < 0.1`。
4. 用真实 Reward/Data paired samples 计算 Spearman，低于 0.6 则回到 Pilot 布局和 reward 设计。
5. 只有上述门槛通过后，再启动 Continual PPO 的 5 seed × 300 frame 开发实验。

## 8. 复现实验路径

```powershell
.\.venv-gpu\Scripts\python.exe -m pytest -q -p no:cacheprovider
.\.venv-gpu\Scripts\python.exe calibrate_channel.py --candidates 32 --frames-per-config 2 --output artifacts/final_calibration_smoke --resume
.\.venv-gpu\Scripts\python.exe pretrain.py --config configs/continual_ppo.json --stage all --steps 2 --batch-size 1 --amp --save-dir pretrained/final_smoke
.\.venv-gpu\Scripts\python.exe pretrain.py --config configs/continual_ppo.json --stage meta --steps 2 --batch-size 1 --resume pretrained/final_smoke/last.pt --save-dir pretrained/final_meta_smoke
.\.venv-gpu\Scripts\python.exe online_train.py --config configs/continual_ppo.json --pretrained pretrained/final_meta_smoke/model_best.pt --frames 65 --num-seeds 1 --update-interval 32 --amp --output-dir logs/final_online_smoke
.\.venv-gpu\Scripts\python.exe compare.py --config configs/continual_ppo.json --pretrained pretrained/final_meta_smoke/model_best.pt --policy logs/final_online_smoke/policy.pt --delays 20 40 --snrs 10 20 --num-seeds 1 --frames 2 --output-dir logs/final_compare_smoke
```
