# 论文结论建议 2026-07-15 00:18:50

## 不建议使用的强表述

当前实验结果不支持以下表述：

```text
强化学习均衡方法全面优于传统均衡算法。
```

原因：

- CFO 场景下 PEFT/RL 与传统方法都接近随机判决。
- Doppler、非线性、低 pilot overhead 场景下，MMSE/RLS 仍然更强。
- imitation policy 的 action accuracy 仍低，说明当前 observation 还不能稳定预测 data-oracle action。

## 建议使用的稳妥表述

可以将论文主张调整为：

```text
本文提出一种 model-assisted RL 控制的 residual PEFT 在线均衡框架。
该框架严格遵守 pilot-only online observability：在线阶段不使用 data 标签作为 reward，
RL agent 只控制少量参数更新、伪标签利用和同步/残差校正强度。
实验表明，residual-assisted 离线预训练可显著改善神经均衡器的初始泛化能力；
但在 CFO-dominated 和强传统自适应滤波 baseline 存在时，性能收益依赖于显式同步建模、
selective distillation 和足够规模的 data-oracle imitation policy 预训练。
```

## 可作为论文贡献点的内容

1. 提出两阶段框架：
   - Offline residual-assisted neural equalizer pretraining
   - Online pilot-only RL-controlled PEFT adaptation

2. 引入严格在线可观测约束：
   - `BER_data` 只用于仿真评估
   - reward 不使用 data 标签
   - policy 使用 pilot loss、pilot BER、置信度、残差相关性和历史状态

3. 引入 model-assisted residual learning：
   - MMSE soft
   - MMSE residual
   - neural-MMSE disagreement
   - residual correction head

4. 增加失配场景实验：
   - CFO
   - Doppler
   - nonlinear compression
   - low pilot overhead

5. 给出负结果和诊断：
   - pilot-only reward 与 data BER 仍存在错配
   - CFO 场景需要显式同步估计
   - RLS 在低 pilot overhead 和线性时变信道上仍是强 baseline

## 下一版实验设计建议

论文实验应避免只展示单一平均 BER。建议采用以下表格：

| 实验组 | 目的 |
|---|---|
| no adaptation | 检验离线模型初始能力 |
| fixed PEFT | 检验在线更新本身是否有效 |
| imitation policy | 检验 policy 是否能复现 data-oracle action |
| PPO fine-tune | 检验 pilot-only reward 是否进一步提升 |
| data-oracle action | 给出 action space 上限 |
| MMSE / RLS / LMS / ZF | 传统 baseline |

每个实验应报告：

- `BER_data`
- `BER_pilot`
- `pilot_loss`
- `adapt_params`
- `adapt_steps`
- `latency_ms`
- action accuracy
- oracle regret
- 多 seeds 均值和标准差

## 当前最合理的论文定位

当前项目更适合定位为：

```text
面向在线可观测约束的 model-assisted RL 神经均衡框架与诊断研究
```

而不是：

```text
一种全面超过传统均衡器的强化学习均衡算法
```

后续如果希望支撑更强结论，优先补：

1. 更强 CFO estimator / synchronizer。
2. 1000+ 帧 imitation policy 训练。
3. selective distillation 的场景级 ablation。
4. RLS/MMSE 的公平性、多 seeds 标准差和复杂度对比。
