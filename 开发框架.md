# RL4EQ 开发框架

本文档是 RL4EQ 后续开发的标准架构说明。所有新功能、重构、实验入口和测试都应围绕“离线大规模监督预训练 + 在线强化学习控制参数高效微调”的两阶段框架展开。

## 1. 总体目标

RL4EQ 面向无显式信道状态信息的在线自适应信道均衡。系统先在离线阶段通过大规模随机无线信道生成数据，监督训练一个通用 Neural Equalizer，得到预训练参数 `θ_pre`；再在在线阶段仅使用当前帧的 Pilot 可观测信息，由 RL Agent 决定少量参数如何更新，最终均衡 Data 段。

核心约束：

- 离线阶段可以使用 `Pilot + Data` 的完整标签进行监督学习。
- 在线阶段的 RL reward 只能来自 Pilot 可观测信息，不能依赖真实 Data 标签。
- 在线阶段只允许更新少量参数，例如 LoRA、Adapter、输出头或少数可控层。
- 评估阶段可以使用 Data 标签计算 `BER_data`，但不能把它混入真实在线 reward。
- MMSE baseline 必须作为每次实验的对照项。

## 2. 标准系统架构

```text
                    Offline Stage
┌──────────────────────────────────────────┐
│ 随机生成大规模无线信道                    │
│                                          │
│ AWGN、Rayleigh、Rician、EME、多径、频偏等 │
│                  │                       │
│                  ▼                       │
│      Neural Equalizer (CNN+GRU/Transformer) │
│                  │                       │
│        Pilot + Data 监督学习              │
│                  │                       │
│        得到预训练参数 θ_pre               │
└──────────────────────────────────────────┘
                    │
                    ▼
              Online Stage
┌──────────────────────────────────────────┐
│ 接收一帧：Pilot + Data                    │
│                  │                       │
│          Pilot Equalization              │
│                  │                       │
│      Pilot Loss / SNR / CFO / 历史状态    │
│                  │                       │
│      RL Agent (PPO / DDPG / SAC)          │
│                  │                       │
│ 输出：LoRA参数更新、学习率或层冻结策略      │
│                  │                       │
│ 更新均衡器（仅少量参数）                   │
│                  │                       │
│         Equalize Data                     │
└──────────────────────────────────────────┘
```

当前代码主线采用 Transformer + Adapter 的参数高效版本。LoRA、CNN+GRU、DDPG、SAC 和更完整的信道族应作为可插拔扩展加入，不应破坏现有主线接口。

## 3. 当前主线文件

| 文件 | 职责 |
|------|------|
| `pretrain.py` | 离线监督预训练入口，输出可被在线阶段直接加载的 `AdapterEqualizer` 权重 |
| `online_train.py` | 在线 PEFT + PPO 自适应主入口，统计 PEFT 与 MMSE 对比指标 |
| `compare.py` | SNR 扫描对比入口，输出 PEFT 与 MMSE 的可视化结果 |
| `agent/neural_equalizer.py` | Neural Equalizer 主体，目前为 Transformer + Adapter |
| `agent/adaptation_controller.py` | 执行在线少量参数更新，计算 Pilot reward 和指标 |
| `agent/adaptation_policy.py` | 帧级离散 PPO 策略，用于选择在线更新动作 |
| `env/comm_env.py` | 通信环境，负责帧生成、信道调用、状态构建 |
| `env/frame_structure.py` | 帧结构与 Pilot/Data mask |
| `env/rayleigh_channel.py` | Rayleigh 多径信道 |
| `env/_3gpp_channel.py` | 3GPP EPA/EVA/ETU 信道 |
| `baseline/mmse_equalizer.py` | MMSE baseline |
| `tests/test_parameter_efficient_adaptation.py` | 当前主线契约测试 |

不再新增 `actor_critic.py`、`actor_critic_v2.py`、`ppo.py` 这类旧命名文件。强化学习策略应放在 `agent/adaptation_policy.py` 或按算法拆成 `agent/policies/`。

## 4. Offline Stage 开发标准

### 4.1 信道生成

离线阶段的目标是覆盖足够多的信道扰动，使 `θ_pre` 具备跨信道初始化能力。信道生成应统一收敛到一个可配置采样器，长期目标包括：

- AWGN：单纯加性白噪声，用于验证上限。
- Rayleigh：频率选择性多径衰落。
- Rician：含直达径的衰落信道，参数包括 K 因子。
- EME：扩展移动环境或增强多径环境，用于覆盖更极端的时延扩展与移动场景。
- 3GPP EPA/EVA/ETU：标准化多径 profile。
- CFO：载波频偏。
- Doppler：时变信道。
- 相位噪声、IQ imbalance、定时偏差：作为后续扩展。

当前实现位于 `pretrain.py` 的 `_sample_channel_config()`。后续如果信道族继续扩张，应把它迁移为 `env/channel_factory.py` 和 `env/impairments.py`，并保持 `pretrain.py` 只负责训练流程。

### 4.2 数据与状态

标准帧结构：

```text
| Training(128) | Pilot(64) | Data(128) | Pilot(64) | Data(128) |
```

离线监督信号：

- Training 段：已知标签。
- Pilot 段：已知标签。
- Data 段：离线仿真可知标签。

离线输入状态必须与在线阶段一致，当前标准为：

```text
state_dim = 45 = 接收 IQ 窗口 2*(2K+1) + 位置编码 3
K = 10
```

禁止离线使用一种状态维度、在线使用另一种状态维度。`pretrained/model_best.pt` 必须能被在线模型 `strict=True` 加载。

### 4.3 模型

当前标准模型：

```text
AdapterEqualizer
  输入状态: (B, T, 45)
  input_proj: Linear + LayerNorm
  backbone: TransformerEncoder
  adapter: bottleneck residual adapter
  output_head: bit logits
```

离线阶段训练完整模型参数。在线阶段冻结主干，仅更新 Adapter 和输出头。

长期可扩展模型：

- CNN+GRU Equalizer
- LoRA Transformer Equalizer
- Hybrid CNN+Transformer Equalizer

扩展模型必须实现与 `AdapterEqualizer` 等价的接口：

```python
forward(states) -> (logits, probs)
enable_parameter_efficient_tuning(...)
set_trainable_targets(...)
trainable_parameters()
trainable_parameter_count()
```

### 4.4 离线损失与 checkpoint

基础损失：

```text
L_offline = BCE(logits_all, bits_all)
```

可扩展损失：

```text
L_offline =
  BCE(logits_all, bits_all)
  + λ_pilot * BCE(logits_pilot, bits_pilot)
  + λ_conf * confidence_regularization
```

标准输出：

```text
pretrained/model_best.pt
pretrained/model_final.pt
pretrained/model_config.json
pretrained/pretrain_curve.png
```

`model_best.pt` 是在线阶段默认加载对象，命令为：

```bash
python online_train.py --pretrained pretrained/model_best.pt
```

## 5. Online Stage 开发标准

### 5.1 在线流程

每一帧按以下顺序处理：

```text
1. 接收 Pilot + Data 帧
2. Neural Equalizer 初始推理
3. 只在 Pilot 段计算 Pilot Loss / Pilot BER / 置信度
4. 构建 RL observation
5. RL Agent 输出在线更新动作
6. AdaptationController 执行少量参数更新
7. 重新推理整帧
8. 输出 Data 段判决
9. 记录指标并与 MMSE 对比
```

### 5.2 RL observation

当前在线可观测状态包括：

```text
pilot_loss
BER_pilot
pilot_conf_mean
pilot_conf_std
SNR
loss_ema
ber_ema
last_reward
```

后续加入 CFO 估计后，observation 应扩展为：

```text
pilot_loss
BER_pilot
pilot_conf_mean
pilot_conf_std
SNR_hat
CFO_hat
loss_ema
ber_ema
last_reward
frame_index_norm
```

### 5.3 RL action

第一阶段标准 action 是离散策略表：

```text
skip
head-slow
head-fast
adapter-slow
adapter-fast
both-slow
both-fast
both-deep
```

这些 action 映射为：

```text
更新模块: output_head / adapter / both / none
学习率: 1e-4 / 5e-4
更新步数: 0 / 1 / 3
冻结策略: backbone frozen
```

后续 LoRA 版本可以扩展为：

```text
module_id: LoRA_QV / LoRA_FFN / output_head / mixed
lr_id: low / mid / high
steps_id: 0 / 1 / 3 / 5
rank_id: fixed / dynamic
freeze_id: frozen_backbone / unfreeze_last_block
```

### 5.4 Reward

在线 reward 只允许来自 Pilot：

```text
reward =
  pilot_loss_before - pilot_loss_after
  - update_step_penalty
  - parameter_change_penalty
```

禁止使用 `BER_data` 作为在线 reward。`BER_data` 只用于仿真评估和论文图表。

### 5.5 在线指标

所有在线实验必须输出：

| 指标 | 含义 | 是否在线可观测 |
|------|------|----------------|
| `BER_data` | 数据段 BER，主指标 | 否，仅仿真评估 |
| `BER_pilot` | Pilot 段 BER | 是 |
| `pilot_loss` | RL reward 来源 | 是 |
| `adapt_params` | 在线更新参数量 | 是 |
| `adapt_steps` | 每帧更新步数 | 是 |
| `latency_ms` | 每帧在线更新时间 | 是 |
| `generalization` | 未见信道类型 BER | 否，评估指标 |

## 6. Baseline 与可视化标准

每次主实验必须包含 MMSE baseline。推荐对比项：

```text
MMSE
预训练模型，无在线更新
固定学习率 PEFT
PPO 控制 PEFT
LoRA 控制 PEFT
```

标准图表：

- `online_adapt_metrics.png`：帧级 BER、Pilot loss、更新步数和时延。
- `mmse_baseline_comparison.png`：平均 `BER_data` 对比。
- `generalization.png`：未见信道类型泛化。
- `snr_mmse_comparison.png`：SNR 扫描曲线。

## 7. 当前命令

离线预训练：

```bash
python pretrain.py --num_steps 1000 --batch_size 4 --save_dir pretrained
```

在线自适应：

```bash
python online_train.py --num_frames 300 --snr 10 --pretrained pretrained/model_best.pt --output_dir logs/peft_online
```

SNR 扫描：

```bash
python compare.py --snr_min 0 --snr_max 20 --snr_step 5 --num_frames 50 --pretrained pretrained/model_best.pt --output_dir logs/peft_snr_compare
```

测试：

```bash
python -m pytest tests/test_parameter_efficient_adaptation.py -q
```

## 8. 代码整洁规则

保留主线：

```text
pretrain.py
online_train.py
compare.py
agent/neural_equalizer.py
agent/adaptation_controller.py
agent/adaptation_policy.py
env/*.py
baseline/mmse_equalizer.py
tests/test_parameter_efficient_adaptation.py
```

禁止保留：

```text
__pycache__/
*.pyc
*.bak
临时 smoke 输出目录
未被当前入口引用的旧训练脚本
旧 Actor-Critic 判 bit 实现
重复的实验入口
```

新增功能必须满足：

- 有清晰模块边界。
- 有测试覆盖核心接口或行为。
- 不新增与主线并行的临时脚本。
- 可视化输出进入 `logs/`。
- 模型权重输出进入 `pretrained/` 或用户显式指定目录。
- 若改变 checkpoint 结构，必须更新离线到在线加载测试。

## 9. 后续路线

优先级从高到低：

1. 增加 `env/channel_factory.py`，统一 AWGN、Rayleigh、Rician、EME、3GPP 和 CFO 采样。
2. 增加 `env/impairments.py`，实现 CFO、相位噪声、IQ imbalance。
3. 将 Adapter 扩展为 LoRA Transformer，保持相同 PEFT 接口。
4. 让 `online_train.py` 自动读取 `model_config.json`，避免手动同步 `d_model/n_layers/adapter_rank`。
5. 增加固定 PEFT baseline，与 PPO 控制 PEFT 做消融对比。
6. 增加 DDPG/SAC 连续动作版本，控制学习率、更新幅度或 LoRA delta。
7. 扩展 `compare.py`，输出多信道、多 SNR、多方法的统一表格和图。

本项目后续所有实现应以本文档为准；若论文思路或实验标准变化，应先修改本文档，再修改代码。
