# RL4EQ 极端长时延信道开发框架

本文档是 RL4EQ 后续开发的唯一架构标准。

## 1. 研究问题

传统 LMMSE、RLS 和 DFE 在常规、短记忆、近似线性的无线信道上具有强物理先验，神经均衡器很难稳定建立优势。RL4EQ 因此转向 EME 启发的极端稀疏长回波场景：多径之间的相对时延达到 20–40 个符号，产生传统短滤波器难以处理的长程 ISI。

“EME 启发”不等于完整地月链路仿真。完成粗同步后，绝对传播时延不属于均衡器需要消除的 ISI；本项目只研究相对长时延扩展、稀疏路径、随机复增益和跨帧慢变。

## 2. 唯一系统架构

```text
Offline A：完整随机序列
  -> 稀疏长回波信道
  -> 扩张卷积 + Transformer 神经均衡器
  -> 全序列监督 BCE

Offline B：随机 Pilot 开销
  -> Adapt Pilot 条件向量
  -> Data 为主的联合监督
  -> 同构 checkpoint

Online：Adapt Pilot 48 + Reward Pilot 16 + Data 448
  -> Adapt Pilot observation
  -> 帧级离散 PPO
  -> Adapter/输出头少量更新
  -> Reward Pilot 留出奖励
  -> Data 仿真评估
```

## 3. 信道标准

- 单载波 BPSK，默认帧长 512。
- 每个 episode 具有 3–7 条稀疏路径，时延必含 0 和当前最大时延。
- 离线最大时延在 12–40 符号间采样；正式测试为 20/30/40；外推测试为 50。
- 训练 SNR 为 -5–20 dB。
- episode 内路径位置不变，复增益按 Gauss-Markov 过程缓慢变化。
- 信道必须保留上一帧发送尾部，模拟跨帧 ISI。
- 离线独立样本必须使用随机 warm-up，禁止用全零边界制造虚假低 BER。

第一版不加入 CFO、非线性、LDPC、多调制、MIMO、RIS 或标准 3GPP profile。

## 4. 帧与标签边界

在线默认帧：

```text
| Adapt Pilot 48 | Reward Pilot 16 | Data 448 |
```

- Adapt Pilot：Pilot Conditioner、PPO observation、梯度更新。
- Reward Pilot：动作执行后的留出 loss、BER 和 reward。
- Data：只允许离线监督和仿真评估。

均衡主干不能直接接收当前位置的真实 Pilot 标签。Adapt Pilot 的已知发送符号只能进入帧级 Pilot Conditioner；Reward Pilot 标签不得进入模型输入或动作前 observation。

## 5. 模型标准

`ExtremeDelayEqualizer` 默认包含：

- dilation `[1,2,4,8,16]` 的扩张卷积前端。
- `d_model=96`、4 heads、3 个 Transformer 块、FFN 192。
- 使用 Adapt Pilot `(RX IQ, TX symbol)` 的 masked Pilot Conditioner。
- 帧级 FiLM 条件调制。
- 每个 Transformer 块一个 rank-8 Adapter。
- BPSK bit-logit 输出头。

离线训练全部参数；在线只开放 Adapter 和输出头。checkpoint 必须通过 `model_config.json` 创建模型，并使用 `strict=True` 加载。

## 6. PPO 标准

PPO observation 固定为 10 维：Adapt Pilot loss/BER、置信度均值/方差、估计 SNR、估计最大相关时延、长回波能量占比、loss EMA、上一帧 reward、上一帧参数变化范数。

动作固定为：skip、head-light、adapters-light、adapters+head-light、adapters+head-fast、adapters+head-deep、rollback-last-good。

在线奖励：

```text
reward =
  reward_pilot_loss_before - reward_pilot_loss_after
  - 0.002 * adapt_steps
  - 1e-6 * adapt_params
  - 0.001 * log(1 + parameter_delta_norm)
```

`BER_data` 不得进入 reward、observation 或动作选择。

## 7. 公平比较

正式比较项：LMMSE-FIR、DFE-RLS、预训练无微调、固定 PEFT、Pilot 规则控制、PPO-PEFT、Data Oracle 诊断上界。

传统算法只能使用 Adapt Pilot，不能使用 Reward Pilot 或 Data 标签。所有方法必须处理同一接收帧和同一随机信道。

## 8. 指标与结论

在线实验至少报告：`BER_data`、`BER_adapt_pilot`、`BER_reward_pilot`、`pilot_loss`、`adapt_params`、`adapt_steps`、`latency_ms`、`parameter_delta_norm`、`generalization`。

`generalization` 是未见 50 符号最大时延和 8–10 路径上的平均 `BER_data`。正式论文结论必须基于 5 个固定随机种子、均值、标准差和 95% 置信区间。

## 9. 唯一入口

```bash
python pretrain.py --config configs/extreme_delay.json --save_dir pretrained
python online_train.py --config configs/extreme_delay.json --pretrained pretrained/model_best.pt --output_dir logs/online
python compare.py --config configs/extreme_delay.json --pretrained pretrained/model_best.pt --policy logs/online/policy.pt --output_dir logs/compare
python -m pytest tests/test_extreme_delay_adaptation.py -q
```

