# RL4EQ 开发框架

## 1. 单一目标

项目目标是在 Level B 极端稀疏长回波信道下，验证 `RL-Modulated Neural Block Equalizer` 是否能够超过传统非神经、非 RL 均衡器，并在每个主配置达到 `BER_data < 0.01`。

Level A 用于课程学习和接收机可达性校准。Level C 只做压力测试，不混入 Level B 主平均。

## 2. 信道与帧结构

信道族由 `env/channel_profiles.py` 和 `env/extreme_delay_channel.py` 管理：

- 稀疏路径包含 0 时延和最大时延。
- episode 内 support 固定，复增益按 Gauss-Markov 缓慢演化。
- 信道维护上一帧 tail，显式产生跨帧 ISI。
- 噪声按固定 Es/N0 注入，不按当前输出功率重新缩放。

帧结构由 `env/frame_structure.py` 管理。Pilot 候选来自 `64/96/128/160/192 × prefix/two_block/multi_block`，其中 Adapt/Reward 按 3:1 切分。Pilot 标签不能直接进入未知区域的均衡输入。

## 3. 接收机与 RL 调制

接收机是整帧缓冲、非因果神经块均衡器：

- `HybridCIREstimator` 或 acquisition 估计产生显式 CIR 条件。
- `UnfoldedEqualizer` 使用显式 `H/H^H` 迭代、Transformer denoiser、FiLM、Adapter 和 LoRA。
- `ModulationState` 是神经均衡器可接受的有界低维调制状态，包含 Adapter gate、FiLM residual scale、LoRA scale、head temperature、head bias 和 confidence threshold。
- `DiscreteSafePolicy` 在部署期间根据接收端可观测状态选择离散安全动作。
- `training/windowed_discrete_ppo.py` 是 proposed 在线 runner：同一动作作用于一个短窗口，reward 使用窗口内 Reward Pilot BER/loss 改善聚合。

RL 不直接判决 Data bit，不直接读取 Data 标签，不直接生成完整高维参数增量。当前不再把逐帧连续 modulation 作为主路线；它只保留为历史诊断兼容。

## 4. 训练路径

训练路径固定为：

```text
真实信道 Pilot 条件课程监督预训练
-> strict-load checkpoint
-> RL-Modulated 在线 smoke
-> append-safe compare
-> 小样本门槛矩阵
-> pilot sweep
-> 正式配对主矩阵
```

Data 标签只在离线监督作用域和仿真评估作用域内使用，不进入在线 observation、reward、动作选择或调制更新；在线流程明确不使用数据标签上界。

## 5. 比较方法

主 baseline 是传统非神经、非 RL 方法：

```text
LMMSE-FIR
LMS
NLMS
RLS Linear
DFE-RLS
SC-FDE-MMSE
```

Proposed 方法族：

```text
Offline NN only
NN + Fixed Modulation
NN + Rule Modulation
NN + Discrete PEFT Scheduler
RL-Modulated Neural Block Equalizer
```

诊断参考：

```text
Perfect-CSI Block
Fixed CG-BPSK-DD Block Detector
```

诊断参考不作为传统 baseline，不纳入主成功门槛。

## 6. 指标

统一 frame metric 至少包含：

```text
method, level, delay, snr_db, rho, pilot_total, pilot_layout, seed, frame,
ber_data, ber_adapt_pilot, ber_reward_pilot, pilot_loss, cir_nmse_db,
adapt_params, adapt_steps, detector_iterations, latency_ms,
parameter_delta_norm, rollback, effective_goodput
```

主矩阵逐配置汇总，不允许用平均值掩盖单个配置失败。正式统计采用 seed 与连续帧块层级 bootstrap。

## 7. 唯一入口

```powershell
.\.venv-gpu\Scripts\python.exe calibrate_channel.py --candidates 32 --frames-per-config 2 --output artifacts/calibration_smoke
.\.venv-gpu\Scripts\python.exe pretrain.py --config configs/continual_ppo.json --stage all --steps 2 --batch-size 1 --amp --save-dir pretrained/final_smoke
.\.venv-gpu\Scripts\python.exe online_train.py --config configs/continual_ppo.json --pretrained pretrained/final_smoke/model_best.pt --frames 8 --num-seeds 1 --window-size 4 --update-interval 4 --delays 20 --snrs 10 --pilot-total 64 --pilot-layout multi_block --amp --output-dir logs/final_online_smoke
.\.venv-gpu\Scripts\python.exe compare.py --config configs/continual_ppo.json --method-group main --pretrained pretrained/final_smoke/model_best.pt --delays 20 --snrs 10 --num-seeds 1 --frames 1 --pilot-total 64 --pilot-layout multi_block --resume --output-dir logs/final_compare_smoke
.\.venv-gpu\Scripts\python.exe -m pytest -q -p no:cacheprovider
```

## 8. 禁止项

当前路线不恢复旧逐符号即时输出、旧 A2C、旧逐符号 PPO、Data Oracle、信道编码扩展、载波频偏实验、多载波、多调制、MIMO、RIS 或非线性路线。
