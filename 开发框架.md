# RL4EQ 开发框架

## 1. 单一目标

项目目标是在 EME 启发但不等同于完整物理 EME 仿真的极端长时延扩展场景中，建立可复现的块均衡研究路线。主配置为 Level B，包含 20/30/40 符号最大相对时延、10/15/20 dB SNR 和缓慢漂移长回波。成功标准是 Continual PPO 在每个主配置达到 `BER_data < 0.01`，同时相对 Best Fixed、规则控制和 Bandit baseline 进一步降低 BER。

Level A 用于课程学习和接收机可达性校准。Level C 只做压力测试，不混入 Level B 主平均，也不要求全部达到 `BER_data < 0.01`。

## 2. 信道与帧结构

信道族由 `env/channel_profiles.py` 和 `env/extreme_delay_channel.py` 管理：

- 稀疏路径包含 0 时延和最大时延。
- episode 内 support 固定，复增益按 Gauss-Markov 缓慢演化。
- 信道维护上一帧 tail，显式产生跨帧 ISI。
- 噪声按固定 Es/N0 注入，不按当前输出功率重新缩放。

帧结构由 `env/frame_structure.py` 管理，默认普通帧 512 符号。Pilot 候选来自 `64/96/128/160 × prefix/two_block/multi_block`。Pilot 按 3:1 分成 Adapt 与 Reward，多块布局必须互斥覆盖，不允许 Pilot 标签直连到未知区域。

## 3. 接收机

接收机是整帧缓冲、非因果的 Physics-Guided Unfolded Equalizer：

- `HybridCIREstimator` 从 acquisition 和 Adapt Pilot 估计稀疏 CIR、support、噪声、置信度和 latent residual。
- `UnfoldedEqualizer` 使用显式 `H/H^H` 迭代、Transformer denoiser、FiLM、Adapter 和 LoRA。
- 在线 PEFT 参数组包括 `conditioner_film`、`adapter`、`attention_lora`、`ffn_lora`、`head` 及组合别名。
- 传统 LMMSE、DFE 和块检测 baseline 只能使用 acquisition/Adapt Pilot；Reward/Data 标签只用于评估。

## 4. 训练路径

训练路径固定为：

```text
全程 Pilot 条件课程监督预训练
-> first-order episodic meta-training
-> Best Fixed gate
-> Reward/Data Spearman gate
-> Continual PPO
-> 配对正式比较
```

课程预训练不再包含无 Pilot 阶段；这是当前确认后的唯一方案。Data 标签只在离线监督和 meta outer loss 作用域内使用，不进入在线 observation、reward、动作选择或 PEFT 更新。

## 5. Continual PPO

PPO 不是直接判决 Data bit，而是选择和调节接收机适配动作。策略是 recurrent mixed-action：

- mode：skip、update-channel、update-equalizer、joint-update、detector-refine、rollback。
- 离散参数：PEFT group、steps、detector iterations。
- 连续参数：learning rate、proximal weight、reconstruction weight、damping、CIR trust。
- 每 32 帧按 prequential 时序更新 policy；先评估当前帧，再执行当前帧动作和后续 PPO 更新。

Reward 使用 Reward Pilot loss 的即时改善和 episode shadow receiver 的累计改善。普通性能下降不自动回滚；非有限 loss/梯度或硬安全阈值触发回滚。

## 6. 指标与比较

统一 frame metric 至少包含：

```text
method, level, delay, snr_db, rho, pilot_total, pilot_layout, seed, frame,
ber_data, ber_adapt_pilot, ber_reward_pilot, pilot_loss, cir_nmse_db,
adapt_params, adapt_steps, detector_iterations, latency_ms,
parameter_delta_norm, rollback, effective_goodput
```

主矩阵逐配置汇总，不允许用平均值掩盖单个配置失败。Level C/generalization 单列。正式统计采用 seed 与连续帧块层级 bootstrap，并输出均值、标准差、95% CI、最坏 seed 和成功 seed 比例。

## 7. 唯一入口

```powershell
.\.venv-gpu\Scripts\python.exe calibrate_channel.py --candidates 32 --frames-per-config 2 --output artifacts/calibration_smoke
.\.venv-gpu\Scripts\python.exe pretrain.py --config configs/continual_ppo.json --stage all --steps 2 --batch-size 1 --amp --save-dir pretrained/final_smoke
.\.venv-gpu\Scripts\python.exe online_train.py --config configs/continual_ppo.json --pretrained pretrained/final_smoke/model_best.pt --frames 65 --num-seeds 1 --update-interval 32 --amp --output-dir logs/final_online_smoke
.\.venv-gpu\Scripts\python.exe compare.py --config configs/continual_ppo.json --pretrained pretrained/final_smoke/model_best.pt --policy logs/final_online_smoke/policy.pt --delays 20 40 --snrs 10 20 --num-seeds 1 --frames 2 --output-dir logs/final_compare_smoke
.\.venv-gpu\Scripts\python.exe -m pytest -q -p no:cacheprovider
```

## 8. 禁止项

当前路线不恢复旧的逐符号判决式在线输出，不做信道编码扩展，不做载波频偏扩展，不做 Frozen PPO，不做从随机策略直接正式评估，不使用数据标签上界。
