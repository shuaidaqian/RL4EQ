# 迭代记录：CFO/相位补偿传统 baseline 过估计修复

时间戳：2026-08-13 00:06:25  
分支：`codex/continual-ppo-unfolded-equalizer`  
工作目录：`D:\Research\RL4EQ`

## 1. 本轮目标

上一轮 `cfo_phase_tiny` 初筛发现带补偿的传统 baseline 出现异常劣化。逐帧 Adapt Pilot 相位拟合会把 tiny residual CFO 估计到 `0.008–0.014 cycles/symbol`，比 `cfo_phase_tiny` profile 的真实量级大一个数量级，导致反向旋转过补偿。

本轮目标：

```text
用 TDD 修复 CFO/phase traditional compensation 的过估计问题，
使补偿版 baseline 不再因为错误斜率估计而被人为打残。
```

## 2. RED 测试

新增测试文件位置：

```text
tests/test_traditional_baselines.py
```

新增两个测试：

```python
test_pilot_phase_line_is_bounded_under_cfo_phase_tiny_long_isi
test_cfo_corrected_lmmse_does_not_overrotate_cfo_phase_tiny_smoke
```

### 2.1 CFO 过估计复现

初次运行：

```powershell
.\.venv-gpu\Scripts\python.exe -m pytest tests/test_traditional_baselines.py::test_pilot_phase_line_is_bounded_under_cfo_phase_tiny_long_isi tests/test_traditional_baselines.py::test_cfo_corrected_lmmse_does_not_overrotate_cfo_phase_tiny_smoke -q -p no:cacheprovider
```

结果：

```text
2 failed
```

关键失败：

```text
PilotPhaseEstimate(... cfo_cycles_per_symbol=0.014410605654120445 ...)
assert 0.014410605654120445 <= 0.004
```

这证明测试稳定复现了之前诊断中的过估计问题。

## 3. 修复内容

### 3.1 底层 phase line estimator 支持可选 CFO 限幅

修改文件：

```text
baseline/synchronization_compensation.py
```

`estimate_pilot_phase_line()` 新增参数：

```python
max_abs_cfo_cycles_per_symbol: float | None = None
```

默认值为 `None`，即底层函数保持通用能力，不破坏合成大 CFO 测试；只有传统 residual compensation 路径显式传入限幅。

### 3.2 传统 residual compensation 路径限制每帧 CFO

修改文件：

```text
baseline/traditional_equalizers.py
```

在 `run_traditional_equalizer()` 内部设置：

```python
residual_cfo_limit = 0.001
```

用于：

- `CFO+DD-Phase ...`
- `CFO-Corrected ...`

### 3.3 `CFO-Corrected` 改为保守公共相位补偿

核心变化：

```text
旧：逐帧 pilot_linear_fit 同时校正 phase0 和 CFO slope
新：逐帧只使用可靠 Adapt Pilot 的 median common phase 做公共相位补偿
```

新增 helper：

```python
_pilot_common_phase(receiver_view, reference_symbols, reference_mask)
```

原因：

- 在长 ISI + prefix Pilot 下，逐帧 slope 拟合很容易被 ISI residual 和深衰落点带偏。
- 对 `cfo_phase_tiny` 这种 residual 场景，更合理的传统补偿是 acquisition 已经承担主 CFO，普通帧只做保守公共相位或极小 residual 修正。
- 这避免了补偿版 traditional baseline 被错误斜率人为打残。

补偿 extra 字段现在为：

```text
phase_compensation = pilot_common_phase
estimated_cfo_cycles_per_symbol = 限幅后的诊断值
estimated_phase0 = median common phase
```

## 4. 测试结果

### 4.1 新增 RED 测试转绿

命令：

```powershell
.\.venv-gpu\Scripts\python.exe -m pytest tests/test_traditional_baselines.py::test_pilot_phase_line_is_bounded_under_cfo_phase_tiny_long_isi tests/test_traditional_baselines.py::test_cfo_corrected_lmmse_does_not_overrotate_cfo_phase_tiny_smoke -q -p no:cacheprovider
```

结果：

```text
2 passed in 4.35s
```

### 4.2 traditional baseline 全文件测试

命令：

```powershell
.\.venv-gpu\Scripts\python.exe -m pytest tests/test_traditional_baselines.py -q -p no:cacheprovider
```

结果：

```text
13 passed in 9.72s
```

### 4.3 compare/receiver 相关契约测试

命令：

```powershell
.\.venv-gpu\Scripts\python.exe -m pytest tests/test_evaluation_contract.py::test_compare_cli_writes_real_level_b_metrics tests/test_evaluation_contract.py::test_compare_cli_can_run_selected_methods_for_affected_matrix tests/test_receiver_architecture.py::test_condition_from_cir_accepts_rich_phase_feature_vector -q -p no:cacheprovider
```

结果：

```text
3 passed in 19.72s
```

## 5. 修复后小矩阵 smoke

使用上一轮 500-step checkpoint，不重新训练，只重跑 compare：

```powershell
.\.venv-gpu\Scripts\python.exe compare.py --config configs/continual_ppo_cfo_phase_tiny.json --method-group all --pretrained pretrained/cfo_phase_tiny_phasevector_500steps_2026-08-12/model_best.pt --delays 20 30 40 --snrs 0 5 10 15 --num-seeds 1 --frames 5 --pilot-total 128 --pilot-layout prefix --impairment-profile cfo_phase_tiny --output-dir logs/compare_cfo_phase_tiny_phasevector_500steps_compfix_smoke_2026-08-13
```

结果：

```text
saved logs/compare_cfo_phase_tiny_phasevector_500steps_compfix_smoke_2026-08-13
```

共 1020 行，`impairment_profile` 全部为 `cfo_phase_tiny`。

## 6. 修复后 5-frame 小矩阵结果

| 方法 | mean BER_data | 样本数 |
|---|---:|---:|
| RLS Linear | 0.172092 | 60 |
| LMMSE-FIR | 0.172917 | 60 |
| CFO-Corrected LMMSE-FIR | 0.172917 | 60 |
| CFO-Corrected DFE-RLS | 0.178385 | 60 |
| LMS | 0.232595 | 60 |
| CFO+DD-Phase DFE-RLS | 0.236328 | 60 |
| CFO+DD-Phase LMMSE-FIR | 0.239931 | 60 |
| NLMS | 0.278993 | 60 |
| NN + Rule Modulation | 0.299609 | 60 |
| RL-Modulated Neural Block Equalizer | 0.311068 | 60 |
| Offline NN only | 0.311719 | 60 |
| NN + Fixed Modulation | 0.311719 | 60 |
| NN + Discrete PEFT Scheduler | 0.311719 | 60 |
| DFE-RLS | 0.548264 | 60 |
| Fixed CG-BPSK-DD Block Detector | 0.552821 | 60 |
| SC-FDE-MMSE | 0.554601 | 60 |
| Perfect-CSI Block | 0.576997 | 60 |

补偿器诊断：

```text
CFO-Corrected LMMSE-FIR max_abs_cfo = 0.001000
CFO-Corrected DFE-RLS   max_abs_cfo = 0.001000
CFO+DD-Phase*           max_abs_cfo ≈ 0.000884
```

这说明过估计问题已被压住。

## 7. 修复后的研究结论变化

修复前，补偿版传统 baseline 明显异常劣化，因此 Proposed 看起来超过了很多传统方法。

修复后，公平传统 baseline 变强：

```text
RLS Linear / LMMSE-FIR / CFO-Corrected LMMSE-FIR / CFO-Corrected DFE-RLS
均明显优于 Proposed。
```

`RL-Modulated Neural Block Equalizer` 相对关键 baseline：

```text
RL - LMMSE-FIR:                 +0.138151
RL - RLS Linear:                +0.138976
RL - CFO-Corrected LMMSE-FIR:   +0.138151
RL - CFO-Corrected DFE-RLS:     +0.132682
RL - DFE-RLS:                   -0.237196
RL - SC-FDE-MMSE:               -0.243533
```

解释：

- Proposed 仍然超过未补偿 DFE-RLS 和 SC-FDE-MMSE。
- Proposed 明显弱于修复后的 LMMSE/RLS 类传统 baseline。
- PPO 仍基本等同 Offline NN，没有带来稳定在线收益。

因此，修复后的公平结论更严格：

```text
当前 Proposed 还没有达到“超过传统非神经 baseline”的论文第一目标。
```

## 8. 下一步建议

不建议继续扩大 PPO。下一步应优先做 Proposed 接收机本体增强：

1. 显式 phase/CFO correction branch：
   - 使用 phase vector conditioner 预测整帧相位轨迹或分段相位校正；
   - 在神经均衡主干前先做可学习的相位对齐；
   - 该分支只使用 Adapt Pilot/CIR/soft tail，不读取 Reward/Data 标签。

2. 训练目标增加 phase-consistency auxiliary loss：
   - 离线可用 Data 标签监督最终 bit；
   - 但在线仍只使用 Reward Pilot 评估动作。

3. 修复后重跑 20-frame 小矩阵：
   - 目前 5-frame smoke 证明补偿器不再过估计；
   - 下一步需要用同样修复后的代码重跑 20-frame 矩阵替代上一轮旧结果。

4. 如果神经本体仍弱于 LMMSE/RLS，不启动 PPO 正式矩阵。

