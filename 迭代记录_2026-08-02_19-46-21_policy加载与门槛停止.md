# 迭代记录：policy 加载修复与门槛停止

时间戳：2026-08-02 19:46:21 +08:00

## 1. 本轮执行目标

本轮按“门槛式自动推进，失败即停止分析”继续执行，不直接启动正式 pilot sweep 或正式主矩阵。执行顺序为：

1. 修复 `online_train.py` / `compare.py` 中 RL-Modulated policy 的保存与加载链路。
2. 确认 compare 阶段真正加载训练后的 `policy.pt`，而不是退化为 identity prior。
3. 使用当前最佳 Level B checkpoint 做短在线 PPO probe。
4. 用相同 checkpoint 与已训练 policy 做小规模 compare。
5. 若 proposed 仍未达到继续推进门槛，则停止正式实验，记录原因和下一步分析方向。

当前研究路线仍保持：

- 主论文场景：Level B 极端稀疏长回波信道。
- 接收机：整帧缓冲、非因果块神经均衡器。
- proposed：唯一使用神经网络与 RL 的方法，即 `RL-Modulated Neural Block Equalizer`。
- baseline：传统非神经、非 RL 均衡器。
- Data 标签：只用于离线监督与仿真评估，不进入在线 observation、reward、动作选择或 PPO 更新。

## 2. 已修复的问题

### 2.1 `online_train.py` 原本保存空 policy

问题：

- `training/rl_modulated_online.py` 原先保存的 `policy.pt` 中 `state_dict={}`。
- 这会导致后续 `compare.py --policy ...` 即使传入 policy 文件，也无法评估训练后的 PPO。

修复：

- 在线训练 runner 记录最后一个真实 `ContinuousModulationPolicy`。
- 结束时保存：

```text
schema_version = continuous-modulation-policy-v1
state_dict = last_policy.state_dict()
modulation_config.num_adapter_gates
modulation_config.num_lora_scales
```

验证：

```powershell
.\.venv-gpu\Scripts\python.exe -m pytest tests\test_rl_modulated_equalizer.py::test_rl_modulated_online_runner_reports_metrics_without_data_reward -q -p no:cacheprovider
```

结果：通过。

### 2.2 `compare.py` 原本没有真正加载 policy

问题：

- `compare.py` 已有 `--policy` 参数，但 policy 加载逻辑未完整接入。
- 中间实现曾调用 `_load_policy_if_available()`，但 helper 尚未定义，导致测试失败。

RED 失败证据：

```text
NameError: name '_load_policy_if_available' is not defined
```

修复：

- 新增 `_load_policy_if_available(policy, policy_path, device)`。
- 使用 `torch.load(..., weights_only=False)` 读取 policy checkpoint。
- 若 checkpoint 中存在非空 `state_dict`，则用 `strict=True` 加载。
- `compare.py` 的 RL-Modulated 输出中增加：

```text
policy_loaded
pretrained_loaded
```

验证：

```powershell
.\.venv-gpu\Scripts\python.exe -m pytest tests\test_rl_modulated_equalizer.py::test_compare_rl_modulated_loads_policy_checkpoint -q -p no:cacheprovider
```

结果：通过。

## 3. 本轮测试结果

### 3.0 最终全量回归测试

命令：

```powershell
.\.venv-gpu\Scripts\python.exe -m pytest -q -p no:cacheprovider
```

结果：

```text
104 passed in 240.81s (0:04:00)
```

说明：

- 独立 reviewer 发现 `compare.py --policy` 的显式路径仍可能静默降级后，本轮新增了严格加载负例测试；复核时又发现 `--policy=path` 这种 argparse 合法写法仍会绕过显式检测，已补充等号形式负例并修复。
- 之后发现 RL-Modulated compare 中随机动作采样未固定 torch RNG，导致同一 seed / 同一 policy 的 reward 与 modulation 轨迹可能不一致；已新增确定性测试并修复。
- 因此最终测试数从 101 增加到 104。

### 3.1 policy 保存/加载定向测试

命令：

```powershell
.\.venv-gpu\Scripts\python.exe -m pytest tests\test_rl_modulated_equalizer.py::test_rl_modulated_online_runner_reports_metrics_without_data_reward tests\test_rl_modulated_equalizer.py::test_compare_rl_modulated_loads_policy_checkpoint -q -p no:cacheprovider
```

结果：

```text
2 passed in 13.06s
```

### 3.2 RL modulation 与 curriculum 相关回归测试

命令：

```powershell
.\.venv-gpu\Scripts\python.exe -m pytest tests\test_rl_modulated_equalizer.py tests\test_meta_adaptation.py::test_curriculum_trainer_can_resume_strict_checkpoint tests\test_meta_adaptation.py::test_level_b_curriculum_phase_uses_configured_pilot_total tests\test_meta_adaptation.py::test_curriculum_step_loss_samples_episode_frame_offsets -q -p no:cacheprovider
```

结果：

```text
14 passed in 54.90s
```

新增覆盖：

- 显式传入空 `policy.pt` 时，`compare.py` 必须失败，不能静默退回 identity prior。
- 使用 `--policy=path` 等号形式显式传入空 `policy.pt` 时，也必须失败。
- 同一 seed、同一 checkpoint、同一配置下，RL-Modulated compare 的 `ber_data`、`reward` 和 `modulation_delta_norm` 必须可复现。

## 3.3 独立 reviewer 发现与修复

独立 reviewer 做了只读审查，结论为小范围 Needs changes：

1. 已确认 `training/rl_modulated_online.py` 保存真实 `ContinuousModulationPolicy.state_dict()`。
2. 已确认 `compare.py` 在有效 policy 下使用 `strict=True` 加载，并输出 `policy_loaded=True`。
3. 未发现在线 observation、reward、动作选择或 PPO update 使用 Data 标签。
4. 发现 Important 问题：显式传入 `--policy` 时，路径错误或 checkpoint 损坏仍可能静默降级。

修复结果：

- `compare.py` 区分“用户未显式传入 policy”和“用户显式传入 policy”。
- 当选择 `RL-Modulated Neural Block Equalizer` 且显式传入 `--policy` 时：
  - 路径不存在：直接失败。
  - checkpoint 不是字典：直接失败。
  - 缺少 `state_dict`：直接失败。
  - `state_dict` 为空：直接失败。
  - 结构不匹配：由 `load_state_dict(..., strict=True)` 直接失败。
- 显式检测同时支持 `--policy path` 和 `--policy=path` 两种 argparse 合法写法。
- 如果没有显式传入 `--policy`，仍允许默认 identity prior fallback，便于只评估未训练 policy 的 proposed 链路。

额外修复：

- `compare.py` 为 RL-Modulated method state 增加稳定随机种子，种子由 `method + seed + delay + snr_db` 哈希生成。
- 这样正式配对矩阵中同一配置的 RL 动作采样可复现，不会因为进程随机初始状态改变结果。

## 4. 使用的 checkpoint 与设备

checkpoint：

```text
pretrained\level_b_probe_from_a_1500_p128_offset\model_best.pt
```

GPU 检查：

```text
cuda_available = True
device_count = 1
device_name = NVIDIA GeForce GTX 1650
```

说明：

- 本轮短 probe 使用 GPU 环境 `.venv-gpu`。
- 当前 checkpoint 是此前 Level A + Level B 续训 + frame offset sampling 后得到的最佳 probe checkpoint。

## 5. 短在线 PPO probe

命令：

```powershell
.\.venv-gpu\Scripts\python.exe online_train.py --config configs/continual_ppo.json --pretrained pretrained\level_b_probe_from_a_1500_p128_offset\model_best.pt --frames 64 --num-seeds 1 --update-interval 8 --delays 20 --snrs 10 --pilot-total 128 --pilot-layout two_block --amp --output-dir logs\online_policy_probe_64
```

结果：

```text
saved logs\online_policy_probe_64
```

生成文件：

```text
logs\online_policy_probe_64\frame_metrics.jsonl
logs\online_policy_probe_64\online_metrics.json
logs\online_policy_probe_64\policy.pt
```

关键点：

- `policy.pt` 现在包含真实 `state_dict`，不是空字典。
- 该 policy 随后被 compare 显式加载。

## 6. 加载 policy 后的 compare probe

命令：

```powershell
.\.venv-gpu\Scripts\python.exe compare.py --config configs/continual_ppo.json --method-group main --pretrained pretrained\level_b_probe_from_a_1500_p128_offset\model_best.pt --policy logs\online_policy_probe_64\policy.pt --delays 20 --snrs 10 --num-seeds 1 --frames 5 --pilot-total 128 --pilot-layout two_block --output-dir logs\compare_policy_probe_64_strict_seeded
```

结果：

```text
saved logs\compare_policy_probe_64_strict_seeded
```

逐方法 `BER_data` 均值：

| 方法 | mean BER_data | 帧数 | 备注 |
|---|---:|---:|---|
| DFE-RLS | 0.003646 | 5 | 传统非神经 baseline，当前最强 |
| SC-FDE-MMSE | 0.010417 | 5 | 传统非神经 baseline，接近 0.01 |
| LMMSE-FIR | 0.091667 | 5 | 传统 baseline |
| RLS Linear | 0.092708 | 5 | 传统 baseline |
| LMS | 0.204167 | 5 | 传统 baseline |
| NLMS | 0.315625 | 5 | 传统 baseline |
| Offline NN only | 0.029167 | 5 | 神经网络，无 RL |
| NN + Fixed Modulation | 0.029167 | 5 | 神经网络固定调制，等价安全 identity |
| NN + Rule Modulation | 0.053646 | 5 | 规则调制，退化 |
| NN + Discrete PEFT Scheduler | 0.029167 | 5 | 当前实现仍接近固定策略 |
| RL-Modulated Neural Block Equalizer | 0.029688 | 5 | proposed，严格加载训练 policy 后略弱于 Offline NN |

RL-Modulated 附加检查：

```text
policy_loaded = True
pretrained_loaded = True
reward = [0.003273, 0.000739, -0.000354, -0.001420, 0.001414]
modulation_delta_norm = [0.023864, 0.037209, 0.031892, 0.024121, 0.042148]
```

## 7. 门槛判断

当前门槛结果：失败，停止正式推进。

原因：

1. Proposed 没有超过最强传统 baseline。
   - `RL-Modulated Neural Block Equalizer`: mean BER_data = 0.029688
   - `DFE-RLS`: mean BER_data = 0.003646
   - `SC-FDE-MMSE`: mean BER_data = 0.010417

2. Proposed 也没有超过自己的无 RL 版本。
   - `Offline NN only`: mean BER_data = 0.029167
   - `RL-Modulated Neural Block Equalizer`: mean BER_data = 0.029688

3. PPO policy 已经被真实加载，因此不能再把该结果解释为“compare 没有用到训练后的 policy”。

4. 当前 RL modulation 有动作变化，但 reward 序列多数为负，说明 policy 对 reward pilot 的局部改动没有稳定转化为 Data BER 改善。

因此，本轮不启动：

- 正式 pilot sweep。
- 9 主配置 × 10 seeds × 1000 frames 正式矩阵。
- Level C 压力测试。

## 8. 对当前研究假设的反思

### 8.1 已被支持的点

1. 神经网络确实能学习该信道。
   - Level A 50-step probe 中，offline NN validation 已降到约 0.00793。
   - Level B 续训后，compare 中 Offline NN 从约 0.08229 改善到约 0.02917。

2. 训练/比较分布不一致是实际问题。
   - 修复 `pilot_total=128` mismatch 后，Level B 训练与 compare 更一致。
   - 加入 frame offset sampling 后，compare 多帧结果略有改善。

3. policy 保存/加载链路现在可验证。
   - compare 输出 `policy_loaded=True`，训练 policy 已进入评估链路。

### 8.2 未被支持的点

1. 当前 proposed 尚不能证明“RL 进一步降低 BER”。
   - 短 probe 中 RL 比 Offline NN 略差。

2. 当前 proposed 尚不能证明“神经 + RL 超过传统均衡器”。
   - DFE-RLS 仍显著优于 proposed。
   - SC-FDE-MMSE 也优于 proposed。

3. 当前 Level B 设定下，传统 baseline 并没有如最初直觉那样明显失效。
   - 尤其 DFE-RLS 在 delay=20、SNR=10、pilot_total=128、two_block 下 BER 约 0.003646。

### 8.3 当前可能的技术原因

1. Level B 可能仍然对传统 DFE-RLS 太友好。
   - Pilot 较长。
   - 无 CFO。
   - 无额外相位扰动。
   - 无非线性。
   - 无信道编码。
   - 支持集不变。
   - 相同帧结构下，传统方法可以可靠估计并跟踪线性信道。

2. 当前 neural equalizer 的训练目标与在线控制目标仍有错位。
   - 离线监督优化 BCE/BER。
   - 在线 policy 优化 Reward Pilot loss 改善。
   - Reward Pilot loss 的短期改善不一定和 Data BER 改善一致。

3. 当前 modulation 动作空间可能表达力不足。
   - 只调制 adapter gate / LoRA scale / head scale 类低维控制。
   - 没有真正更新神经均衡器权重。
   - 对已经训练好的模型而言，小幅 modulation 可能只会扰动已学到的判决边界。

4. 当前 policy 训练样本量极小。
   - 本轮仅 1 seed、64 frames。
   - 只能作为链路验证，不能作为最终性能结论。
   - 但由于它已经没有超过 Offline NN，按门槛式流程不能继续烧正式矩阵。

## 9. 下一阶段优先问题

下一阶段不应直接扩大矩阵，而应先分析 PPO 为什么弱于固定策略。

建议优先级：

1. 验证 Reward Pilot loss 与 Data BER 的相关性。
   - 如果 Spearman 相关性不足，则 RL reward 不可靠。
   - 需要按真实 paired samples 统计，不使用 Data 标签训练在线 policy。

2. 做动作空间单步扫描。
   - 不使用 Data Oracle 作为正式方法。
   - 但可以在离线开发分析中检查不同 modulation 动作对 Reward Pilot 与 Data BER 的方向是否一致。
   - 如果所有非 identity 动作大多退化，则 RL 没有可学动作。

3. 检查 proposed 是否需要真正 PEFT 参数更新，而不仅是 modulation。
   - 用户已经提出“能不能直接调整参数”。
   - 当前结果说明：只做低维 modulation 可能不足以超过传统 baseline。
   - 下一轮应考虑 RL 选择在线 PEFT 策略，但执行动作必须是真实 adapter/head 参数更新。

4. 重新校准 Level B 难度。
   - 保持用户已确认的约束：无 CFO、无额外相位扰动、无非线性、无编码。
   - 但可以检查更低 SNR、更短 pilot、更多路径、delay=30/40 下传统 baseline 是否仍保持强势。
   - 如果传统 baseline 在所有主配置都强，论文目标需要改为“低复杂度/低 pilot/泛化优势”等其他可验证优势；但用户当前目标仍是 BER 超越传统，因此应先寻找传统确实困难、但仍合理的 Level B 子区间。

## 10. 当前结论

本轮完成了关键工程闭环：`online_train.py` 能保存真实 policy，`compare.py` 能加载真实 policy，并在输出中记录 `policy_loaded=True`。

但是，当前短 probe 的实验证据不支持继续进入正式矩阵：

- PPO 已达到“绝对 BER < 0.01”吗？没有。
- PPO 已超过 Offline NN only 吗？没有。
- PPO 已超过传统 baseline 吗？没有。

因此按门槛式自动推进原则，本轮应停止正式实验推进，转入“PPO 为什么退化到弱于固定策略 / 为什么传统 baseline 在当前 Level B 下仍强”的诊断阶段。
