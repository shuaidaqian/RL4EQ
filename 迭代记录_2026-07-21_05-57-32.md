# RL4EQ 全量 GPU 验证迭代记录（2026-07-21 05:57:32）

## 1. 本轮目标与结论摘要

本轮在 `codex/eme-extreme-delay-rl` 分支上完成 GPU 环境搭建、两阶段离线训练、在线 PPO 训练、5 随机种子正式比较、50 符号外推测试、代码契约补全和目录清理。

核心结论如下：

1. 在当前 EME 启发的稀疏长时延仿真和当前两种传统基线下，预训练神经均衡器的平均 `BER_data=0.357153`，低于 DFE-RLS 的 `0.445193` 和 LMMSE-FIR 的 `0.486178`。按随机种子配对后，神经模型相对 DFE-RLS 的绝对改善为 `0.088041 ± 0.010941`，相对 LMMSE-FIR 的绝对改善为 `0.129025 ± 0.029466`，这里的 `±` 为 95% 置信区间半宽。
2. 离线 Stage A 验证 BER 为 `0.480227`，接近随机判决；Stage B 降到 `0.200595`。这说明“无 Pilot、随机未知复信道下直接监督恢复绝对比特”的 Stage A 没有成立，模型能力主要来自 Pilot 条件对齐阶段。
3. PPO-PEFT 与 Pretrained 的正式比较逐 seed 完全相同，增益严格为 `0`。策略在 90 个正式配置首帧上 90/90 选择 `skip`，属于明确的策略塌缩，而不是策略文件加载失败。
4. Fixed PEFT 只带来 `0.000085 ± 0.000041` 的绝对 BER 改善；Data Oracle 也只带来 `0.002757 ± 0.000306`。因此当前首要瓶颈是可调参数与动作空间的适配上限，其次才是 PPO 的 observation/reward/训练问题。
5. 当前结果支持“神经模型在本项目极端信道和当前基线实现下有优势”，不支持“PPO 优于固定微调”，也不能外推为“神经均衡普遍优于传统算法”或“已证明适用于真实物理 EME 链路”。

## 2. 分支与代码基点

- 当前分支：`codex/eme-extreme-delay-rl`
- `main` 基点：`69b406481939788205762b2288553faa4f74dcbb`
- 当前分支与 `main` 的 merge-base：`69b406481939788205762b2288553faa4f74dcbb`
- 未整体合并 `codex/parameter-efficient-rl-adapt` 的 18 个历史提交。
- 最终检查确认旧 `actor_critic.py`、`agent/ppo.py`、`channel_models.py` 和 `ldpc_coding.py` 均不存在。

## 3. GPU 虚拟环境

本轮新建并保留本机环境：

```text
D:\Research\RL4EQ\.venv-gpu
```

环境信息：

| 项目 | 版本或状态 |
|---|---:|
| GPU | NVIDIA GeForce GTX 1650，4 GB |
| NVIDIA Driver | 591.44 |
| 驱动报告最高 CUDA | 13.1 |
| Python | 3.12.7 |
| PyTorch | 2.11.0+cu128 |
| PyTorch CUDA Runtime | 12.8 |
| cuDNN | 9.19 |
| NumPy | 1.26.4 |
| Matplotlib | 3.11.1 |
| Pytest | 9.1.1 |
| `torch.cuda.is_available()` | `True` |

默认模型在 `batch_size=8`、序列长 512 时已完成 GPU 前向和反向，PyTorch 峰值 allocated 显存约 128 MB。正式训练和比较时整进程显存约 1.3 GB，GPU 温度约 50°C，没有出现 CUDA OOM。

`.venv-gpu` 通过 `.gitignore` 中的 `.venv*/` 排除，不提交虚拟环境二进制文件。

## 4. 执行范围与命令

### 4.1 离线训练

```powershell
.\.venv-gpu\Scripts\python.exe -u pretrain.py `
  --config configs/extreme_delay.json `
  --stage_a_steps 4000 `
  --stage_b_steps 4000 `
  --batch_size 8 `
  --device cuda `
  --save_dir pretrained/full_gpu_2026-07-20_23-17-17
```

约耗时 9 分钟。

### 4.2 在线 PPO

```powershell
.\.venv-gpu\Scripts\python.exe -u online_train.py `
  --config configs/extreme_delay.json `
  --pretrained pretrained/full_gpu_2026-07-20_23-17-17/model_best.pt `
  --num_episodes 50 `
  --frames_per_episode 100 `
  --device cuda `
  --output_dir logs/full_gpu_2026-07-20_23-17-17/online
```

总计 5,000 在线帧，约耗时 6 分钟。

### 4.3 正式统一比较

第一次完整比较发现两个工程契约问题：记录只保留 `BER_data`，且随机种子包含 SNR，导致同一 delay/seed 的不同 SNR 点并非同一信道。修正并增加回归测试后重新执行：

```powershell
.\.venv-gpu\Scripts\python.exe -u compare.py `
  --config configs/extreme_delay.json `
  --pretrained pretrained/full_gpu_2026-07-20_23-17-17/model_best.pt `
  --policy logs/full_gpu_2026-07-20_23-17-17/online/policy.pt `
  --delays 20 30 40 `
  --snrs -5 0 5 10 15 20 `
  --num_seeds 5 `
  --num_frames 200 `
  --device cuda `
  --output_dir logs/full_gpu_2026-07-20_23-17-17/compare
```

主矩阵规模为：

```text
3 delays × 6 SNR × 5 seeds × 200 frames = 18,000 信道帧
18,000 帧 × 7 方法 = 126,000 方法记录
```

另执行：

```text
delay=50，paths=8–10，SNR=10 dB，5 seeds × 200 frames
= 1,000 泛化帧
= 7,000 方法记录
```

修正后的正式比较约耗时 2 小时 58 分钟。第一次不满足最终契约的比较目录已删除，只保留最终配对结果。

## 5. 离线预训练结果

| 阶段 | 独立验证集 `BER_data` | 判断 |
|---|---:|---|
| Stage A：无 Pilot 长记忆训练 | 0.480227 | 接近随机判决，未达到预期 |
| Stage B：Pilot 条件对齐 | 0.200595 | 明显改善，模型开始有效恢复数据 |

训练末端日志：

```text
stage_a 4000/4000: loss=0.69351, BER=0.54102
stage_b 4000/4000: loss=0.60008, BER=0.21094
```

Stage A 的问题不宜简单归因于“训练步数还不够”。随机未知复信道包含随机相位，BPSK 在没有 Pilot、信道标签或固定相位约束时存在全局符号/相位歧义；模型无法从单个未知信道序列唯一确定绝对比特极性。因此当前 Stage A 监督目标在统计上接近不可辨识。后续应选择以下一种明确路线：

1. 把 Stage A 改为自监督或对比式长记忆表征学习，不要求绝对比特判决；
2. 为 Stage A 提供消除全局相位歧义的最小锚点；
3. 删除“无 Pilot 即可恢复绝对比特”的论文主张，把 Stage B 作为真正的条件监督预训练起点。

当前最诚实的表述是：通用均衡能力主要由 Pilot Conditioner 和 Stage B 条件对齐形成，Stage A 尚未证明有效。

## 6. 在线 PPO 结果

| 指标 | 结果 |
|---|---:|
| `BER_data` | 0.167084 |
| `BER_adapt_pilot` | 0.150175 |
| `BER_reward_pilot` | 0.168888 |
| `pilot_loss` | 0.277299 |
| `adapt_params` | 2784.1494 |
| `adapt_steps` | 1.0294 |
| `latency_ms` | 44.9476 |
| `parameter_delta_norm` | 0.010489 |
| `generalization` | 0.25 |
| 平均 reward | -0.004738 |
| 正 reward 帧 | 78 / 5000（1.56%） |

动作采样分布：

| 动作 | 次数 | 平均 reward | 正 reward 比例 |
|---|---:|---:|---:|
| `skip` | 841 | 0.000000 | 0% |
| `head-light` | 757 | -0.002050 | 0% |
| `adapters-light` | 720 | -0.006889 | 0% |
| `adapters+head-light` | 626 | -0.006927 | 0% |
| `adapters+head-fast` | 620 | -0.006583 | 1.61% |
| `adapters+head-deep` | 808 | -0.010805 | 0.12% |
| `rollback-last-good` | 628 | -0.000046 | 10.67% |

50 个 episode 的平均 BER 标准差为 `0.133469`，最好 episode 为约 `0.000045`，最差为 `0.464688`。这里既包含信道实例难度差异，也包含在线更新差异，不能把波动全部解释为 PPO 不稳定。

正式评估使用确定性策略。在 90 个正式 delay/SNR/seed 首帧上，策略 90/90 选择 `skip`；平均 action logits 中 `skip=0.587511`，次高的 `head-light=0.390752`。因此 PPO 与 Pretrained 完全一致是策略学习结果，不是 checkpoint 未加载。

## 7. 正式统一比较

### 7.1 全局 BER 与 95% 置信区间

统计先在每个 seed 内对全部 delay/SNR/frame 聚合，再跨 5 个 seed 计算标准差和 95% 置信区间。

| 方法 | 平均 `BER_data` | seed 标准差 | 95% CI 半宽 |
|---|---:|---:|---:|
| LMMSE-FIR | 0.486178 | 0.007336 | 0.006430 |
| DFE-RLS | 0.445193 | 0.032306 | 0.028317 |
| Pretrained / No Adapt | 0.357153 | 0.039902 | 0.034976 |
| Fixed adapters+head-light | 0.357067 | 0.039887 | 0.034962 |
| Pilot Rule Controller | 0.357101 | 0.039881 | 0.034957 |
| PPO-PEFT | 0.357153 | 0.039902 | 0.034976 |
| Data Oracle | 0.354395 | 0.039562 | 0.034678 |

配对差值比两个独立均值区间更适合判断方法差异：

| 对比 | `BER(后者) - BER(前者)` | 95% CI 半宽 | 解释 |
|---|---:|---:|---|
| Pretrained vs LMMSE-FIR | 0.129025 | 0.029466 | 当前实验下神经模型更好 |
| Pretrained vs DFE-RLS | 0.088041 | 0.010941 | 当前实验下神经模型更好 |
| Fixed PEFT vs Pretrained | 0.000085 | 0.000041 | 可重复但工程收益极小 |
| Pilot Rule vs Pretrained | 0.000051 | 0.000031 | 可重复但工程收益极小 |
| PPO-PEFT vs Pretrained | 0.000000 | 0.000000 | 没有任何增益 |
| Data Oracle vs Pretrained | 0.002757 | 0.000306 | 动作空间存在小幅上限，但远低于预期 |
| PPO-PEFT vs Fixed PEFT | -0.000085 | 0.000041 | PPO 反而不如固定轻量微调 |

### 7.2 分最大时延

| 最大时延 | LMMSE | DFE-RLS | Pretrained | Fixed | Rule | PPO | Oracle |
|---:|---:|---:|---:|---:|---:|---:|---:|
| 20 | 0.469997 | 0.381155 | 0.297440 | 0.297382 | 0.297427 | 0.297440 | 0.294980 |
| 30 | 0.492012 | 0.462967 | 0.357902 | 0.357815 | 0.357843 | 0.357902 | 0.355050 |
| 40 | 0.496525 | 0.491458 | 0.416116 | 0.416005 | 0.416034 | 0.416116 | 0.413156 |

时延从 20 增加到 40 后，所有方法均明显退化。神经模型仍优于当前传统实现，但 40 符号时自身 BER 已达到 `0.416116`，距离可靠通信仍很远。因此当前成果更像“在困难场景中相对更不差”，而不是“解决了极端长时延均衡”。

### 7.3 分 SNR

修正后同一 delay/seed 在不同 SNR 下复用路径、帧和标准化噪声随机序列，SNR 只控制噪声尺度，曲线基本单调。

| SNR (dB) | LMMSE | DFE-RLS | Pretrained | Fixed | Rule | PPO | Oracle |
|---:|---:|---:|---:|---:|---:|---:|---:|
| -5 | 0.497039 | 0.484140 | 0.437048 | 0.437016 | 0.437019 | 0.437048 | 0.433321 |
| 0 | 0.492641 | 0.465481 | 0.393023 | 0.392972 | 0.393001 | 0.393023 | 0.389992 |
| 5 | 0.486986 | 0.444327 | 0.352749 | 0.352685 | 0.352687 | 0.352749 | 0.350114 |
| 10 | 0.482161 | 0.430591 | 0.327869 | 0.327752 | 0.327772 | 0.327869 | 0.325385 |
| 15 | 0.479690 | 0.424353 | 0.317879 | 0.317797 | 0.317829 | 0.317879 | 0.315624 |
| 20 | 0.478550 | 0.422269 | 0.314347 | 0.314182 | 0.314298 | 0.314347 | 0.311935 |

高 SNR 从 15 到 20 dB 的改善很小，说明系统已经从噪声受限转为残余 ISI、信道不可辨识、Pilot 长度或模型偏差受限。传统基线在该长时延下也接近失效，尤其 LMMSE-FIR 的 Adapt Pilot 拟合不足以泛化到 Reward/Data 区域。

### 7.4 Pilot、开销与延迟

| 方法 | Adapt BER | Reward BER | Pilot loss | 平均适配参数 | 平均步数 | 平均延迟 ms | 参数变化范数 |
|---|---:|---:|---:|---:|---:|---:|---:|
| LMMSE-FIR | 0.022204 | 0.489875 | 0.356567 | 0 | 0 | 7.681 | 0 |
| DFE-RLS | 0.072611 | 0.451101 | 0.435845 | 0 | 0 | 19.765 | 0 |
| Pretrained | 0.315604 | 0.357330 | 0.546547 | 0 | 0 | 7.069 | 0 |
| Fixed PEFT | 0.315035 | 0.357229 | 0.545745 | 5017 | 1 | 48.037 | 0.007072 |
| Pilot Rule | 0.315155 | 0.357271 | 0.545956 | 3201.1 | 0.6381 | 40.819 | 0.004511 |
| PPO-PEFT | 0.315604 | 0.357330 | 0.546547 | 0 | 0 | 28.312 | 0 |
| Data Oracle | 0.314427 | 0.356701 | 0.545044 | 2111.5 | 0.6840 | 257.187 | 0.012675 |

LMMSE 和 DFE 在 Adapt Pilot 上 BER 很低，但在 Reward Pilot 与 Data 上接近随机，说明短 Adapt Pilot 上的拟合不能泛化到长程数据段。神经模型的 Adapt BER 并不低，但跨区域泛化更一致。

延迟口径仍有局限：传统方法测量一次均衡，Pretrained 测量一次神经前向，在线方法的延迟主要测 observation 后的适配控制，Oracle 延迟为枚举 6 个动作的总时间。这些数字适合本实现的工程预算比较，不是严格的端到端硬件吞吐基准。

## 8. 外推泛化

外推条件固定为最大时延 50、8–10 条路径、10 dB、5 seeds × 200 帧。

| 方法 | 平均 `generalization` | seed 标准差 | 95% CI 半宽 |
|---|---:|---:|---:|
| LMMSE-FIR | 0.439942 | 0.038944 | 0.034136 |
| DFE-RLS | 0.378145 | 0.075192 | 0.065909 |
| Pretrained | 0.131984 | 0.135464 | 0.118740 |
| Fixed PEFT | 0.131951 | 0.135388 | 0.118673 |
| Pilot Rule | 0.131935 | 0.135395 | 0.118679 |
| PPO-PEFT | 0.131984 | 0.135464 | 0.118740 |
| Data Oracle | 0.131100 | 0.134654 | 0.118029 |

神经模型的外推平均值看起来优于 20–40 符号主矩阵，但 seed 均值从 `0.021808` 到 `0.353147`，离散度极大。这个结果不能解释为“50 符号比 40 符号更容易”或“模型已可靠外推”。更合理的判断是当前 5 个外推信道实例的难度分布不均，平均值受到少数容易实例强烈影响。下一轮需要按路径功率比、主回波占比、最小路径间隔和信道条件数分层采样，并增加外推 seed 数。

## 9. 是否符合预期

### 符合预期的部分

- 极端长时延下，当前 LMMSE-FIR 和 DFE-RLS 表现很差，研究场景确实避开了常规信道中传统均衡器已非常强的区域。
- Pilot 条件长上下文神经模型能学到比当前传统基线更好的跨区域恢复能力。
- Adapt/Reward Pilot 隔离有效揭示了传统方法在 Adapt Pilot 上拟合很好、对留出区域却失败的问题。
- Data Oracle 确实优于无适配模型，说明在线适配不是严格无效，只是当前上限很低。

### 不符合预期的部分

- Stage A 没有学出无 Pilot 通用恢复能力，且存在基础可辨识性问题。
- PPO 没有学到有效动作选择，而是塌缩为 `skip`。
- 固定 PEFT 和 Oracle 增益都很小，无法支撑“在线微调显著改善数据均衡”的论文贡献。
- 40 符号时延下神经 BER 仍高达约 0.416，绝对性能不足。
- 外推泛化的 seed 方差过大，当前 `generalization` 均值不够稳健。
- 正式比较耗时约 3 小时，但现有入口只在结束时写 JSON；虽然已增加逐配置进度输出，仍缺少断点续跑。

## 10. 根因判断

按优先级排序：

1. **Stage A 目标不可辨识。** 随机复信道相位与 BPSK 全局符号翻转造成绝对比特歧义；继续单纯增加训练步数不一定有效。
2. **动作空间适配上限过低。** Oracle 绝对改善仅约 0.00276，表明当前 Adapter/输出头的一至三步更新无法充分改变长程均衡行为。
3. **Reward 成本压倒收益。** 除 `skip` 外大多数动作平均 reward 为负，策略选择不更新是当前奖励定义下的理性结果。
4. **策略训练信号稀疏。** 只有 1.56% 在线帧 reward 为正，且 50 个 episode 对 PPO 来说样本量很小。
5. **Observation 预测动作收益的能力不足。** 现有 10 维统计量描述 Pilot 状态，但没有直接估计各动作的预期 improvement/cost。
6. **Pilot 相对最大时延偏短。** Adapt Pilot 为 48，而时延可达 40；传统基线可用于稳健拟合的有效窗口样本非常有限。
7. **信道难度采样不均。** 主矩阵 seed 标准差和外推方差都很大，需要按信道结构分层，而不是只按随机 seed 平均。

## 11. 本轮代码修正与清理

### 代码修正

- `compare.py` 现在对全部 7 种方法输出：
  - `BER_data`
  - `BER_adapt_pilot`
  - `BER_reward_pilot`
  - `pilot_loss`
  - `adapt_params`
  - `adapt_steps`
  - `latency_ms`
  - `parameter_delta_norm`
  - `generalization`
- 正式比较种子不再包含 SNR，同一 delay/seed 可跨 SNR 配对。
- 增加 50 符号、8–10 路径泛化记录。
- Data Oracle 的延迟按枚举全部 6 个候选动作的总时间统计。
- 移除重构后未使用的 `_soft_ber` 辅助函数。
- 测试增加到 23 项，新增完整指标和跨 SNR 配对种子契约。
- README 更新为实际 CUDA 环境和 PowerShell GPU 命令。

### 产物清理

- 删除中断的 CPU 训练目录 `pretrained/full_2026-07-20_23-17-17/`。
- 删除第一次指标不全、SNR 未配对的比较目录。
- 最终只保留：
  - `pretrained/full_gpu_2026-07-20_23-17-17/`
  - `logs/full_gpu_2026-07-20_23-17-17/online/`
  - `logs/full_gpu_2026-07-20_23-17-17/compare/`
  - `.venv-gpu/`
- 删除项目源码下的 `__pycache__/`、`.pytest_cache/` 和 `*.pyc`。
- 保留最终 checkpoint、策略、训练曲线、逐帧记录、比较摘要和比较曲线。

## 12. 最终验证

清理后执行：

```text
23 passed in 7.33s
语法解析通过：15 个 Python 文件
结果契约通过：主记录 126000，泛化记录 7000，方法 7，policy_loaded=True
过时文件和源码缓存检查通过
git diff --check：通过，仅有 Git 的 LF/CRLF 提示
git fsck --connectivity-only --no-dangling：通过
```

Git 忽略状态只保留预期的大型本地产物：

```text
!! .venv-gpu/
!! logs/
!! pretrained/
```

## 13. 下一轮优先级

1. 先重构 Stage A 目标，解决随机复信道下绝对比特不可辨识问题；不要先盲目扩大模型或训练步数。
2. 在 PPO 之前先提高 Oracle 上限：测试更强但仍受控的 PEFT 目标，例如 Pilot Conditioner FiLM、最后一个 Transformer block 的 LoRA、按层 Adapter 组合，以及更多但受 early stopping 约束的更新步数。
3. 当 Oracle 的稳定绝对 BER 改善达到至少 `0.01` 后，再重新训练 PPO；否则策略没有足够可学习的动作差异。
4. 重新标定 reward：分别记录纯 Reward Pilot 改善和成本项，避免所有更新动作先验上都被惩罚成负值；同时保持 Data 标签绝不进入 reward。
5. 增加策略熵、确定性动作分布、每动作 counterfactual regret 和行为策略覆盖率诊断。
6. 强化传统基线，包括更合理的频域/块 LMMSE、带延迟选择的 DFE、训练序列辅助的稀疏信道估计，避免只与实现较弱的传统方法比较。
7. 对信道按最大时延、路径数、最强/次强路径比、长回波能量比例和条件数分层，使用更多 seed 做主矩阵与外推矩阵。
8. 为 `compare.py` 增加按配置增量保存和断点续跑，降低 3 小时任务因意外中断全部重算的风险。

## 14. 当前可以与不可以声称的结论

### 可以声称

- 在本项目定义的 EME 启发稀疏长时延信道、当前模型、当前训练预算和当前两种传统基线实现下，Pilot 条件预训练神经均衡器在 5 个配对 seed 上取得更低 BER。
- Adapt Pilot 与 Reward Pilot 隔离能揭示短 Pilot 拟合和留出区域泛化之间的差距。
- 当前 PPO 策略没有带来数据 BER 改善，并确定性地选择 `skip`。
- 当前 PEFT 动作空间的 Oracle 上限仅有小幅改善。

### 不可以声称

- 不可以声称 Stage A 已学到无 Pilot 通用均衡能力。
- 不可以声称 PPO 显著优于固定微调或无微调。
- 不可以声称在线 PEFT 已显著改善数据段均衡。
- 不可以声称神经网络普遍优于传统均衡算法；当前只覆盖两种基线实现。
- 不可以声称已经完成真实物理 EME 信道仿真或真实链路验证。
- 不可以把外推平均 BER `0.131984` 单独解释为稳定泛化，因为其 95% CI 半宽达到约 `0.118740`。
- 不可以把 Data Oracle 当作可部署方法；它读取 Data 标签，仅用于诊断动作空间上界。

## 15. 最终产物索引

- 离线最佳模型：`pretrained/full_gpu_2026-07-20_23-17-17/model_best.pt`
- 离线训练指标：`pretrained/full_gpu_2026-07-20_23-17-17/pretrain_metrics.json`
- 在线策略：`logs/full_gpu_2026-07-20_23-17-17/online/policy.pt`
- 在线汇总：`logs/full_gpu_2026-07-20_23-17-17/online/online_summary.json`
- 正式比较摘要：`logs/full_gpu_2026-07-20_23-17-17/compare/comparison_summary.json`
- 正式逐帧记录：`logs/full_gpu_2026-07-20_23-17-17/compare/comparison_records.json`
- 外推逐帧记录：`logs/full_gpu_2026-07-20_23-17-17/compare/generalization_records.json`
- 比较曲线：`logs/full_gpu_2026-07-20_23-17-17/compare/ber_snr_comparison.png`

