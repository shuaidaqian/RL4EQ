# 迭代记录：第一性原理接收机验证与 best checkpoint 选择

时间戳：2026-08-09 23:55:04（Asia/Shanghai）

## 1. 本轮目标

用户要求先提交和推送当前阶段成果，然后从第一性原理出发，继续推进 Level B 极端稀疏长回波信道下的 `RL-Modulated Neural Block Equalizer`。

本轮没有直接启动 PPO 大矩阵。原因是前序结果已经表明：如果 Offline NN / 神经块接收机本体还不能稳定低于 `BER_data < 0.01`，PPO 只能在一个不够强的接收机上做动作搜索，很难稳定超过传统 baseline。

因此本轮执行顺序为：

1. 提交并推送已有代码和实验记录。
2. 诊断当前 checkpoint 的真实瓶颈：CIR 跟踪、tail 误差还是神经检测器本体。
3. 修复训练阶段 `model_best.pt` 选择机制，避免最后一步退化模型覆盖 best。
4. 将 Offline NN validation 对齐真实在线序列状态：连续帧、soft tail、DD-CIR、online seed base。
5. 尝试低学习率 hard focus 续训与可选 Data margin loss。

## 2. 提交与推送

提交：

```text
0c0ed26 research: 推进Level B连续PPO均衡实验
```

推送分支：

```text
RL4EQ/codex/continual-ppo-unfolded-equalizer
```

提交前 fresh pytest：

```text
133 passed in 387.64s
```

## 3. 第一性原理诊断

### 3.1 问题拆解

当前目标是让 Proposed 在 Level B 主工作区间达到 `BER_data < 0.01` 并超过传统非神经、非 RL baseline。

从接收链路看，BER 主要由三类误差决定：

1. CIR 条件误差：acquisition/fixed CIR 是否跟不上长 episode 中的信道缓慢变化。
2. 跨帧 tail 误差：跨帧 ISI 的上一帧尾部估计是否错误累积。
3. 神经块检测器本体能力：即使给 oracle CIR / oracle tail，模型是否仍无法把长程 ISI 解干净。

### 3.2 当前最好 checkpoint 的 long-episode 诊断

checkpoint：

```text
pretrained/prefix_focus_hard_snr10_15_1400_from_plus50_2026-08-08/model_best.pt
```

#### d30/s10/p64，64 frames，seeds 0/1

```text
fixed CIR            BER_data = 0.05251
decision_directed CIR BER_data = 0.01693
oracle CIR           BER_data = 0.01358
```

#### d40/s10/p128，64 frames，seeds 0/1

```text
fixed CIR            BER_data = 0.05113
decision_directed CIR BER_data = 0.01815
oracle CIR           BER_data = 0.01516
```

结论：

- fixed acquisition CIR 在长 episode 中明显失效，BER 漂到约 `0.05`。
- DD-CIR 能显著恢复到约 `0.017–0.018`。
- 但 oracle CIR 仍高于 `0.01`，说明瓶颈不只是 CIR 跟踪。

### 3.3 oracle tail 诊断

d40/s10/p128，oracle CIR：

```text
soft tail   BER_data = 0.01516
oracle tail BER_data = 0.01522
```

结论：

- oracle tail 没有改善。
- 当前 `<0.01` 的主要瓶颈不是跨帧 tail，而是神经块检测器本体/训练目标仍不足。

## 4. Enhanced receiver 尝试

已有增强配置：

```text
configs/continual_ppo_enhanced.json
```

关键差异：

- unfolded iterations 从 4 增加到 6。
- `conditioner_uses_cir_summary=true`，让 conditioner 显式使用 CIR magnitude、support、noise、confidence。

### 4.1 enhanced 80-step 从头训练

输出：

```text
pretrained/enhanced_cir_summary_80_2026-08-09
```

d40/s10/p128 诊断：

```text
decision_directed CIR BER_data = 0.05170
oracle CIR            BER_data = 0.04647
```

### 4.2 enhanced + 400 hard 续训

输出：

```text
pretrained/enhanced_cir_summary_80_plus400hard_2026-08-09
```

d40/s10/p128 诊断：

```text
decision_directed CIR BER_data = 0.04740
oracle CIR            BER_data = 0.03819
```

结论：

- 增强架构在当前训练预算下明显不如旧 checkpoint。
- 不能据此否定显式 CIR conditioner，但不能作为当前立即推进的主线。
- 当前更实际的路线是：先在旧模型结构上修训练选择机制和 hard focus 训练策略。

## 5. 发现的训练选择问题

旧 `CurriculumTrainer.save()` 把当前模型同时保存成：

```text
model_best.pt
model_final.pt
last.pt
```

这意味着：

- `model_best.pt` 实际不是 best；
- 续训后即使 hard BER 退化，也会覆盖 best；
- 后续 online / compare 可能加载退化 checkpoint。

本轮增加测试并修复：

```text
tests/test_meta_adaptation.py::test_curriculum_save_keeps_distinct_best_and_final_checkpoints
```

修复后：

- 训练器维护 `best_model_state_dict`。
- `model_best.pt` 保存历史最优权重。
- `model_final.pt` / `last.pt` 保存最后权重。
- `pretrain_metrics.json` 记录：
  - `best_model_metric`
  - `best_offline_nn_validation`
  - `selected_at`

## 6. 发现的 validation 错配问题

旧 Offline NN validation 存在两个问题：

1. 使用过短、过理想化的验证；
2. seed base 固定为 `90000`，而 online/diagnostic 轨迹使用 `70000 + seed`。

结果是：

- 训练内 validation 可以显示 `BER_data ≈ 0.00195`；
- 但同一 checkpoint 在真实 64-frame online-like diagnostic 上是 `BER_data ≈ 0.01815`。

这说明旧 validation 不能作为 hard checkpoint selection 依据。

本轮增加测试并修复：

```text
tests/test_meta_adaptation.py::test_curriculum_offline_validation_can_match_online_sequence_state
```

新增能力：

- `model_validation_sequence_state=true`
- `model_validation_seed_base`
- `model_validation_cir_alpha`
- validation row 显式记录：
  - `sequence_state`
  - `tail_mode`
  - `cir`
  - `seed_base`
  - `cir_update_uses_data_labels=false`

sequence validation 行为：

- 同一 seed 内连续生成多帧。
- 维护 soft tail。
- 支持 `decision_directed` CIR 更新。
- Data 标签只用于计算 BER，不进入 CIR 更新。

## 7. d40/s10 hard focus 续训结果

### 7.1 原始 aggressive focus 配置

配置问题：

```text
learning_rate = 1e-4
noise_std = 0.05
dd_flip_probability = 0.02
dd_blend_alpha = 0.2
```

从旧最好 checkpoint 续训 500 步后：

```text
原始 checkpoint:
  decision_directed CIR BER_data = 0.01815
  oracle CIR            BER_data = 0.01516

plus500 aggressive:
  decision_directed CIR BER_data = 0.01923
  oracle CIR            BER_data = 0.01636
```

结论：aggressive focus 训练退化。

### 7.2 best-select + sequence validation

修复 best selection 后，重新训练发现：

```text
selected_at = initial
```

说明训练过程没有找到比初始 checkpoint 更好的点，best 机制成功避免了退化权重覆盖。

### 7.3 低学习率 + 收缩 CIR augmentation

配置改为：

```text
learning_rate = 1e-5
modes = ["acquisition", "dd_like", "acquisition", "dd_like", "noisy"]
noise_std = 0.012
dd_flip_probability = 0.003
dd_blend_alpha = 0.08
```

内部 seq64 validation：

```text
initial best ≈ 0.01719
step 100 best ≈ 0.01701
step 200 final ≈ 0.01705
selected_at = estimated_cir_level_b:100
```

外部 64-frame diagnostic：

```text
原始 checkpoint:
  decision_directed CIR BER_data = 0.01815
  oracle CIR            BER_data = 0.01516

低学习率 best:
  decision_directed CIR BER_data = 0.01790
  oracle CIR            BER_data = 0.01471
```

结论：低学习率 hard focus 有真实但很小的改善。

## 8. Data margin loss 尝试

新增可选离线监督项：

```text
loss =
  BCE_data
  + 0.25 * BCE_adapt_pilot
  + 0.25 * BCE_reward_pilot
  + data_margin_loss_weight * mean(relu(data_margin - signed_data_logit))
```

对应测试：

```text
tests/test_meta_adaptation.py::test_supervised_equalization_loss_can_add_data_margin_penalty
```

配置：

```text
data_margin_loss_weight = 0.05
data_margin = 1.0
```

内部 seq64 validation：

```text
best ≈ 0.01687
selected_at = estimated_cir_level_b:100
```

外部 64-frame diagnostic：

```text
margin best:
  decision_directed CIR BER_data = 0.01792
  oracle CIR            BER_data = 0.01477
```

结论：

- margin loss 在内部 validation 上略好；
- 但外部 diagnostic 不优于纯低学习率版本；
- 当前不能把 margin 视为有效突破口，只能保留为可选训练开关。

## 9. 当前是否达到目标

未达到最终目标。

当前最佳外部诊断结果仍是低学习率 hard focus：

```text
d40/s10/p128:
  decision_directed CIR BER_data = 0.01790
  oracle CIR            BER_data = 0.01471
```

相比原始 checkpoint：

```text
DD-CIR 改善：0.01815 -> 0.01790
oracle 改善：0.01516 -> 0.01471
```

改善存在，但幅度远不足以达到：

```text
BER_data < 0.01
```

## 10. 本轮结论

1. 当前瓶颈不是 tail。
2. 当前 fixed acquisition CIR 明显不够，DD-CIR 是必要条件。
3. 即使 oracle CIR，旧神经块检测器仍在 d40/s10 约 `0.015`，所以接收机本体仍不够强。
4. 继续训练如果没有真实 sequence validation，会保存退化 checkpoint。
5. 修复 best selection 和 sequence validation 是必要工程基础。
6. 低学习率 hard focus 可以小幅改善，但不是突破性方案。
7. margin loss 当前没有带来可复现外部收益。

## 11. 下一阶段建议

下一阶段不应马上启动 PPO。建议继续按门槛式推进：

1. 先使用低学习率 hard focus，从当前低学习率 best checkpoint 继续训练更长步数，并保持 seq64 validation selection。
2. 若 improvement 继续停在 `1e-4` 量级，停止单纯续训。
3. 转向模型本体增强，但不能只靠 enhanced 从头短训：
   - 需要更长预训练预算；
   - 或设计兼容旧 checkpoint 的轻量残差增强模块；
   - 或增加 unfolded residual correction / multi-pass refinement。
4. 当 Offline NN + DD-CIR 在 d30/d40 hard 点接近或低于 `0.01` 后，再启动 RL/PPO。

当前最重要的工程判断：

```text
PPO 不是现在的主瓶颈；接收机本体和验证选择机制才是。
```

