# 迭代记录：低学习率续训、DD-CIR alpha 修复与保守 PPO

时间戳：2026-08-11 00:03:45（Asia/Shanghai）

## 1. 本轮目标

继续上一轮门槛式推进：

1. 先把 Offline NN / 神经块接收机本体继续往 `BER_data < 0.01` 推。
2. 如果单纯续训停止改善，则不盲目堆训练。
3. 修正实验工具中的配置错配，保证诊断和 compare 真正复现实验配置。
4. 检查 PPO 是否仍在扰动强 Offline NN；如果是，收缩策略先验。

本轮仍没有启动正式大矩阵。原因是当前主瓶颈仍在：

```text
Offline NN + DD-CIR 还没有在 hard diagnostic 全面低于 0.01；
PPO 对 Offline NN only 尚未形成稳定正收益。
```

## 2. 低学习率 hard focus 续训

上一轮最优 checkpoint：

```text
pretrained/focus_d40_snr10_seq64best_lr1e5_200_2026-08-09/model_best.pt
```

本轮先把主 focus 配置中的 `data_margin_loss_weight` 从 `0.05` 改回 `0.0`。

原因：上一轮外部 diagnostic 显示 margin loss 没有超过纯低学习率版本；保留 margin 作为可选训练项，但不作为当前主路线。

## 3. 继续 200 + 200 步低学习率训练

### 3.1 plus200

输出：

```text
pretrained/focus_d40_snr10_seq64best_lr1e5_plus200_2026-08-10
```

内部 seq64 validation：

```text
上一轮 best = 0.0170084640
plus200 best = 0.0166829432
selected_at = estimated_cir_level_b:100
```

外部 64-frame diagnostic，d40/s10/p128：

```text
原始 checkpoint:
  DD-CIR     BER_data = 0.0181477869
  oracle CIR BER_data = 0.0151570642

低学习率 200:
  DD-CIR     BER_data = 0.0179036463
  oracle CIR BER_data = 0.0147094730

plus200:
  DD-CIR     BER_data = 0.0177815760
  oracle CIR BER_data = 0.0144653324
```

结论：仍有真实改善。

### 3.2 plus400

输出：

```text
pretrained/focus_d40_snr10_seq64best_lr1e5_plus400_2026-08-10
```

内部 seq64 validation：

```text
plus200 best = 0.0166829432
plus400 best = 0.0163777675
selected_at = estimated_cir_level_b:100
```

外部 64-frame diagnostic，alpha 当时仍按旧 diagnostic 默认 0.2：

```text
plus400, alpha=0.2:
  DD-CIR     BER_data = 0.0175984705
  oracle CIR BER_data = 0.0146280928
```

这里随后发现一个关键诊断错配：long-episode diagnostic 的 DD-CIR alpha 被硬编码为 `0.2`。

## 4. 修复 long-episode diagnostic 的 DD-CIR alpha 错配

### 4.1 问题

`scripts/diagnose_long_episode_drift.py` / `evaluation/long_episode_diagnostics.py` 之前写死：

```text
decision_directed_cir_update(..., alpha=0.2)
```

但当前 focus validation 和 d40 alpha 搜索需要：

```text
alpha = 0.4 或 0.6
```

这导致外部 diagnostic 与训练内 validation 不一致。

### 4.2 修复

新增参数：

```text
--cir-alpha
```

结果行新增：

```text
cir_update_alpha
cir_update_uses_data_labels = false
```

新增测试：

```text
tests/test_long_episode_diagnostics.py::test_long_episode_diagnostic_uses_configurable_dd_cir_alpha
```

### 4.3 修复后的结果

同一个 plus400 checkpoint，d40/s10/p128：

```text
alpha=0.2:
  DD-CIR BER_data = 0.0175984705

alpha=0.4:
  DD-CIR BER_data = 0.0163777675
```

说明之前的外部 diagnostic 低估了当前路线。

## 5. 当前 checkpoint 的 alpha 搜索

checkpoint：

```text
pretrained/focus_d40_snr10_seq64best_lr1e5_plus400_2026-08-10/model_best.pt
```

d40/s10/p128，64 frames，seeds 0/1：

```text
alpha=0.3  BER_data = 0.0168253585
alpha=0.4  BER_data = 0.0163777675
alpha=0.5  BER_data = 0.0159708664
alpha=0.6  BER_data = 0.0159098312
alpha=0.7  BER_data = 0.0159098312
alpha=0.8  BER_data = 0.0162150069
```

结论：

```text
alpha=0.6/0.7 最优，本轮采用 alpha=0.6。
```

随后把 [configs/continual_ppo_focus_d40_snr10.json](configs/continual_ppo_focus_d40_snr10.json) 中的 validation alpha 改为 `0.6`。

## 6. alpha=0.6 对齐后的续训

输出：

```text
pretrained/focus_d40_snr10_seq64best_lr1e5_plus600_alpha0p6_2026-08-10
```

内部 seq64 validation：

```text
plus400 best, alpha=0.4 validation = 0.0163777675
plus600 best, alpha=0.6 validation = 0.0157267257
selected_at = estimated_cir_level_b:100
```

外部 64-frame diagnostic：

```text
plus600, alpha=0.6:
  DD-CIR     BER_data = 0.0157267257
  oracle CIR BER_data = 0.0145060225
```

继续再训 200 步：

```text
pretrained/focus_d40_snr10_seq64best_lr1e5_plus800_alpha0p6_2026-08-10
```

结果：

```text
best_model_metric = 0.0157267257
selected_at = initial
```

结论：

- plus600 alpha=0.6 是当前 d40/s10/p128 最优点。
- 再继续 200 步没有改善，单点低学习率续训应停止。

## 7. d30 泛化检查

用 d40-focused plus600 checkpoint 检查 d30/s10/p64：

```text
原始 checkpoint:
  DD-CIR     BER_data = 0.0169328970
  oracle CIR BER_data = 0.0135846825

d40 plus600 checkpoint:
  DD-CIR     BER_data = 0.0150320877
  oracle CIR BER_data = 0.0128348220
```

结论：

- d40 hard focus 没有破坏 d30；
- 对 d30 也有明显改善；
- 但仍没有低于 0.01。

## 8. p160 检查

用 plus600 checkpoint 检查 d40/s10/p160：

```text
d40/s10/p128:
  DD-CIR     BER_data = 0.0157267257
  oracle CIR BER_data = 0.0145060225

d40/s10/p160:
  DD-CIR     BER_data = 0.0168457037
  oracle CIR BER_data = 0.0154030544
```

结论：

- 当前模型下 p160 反而更差；
- 当前主配置继续使用 p128。

## 9. 尝试置信门控 DD-CIR

### 9.1 动机

当前 DD-CIR 使用所有 Data 硬判决做 LS 更新。低置信错误判决可能污染 CIR。

因此实现了可选参数：

```python
decision_directed_cir_update(..., confidence_threshold=...)
```

新增测试：

```text
tests/test_receiver_architecture.py::test_decision_directed_cir_update_can_gate_low_confidence_data_decisions
```

同时接入 long-episode diagnostic：

```text
--cir-confidence-threshold
```

结果行记录：

```text
cir_confidence_threshold
cir_update_uses_data_labels = false
```

### 9.2 实验结果

d40/s10/p128，plus600 checkpoint，alpha=0.6：

```text
no threshold  BER_data = 0.0157267257
threshold=0.7 BER_data = 0.0159505213
threshold=0.8 BER_data = 0.0160522465
threshold=0.9 BER_data = 0.0161539718
```

结论：

- 置信门控在当前模型上无效；
- 阈值越高，丢掉越多有用 DD 信息，BER 变差；
- 保留该功能作为诊断开关，但不进入主配置。

## 10. 修复 compare.py 的 cir-alpha 接口

### 10.1 问题

`online_train.py` 支持：

```text
--cir-alpha
```

但 `compare.py` 之前只支持：

```text
--cir-update
```

神经 proposed 消融里 alpha 仍硬编码为 `0.2`。

这会导致 compare 不能复现当前最优 `alpha=0.6`。

### 10.2 修复

`compare.py` 新增：

```text
--cir-alpha
```

并传入：

- `Offline NN only`
- `NN + Fixed Modulation`
- `NN + Rule Modulation`
- `NN + Discrete PEFT Scheduler`
- `RL-Modulated Neural Block Equalizer`

新增/修改测试：

```text
tests/test_windowed_discrete_ppo.py::test_compare_can_enable_decision_directed_cir_update_for_proposed
```

## 11. 小型公平 compare 切片

配置：

```text
delay = 40
SNR = 10 dB
pilot_total = 128
pilot_layout = prefix
frames = 64
num_seeds = 1
cir_update = decision_directed
cir_alpha = 0.6
checkpoint = pretrained/focus_d40_snr10_seq64best_lr1e5_plus600_alpha0p6_2026-08-10/model_best.pt
```

方法：

```text
LMMSE-FIR
DFE-RLS
Offline NN only
RL-Modulated Neural Block Equalizer
```

### 11.1 旧 PPO 先验

```text
DFE-RLS                              BER_data = 0.0035400391
LMMSE-FIR                            BER_data = 0.1853027341
Offline NN only                      BER_data = 0.0045166017
RL-Modulated Neural Block Equalizer  BER_data = 0.0051676433
```

动作统计：

```text
sampled:
  peft_head_light                 16
  peft_adapter_lora_head_light    16
  peft_adapter_lora_conservative  16
  identity                         8
  peft_head_fast                   8

applied:
  identity                        36
  peft_head_light                 11
  peft_adapter_lora_head_light     9
  peft_adapter_lora_conservative   5
  peft_head_fast                   3
```

结论：未训练 PPO 初始探索过强，扰动了已经很强的 Offline NN。

## 12. 保守 PPO 先验

修改 [agent/discrete_safe_policy.py](agent/discrete_safe_policy.py)：

```text
identity bias = 3.0
rollback_identity bias = 0.0
轻量 PEFT bias = -0.2 / -0.5
```

新增/修改测试：

```text
tests/test_windowed_discrete_ppo.py::test_discrete_policy_prior_starts_conservatively_from_identity
```

要求：

```text
identity 初始概率 > 0.6
peft_head_fast 初始概率 < 0.1
```

### 12.1 保守先验 compare 结果

同一切片：

```text
DFE-RLS                              BER_data = 0.0035400391
LMMSE-FIR                            BER_data = 0.1853027341
Offline NN only                      BER_data = 0.0045166017
RL-Modulated Neural Block Equalizer  BER_data = 0.0046386720
```

动作统计：

```text
sampled:
  identity                        40
  peft_head_light                  8
  peft_adapter_lora_conservative   8
  peft_adapter_lora_head_light     8

applied:
  identity                        56
  peft_head_light                  4
  peft_adapter_lora_head_light     4
```

结论：

- 保守先验显著降低了 RL 退化；
- 但 RL 仍没有超过 Offline NN only；
- 更没有超过 DFE-RLS。

## 13. 在线训练 policy 后的结果

命令配置：

```text
frames = 300
window_size = 4
update_interval = 16
d40/s10/p128
cir_alpha = 0.6
```

输出：

```text
logs/online_dev_d40s10p128_plus600_alpha0p6_conservative_300f_2026-08-10
```

在线训练自身：

```text
RL mean BER_data             = 0.0151041671
same-state offline_identity  = 0.0150781254
```

动作：

```text
sampled:
  identity                        272
  peft_adapter_lora_conservative   12
  peft_head_fast                    8
  peft_adapter_lora_light           8

applied:
  identity                        286
  peft_head_fast                    8
  peft_adapter_lora_conservative    4
  peft_adapter_lora_light           2
```

结论：在线训练 policy 后仍未产生正收益。

### 13.1 加载 trained policy 的 compare

```text
DFE-RLS                              BER_data = 0.0035400391
Offline NN only                      BER_data = 0.0045166017
RL-Modulated Neural Block Equalizer  BER_data = 0.0046386720
```

trained policy 与未训练保守先验结果基本相同。

结论：

- 当前 PPO 主要学到少动；
- 仍没有学到能稳定降低 Data BER 的 PEFT 动作；
- Reward Pilot 改善不足以让当前动作空间形成可迁移正收益。

## 14. 当前最好结果

### 14.1 Diagnostic hard trajectory

d40/s10/p128，64 frames，seeds 0/1：

```text
DD-CIR BER_data = 0.0157267257
oracle CIR BER_data = 0.0145060225
```

d30/s10/p64：

```text
DD-CIR BER_data = 0.0150320877
oracle CIR BER_data = 0.0128348220
```

### 14.2 Compare slice

d40/s10/p128，seed=50000，64 frames：

```text
DFE-RLS                              BER_data = 0.0035400391
Offline NN only                      BER_data = 0.0045166017
RL-Modulated Neural Block Equalizer  BER_data = 0.0046386720
LMMSE-FIR                            BER_data = 0.1853027341
```

解释：

- 在 compare 这一条轨迹上，Offline NN only 已经低于 `0.01`；
- 但 DFE-RLS 仍更强；
- RL 没有超过 Offline NN only。

## 15. 本轮结论

1. 低学习率 hard focus + alpha=0.6 明确有效，把 d40/s10/p128 diagnostic 从约 `0.01815` 推到 `0.01573`。
2. 继续单点续训到 plus800 不再改善，应停止该路线。
3. diagnostic / compare 的 `cir_alpha` 接口修复是必要的，否则实验会低估或错估结果。
4. 置信门控 DD-CIR 在当前模型上无效，不能作为主路线。
5. 保守 PPO 先验能减少退化，但不能产生正收益。
6. 300-frame online policy 训练后，PPO 仍没有超过 Offline NN only。
7. 当前研究主瓶颈已经从“绝对 BER 是否能低于 0.01”部分转移到：

```text
如何让 Proposed 稳定超过 DFE-RLS；
如何让 RL/PEFT 在强 Offline NN 上产生真实正收益。
```

## 16. 下一步建议

下一步不建议继续盲目调 PPO policy。应优先做两个诊断：

### 16.1 找传统 DFE-RLS 强的根因

当前 DFE-RLS 在 compare seed 上 `0.00354`，明显强于 Offline NN only。

需要检查：

1. DFE-RLS 是否使用了和 Proposed 相同的 acquisition/Adapt Pilot 约束。
2. DFE-RLS 的 CIR 更新是否隐含使用了过强的 DD 数据。
3. DFE-RLS 在不同 seed、不同 SNR、不同 delay 下是否都这么强。
4. 如果 DFE-RLS 合理且稳定强于神经接收机，那么当前“极端长时延传统方法肯定差”的假设需要继续细化。

### 16.2 重构 RL 动作空间

当前 PEFT 动作对强 Offline NN 多数是扰动。

下一阶段更值得测试：

1. RL 不直接选择 PEFT 更新，而是选择接收机级超参数：
   - DD-CIR alpha；
   - 是否更新 CIR；
   - 是否启用更强/更弱的 refine pass；
   - window size；
   - rollback 策略。
2. PEFT 更新只作为后备动作，不作为初始主动作。
3. reward 仍只用 Reward Pilot，但应更关注窗口级 BER 改善，而不是单帧 loss 抖动。

当前可执行门槛：

```text
先让 RL 在 compare slice 上至少满足：
RL-Modulated BER_data <= Offline NN only BER_data

再谈超过 DFE-RLS。
```

