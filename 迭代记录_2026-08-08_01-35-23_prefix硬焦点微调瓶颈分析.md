# 迭代记录：prefix-only 硬焦点微调与收敛瓶颈分析

时间戳：2026-08-08 01:35:23（Asia/Shanghai）

## 1. 本轮目标

上一轮已经确认：

- prefix-only 监督微调有效；
- 继续增加训练步数能继续改善，但收益变小；
- SNR=15 已全线 `<0.01`；
- SNR=10 的 `delay=30/40` 仍没完全过线。

本轮进一步尝试的思路不是再平均地训练，而是把训练分布更偏向最难但仍有希望达标的区间：

```text
delay = 30/40
snr   = 10/15
pilot = 64/96/128
```

目的是判断：

1. 更偏硬的训练分布是否能继续压低 SNR=10 的难点；
2. 如果不能，说明监督训练本身已经接近瓶颈，应换训练目标或训练结构，而不是继续堆步数。

## 2. 新建 hard-focus 配置

新增临时配置：

- [configs/continual_ppo_prefix_focus_hard_snr10_15.json](D:/Research/RL4EQ/configs/continual_ppo_prefix_focus_hard_snr10_15.json)

这个配置比上一版更偏向难点，主要变化：

### 2.1 训练分布更偏难点

```text
snrs_main  = [10, 10, 10, 15]
main_delays = [20, 30, 30, 40, 40]
```

也就是说：

- SNR=10 被过采样；
- delay=30/40 被过采样；
- 目标是让模型更关注最难的部分，而不是平均对待所有样本。

### 2.2 augmentation 再次收紧

```text
modes = acquisition, dd_like, acquisition, dd_like, noisy
noise_std = 0.012
dd_flip_probability = 0.003
dd_blend_alpha = 0.08
```

比上一轮更保守，避免 augmentation 过强把模型训练偏。

### 2.3 学习率降低

```text
learning_rate = 3e-5
```

希望降低训练震荡，观察是否能把 SNR=10 的边界样本再压低一点。

## 3. smoke 验证

先做 smoke：

```powershell
.\.venv-gpu\Scripts\python.exe pretrain.py --config configs/continual_ppo_prefix_focus_hard_snr10_15.json --stage estimated_cir_level_b --steps 2 --batch-size 1 --amp --resume pretrained/prefix_focus_snr10_15_800_from_plus50_2026-08-08/model_best.pt --save-dir pretrained/prefix_focus_hard_snr10_15_smoke_2026-08-08
```

结果成功，说明：

- 新配置与现有 checkpoint 兼容；
- 训练保存链路正常；
- 适合继续做长微调。

## 4. 1400-step hard-focus 微调

从 800-step checkpoint 再训练 600 steps，得到 1400-step checkpoint：

```powershell
.\.venv-gpu\Scripts\python.exe pretrain.py --config configs/continual_ppo_prefix_focus_hard_snr10_15.json --stage estimated_cir_level_b --steps 600 --batch-size 4 --amp --resume pretrained/prefix_focus_snr10_15_800_from_plus50_2026-08-08/model_best.pt --save-dir pretrained/prefix_focus_hard_snr10_15_1400_from_plus50_2026-08-08
```

输出：

- [pretrained/prefix_focus_hard_snr10_15_1400_from_plus50_2026-08-08/model_best.pt](D:/Research/RL4EQ/pretrained/prefix_focus_hard_snr10_15_1400_from_plus50_2026-08-08/model_best.pt)

## 5. 1400-step 评估结果

评估同样的 18 个候选配置：

```text
delay = 20, 30, 40
snr   = 10, 15
pilot = 64, 96, 128
seeds = 0, 1, 2
frames = 16
```

### 5.1 与 800-step 对比

| 指标 | 800-step | 1400-step hard-focus |
|---|---:|---:|
| 18 个候选配置平均 BER | 0.007803 | 0.007748 |
| SNR=10 平均 BER | 0.010482 | 0.010389 |
| SNR=15 平均 BER | 0.005125 | 0.005108 |
| `< 0.01` 配置数 | 12 / 18 | 12 / 18 |

总体上只有极小幅改善。

### 5.2 逐点结论

#### SNR=10

`delay=20` 的三个配置都稳定进 `<0.01`：

- `d20/p64 = 0.009068`
- `d20/p96 = 0.008664`
- `d20/p128 = 0.008789`

但：

- `delay=30` 三个配置仍在 `0.0103–0.0106`
- `delay=40` 三个配置仍在 `0.0117–0.0120`

也就是说，`delay=30/40` 的 SNR=10 仍未过线。

#### SNR=15

全部依然 `<0.01`，但相对 800-step 几乎只是微幅波动。

### 5.3 重要现象：hard-focus 并没有解决瓶颈

本轮最关键的结果不是“有没有继续变好”，而是：

```text
hard-focus 训练只带来极小改善，并没有把 delay=30/40 的 SNR=10 拉进 <0.01。
```

同时还出现了局部退化：

- `d30/s10` 多个点相较 800-step 有轻微变差；
- `d40/s10/p64` 也略有变差；
- 说明过度偏向难点样本，可能已经开始牺牲一部分稳定性。

这意味着：

1. 单纯继续增加监督训练步数，收益非常有限；
2. 单纯把训练采样更多压向难点，也不是万能解；
3. 当前瓶颈大概率已经不是“再多训几百步就行”，而是训练目标或课程结构本身需要改变。

## 6. 当前结论

本轮之后，判断更明确了：

### 6.1 监督训练仍有效，但已进入明显瓶颈区

证据：

- 800-step 比 200-step 好；
- 1400-step hard-focus 又比 800-step 略好；
- 但每一步改进都很小，且难点区域仍没跨过门槛。

### 6.2 当前主要卡点就是 SNR=10 的 delay=30/40

这两个难点没有被当前监督训练策略攻克。

### 6.3 继续盲目堆监督步数，不是最优路径

因为：

- 边际收益很小；
- 局部稳定性开始受到影响；
- 说明模型的可学习增量正在枯竭。

## 7. 对下一步路线的判断

我建议下一步不要继续沿着“同类监督训练更久”走，而是转向两条更有信息量的检查：

### 7.1 先做 reward / Data 相关性诊断

目标是检查：

```text
Reward Pilot 的改善是否真的能预测 Data BER 的改善？
```

如果相关性不够强，那么 RL 本身很难稳定起作用。

### 7.2 再考虑训练结构，而不是继续堆步数

如果相关性诊断也不理想，就说明：

- 不只是监督训练瓶颈；
- 连后续 RL 的 reward 设计也要重做；
- 需要回到“目标函数与动作空间是否正确”这个层面，而不是继续加训练量。

## 8. 验证

本轮验证通过。

### 关键测试

```powershell
.\.venv-gpu\Scripts\python.exe -m pytest tests\test_evaluation_contract.py::test_config_exposes_new_snr_and_pilot_sweep_contract tests\test_meta_adaptation.py::test_pretrain_smoke_writes_strict_loadable_checkpoint -q -p no:cacheprovider
```

结果：

```text
2 passed in 27.03s
```

### 全量测试

```powershell
.\.venv-gpu\Scripts\python.exe -m pytest -q -p no:cacheprovider
```

结果：

```text
124 passed in 280.49s (0:04:40)
```

## 9. 本轮最终判断

本轮的核心判断可以概括为一句话：

```text
监督训练还能带来小幅提升，但已经接近瓶颈；
当前最难的 SNR=10 / delay=30-40 仍未过线；
继续堆同类训练步数不是最优解，下一步应该转向 reward / Data 相关性诊断和训练结构调整。
```

这意味着：

- 研究方向并未失败；
- 但“先把 Offline NN 继续靠监督压过线”这条路已经不适合无上限延伸；
- 需要更早进入“诊断 reward 是否有效”的阶段。
