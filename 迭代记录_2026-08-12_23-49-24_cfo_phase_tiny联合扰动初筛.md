# 迭代记录：cfo_phase_tiny 联合扰动 500-step 初筛

时间戳：2026-08-12 23:49:24  
分支：`codex/continual-ppo-unfolded-equalizer`  
工作目录：`D:\Research\RL4EQ`

## 1. 本轮目标

用户指出前面已经要求加入 CFO 和相位扰动，因此本轮不再只跑 `phase_tiny` 单项扰动，而是明确启用 `cfo_phase_tiny`，验证联合 residual CFO + 慢相位扰动下：

- Proposed 是否开始超过传统非神经、非 RL baseline。
- 带 Pilot 补偿的传统 baseline 是否合理。
- 当前 phase-vector conditioner 是否能在联合扰动场景下提供更有意义的优势。

本轮仍遵守新的项目契约：

- 主场景 Level B。
- 主 SNR 固定为 `0/5/10/15 dB`。
- Pilot 只放前缀。
- Proposed 是唯一使用神经网络和 RL 的方法。
- `BER_data < 0.01` 只作为辅助系统指标，不再作为第一门槛。
- 第一目标是 proposed 逐配置明显超过传统非神经、非 RL baseline，并且优势随在线帧数增加更明显。

## 2. 新增配置

新增配置文件：

```text
configs/continual_ppo_cfo_phase_tiny.json
```

关键字段：

```json
{
  "pilot_layout": "prefix",
  "pilot_total": 128,
  "impairment_profile": "cfo_phase_tiny",
  "snrs_main": [0, 5, 10, 15],
  "optional_impairments": {
    "cfo": true,
    "phase_perturbation": true,
    "nonlinearity": false,
    "coding": false,
    "higher_order_modulation": false
  }
}
```

该配置是在 `phase_tiny` 基础上打开 CFO，并保持非线性、编码、高阶调制关闭。

## 3. Smoke 验证

### 3.1 预训练 smoke

命令：

```powershell
.\.venv-gpu\Scripts\python.exe pretrain.py --config configs/continual_ppo_cfo_phase_tiny.json --stage all --steps 2 --batch-size 1 --amp --save-dir pretrained/cfo_phase_tiny_phasevector_smoke_2026-08-12
```

结果：

```text
saved pretrained/cfo_phase_tiny_phasevector_smoke_2026-08-12
```

### 3.2 compare smoke

命令：

```powershell
.\.venv-gpu\Scripts\python.exe compare.py --config configs/continual_ppo_cfo_phase_tiny.json --method-group proposed --pretrained pretrained/cfo_phase_tiny_phasevector_smoke_2026-08-12/model_best.pt --delays 20 --snrs 10 --num-seeds 1 --frames 1 --pilot-total 128 --pilot-layout prefix --impairment-profile cfo_phase_tiny --resume --output-dir logs/compare_cfo_phase_tiny_phasevector_smoke_2026-08-12
```

结果：

```text
saved logs/compare_cfo_phase_tiny_phasevector_smoke_2026-08-12
```

结论：`cfo_phase_tiny` 配置、预训练入口、compare 入口和 phase-vector conditioner 链路可运行。

## 4. 500-step 预训练

命令：

```powershell
.\.venv-gpu\Scripts\python.exe pretrain.py --config configs/continual_ppo_cfo_phase_tiny.json --stage all --steps 500 --batch-size 2 --amp --save-dir pretrained/cfo_phase_tiny_phasevector_500steps_2026-08-12
```

结果：

```text
saved pretrained/cfo_phase_tiny_phasevector_500steps_2026-08-12
```

耗时约 26 分 54 秒。

输出 checkpoint：

```text
pretrained/cfo_phase_tiny_phasevector_500steps_2026-08-12/model_best.pt
pretrained/cfo_phase_tiny_phasevector_500steps_2026-08-12/model_final.pt
pretrained/cfo_phase_tiny_phasevector_500steps_2026-08-12/pretrain_metrics.json
```

## 5. 1 seed × 20 frames × 12 主配置初筛矩阵

命令：

```powershell
.\.venv-gpu\Scripts\python.exe compare.py --config configs/continual_ppo_cfo_phase_tiny.json --method-group all --pretrained pretrained/cfo_phase_tiny_phasevector_500steps_2026-08-12/model_best.pt --delays 20 30 40 --snrs 0 5 10 15 --num-seeds 1 --frames 20 --pilot-total 128 --pilot-layout prefix --impairment-profile cfo_phase_tiny --resume --output-dir logs/compare_cfo_phase_tiny_phasevector_500steps_2026-08-12
```

结果：

```text
saved logs/compare_cfo_phase_tiny_phasevector_500steps_2026-08-12
```

输出共 4080 行 frame metrics，`impairment_profile` 全部为 `cfo_phase_tiny`。

## 6. 方法级均值

| 方法 | mean BER_data | 样本数 |
|---|---:|---:|
| LMMSE-FIR | 0.283767 | 240 |
| RLS Linear | 0.284071 | 240 |
| LMS | 0.314355 | 240 |
| NLMS | 0.352246 | 240 |
| RL-Modulated Neural Block Equalizer | 0.368056 | 240 |
| Offline NN only | 0.368392 | 240 |
| NN + Fixed Modulation | 0.368392 | 240 |
| NN + Discrete PEFT Scheduler | 0.368392 | 240 |
| NN + Rule Modulation | 0.376584 | 240 |
| CFO-Corrected LMMSE-FIR | 0.461708 | 240 |
| CFO-Corrected DFE-RLS | 0.466439 | 240 |
| CFO+DD-Phase DFE-RLS | 0.471376 | 240 |
| CFO+DD-Phase LMMSE-FIR | 0.477593 | 240 |
| Fixed CG-BPSK-DD Block Detector | 0.522732 | 240 |
| SC-FDE-MMSE | 0.524425 | 240 |
| Perfect-CSI Block | 0.537565 | 240 |
| DFE-RLS | 0.538303 | 240 |

## 7. 关键对比

`RL-Modulated Neural Block Equalizer` 相对传统方法的差值：

```text
RL - DFE-RLS:                  -0.170247
RL - CFO-Corrected DFE-RLS:    -0.098383
RL - CFO+DD-Phase DFE-RLS:     -0.103320
RL - SC-FDE-MMSE:              -0.156369
RL - LMMSE-FIR:                +0.084288
RL - RLS Linear:               +0.083984
```

解释：

- Proposed 已经明显超过 `DFE-RLS`、`SC-FDE-MMSE`、`CFO-Corrected DFE-RLS` 和 `CFO+DD-Phase DFE-RLS`。
- Proposed 没有超过 `LMMSE-FIR` 和 `RLS Linear`，后两者当前是最强传统 baseline。
- 这说明联合 CFO + 慢相位扰动确实让部分传统强算法明显退化，但不能直接声称 Proposed 已经超过所有传统 baseline。

## 8. 逐配置结果摘要

每个配置中，`Offline NN only` 和 `RL-Modulated Neural Block Equalizer` 基本相同，说明 PPO 当前没有带来实质在线收益。

典型配置：

```text
d20/s0:
  RL-Modulated: 0.445182
  Offline NN:   0.445443
  CFO-DFE:      0.496224
  SC-FDE:       0.517188
  DFE-RLS:      0.528906

d30/s10:
  Offline NN:   0.311458
  RL-Modulated: 0.311458
  CFO-DFE:      0.470703
  SC-FDE:       0.525260
  DFE-RLS:      0.537760

d40/s15:
  Offline NN:   0.238021
  RL-Modulated: 0.238021
  CFO-DFE:      0.391536
  SC-FDE:       0.529948
  DFE-RLS:      0.546224
```

但在所有主配置里，`LMMSE-FIR` / `RLS Linear` 仍经常强于 Proposed，因此当前未达成“超过传统非神经 baseline”的第一目标。

## 9. 在线帧趋势

按前 5 帧与后 5 帧均值观察：

| 方法 | 前 5 帧 mean BER | 后 5 帧 mean BER |
|---|---:|---:|
| Offline NN only | 0.311719 | 0.449783 |
| NN + Rule Modulation | 0.299609 | 0.482118 |
| RL-Modulated Neural Block Equalizer | 0.311068 | 0.449783 |
| DFE-RLS | 0.548264 | 0.492795 |
| CFO+DD-Phase DFE-RLS | 0.391102 | 0.501997 |
| SC-FDE-MMSE | 0.554601 | 0.449523 |

解释：

- 当前 PPO 没有体现“随在线帧数增加优势更明显”。
- `RL-Modulated` 与 `Offline NN only` 几乎重合，说明在线策略没有学到稳定有效动作。
- `NN + Rule Modulation` 前几帧略好，但后期明显变差，说明规则动作可能过补偿或不稳定。

## 10. 传统补偿 baseline 异常诊断

本轮发现带补偿的传统 baseline 在若干配置下反而比未补偿传统更差。不能直接把这个结果当作“传统补偿也不行”的最终结论。

已做最小诊断：

- 在同一 `cfo_phase_tiny` 帧下比较 raw DFE、CFO-corrected DFE、DD-phase DFE。
- 同时打印 Adapt/Reward/Data BER 和估计 CFO。

典型现象：

```text
profile 真实 CFO 范围约 0.0002–0.0008 cycles/symbol
但逐帧 Adapt Pilot 拟合得到的 estimated_cfo_cycles_per_symbol 可达 0.008–0.014
```

这比 profile 范围大一个数量级，导致反向旋转过补偿，进而使 Data BER 接近随机。

当前推断的根因：

- prefix Adapt Pilot 处在强长 ISI 区域。
- `_pilot_reference_from_cir()` 构造的参考波形在稀疏长回波、soft tail 误差和 residual phase 下仍有较大误差。
- 直接对 `rx * conj(reference)` 相位做 unwrap + 线性拟合，容易被深衰落点或 ISI residual 带偏。
- 因此 CFO/phase compensation 需要鲁棒拟合、幅度门限、斜率限幅，或改为 acquisition CFO + 每帧只估计相位截距/小残差，而不是每帧重新自由估计大斜率。

重要结论：

```text
当前补偿版传统 baseline 不能作为最终公平 baseline；
必须先修复或重校准 CFO/phase 补偿，再判断 Proposed 是否真正超过“合理补偿”的传统方法。
```

## 11. 本轮研究判断

本轮比 `phase_tiny` 单项扰动更接近用户要验证的方向：

- 加入 CFO + 慢相位后，传统 DFE-RLS 和 SC-FDE-MMSE 明显退化。
- 神经块均衡器在该 impaired 场景下已经超过部分传统强 baseline。
- 但最强的 `LMMSE-FIR` / `RLS Linear` 仍优于 Proposed。
- PPO 尚未贡献在线提升。
- 传统补偿器存在明显估计偏差，不能用当前补偿结果写论文结论。

因此当前路线不是失败，但还没有达到论文目标。

下一步优先级应是：

1. 修复传统 CFO/phase compensation，使其成为真正公平 baseline。
2. 同时增强 Proposed 的 phase/CFO 显式校正分支，让神经网络不只是接收 16 维统计，而是能实际校正整帧相位轨迹。
3. 在修复后的传统 baseline 上重跑 `cfo_phase_tiny` 小矩阵。
4. 只有 Proposed 超过最强传统 baseline，且随帧数增加优势扩大，才扩大 PPO 训练。

## 12. 文档同步

本轮检查了：

- `AGENTS.md`
- `README.md`
- `开发框架.md`
- `RL信道均衡研究分析.md`

`AGENTS.md` 已经与用户最新契约一致。

已修正 `开发框架.md` 中旧表述：

- 原先写 `prefix/two_block/multi_block` 均为 Pilot 候选。
- 已改为主实验只使用 prefix；`two_block` 和 `multi_block` 只作为历史兼容或诊断入口。

## 13. 下一步建议

建议立即做一个 TDD 修复任务：

```text
目标：让 CFO/phase traditional compensation 不在 cfo_phase_tiny 下出现数量级错误的 CFO 估计。
```

最小测试建议：

- 构造 `cfo_phase_tiny` Level B 帧。
- 使用 acquisition CFO-aware CIR。
- 对 Adapt Pilot 做 phase line 估计。
- 要求估计 CFO 绝对值被合理限幅，例如不超过 profile 上界若干倍。
- 要求 `CFO-Corrected LMMSE-FIR` 在多帧 smoke 中不明显劣于 `LMMSE-FIR`。

修复方向：

- 对逐帧 Adapt Pilot CFO 估计增加斜率限幅。
- 使用幅度门限过滤低置信 pilot reference 点。
- 对 `CFO-Corrected` 类方法默认使用 acquisition CFO 作为主 CFO，每帧只估计 phase intercept 或极小 residual CFO。
- 对 `CFO+DD-Phase` 使用平滑残差，而不是自由重估大斜率。

