# 迭代记录：prefix-only 传统 baseline 难度校准与主契约修正

时间戳：2026-08-06 00:08:30（Asia/Shanghai）

## 1. 本轮更正后的研究契约

用户本轮明确更正如下：

1. Pilot 只放在帧前缀，不再把 two-block / multi-block 作为主论文 Pilot 布局。
2. 主 SNR 改为 `0, 5, 10, 15 dB`，不再把 `20 dB` 放入主配置。
3. 在继续训练 Proposed / PPO 前，先做“传统 baseline 难度校准脚本/实验”。
4. CFO、额外相位扰动、非线性、编码、高阶调制可以后续按需加载；当前只保留开关语义，默认关闭，不直接混入主实验。

该契约对之前结论有直接影响：此前传统 baseline 很强，很大程度来自 multi-block / two-block Pilot 在帧中间给传统自适应方法提供了额外训练锚点。prefix-only 后，传统方法只能依赖前缀 Pilot 与随后 decision-directed / 自适应规则跟踪，难度明显提高。

## 2. 已完成代码/配置同步

本轮将主默认从旧布局切换到 prefix-only：

- `configs/continual_ppo.json`
  - `pilot_layout = "prefix"`
  - `snrs_main = [0, 5, 10, 15]`
  - `snrs_pressure = []`
  - `pilot_sweep_layout = "prefix"`
  - 新增 `optional_impairments`，默认全部关闭：
    - `cfo = false`
    - `phase_perturbation = false`
    - `nonlinearity = false`
    - `coding = false`
    - `higher_order_modulation = false`
- `configs/continual_ppo_enhanced.json`
- `configs/continual_ppo_focus_d40_snr10.json`
- `configs/continual_ppo_focus_snr10.json`
- `compare.py`
  - 默认 SNR 改为 `[0, 5, 10, 15]`
  - 未显式传 `--pilot-layout` 时回退 `prefix`
- `online_train.py`
  - 默认 `--pilot-layout prefix`
- `env/frame_structure.py`
  - `FrameConfig.layout` 默认改为 `prefix`
- `env/comm_env.py`
  - `CommEnvConfig.layout` 默认改为 `prefix`
- `training/windowed_discrete_ppo.py`
- `training/rl_modulated_online.py`
- `training/continual_ppo.py`
- `training/meta_training.py`
- `training/curriculum.py`
- `evaluation/long_episode_diagnostics.py`
- `evaluation/metrics.py`
- `scripts/diagnose_long_episode_drift.py`
- `scripts/diagnose_research_assumptions.py`
- `scripts/run_level_b_gates.py`
- `README.md`
- `AGENTS.md`

注意：two-block / multi-block 代码路径仍保留，用于历史兼容或诊断消融；但不再作为主论文默认路线。

## 3. 已完成的传统 baseline 难度校准脚本

新增入口：

```powershell
.\.venv-gpu\Scripts\python.exe scripts\calibrate_traditional_difficulty.py
```

默认参数：

```text
delays       = 20, 30, 40
snrs         = 0, 5, 10, 15
pilot_totals = 32, 48, 64, 96, 128
pilot_layout = prefix
methods      = DFE-RLS, RLS Linear, SC-FDE-MMSE, LMMSE-FIR
seeds        = 0, 1, 2
frames       = 16
```

可选 impairment 开关：

```text
--enable-cfo
--enable-phase-perturbation
--enable-nonlinearity
--enable-coding
--enable-higher-order-modulation
```

当前这些开关只记录研究意图；如果启用，脚本会退出并提示需要先实现对应物理链路，避免“配置里写了但链路里没做”的虚假实验。

## 4. 本轮传统 baseline smoke 校准结果

已完成小规模校准：

```powershell
.\.venv-gpu\Scripts\python.exe scripts\calibrate_traditional_difficulty.py `
  --delays 20 30 40 `
  --snrs 0 5 10 15 `
  --pilot-totals 64 96 128 `
  --pilot-layouts prefix `
  --seeds 0 `
  --frames 4 `
  --methods DFE-RLS "RLS Linear" SC-FDE-MMSE LMMSE-FIR `
  --output-dir logs\traditional_difficulty_prefix_snr0_15_smoke_2026-08-05
```

输出文件：

```text
logs/traditional_difficulty_prefix_snr0_15_smoke_2026-08-05/traditional_difficulty_calibration.json
```

难度分布：

```text
easy_ber_lt_0.01       = 0
useful_0.01_to_0.1     = 40
too_hard_ge_0.1        = 104
```

方法整体均值：

| method | mean BER_data | min | max |
|---|---:|---:|---:|
| DFE-RLS | 0.281850 | 0.022135 | 0.460379 |
| LMMSE-FIR | 0.258971 | 0.013021 | 0.441964 |
| RLS Linear | 0.259365 | 0.011068 | 0.446429 |
| SC-FDE-MMSE | 0.065837 | 0.015625 | 0.167969 |

关键判断：

- prefix-only 后，DFE-RLS 从之前“非常强”变成明显困难。
- 之前传统算法低 BER 的主要原因不是“极端信道对传统方法仍然天然容易”，而是 Pilot 布局给了传统自适应方法过强的中间锚点。
- 在 prefix-only 下，SC-FDE-MMSE 成为这个 smoke 中最强传统方法，而不是 DFE-RLS。
- 0 dB 的多数配置对传统方法过难；适合作压力或低 SNR robustness，不适合直接作为唯一主成功门槛。

## 5. Offline NN + DD-CIR prefix 小矩阵结果

使用旧 +50 checkpoint：

```text
pretrained/level_b_focus_d40_snr10_1650_from_1600_p128_2026-08-04_01-16-35/model_best.pt
```

该 checkpoint 不是为 prefix-only 主契约重新训练的，只用于快速判断方向。

评估命令按 pilot_total 分三次运行：

```powershell
.\.venv-gpu\Scripts\python.exe compare.py `
  --config configs/continual_ppo.json `
  --methods "Offline NN only" `
  --pretrained pretrained/level_b_focus_d40_snr10_1650_from_1600_p128_2026-08-04_01-16-35/model_best.pt `
  --delays 20 30 40 `
  --snrs 0 5 10 15 `
  --num-seeds 1 `
  --frames 4 `
  --pilot-total <64|96|128> `
  --pilot-layout prefix `
  --cir-update decision_directed `
  --device cuda `
  --resume `
  --output-dir logs/nn_prefix_snr0_15_smoke_2026-08-05_p<pilot>
```

整体：

```text
Offline NN + DD-CIR mean BER_data  = 0.050777
Best traditional mean BER_data     = 0.065656
```

逐配置摘要：

| delay | SNR | pilot | NN + DD-CIR | 最强传统 | 判断 |
|---:|---:|---:|---:|---:|---|
| 20 | 0 | 64 | 0.137835 | 0.167969 | NN 胜但未达绝对目标 |
| 20 | 0 | 96 | 0.129808 | 0.165264 | NN 胜但未达绝对目标 |
| 20 | 0 | 128 | 0.126953 | 0.161458 | NN 胜但未达绝对目标 |
| 20 | 5 | 64 | 0.049107 | 0.068080 | NN 胜但未达绝对目标 |
| 20 | 5 | 96 | 0.045673 | 0.069111 | NN 胜但未达绝对目标 |
| 20 | 5 | 128 | 0.044271 | 0.069010 | NN 胜但未达绝对目标 |
| 20 | 10 | 64 | 0.016741 | 0.021763 | NN 胜但未达绝对目标 |
| 20 | 10 | 96 | 0.014423 | 0.022837 | NN 胜但未达绝对目标 |
| 20 | 10 | 128 | 0.011719 | 0.022786 | NN 胜但接近目标 |
| 20 | 15 | 64 | 0.005580 | 0.016183 | 强候选 |
| 20 | 15 | 96 | 0.004808 | 0.016226 | 强候选 |
| 20 | 15 | 128 | 0.003906 | 0.011068 | 强候选 |
| 30 | 0 | 64 | 0.137835 | 0.153460 | NN 胜但未达绝对目标 |
| 30 | 0 | 96 | 0.136418 | 0.153846 | NN 胜但未达绝对目标 |
| 30 | 0 | 128 | 0.131510 | 0.152344 | NN 胜但未达绝对目标 |
| 30 | 5 | 64 | 0.054688 | 0.050781 | NN 落后 |
| 30 | 5 | 96 | 0.055889 | 0.052284 | NN 落后 |
| 30 | 5 | 128 | 0.052083 | 0.051432 | NN 接近但落后 |
| 30 | 10 | 64 | 0.016183 | 0.022879 | NN 胜但未达绝对目标 |
| 30 | 10 | 96 | 0.015024 | 0.024038 | NN 胜但未达绝对目标 |
| 30 | 10 | 128 | 0.014974 | 0.024740 | NN 胜但未达绝对目标 |
| 30 | 15 | 64 | 0.010045 | 0.015625 | NN 胜但略高于目标 |
| 30 | 15 | 96 | 0.008413 | 0.016827 | 强候选 |
| 30 | 15 | 128 | 0.008464 | 0.017578 | 强候选 |
| 40 | 0 | 64 | 0.135045 | 0.138393 | NN 略胜但未达绝对目标 |
| 40 | 0 | 96 | 0.136418 | 0.143630 | NN 略胜但未达绝对目标 |
| 40 | 0 | 128 | 0.134115 | 0.144531 | NN 胜但未达绝对目标 |
| 40 | 5 | 64 | 0.045201 | 0.068638 | NN 胜但未达绝对目标 |
| 40 | 5 | 96 | 0.043269 | 0.071514 | NN 胜但未达绝对目标 |
| 40 | 5 | 128 | 0.044271 | 0.070964 | NN 胜但未达绝对目标 |
| 40 | 10 | 64 | 0.013951 | 0.033482 | NN 胜但未达绝对目标 |
| 40 | 10 | 96 | 0.012620 | 0.034856 | NN 胜但未达绝对目标 |
| 40 | 10 | 128 | 0.012370 | 0.037109 | NN 胜但接近目标 |
| 40 | 15 | 64 | 0.007813 | 0.022879 | 强候选 |
| 40 | 15 | 96 | 0.006010 | 0.024639 | 强候选 |
| 40 | 15 | 128 | 0.004557 | 0.025391 | 强候选 |

## 6. 对“传统方法为什么之前跑得很好”的重新解释

当前证据支持以下解释：

1. 不是“极端长时延下传统方法一定天然很强”。
2. 也不是“神经网络/RL 方向必然不可行”。
3. 主要偏差来自之前的 Pilot 布局：
   - multi-block / two-block 给传统自适应均衡器提供了帧中间监督点；
   - DFE-RLS 等方法可以在数据段附近获得更好的局部信道跟踪；
   - 这会显著降低传统 baseline 的 BER，使场景对传统方法过于友好。
4. 用户更正为 prefix-only 后，传统 baseline 难度显著上升，符合“长回波 + 长数据段 + 只有前缀 Pilot”对传统自适应方法不利的直觉。

因此，之前“传统算法强到难以超过”的结果不应直接否定创新点，而应视为实验契约偏离目标后的现象。

## 7. 当前风险与约束

虽然 prefix-only 后 NN 在多数配置上已经超过最强传统 smoke baseline，但还不能写论文结论，原因：

1. 目前只跑了 `seed=0, frames=4` 的 smoke，不是正式配对矩阵。
2. 使用的是旧 +50 checkpoint，不是按 prefix-only + SNR 0/5/10/15 重新预训练的 checkpoint。
3. 0 dB 配置下 NN 与传统都很差，`BER_data < 0.01` 不现实，建议作为压力点或单独报告。
4. Reward Pilot 与 Data BER 的相关性在 prefix-only 新契约下出现下降：
   - 单元测试中真实 grouped pair 的 Spearman 约为 `0.571`，低于旧门槛 `0.6`。
   - 这意味着 PPO reward 设计需要重新校准，否则“Reward Pilot 改善”不一定稳定预测 Data BER 改善。

## 8. 下一步建议

建议采用门槛式推进：

1. 先做正式传统 baseline 难度校准：
   - prefix-only
   - SNR 0/5/10/15
   - pilot_total 64/96/128/160/192
   - seeds 至少 0/1/2
   - frames 至少 16
2. 根据传统 baseline 的 BER 区间重新定义 Level B 主工作区间：
   - 主平均优先考虑传统 baseline 约 `0.01–0.1`、NN 有机会 `<0.01` 的配置；
   - 0 dB 和明显过难配置单独列为低 SNR 压力测试；
   - 不用“所有配置平均”掩盖配置级失败。
3. 重新做 prefix-only 课程预训练：
   - 训练分布必须覆盖 prefix-only；
   - 主 SNR 使用 0/5/10/15；
   - 优先增强 Offline NN + DD-CIR，使其在候选主配置稳定达到 `<0.01`。
4. Offline NN 达标后再启动 RL：
   - RL 目标不是拯救弱接收机；
   - RL 目标应是在 Offline NN 已经强的基础上，用 Reward Pilot 窗口反馈进一步超过 `Offline NN only` 和传统 baseline。
5. PPO 前必须重新验证 Reward Pilot/Data 相关性：
   - 如果 Spearman 仍低于 0.6，先改 reward 窗口长度、Reward Pilot 分配或 action guard；
   - 不应直接扩大 PPO 训练规模。

## 9. 本轮验证

全量测试：

```powershell
.\.venv-gpu\Scripts\python.exe -m pytest -q -p no:cacheprovider
```

结果：

```text
124 passed in 264.53s (0:04:24)
```

本轮新增/更新的关键测试：

- `test_config_exposes_new_snr_and_pilot_sweep_contract`
- `test_default_entrypoints_use_prefix_pilot_and_correct_main_snrs`
- `test_traditional_difficulty_calibration_cli_uses_prefix_and_low_snr_defaults`
- `test_curriculum_validate_level_b_runs_real_twelve_config_gate`
- `test_reward_data_alignment_uses_real_grouped_pairs`

## 10. 当前结论

当前方向没有被证伪。更准确地说：

- 之前传统方法过强，主要是实验布局给传统 baseline 太多中间 Pilot 支撑。
- prefix-only 后，传统方法难度回到合理区间。
- 旧 checkpoint 的 Offline NN 在多数 smoke 配置已经超过最强传统 baseline，但在 0/5/10 dB 多数配置未达 `BER_data < 0.01`。
- 下一阶段不应立刻训练 PPO；应先按 prefix-only 重新训练 Offline NN，并筛选合理 Level B 主工作区间。
