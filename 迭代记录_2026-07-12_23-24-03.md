# 迭代记录 2026-07-12 23:24:03

## 目标

参照 `RL信道均衡研究分析.md` 第 5 节，在保持 `开发框架.md` 不变的前提下，按优先级执行优化：

1. 把实验闭环做严谨。
2. 量化 pilot reward 与 data BER 的相关性。
3. 增强 RL observation。
4. 增强 reward，但仍只使用 pilot 可观测量。
5. 让 RL action 更有物理意义。
6. 把 MMSE 从对手变成教师/特征。
7. 做 pilot overhead 敏感性实验。

本轮没有修改 `开发框架.md`。

## 基础模型

本轮以当前标准模型作为主要比较基线：

```text
pretrained/model_best.pt
pretrained/model_config.json
```

该模型来自上一轮方案 B：

```text
Transformer + Adapter + NeuralChannelEncoder + SyncPhaseDelayHead
window_K = 14
d_model = 128
n_layers = 3
adapter_rank = 16
```

上一轮独立评估：

```text
Neural mean BER_data = 0.01109
MMSE mean BER_data   = 0.00840
```

## 优先级 1：实验闭环

### 实现

新增函数：

```python
run_strategy_diagnostics(...)
```

位置：

```text
online_train.py
```

对同一批 frame seed 同时评估：

| 方法 | 含义 |
|---|---|
| MMSE | 强传统 baseline |
| skip | no adaptation |
| head-slow / head-fast | 固定输出头更新 |
| adapter-slow / adapter-fast | 固定 adapter/sync 更新 |
| both-slow / both-fast / both-deep | 固定组合更新 |
| PPO policy | 当前 RL 策略 |
| pilot-oracle action | 每帧选择 pilot loss 最低的 action，仅诊断 |
| data-oracle action | 每帧选择 data BER 最低的 action，仅仿真诊断 |

所有候选 action 都从同一帧的 `θ_pre` 快照恢复后执行，避免候选之间互相污染。

### 验证实验

命令等效配置：

```text
frames = 60
SNR = 10 dB
profile = Rayleigh 可变抽头
pretrained = pretrained/model_best.pt
output_dir = logs/priority1_2_strategy_diagnostics_20260713
```

结果：

| Method | BER_data | BER_pilot | pilot_loss |
|---|---:|---:|---:|
| MMSE | 0.01504 | 0.01094 | 0.00000 |
| skip | 0.03268 | 0.01250 | 0.03715 |
| head-slow | 0.03275 | 0.01237 | 0.03706 |
| head-fast | 0.03268 | 0.01224 | 0.03670 |
| adapter-slow | 0.03281 | 0.01198 | 0.03491 |
| adapter-fast | 0.03210 | 0.00951 | 0.02772 |
| both-slow | 0.03281 | 0.01185 | 0.03482 |
| both-fast | 0.03210 | 0.00911 | 0.02737 |
| both-deep | 0.03275 | 0.00586 | 0.01694 |
| PPO policy | 0.03184 | 0.01029 | 0.03109 |
| pilot-oracle | 0.03275 | 0.00586 | 0.01694 |
| data-oracle | 0.02995 | 0.01068 | 0.03091 |

输出：

```text
logs/priority1_2_strategy_diagnostics_20260713/strategy_diagnostics.json
logs/priority1_2_strategy_diagnostics_20260713/strategy_matrix.png
logs/priority1_2_strategy_diagnostics_20260713/pilot_data_correlation.png
```

### 反思

不符合预期。

PPO 没有稳定优于固定策略；pilot-oracle 虽然显著降低 pilot loss，但 data BER 没有同步改善。这说明当前 action space 和 pilot reward 对 data BER 的控制力不足。

## 优先级 2：pilot reward 与 data BER 相关性

### 实现

`run_strategy_diagnostics()` 同时输出每帧、每 action 的：

```text
Δpilot_loss
ΔBER_data
BER_pilot
BER_data
action_id
strategy
```

并计算：

```text
Pearson(Δpilot_loss, ΔBER_data)
Pearson(BER_pilot, BER_data)
```

### 验证结果

第一轮诊断：

```text
Δpilot_loss vs ΔBER_data: -0.0467
BER_pilot vs BER_data:     0.7356
```

### 反思

关键结论：

- `Δpilot_loss` 与 `ΔBER_data` 基本无相关，甚至略为负。
- `BER_pilot` 与 `BER_data` 有较高相关。

这说明“更新动作带来的 pilot loss 下降”不是可靠 reward；但“更新后的 pilot BER 水平”对 data BER 更有诊断价值。后续 reward 应加入 pilot BER、置信度、残差相关性和参数漂移惩罚。

## 优先级 3：增强 observation

### 实现

修改文件：

```text
agent/adaptation_controller.py
online_train.py
```

新增常量：

```python
OBS_DIM = 14
```

observation 从 8 维扩展为 14 维：

```text
pilot_loss
BER_pilot
pilot_conf_mean
pilot_conf_std
pilot_conf_q10
pilot_conf_q50
pilot_conf_q90
pilot_error_burst_len
pilot_residual_lag1_autocorr
SNR/30
loss_ema
ber_ema
last_reward
last_latency_ms/100
```

所有特征只来自 pilot 和历史在线可观测量，不使用 data 标签。

### 测试

新增测试：

```text
test_adaptation_observation_uses_enhanced_pilot_only_features
```

结果：

```text
通过
```

### 反思

符合预期。

observation 现在能表达 pilot 错误分布、置信度分位数和残差时序相关性，比单一 loss/BER 更能描述失败模式。但仅增强 observation 不会自动提升 PPO，因为当前 policy 仍缺少充分训练。

## 优先级 4：增强 reward

### 实现

修改 `AdaptationController.adapt_frame()`。

旧 reward：

```text
pilot_loss_before - pilot_loss_after - 0.01 * steps
```

新 reward：

```text
reward =
  Δpilot_loss
  + 0.5 * Δpilot_BER
  + 0.05 * Δpilot_confidence
  - regularization * parameter_delta_norm
  - 0.01 * update_steps
  - 0.0001 * latency_ms
  - 0.02 * |pilot_residual_lag1_autocorr|
```

新增指标：

```text
parameter_delta_norm
```

### 验证实验

输出目录：

```text
logs/priority3_4_enhanced_obs_reward_20260713
```

结果：

| Method | BER_data | BER_pilot | pilot_loss | parameter_delta_norm |
|---|---:|---:|---:|---:|
| MMSE | 0.01523 | 0.01250 | 0.00000 | 0.00000 |
| skip | 0.03444 | 0.01042 | 0.03304 | 0.00000 |
| adapter-fast | 0.03294 | 0.00833 | 0.02509 | 0.00813 |
| both-fast | 0.03268 | 0.00820 | 0.02474 | 0.00813 |
| both-deep | 0.03216 | 0.00508 | 0.01525 | 0.02060 |
| PPO policy | 0.03372 | 0.00833 | 0.02774 | 0.00671 |
| pilot-oracle | 0.03216 | 0.00508 | 0.01525 | 0.02060 |
| data-oracle | 0.03034 | 0.00833 | 0.02611 | 0.00674 |

相关性：

```text
Δpilot_loss vs ΔBER_data: 0.2668
BER_pilot vs BER_data:    0.4147
```

### 反思

部分符合预期。

复合 reward 改善了 `Δpilot_loss` 与 `ΔBER_data` 的相关性，但 PPO policy 仍没有超过固定策略。原因是 policy 仍在线短 rollout 学习，样本效率不足。下一步如果要证明 RL 有贡献，需要离线训练 policy 或 imitation pilot/data-oracle policy。

## 优先级 5：更有物理意义的 action

### 实现

修改文件：

```text
agent/adaptation_controller.py
online_train.py
```

扩展 `AdaptationStrategy`：

```python
pseudo_label_gate
pseudo_weight
regularization
```

新增 action：

```text
pseudo-both-fast
pseudo-both-deep
```

伪标签规则：

```text
data 位置满足：
  neural confidence >= 0.85
  MMSE confidence >= 0.65
  neural decision == MMSE decision
```

这些位置作为 pseudo-pilot 参与在线 PEFT loss。该过程不使用 data 真实标签。

### 测试

新增测试：

```text
test_pseudo_label_strategy_updates_with_pseudo_mask
```

结果：

```text
通过
```

### 验证实验

输出目录：

```text
logs/priority5_pseudo_actions_20260713
```

结果：

| Method | BER_data | BER_pilot | pilot_loss |
|---|---:|---:|---:|
| MMSE | 0.01543 | 0.01029 | 0.00000 |
| skip | 0.03066 | 0.01172 | 0.03323 |
| both-fast | 0.03040 | 0.00898 | 0.02517 |
| both-deep | 0.03099 | 0.00456 | 0.01643 |
| pseudo-both-fast | 0.03040 | 0.01042 | 0.02799 |
| pseudo-both-deep | 0.03040 | 0.00664 | 0.02072 |
| PPO policy | 0.03073 | 0.00911 | 0.02627 |
| pilot-oracle | 0.03099 | 0.00456 | 0.01643 |
| data-oracle | 0.02760 | 0.00898 | 0.02684 |

相关性：

```text
Δpilot_loss vs ΔBER_data: 0.1217
BER_pilot vs BER_data:    0.7714
```

### 反思

不符合预期。

pseudo action 没有显著降低 BER。主要原因可能是：

1. 当前伪标签门控过保守，选中的 data 较少。
2. MMSE 与 neural 一致的位置多半已经容易，提供的新信息有限。
3. 单帧短步更新不足以利用 pseudo-label。

下一步如果继续做，应把 pseudo-label gate 从固定规则改成 RL 控制阈值和比例，而不是仅新增两个固定 action。

## 优先级 6：MMSE-assisted 特征

### 实现

修改文件：

```text
env/comm_env.py
pretrain.py
online_train.py
compare.py
```

新增配置：

```python
EnvConfig.use_mmse_features
--use_mmse_features
```

启用后状态增加两维：

```text
mmse_soft_norm
mmse_conf
```

MMSE soft 只用训练序列估计，不使用 data 真值。

### 训练

输出目录：

```text
pretrained/priority6_mmse_features_20260713
logs/priority6_mmse_features_20260713_pretrain.log
```

训练配置：

```text
num_steps = 700
batch_size = 3
SNR = 10 dB
use_mmse_features = True
init_checkpoint = pretrained/model_best.pt
```

训练验证结果：

```text
best val BER_data = 0.01006
```

### 独立评估

输出目录：

```text
logs/priority6_mmse_features_20260713_final_eval
```

结果：

| Channel | Neural BER_data | MMSE BER_data |
|---|---:|---:|
| Rayleigh | 0.03447 | 0.01712 |
| Rician | 0.01224 | 0.01100 |
| EPA | 0.00072 | 0.00326 |
| EVA | 0.00544 | 0.00638 |
| ETU | 0.00488 | 0.00713 |
| Mean | 0.01155 | 0.00898 |

输出：

```text
logs/priority6_mmse_features_20260713_final_eval/final_profile_metrics.json
logs/priority6_mmse_features_20260713_final_eval/final_profile_comparison.png
```

### 反思

不符合预期。

MMSE 特征没有超过当前标准模型独立评估 `0.01109`。原因是输入层因 state_dim 改变而部分随机初始化，700 步不足以充分重训；同时简单拼接 `mmse_soft/conf` 不等价于 residual learning。后续若继续优先级 6，应加入蒸馏损失和 residual head，而不是只加两维特征。

本轮未把该模型复制到标准 `pretrained/model_best.pt`，因为效果弱于当前标准模型。

## 优先级 7：pilot overhead 实验

### 实现

新增函数：

```python
run_pilot_overhead_study(...)
```

位置：

```text
online_train.py
```

实验设置：

```text
known ratio: 50%, 25%, 12.5%, 6.25%
frame_len = 512
num_pilots = 2
train_len + 2 * pilot_len = known_total
```

### 验证实验

输出目录：

```text
logs/priority7_pilot_overhead_20260713
```

结果：

| Known ratio | train_len | pilot_len | Neural BER_data | MMSE BER_data |
|---:|---:|---:|---:|---:|
| 50.00% | 128 | 64 | 0.03105 | 0.01094 |
| 25.00% | 64 | 32 | 0.09121 | 0.04980 |
| 12.50% | 32 | 16 | 0.16451 | 0.10798 |
| 6.25% | 16 | 8 | 0.26844 | 0.24042 |

输出：

```text
logs/priority7_pilot_overhead_20260713/pilot_overhead.json
logs/priority7_pilot_overhead_20260713/pilot_overhead.png
```

### 反思

不符合“低导频下神经方法优于 MMSE”的预期。

当前模型离线训练固定在 50% 已知位帧结构上，直接换低 pilot overhead 会造成分布外输入。神经模型和 MMSE 都变差，但神经模型退化更快。若要把低导频作为论文贡献，必须重新做多 overhead 混合预训练，而不是只做零样本评估。

## 最终代码验证

测试命令：

```bash
python -B -m pytest -p no:cacheprovider tests/test_parameter_efficient_adaptation.py -q
```

结果：

```text
24 passed in 11.49s
```

新增/更新测试覆盖：

- 策略诊断闭环。
- 增强 observation。
- pseudo-label action。
- MMSE-assisted state。
- pilot overhead study。

## 总体结论

本轮最关键的结论是诊断性的：

1. 当前 PPO 没有稳定优于固定 PEFT。
2. pilot loss 的下降与 data BER 改善相关性弱。
3. data-oracle action 优于 pilot-oracle，说明 action space 有一点潜力，但 pilot reward 选不出来。
4. pseudo-label action 和简单 MMSE 特征没有立即带来稳定提升。
5. 低 pilot overhead 下当前模型分布外退化明显。

因此，下一步不应继续堆 PPO 复杂度或单纯加训练步数。优先应该做：

```text
固定测试集
-> 离线训练 PPO / imitation policy
-> 固定 PEFT vs PPO-controlled PEFT 消融
-> 加入 residual learning 和 MMSE distillation
-> 多 pilot overhead 混合预训练
-> CFO/Doppler/非线性失真场景
```

## 对论文目标的判断

如果论文目标是“强化学习均衡整体优于 MMSE”，当前证据不足。

更可行的论文主张应调整为：

```text
在 pilot-only 在线可观测约束下，RL 控制参数高效神经均衡；
通过 action-space 诊断、pilot-data reward 相关性分析和模型辅助伪标签门控，
证明哪些场景下 RL 控制有效，哪些场景下传统 MMSE 仍有结构优势。
```

当前数据支持的强结论：

- EPA/EVA/ETU 等标准化复杂多径 profile 上，神经模型可优于 MMSE。
- Rayleigh/Rician 线性多径上，MMSE 仍更强。
- RL policy 需要离线训练和更强 reward/observation 才能成为论文主贡献。

## 后续建议

下一轮建议只做三件事：

1. 固定独立测试集文件，所有实验使用同一批 seeds。
2. 新增固定 PEFT baseline 表格：`skip / fixed / PPO / pilot-oracle / data-oracle / MMSE`。
3. 做 imitation learning：用 data-oracle action 作为标签预训练 policy，再用 pilot-only reward 微调。

这比继续随机 PPO 在线探索更有论文价值。
