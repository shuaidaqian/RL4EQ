# 迭代记录 2026-07-15 00:18:50

本轮目标：参照上一轮迭代记录中的 6 条后续改进建议，按优先级顺序依次执行，每个优先级执行后验证效果并反思是否符合预期。全程保持 `开发框架.md` 不变。

## 总体说明

本轮完成的 6 个优先级：

1. 增加显式 CFO latent head，并让 RL action 控制 CFO correction 强度。
2. 扩大 imitation 数据入口，并记录 action accuracy / oracle regret。
3. 将 residual learning 改为 selective distillation。
4. 对低 pilot overhead 单独训练模型，并做 cross-overhead 泛化表。
5. 对 RLS baseline 做严格公平性审计。
6. 输出更稳妥的论文表述建议。

本轮新增/扩展的核心代码：

- `agent/neural_equalizer.py`
  - 新增 `CFOLatentHead`
  - `EqualizerConfig` 新增 `use_cfo_head`
  - `AdapterEqualizer.set_cfo_correction_strength(...)`
  - forward 内支持 CFO latent correction
- `agent/adaptation_controller.py`
  - `AdaptationStrategy` 新增 `cfo_correction_strength`
  - action table 新增 `cfo-light` 和 `cfo-strong`
- `online_train.py`
  - 新增 `summarize_imitation_dataset(...)`
  - imitation dataset 记录每帧候选 action，用于 oracle regret
  - `run_online_adaptation(...)` 支持 `use_cfo_head`
- `pretrain.py`
  - 新增 `selective_distill_weight`
  - 新增 `use_cfo_head`
  - residual-assisted 模型可做 selective distillation
- `compare.py`
  - 新增 `run_low_pilot_specialized_study(...)`
  - 新增 `audit_rls_fairness(...)`
  - `run_mismatch_scenario_comparison(...)` 支持 `use_cfo_head`
- `论文结论建议_2026-07-15_00-18-50.md`
  - 给出当前结果下更稳妥的论文定位和表述。

## 优先级 1：显式 CFO latent head + RL action 控制

### 实现内容

新增 CFO latent head：

```text
known pilot/training observations
-> CFOLatentHead
-> cfo_hat
-> cfo_correction_strength from RL action
-> rotate rx window
-> equalizer
```

新增 action：

```text
cfo-light:  cfo_correction_strength=0.5
cfo-strong: cfo_correction_strength=1.0
```

默认 `use_cfo_head=False`，不影响旧模型。

### 验证配置

训练：

```text
checkpoint: pretrained/cfo_head_2026-07-15_00-18-50/model_best.pt
num_steps=40
batch_size=2
SNR=10 dB
use_mmse_features=True
use_cfo_head=True
train_known_ratios=[0.5, 0.25]
impairment_prob=1.0
selective_distill_weight=0.05
init_checkpoint=pretrained/model_best.pt
```

评估：

```text
scenario=cfo
num_frames=4
imitation_frames=6
imitation_epochs=6
```

输出：

```text
logs/priority_sequence_2026-07-15_00-18-50/priority1_cfo_head/summary.json
logs/priority_sequence_2026-07-15_00-18-50/priority1_cfo_head/eval/mismatch_scenario_comparison.png
```

### 结果

离线训练：

```text
best_data_ber = 0.02109
```

CFO 在线评估：

| Method | BER_data | BER_pilot |
|---|---:|---:|
| PEFT + CFO head | 0.51563 | 0.24414 |
| MMSE | 0.50684 | 0.48633 |
| ZF | 0.48535 | 0.47070 |
| LMS | 0.51563 | 0.48438 |
| RLS | 0.51074 | 0.48047 |

### 反思

不符合预期。

CFO head 虽然已接入 action 控制，但 40 step 小规模训练后 CFO 场景仍接近随机，PEFT 也没有超过 ZF。原因：

1. CFO latent 没有显式 CFO 标签监督，只靠 bit-level loss 间接学习，学习信号弱。
2. 当前 action 只控制 correction strength，没有让 policy 直接选择 CFO sign 或 CFO value。
3. CFO 场景下整帧相位旋转强，pilot-only PEFT 不足以恢复 data 段相位。

下一步需要把 CFO head 改为显式监督：离线阶段已知注入的 `cfo_norm`，可以直接训练 `cfo_hat` 回归；在线阶段再让 RL 控制 correction strength。

## 优先级 2：扩大 imitation 数据并诊断 action accuracy / oracle regret

### 实现内容

`build_oracle_action_dataset(...)` 现在额外返回：

```python
candidate_records
```

每一帧保存所有候选 action 的：

- action id
- strategy name
- `BER_data`
- `BER_pilot`
- `pilot_loss`

新增：

```python
summarize_imitation_dataset(dataset, predicted_actions)
```

输出：

- `num_samples`
- `action_accuracy`
- `oracle_regret`

### 验证配置

```text
scenario=cfo
validated_frames=24
requested_target_frames=1000
imitation_epochs=30
```

说明：代码路径已经支持 1000 帧，但完整 data-oracle 每帧需要遍历所有 action 并执行在线适应，1000 帧在当前 CPU 环境耗时过长。本轮用 24 帧验证训练和诊断闭环，记录中明确标注。

输出：

```text
logs/priority_sequence_2026-07-15_00-18-50/priority2_imitation_scale/imitation_summary.json
```

### 结果

```text
num_samples = 24
action_accuracy = 0.20833
oracle_regret = 0.01367
pretrain_loss = 2.27782
```

### 反思

不符合预期。

action accuracy 只有 20.8%，说明当前 observation 无法稳定预测 data-oracle action。oracle regret=0.01367，说明 policy 选错 action 会造成可观 BER 损失。

下一步应增加：

1. candidate action probing：对每个 action 做 pilot-only 虚拟更新，作为 policy 输入。
2. 场景标签或 latent：CFO/Doppler/nonlinear/low pilot 的可观测 summary。
3. 大规模缓存式 oracle dataset，避免每次重新执行候选 action。

## 优先级 3：selective residual distillation

### 实现内容

`pretrain.py` 新增：

```python
selective_distill_weight
```

当 `use_mmse_features=True` 时：

```text
MMSE 预测正确区域:
  BCE(neural_logits, mmse_soft_target)
  + residual_correction penalty

MMSE 预测错误区域:
  继续由真实 bit supervision 驱动 neural residual correction
```

### 验证配置

训练：

```text
checkpoint: pretrained/selective_distill_2026-07-15_00-18-50/model_best.pt
num_steps=60
batch_size=2
SNR=10 dB
train_known_ratios=[0.5, 0.25, 0.125]
impairment_prob=0.65
selective_distill_weight=0.15
init_checkpoint=pretrained/model_best.pt
```

评估：

```text
scenarios=["nonlinear", "doppler"]
num_frames=4
imitation_frames=8
imitation_epochs=8
```

输出：

```text
logs/priority_sequence_2026-07-15_00-18-50/priority3_selective_distill/summary.json
logs/priority_sequence_2026-07-15_00-18-50/priority3_selective_distill/eval/mismatch_scenario_comparison.png
```

### 结果

离线训练：

```text
best_data_ber = 0.002734
```

在线评估：

| 场景 | PEFT BER_data | MMSE BER_data | ZF BER_data | LMS BER_data | RLS BER_data |
|---|---:|---:|---:|---:|---:|
| nonlinear | 0.04297 | 0.02344 | 0.04199 | 0.02539 | 0.02051 |
| doppler | 0.03809 | 0.01660 | 0.03418 | 0.04688 | 0.02637 |

### 反思

部分符合预期。

离线 `best_data_ber=0.002734` 明显优于上一轮 `0.007031`，说明 selective distillation 对离线 residual-assisted 模型有明显帮助。

但在线仍未超过 MMSE/RLS。原因：

1. 离线能力改善没有完全转化为在线 policy 收益。
2. 在线 action policy 仍然不稳定，imitation accuracy 低。
3. 对 nonlinear 场景，PEFT 接近 ZF，但仍弱于 MMSE/RLS。
4. 对 doppler 场景，PEFT 弱于 MMSE，但优于 LMS。

下一步应把 selective distillation 与更可靠的 policy 输入结合，而不是只增强 equalizer。

## 优先级 4：低 pilot overhead 专用模型和 cross-overhead 表

### 实现内容

新增：

```python
run_low_pilot_specialized_study(...)
```

功能：

1. 对每个 train ratio 单独训练模型。
2. 在多个 eval ratio 上交叉评估。
3. 输出 cross-overhead 泛化矩阵。

### 验证配置

```text
train_ratios=[0.25, 0.125]
eval_ratios=[0.25, 0.125]
train_steps=20
eval_frames=2
d_model=32
n_layers=1
adapter_rank=8
```

输出：

```text
logs/priority_sequence_2026-07-15_00-18-50/priority4_low_pilot_specialized/low_pilot_specialized.json
```

### 结果

| Train ratio | Eval ratio | PEFT BER_data | MMSE BER_data | RLS BER_data |
|---:|---:|---:|---:|---:|
| 0.25 | 0.25 | 0.50911 | 0.05078 | 0.02604 |
| 0.25 | 0.125 | 0.56138 | 0.12723 | 0.03683 |
| 0.125 | 0.25 | 0.57161 | 0.03125 | 0.06771 |
| 0.125 | 0.125 | 0.46540 | 0.10268 | 0.03348 |

### 反思

不符合预期。

20 step 小模型训练不足，PEFT 基本接近随机。该实验验证了 cross-overhead 表和专用训练入口，但不能作为性能结论。

下一步需要：

1. 用标准模型规模 `d_model=128,n_layers=3`。
2. 每个 overhead 至少训练 1000 steps。
3. 使用 `init_checkpoint=pretrained/model_best.pt` 做兼容初始化。
4. 固定 eval seeds，并报告均值/标准差。

## 优先级 5：RLS baseline 公平性审计

### 实现内容

新增：

```python
audit_rls_fairness(...)
```

审计方法：

1. 同一帧上先用真实 bits 评估 RLS。
2. 翻转 training 段之后的 pilot/data bits。
3. 再次运行 RLS。
4. 比较两次 RLS soft output 是否变化。

如果输出不变，说明 RLS 不依赖 pilot/data 标签。

### 验证配置

```text
num_frames=30
SNR=10 dB
```

输出：

```text
logs/priority_sequence_2026-07-15_00-18-50/priority5_rls_fairness/rls_fairness_audit.json
```

### 结果

```text
uses_training_only = true
max_changed_output_delta = 0.0
mean_BER_data = 0.01432
std_BER_data = 0.01797
num_frames = 30
```

### 反思

符合预期。

当前 RLS baseline 确实只依赖 training 段标签，不使用 pilot/data 标签。因此 RLS 在低 pilot overhead 和线性信道上的强性能不能简单归因于标签泄漏，而是其自适应 FIR 结构先验确实很强。

论文对比中必须保留 RLS，并报告复杂度和延迟，而不能只与 MMSE 对比。

## 优先级 6：论文表述建议

### 输出

```text
论文结论建议_2026-07-15_00-18-50.md
```

### 核心建议

不建议写：

```text
强化学习均衡方法全面优于传统均衡算法。
```

建议写：

```text
本文提出一种 model-assisted RL 控制的 residual PEFT 在线均衡框架。
该框架严格遵守 pilot-only online observability：在线阶段不使用 data 标签作为 reward，
RL agent 只控制少量参数更新、伪标签利用和同步/残差校正强度。
实验表明，residual-assisted 离线预训练可显著改善神经均衡器的初始泛化能力；
但在 CFO-dominated 和强传统自适应滤波 baseline 存在时，性能收益依赖于显式同步建模、
selective distillation 和足够规模的 data-oracle imitation policy 预训练。
```

### 反思

符合预期。

当前结果更适合定位为：

```text
面向在线可观测约束的 model-assisted RL 神经均衡框架与诊断研究
```

而不是：

```text
一种全面超过传统均衡器的强化学习均衡算法
```

## 最终测试

命令：

```bash
python -B -m pytest -p no:cacheprovider tests/test_parameter_efficient_adaptation.py -q
```

结果：

```text
39 passed
```

## 本轮总体结论

1. CFO head / action control 已落地，但缺少显式 CFO 回归监督，CFO 场景仍接近随机。
2. imitation 诊断已落地，当前 action accuracy 只有 20.8%，说明 policy 不是主要性能来源。
3. selective distillation 对离线模型有效，best_data_ber 改善到 0.002734。
4. 低 pilot 专用训练入口已落地，但小规模验证结果不可用，需要标准模型和长训练。
5. RLS baseline 公平性审计通过，RLS 强性能不是由 pilot/data 标签泄漏造成。
6. 论文表述应从“全面超越传统均衡”调整为“model-assisted RL residual PEFT 框架及其在线可观测约束下的诊断研究”。

## 下一步建议

按实际收益排序：

1. 给 CFO head 增加显式 `cfo_norm` 回归监督。当前仅靠 BER loss 学 CFO 不够。
2. 实现 candidate action probing observation，使 policy 能看到每个 action 的 pilot-only 虚拟更新效果。
3. 缓存 1000+ 帧 oracle dataset，训练 imitation policy 并报告 action accuracy / regret 曲线。
4. selective distillation 模型做 1000+ steps 训练，并按场景固定验证集 early stopping。
5. 低 overhead 专用模型用标准大模型重新训练，不再用 20 step smoke test 作为性能判断。

## 清理状态

- 未修改 `开发框架.md`。
- 未恢复历史并行实现。
- 已清理 Python 缓存目录。
- 新增功能均有测试覆盖。
