# 迭代记录：窗口级 PPO 与 p64 门槛攻关

时间戳：2026-08-12 01:59:10（Asia/Shanghai）

## 1. 本轮目标

本轮沿着“先让 Offline NN 接近或低于 `BER_data < 0.01`，再让 RL 在此基础上进一步降低 BER”的门槛式路线继续推进，重点检查三个问题：

1. 当前 PPO 动作空间是否仍缺少有效的 receiver-level 动作。
2. 当前实现是否真正符合“同一动作作用于短窗口，reward 使用窗口级 Reward Pilot 改善”的研究契约。
3. 在 pilot_total 可调整的前提下，是否存在更接近 `BER_data < 0.01` 的主工作点。

## 2. 代码修改

### 2.1 修正窗口级 PPO 语义

修改文件：

- `training/windowed_discrete_ppo.py`
- `tests/test_windowed_discrete_ppo.py`

旧实现的问题：

- 虽然文件名和指标写的是 windowed discrete PPO，但动作执行后仍会被逐帧 `should_accept_reward_guard()` 立即拒绝。
- 这会让很多动作无法真正作用完整窗口，实际更像“逐帧 reward guard”，不符合当前路线中“短窗口动作 + 窗口级 reward”的定义。

本轮修正：

- 在窗口开始保存：
  - PEFT 参数快照；
  - 当前 CIR 快照；
  - receiver soft tail 快照。
- 动作在窗口内真实作用。
- 窗口结束后，根据聚合的 Reward Pilot loss/BER 改善计算 window reward。
- 若 window reward `<= 0` 且动作不是 identity，则恢复 PEFT/CIR/tail 快照。
- 该逻辑仍不使用 Data 标签做 online observation、reward、动作选择或状态恢复决策。

新增测试：

- `test_window_action_is_judged_by_window_reward_before_state_rollback`

该测试验证：

- 风险动作先真实作用到当前帧输出；
- Reward Pilot 变差后，在窗口结束时触发 rollback；
- rollback 恢复 CIR 和 receiver tail；
- Data 标签不参与 rollback 判定。

### 2.2 尝试并移除 extra-refine 动作

本轮先按 TDD 加入过两个临时 receiver-level 动作：

- `extra_refine_light`
- `extra_refine_strong`

其设计意图是：同一帧内基于当前硬判决做额外 DD-CIR 条件刷新，然后重新运行神经块均衡器。

诊断结果显示该动作无效：

- 强制 `extra_refine_light`：64 帧全部被 guard 拒绝，平均 BER 与 identity 相同。
- 强制 `extra_refine_strong`：64 帧只接受 1 帧，平均 BER 仍与 identity 相同。
- online 训练中 extra-refine 被探索，但没有形成稳定 Data BER 改善。

因此最终将该动作从正式动作表移除，避免扩大无效动作空间。

当前保留的动作类型：

- `identity`
- `cir_alpha_slow / nominal / fast`
- `peft_head_light / fast`
- `peft_adapter_lora_conservative`
- `peft_adapter_lora_light`
- `peft_adapter_lora_head_light`
- `rollback_identity`

### 2.3 新增实验配置

新增配置文件：

- `configs/continual_ppo_enhanced_focus_d40_snr10.json`
- `configs/continual_ppo_focus_d40_snr10_p160.json`
- `configs/continual_ppo_focus_d40_snr10_p64.json`

用途：

- `enhanced_focus_d40_snr10`：验证显式 CIR/support/noise summary conditioner 是否能改善当前最佳模型。
- `p160`：验证更长 pilot 是否能降低 Offline NN BER。
- `p64`：验证 pilot sweep 中表现最好的短 pilot 配置是否能进一步逼近 `BER_data < 0.01`。

## 3. 关键实验与结果

### 3.1 当前最佳 p128 checkpoint 的基线

checkpoint：

```text
pretrained/focus_d40_snr10_seq64best_lr1e5_plus600_alpha0p6_2026-08-10/model_best.pt
```

代表性结果：

```text
d40 / SNR=10 / pilot_total=128 / prefix / 2 seeds × 32 frames

DFE-RLS                              BER_data = 0.0094401042
Offline NN only                      BER_data = 0.0144856775
RL-Modulated Neural Block Equalizer  BER_data ≈ 0.0144–0.0146
```

结论：

- 当前 p128 神经接收机仍没有过 `BER_data < 0.01`。
- DFE-RLS 明显更强。
- PPO 动作只带来极小扰动，不能改变主结论。

### 3.2 extra-refine receiver 动作诊断

输出目录：

```text
logs/diagnose_forced_receiver_actions_d40s10_2seed32f_2026-08-11
```

强制动作结果：

```text
identity              BER_data = 0.0144856775
extra_refine_light    BER_data = 0.0144856775
extra_refine_strong   BER_data = 0.0144856775
cir_alpha_slow        BER_data = 0.0152587895
cir_alpha_nominal     BER_data = 0.0144856775
cir_alpha_fast        BER_data = 0.0145670577
```

结论：

- extra-refine 没有提供可达收益。
- cir alpha 动作也没有明显正收益。
- 这说明“再加 receiver-level 小动作”不是当前主要突破口。

### 3.3 enhanced CIR summary 架构尝试

已有 enhanced checkpoint：

```text
pretrained/enhanced_cir_summary_80_plus400hard_2026-08-09/model_best.pt
```

同协议 compare：

```text
logs/compare_enhanced_plus400hard_d30d40_s10_2seed32f_2026-08-11
```

结果：

```text
d30 / SNR=10:
  DFE-RLS         0.0075276693
  Offline NN only 0.0278727220

d40 / SNR=10:
  DFE-RLS         0.0094401042
  Offline NN only 0.0290120452
```

随后从 enhanced checkpoint 做 d40/s10 focus 续训 200 步：

```text
pretrained/enhanced_focus_d40_snr10_seq64best_lr1e5_plus200_2026-08-11
```

sequence validation：

```text
best_model_metric = 0.0373738616
```

结论：

- enhanced 架构当前明显差于 4-iteration 当前最佳模型。
- 可能原因是该架构训练不充分，或显式 CIR summary 拼接方式没有解决当前误差来源。
- 本轮停止该方向，避免继续消耗 GPU。

### 3.4 Pilot sweep：p64 反而最好

使用当前最佳 p128 checkpoint，在同一 d40/s10 工作点测试不同 pilot_total：

输出目录：

```text
logs/compare_pilot_sweep_currentbest_p64_d40s10_2seed32f_2026-08-11
logs/compare_pilot_sweep_currentbest_p96_d40s10_2seed32f_2026-08-11
logs/compare_pilot_sweep_currentbest_p128_d40s10_2seed32f_2026-08-11
logs/compare_pilot_sweep_currentbest_p160_d40s10_2seed32f_2026-08-11
```

结果：

```text
pilot_total = 64:
  DFE-RLS         0.0082310269
  Offline NN only 0.0132533488

pilot_total = 96:
  DFE-RLS         0.0088266228
  Offline NN only 0.0140099164

pilot_total = 128:
  DFE-RLS         0.0094401042
  Offline NN only 0.0144856775

pilot_total = 160:
  DFE-RLS         0.0101651281
  Offline NN only 0.0154474435
```

结论：

- 更长 pilot 没有带来更好结果。
- p64 是当前最接近门槛的工作点。
- p64 下 DFE-RLS 也更强，因此该点仍不是“轻松超过传统”的点。

### 3.5 p160 focus 训练

输出：

```text
pretrained/focus_d40_snr10_p160_seq64best_lr1e5_plus200_2026-08-11
```

sequence validation：

```text
best_model_metric = 0.0166903413
```

结论：

- p160 focus 训练没有改善，反而弱于 p128 当前最佳。
- 不继续 p160。

### 3.6 p64 focus 训练

输出：

```text
pretrained/focus_d40_snr10_p64_seq64best_lr1e5_plus200_2026-08-11
pretrained/focus_d40_snr10_p64_seq64best_lr1e5_plus400_2026-08-11
pretrained/focus_d40_snr10_p64_seq64best_lr1e5_plus600_2026-08-11
```

sequence validation：

```text
p64 +200: best_model_metric = 0.0145263678
p64 +400: best_model_metric = 0.0142996658
p64 +600: best_model_metric = 0.0140904024
```

paired compare：

```text
p64 +200:
  DFE-RLS                              0.0082310269
  Offline NN only                      0.0116489960
  RL-Modulated Neural Block Equalizer  0.0116141188

p64 +400:
  DFE-RLS                              0.0082310269
  Offline NN only                      0.0113699782
  RL-Modulated Neural Block Equalizer  0.0114048554

p64 +600:
  DFE-RLS                              0.0082310269
  Offline NN only                      0.0115094871
  RL-Modulated Neural Block Equalizer  0.0114048554
```

结论：

- p64 focus 训练有效，明显优于未 fine-tune 的 p64 结果。
- 但 p64 +400 后 compare 已接近平台，p64 +600 没继续改善 compare。
- 当前最佳 p64 Offline NN 仍为：

```text
Offline NN only = 0.0113699782
```

- 仍未达到 `BER_data < 0.01`。
- 仍弱于 DFE-RLS：

```text
DFE-RLS = 0.0082310269
```

### 3.7 窗口级 PPO 训练

#### 3.7.1 有 extra-refine 的临时版本

输出：

```text
logs/online_p64_plus400_windowguard_d40s10_300f_2026-08-11
logs/online_p64_plus400_windowguard_d40s10_1000f_2026-08-11
```

训练内结果：

```text
300 frames:
  mean BER_data            = 0.0137127983
  offline_identity mean    = 0.0137574411
  mean improvement         ≈ 4.46e-5

1000 frames:
  mean BER_data            = 0.0109084827
  offline_identity mean    = 0.0109575898
  mean improvement         ≈ 4.91e-5
  last200 BER_data         = 0.0141964293
```

paired compare：

```text
logs/compare_p64_plus400_windowguard_policy1000_d40s10_2seed32f_2026-08-11

DFE-RLS                              0.0082310269
Offline NN only                      0.0113699782
RL-Modulated Neural Block Equalizer  0.0115792415
```

结论：

- 训练轨迹上有极小正改善。
- paired compare 变差。
- extra-refine 动作没有贡献，随后移除。

#### 3.7.2 移除 extra-refine 后的 pruned 动作空间

输出：

```text
logs/online_p64_plus400_windowguard_pruned_d40s10_1000f_2026-08-11
logs/compare_p64_plus400_windowguard_pruned_policy1000_d40s10_2seed32f_2026-08-11
logs/compare_p64_plus400_windowguard_pruned_untrained_d40s10_2seed32f_2026-08-11
```

训练内结果：

```text
mean BER_data         = 0.0115736613
offline_identity mean = 0.0115714291
mean improvement      ≈ -2.23e-6
last200 BER_data      = 0.0169084830
```

paired compare：

```text
当前代码 untrained policy:
  DFE-RLS                              0.0082310269
  Offline NN only                      0.0113699782
  RL-Modulated Neural Block Equalizer  0.0116141189

当前代码 trained policy:
  DFE-RLS                              0.0082310269
  Offline NN only                      0.0113699782
  RL-Modulated Neural Block Equalizer  0.0116141189
```

结论：

- policy 加载成功，但 trained 与 untrained 结果完全一致。
- PPO 没学出可迁移动作选择。
- 当前动作空间对 Data BER 的真实改善仍太弱。

## 4. Reward/Data 相关性诊断

p64 +400 下，旧逐帧 guard 与新窗口级 guard 的非 identity 动作相关性：

```text
旧逐帧 guard:
  non-identity n = 72
  Pearson  = 0.5649607834
  Spearman = 0.1282451206
  positive reward actions = 17
  positive data improvements = 2
  mean data delta ≈ 3.10e-5

窗口级 guard:
  non-identity n = 72
  Pearson  = 0.1718872865
  Spearman = 0.1853467118
  positive reward actions = 38
  positive data improvements = 7
  both positive = 6
  mean data delta ≈ 1.86e-4
```

解释：

- 窗口级修正确实让动作有更多机会影响 Data BER。
- 但 Spearman 仍远低于之前设想的 `>= 0.6`。
- Reward Pilot 改善仍不能稳定预测 Data BER 改善。
- 在这种 reward 信号下，PPO 很难稳定学出超过 Offline NN 的策略。

## 5. 当前最强结果

当前最好的 proposed 相关结果来自 p64 +400 Offline NN：

```text
工作点：
  Level B
  delay = 40
  SNR = 10 dB
  pilot_total = 64
  pilot_layout = prefix
  cir_update = decision_directed
  cir_alpha = 0.6
  seeds = 2
  frames = 32

DFE-RLS                              BER_data = 0.0082310269
Offline NN only                      BER_data = 0.0113699782
RL-Modulated Neural Block Equalizer  BER_data = 0.0116141189
```

目标完成情况：

```text
PPO BER_data < 0.01：未达成
Proposed 超过 DFE-RLS：未达成
PPO 超过 Offline NN only：未达成
Offline NN 接近 0.01：部分达成，当前约 0.01137
```

## 6. 对“为什么效果仍不理想”的判断

本轮结果不支持“只要信道极端，传统均衡一定跑不过神经/RL”的简单假设。当前失败更像是以下工程与问题定义共同造成：

1. DFE-RLS 在当前设定下不是弱 baseline  
   它使用 Adapt/acquisition pilot、接收信号和传统 decision-directed 更新；没有读 Reward/Data 标签，也没有神经网络。对于单载波 BPSK、无 CFO、无相位扰动、无非线性、无编码的 Level B，它确实可以做到很低 BER。

2. 当前神经块均衡器还没强到超过传统自适应 DFE  
   p64 focus 已经把 Offline NN 从约 0.01325 拉到约 0.01137，但距离 DFE-RLS 的 0.00823 仍有明显差距。

3. Reward Pilot 对 Data BER 的代理能力不足  
   窗口级修正后 Spearman 仍只有约 0.185。PPO 的 reward 如果不能稳定预测 Data BER，继续加长 PPO 训练也很难稳定超过 Offline NN。

4. 当前动作空间真实有效动作太少  
   PEFT/CIR alpha 动作多数只产生很小扰动。训练轨迹中 positive data improvement 的比例偏低，动作空间本身没有提供足够大的可达收益。

5. Pilot 长度不是单调收益  
   p64 反而优于 p128/p160。更长 pilot 并没有解决当前神经接收机的问题，说明瓶颈不是单纯 pilot 不够。

## 7. 下一阶段建议

不建议继续盲目跑 PPO。下一阶段应按以下顺序推进：

1. 先继续提高 Offline NN 本体，但不要只靠同一配置低学习率续训  
   p64 +400 后收益已明显变慢。应考虑改变检测器本体，例如：
   - 更强的 unfolded update 结构；
   - 把 tail / CIR uncertainty 显式建模；
   - 改进 DD-CIR 与神经检测器之间的耦合；
   - 对 p64 工作点重新设计训练 curriculum。

2. 重做 reward surrogate  
   当前 Reward Pilot 与 Data BER 改善相关性不够。可能需要：
   - reward 使用窗口级多项指标，而不是简单 reward pilot loss/BER；
   - 引入 confidence calibration、margin 分布、CIR 残差一致性等不读 Data 标签的代理；
   - 增大 Reward Pilot 覆盖位置多样性，但这需要重新评估 p64/p96/p128 的 trade-off。

3. 收缩 PPO 动作空间  
   当前应该优先保留少数在诊断中有正收益的动作。无效动作会稀释探索。

4. 在 Offline NN 未低于 0.01 前，不启动正式 10 seeds × 1000 frames 矩阵  
   当前 proposed 尚未达到门槛，扩大正式矩阵只会更精确地确认失败。

## 8. 验证命令

本轮最终全量测试：

```powershell
.\.venv-gpu\Scripts\python.exe -m pytest -q -p no:cacheprovider
```

结果：

```text
141 passed in 275.96s (0:04:35)
```

## 9. 本轮结论

本轮没有达到最终论文目标，但厘清了几个关键事实：

- p64 是当前比 p128/p160 更好的工作点。
- p64 focus 训练能有效改善 Offline NN，但仍未过 `BER_data < 0.01`。
- window-level PPO 语义已修正，但 PPO 仍没有稳定收益。
- extra-refine 是无效动作，已从正式动作空间移除。
- 当前瓶颈不是“PPO 训练不够长”，而是：
  1. Offline NN 本体仍弱于 DFE-RLS；
  2. Reward Pilot 对 Data BER 的代理信号太弱；
  3. 动作空间缺少足够有效、可被 Reward Pilot 稳定识别的动作。

下一步不应直接跑正式矩阵，而应先攻克 Offline NN 和 reward surrogate。
